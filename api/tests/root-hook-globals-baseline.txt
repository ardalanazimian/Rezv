# خط‌مبنایِ «هوکِ ریشه‌ای که حالتِ سراسری می‌نویسد»
# گاردش: tests/root-hook-globals.test.mts
#
# رانرِ ما همه‌ی فایل‌ها را در یک process اجرا می‌کند (`_all.runner.mts`)، پس
# هوکی که بیرونِ هر `describe` نوشته شود به سوئیتِ **ریشه** می‌چسبد و روی کلِ
# سوئیت اثر می‌گذارد، نه فایلِ خودش.
#
# ✅ ۲۰۲۶-۰۸-۲۶ — این فهرست **خالی** است.
#    از ۱۳ موردِ اولیه به صفر رسید، پس گارد حالا سخت‌گیرِ کامل است: هر هوکِ
#    ریشه‌ای که `globalThis.*`, `process.env.*` یا `redis.del/flush` بنویسد
#    بلافاصله قرمز می‌شود.
#
# ⚠️ خالی نگهش دار. اگر تستِ گارد قرمز شد، راهِ درست **بردنِ هوک به داخلِ
#    `describe`ِ همان فایل** است، نه افزودنِ خط به اینجا.
#
# (خودِ هوکِ ریشه ممنوع نیست — ۱۵۸ موردش در ریپو هست و درست‌اند، چون فقط
#  فیکسچرِ خودشان را می‌سازند/پاک می‌کنند. فقط نوشتنِ حالتِ مشترک مسئله است.)
