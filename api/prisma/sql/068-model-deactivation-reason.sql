-- ═══════════════════════════════════════════════════════════════════════
--  068 — علتِ غیرفعال‌شدنِ مدل
--
--  چرا لازم شد: از امروز مدلِ no-show می‌تواند **خودکار** غیرفعال شود
--  (افتِ کارایی در تولید — lib/model-drift.ts:rollbackDriftedModel). بدونِ
--  این ستون، تنها اثرِ آن یک `is_active = false` است که هیچ نمی‌گوید چرا:
--  «هرگز فعال نشد» و «فعال بود و پس گرفته شد» از هم قابلِ تشخیص نیستند.
--
--  داشبوردِ سلامتِ مدل دقیقاً همین تمایز را لازم دارد؛ و اگر یک شب چند
--  رستوران پشتِ‌هم برگردند، متنِ علت اولین سرنخِ تشخیص است.
--
--  جدولِ `model_training_runs` تاریخچه‌ی **آموزش** است (append-only) و
--  غیرفعال‌سازی یک رویدادِ آموزش نیست — پس اینجا جای درستش است.
-- ═══════════════════════════════════════════════════════════════════════

ALTER TABLE "restaurant_no_show_models"
  ADD COLUMN IF NOT EXISTS "activation_reason" TEXT;
