-- ═══════════════════════════════════════════════════════════════════════
--  ۰۹۳ — تیکِ جبرانی: دیرکردی که پس از مهلتِ مهمان ثبت شد (STATE M-13 · F001 · حکمِ CEO D-26)
--
--  چرا: اگر cron به اندازه‌ی مهلت (lateGraceMinutes) خاموش بماند، اولین تیکِ پس از آن رزرو را پس از
--  `guestDeadline` running_late می‌کند. پیامکِ هشدار در آن لحظه ساعتی **گذشته** را می‌گوید و «دیرتر
--  می‌رسم» هم بسته است — مهمان هیچ فرصتی ندارد. حکمِ D-26: در این حالت نه پیامک، نه no_showِ خودکار؛
--  ردیف running_late می‌ماند و پرسنل طبقِ D-20(c) تصمیم می‌گیرند. «قطعیِ cron هرگز نباید جریمه بسازد.»
--
--  reservations.late_catchup_at  لحظه‌ی همان انتقال، اگر پس از guestDeadline بود؛ NULL یعنی انتقالِ عادی.
--
--  چرا ستون و نه خواندنِ زمانِ رویداد از reservation_events: تصمیم باید **همان لحظه** و **اتمیک با خودِ
--  انتقال** ثبت شود. محاسبه‌ی دوباره از زمانِ رویداد با تغییرِ بعدیِ مهلتِ رستوران (D-18) جابه‌جا می‌شد —
--  ردیفی که جبرانی بود ناگهان «عادی» و جریمه‌پذیر می‌شد.
--
--  چرا فایلِ تازه و نه ویرایشِ ۰۹۲: قانونِ مخزن (CLAUDE.md) — فایلِ مهاجرتِ قبلی هرگز ویرایش نمی‌شود،
--  حتی پیش از merge.
--  idempotent: `IF NOT EXISTS`. ستونِ nullable بدونِ DEFAULT — ردیف‌های موجود NULL (= انتقالِ عادی) می‌گیرند،
--  که درست است: پیش از D-26 هیچ تصمیمِ «جبرانی» ثبت نشده بود.
-- ═══════════════════════════════════════════════════════════════════════

ALTER TABLE reservations ADD COLUMN IF NOT EXISTS late_catchup_at TIMESTAMP(3);
