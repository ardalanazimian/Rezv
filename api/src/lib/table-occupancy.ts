import type { PrismaClient } from '@prisma/client';
import { activeStatusList } from './reservation-status';

// ═══════════════════════════════════════════════════════════════════════
//  اشغالِ میز — شاملِ میزهایِ ثانویه‌ی merge (رفعِ باگِ واقعی، ۲۰۲۶-۰۸-۱۳)
//
//  چرا این فایل: EXCLUDE constraintِ دیتابیس (no_table_overlap) فقط رویِ
//  reservations.table_id (میزِ اصلیِ هر رزرو) اعمال می‌شود. رزروهایِ ترکیبی
//  (tryMergeTables) میزهایِ ثانویه رو فقط در آرایه‌ی merged_table_numbers
//  ثبت می‌کنن — بدونِ هیچ ردیفِ رزروِ جداگانه‌ای که EXCLUDE بشناسدش. یعنی
//  بدونِ این کوئریِ اضافه، هم availability.ts (نمایشِ سانسِ آزاد به مشتری)
//  و هم tryMergeTables (انتخابِ میزِ ترکیبِ بعدی) می‌تونستن یک میزِ ثانویه‌یِ
//  یک ترکیبِ فعال رو «آزاد» نشون بدن — double-booking واقعیِ فیزیکی.
//
//  ⚠️ این چک لایه‌ی اپلیکیشنیه (نه DB-level مثلِ EXCLUDE) — یک TOCTOU raceِ
//  باریک بینِ خواندنِ این تابع و commitِ رزروِ جدید هنوز ممکنه (محافظِ نهاییِ
//  میزِ *اصلی* همچنان EXCLUDE constraint است). رفعِ کاملاً ریشه‌ای (ردیفِ
//  blocker برایِ میزهایِ ثانویه هم در DB، یا EXCLUDE رویِ یک ستونِ
//  denormalize‌شده) نیازِ تغییرِ مدلِ داده‌ست و خارج از دامنه‌ی این رفعِ
//  سرراست است — ریسکِ باقی‌مانده‌یِ مستندشده.
//
// ═══════════════════════════════════════════════════════════════════════
//  🚨 این فایل به **دو سؤالِ متفاوت** جواب می‌دهد. قاطی‌کردنشان یک خطای
//     رده‌ای است که تا امروز دو نقص ساخته — پس هر دو صریح نام‌گذاری شده‌اند:
//
//   ۱. «این میز در بازه‌ی [start, blockEnd) اشغال است؟»  → بازه‌ای (windowed)
//      getOccupiedTableNumbers / isTableNumberOccupied
//      منبع: **فقط جدولِ `reservations`**.
//
//   ۲. «این میز همین حالا مدعی دارد؟»                    → نقطه‌ای (point-in-time)
//      getHeldTableNumbers
//      منبع: **فقط `tables.state`**. هیچ بُعدِ زمانی ندارد.
//
//  ── چرا این دو یکی نمی‌شوند (تصمیمِ ۲۰۲۶-۰۹-۰۵، directives/020) ──
//  وسوسه‌ی «یک منبعِ حقیقت» می‌گوید getOccupiedTableNumbers وضعیتِ میز را هم
//  بخواند. آن رفع **بدتر از خودِ نقص** است: یک هولدِ ۵دقیقه‌ایِ لیستِ انتظار
//  به «اشغال در هر بازه‌ی آینده» ترجمه می‌شود و همان میز از رزروِ سه روزِ بعد
//  هم پنهان می‌ماند — یعنی وعده‌ی شکسته‌ی ۵دقیقه‌ای تبدیل می‌شود به ظرفیتِ
//  فروش‌نرفته‌ی دائمی و بی‌صدا. یک پرچمِ نقطه‌ای فقط وقتی معنا دارد که
//  مصرف‌کننده یک **افق** به آن بچسباند (holdHorizonMinutes پایین‌تر).
// ═══════════════════════════════════════════════════════════════════════

/**
 * مجموعه‌ی شماره‌ی میزهایی که در بازه‌ی [start, blockEnd) اشغالند —
 * هم میزِ اصلیِ هر رزروِ فعال (table_id → tables.number)، هم میزهایِ
 * ثانویه‌ی هر رزروِ ترکیبیِ فعال (merged_table_numbers).
 *
 * 🚨 **دامنه‌ی ضمانت — چیزی که این تابع نمی‌داند.** این تابع «اشغال **به‌دستِ
 * یک رزرو** در این بازه» را جواب می‌دهد و بس. `tables.state` را **نمی‌خواند**
 * و از هولدهایِ بدونِ ردیفِ رزرو **بی‌خبر** است. مصداقِ زنده: `promoteNext`
 * میزِ آفرشده را `state='reserved'` می‌کند و هیچ ردیفِ رزروی نمی‌سازد
 * (`waitlist.ts:360-362`) — آن میز برایِ این کوئری **کاملاً نامرئی** است.
 *
 * این نه باگ است نه قرار است رفع شود: قراردادِ این تابع بازه‌ای است و درست
 * است. چیزی که نبود، همین بندِ صریح بود — سکوتِ همین تابع دو نقص ساخت، چون
 * هر نویسنده‌ای «تنها تابعِ موجود» را برمی‌داشت و نقطه‌ی کورش را با خودش
 * می‌برد. اگر سؤالت «همین حالا مدعی دارد؟» است، `getHeldTableNumbers` را
 * صدا بزن، **نه** این را.
 *
 * ⚠️ و هرگز خروجیِ این دو را بی‌قید ادغام نکن: خروجیِ این یکی به بازه‌ی
 * ورودی مقید است، خروجیِ آن یکی به هیچ بازه‌ای مقید نیست.
 */
export async function getOccupiedTableNumbers(
  tx: Pick<PrismaClient, '$queryRaw'>,
  restaurantId: string,
  start: Date,
  blockEnd: Date,
): Promise<Set<number>> {
  const statuses = activeStatusList();
  const rows = await tx.$queryRaw<{ num: number }[]>`
    SELECT t.number AS num
    FROM reservations r
    JOIN tables t ON t.id = r.table_id
    WHERE r.restaurant_id = ${restaurantId}::uuid
      AND r.status::text = ANY(${statuses})
      AND r.table_id IS NOT NULL
      AND tsrange(r.slot_start, r.block_end) && tsrange(${start}::timestamp, ${blockEnd}::timestamp)
    UNION
    SELECT unnest(r.merged_table_numbers) AS num
    FROM reservations r
    WHERE r.restaurant_id = ${restaurantId}::uuid
      AND r.status::text = ANY(${statuses})
      AND cardinality(r.merged_table_numbers) > 0
      AND tsrange(r.slot_start, r.block_end) && tsrange(${start}::timestamp, ${blockEnd}::timestamp)
  `;
  return new Set(rows.map((r) => r.num));
}

/**
 * آیا میزِ فیزیکیِ شماره‌ی `tableNumber` در بازه‌ی [start, blockEnd) اشغال است؟
 *
 * ── چرا این تابع وجود دارد (نه فقط getOccupiedTableNumbers) ──
 * این کلاسِ باگ دوبار در همین مخزن رخ داد و هر دوبار ریشه‌اش یکی بود:
 * **هر مسیری که یک میزِ فیزیکی را تخصیص می‌دهد باید همین سؤال را بپرسد**،
 * ولی هیچ نامِ مشترکی برایِ پرسیدنش نبود. در commit 0113717 (۲۰۲۶-۰۸-۱۳)
 * فهرستِ مصرف‌کننده‌ها از سمتِ *ساختِ merge* شمرده شد (availability.ts،
 * tryMergeTables، createReservation) و دو نویسنده‌ی دیگرِ تخصیصِ میز —
 * createWalkin و promoteNextِ waitlist — از قلم افتادند، هرچند همان commit
 * خودِ createWalkin را برایِ چکِ maintenance باز کرده بود.
 *
 * پس این تابع عمداً یک نامِ صریح و یک نقطه‌ی تکی است: هر نویسنده‌ی جدیدی که
 * میز تخصیص می‌دهد باید این را صدا بزند. `boolean` برمی‌گرداند (نه throw)
 * چون دو مصرف‌کننده دو رفتارِ متفاوت لازم دارند — walk-in باید ۴۰۹ بدهد،
 * ولی ارتقایِ لیستِ انتظار باید بی‌صدا سراغِ کاندیدِ بعدی برود.
 *
 * ⚠️ محدوده‌ی ضمانت: این چکِ لایه‌ی اپلیکیشن است. برایِ میزِ **اصلی** محافظِ
 * نهایی همچنان کانسترینتِ `no_table_overlap` است؛ برایِ میزِ **ثانویه‌یِ
 * merge** هیچ محافظِ DB-levelی وجود ندارد (کانسترینت رویِ یک ستون
 * `table_id` کلید خورده و میزِ ثانویه ردیفِ خودش را ندارد)، پس این تابع
 * تنها محافظِ آن است و باید **داخلِ همان تراکنشِ درج** صدا زده شود.
 *
 * ⚠️ و پیش‌شرطِ آن ضمانت — بندی که نبودش یک P0 ساخت (۲۰۲۶-۰۹-۰۴):
 * «داخلِ همان تراکنش» لازم است ولی **کافی نیست**. آن تراکنش باید
 * `isolationLevel: Serializable` باشد. دلیلش رفتارِ خودِ Postgres است: SSI
 * فقط خواندنِ تراکنش‌هایِ Serializable را ردیابی می‌کند (رویِ خواندنِ یک
 * تراکنشِ READ COMMITTED هیچ SIREAD/predicate lockی گرفته نمی‌شود). پس این
 * کوئری اگر در READ COMMITTED اجرا شود، برایِ تشخیصِ تداخل **نامرئی** است:
 * چرخه‌ی rw-antidependency کامل نمی‌شود، هیچ‌کس abort نمی‌شود، و هر دو
 * نویسنده commit می‌کنند.
 *
 * این فرضی نیست. متنِ قبلیِ همین کامنت هر بندش درست بود و باز هم دو بازبینی
 * را که فعالانه دنبالِ همین کلاسِ باگ بودند گمراه کرد، چون **دامنه**ی ضمانت
 * را نام می‌برد و **پیش‌شرط**ش را نمی‌گفت. createWalkin دقیقاً همین تابع را
 * درست صدا می‌زد، داخلِ تراکنشِ درج، ولی در READ COMMITTED — و نتیجه یک
 * double-bookingِ فیزیکیِ بازتولیدشده در ۴ از ۶ تکرار بود
 * (tests/walkin-merge-occupancy-concurrency.test.mts).
 *
 * قاعده برایِ نویسنده‌ی بعدی: **یک محافظِ یکتا فقط در سطحی محافظ است که
 * خواندنش قابلِ‌اتکا باشد.** اگر مسیرِ تازه‌ای این تابع را صدا می‌زند،
 * تراکنشش باید Serializable باشد و آن مسیر باید retry داشته باشد
 * (`withSerializationRetry` در reservation-helpers.ts) — وگرنه گاردِ درست
 * را در سطحِ اشتباه گذاشته‌ای، که با بی‌گارد بودن یکی است.
 *
 * وضعیتِ فعلیِ فراخوان‌ها (۲۰۲۶-۰۹-۰۵):
 *   • placeReservation  (reservations.ts:389) — Serializable + retry ✔
 *   • createWalkinTx    (reservations.ts:965) — Serializable + retry ✔
 *   • promoteNextTx     (waitlist.ts:513)     — Serializable + retry ✔
 *
 * ⚠️ ردیفِ سوم تا ۲۰۲۶-۰۹-۰۵ در همین فهرست «READ COMMITTED ✗» نوشته شده بود،
 * در حالی که ارتقا همان روزِ قبل انجام شده بود و کامنتِ خودِ `promoteNextTx`
 * (waitlist.ts) آن را «حالا بسته» اعلام می‌کرد. دو کامنت در یک مخزن دو چیزِ
 * متضاد می‌گفتند؛ این یکی عقب مانده بود. اصلاح شد و شماره‌ی خط‌ها هم تازه شد.
 *
 * ⚠️ **بازه‌ای که به این تابع می‌دهی هم بخشی از گارد است، نه یک جزئیاتِ
 * صداکننده.** یک فراخوانِ کاملاً درست با بازه‌ی کوتاه‌تر از بلاکِ واقعیِ رزرو،
 * همان اندازه کور است که اصلاً صدا زده نشود. دقیقاً همین رخ داد: `promoteNextTx`
 * افقش را با `AVG_DINING_MINUTES` (۷۵) می‌ساخت در حالی که رزروِ ساخته‌شده
 * ۱۰۵ دقیقه بلاک می‌کرد. افق را از `holdHorizonMinutes`/`reservationBlockMinutes`
 * بگیر — نه از یک آمار و نه از یک عددِ ثابت.
 *
 * ⚠️ این فهرست تا ۲۰۲۶-۰۹-۰۴ می‌گفت promoteNext «هنوز» فراخوان نیست، در
 * حالی که همان روز فراخوان شد. یک فهرستِ وضعیت که از کدِ کنارش عقب بماند،
 * بدتر از نداشتنش است — خواننده نتیجه می‌گیرد مسیری وجود ندارد که دارد.
 * هر کس فراخوانِ تازه‌ای اضافه می‌کند، همین‌جا هم ردیفش را بنویسد.
 */
export async function isTableNumberOccupied(
  tx: Pick<PrismaClient, '$queryRaw'>,
  restaurantId: string,
  tableNumber: number,
  start: Date,
  blockEnd: Date,
): Promise<boolean> {
  const occupied = await getOccupiedTableNumbers(tx, restaurantId, start, blockEnd);
  return occupied.has(tableNumber);
}

// ═══════════════════════════════════════════════════════════════════════
//  سؤالِ دوم: «این میز همین حالا مدعی دارد؟» — نقطه‌ای، بدونِ بُعدِ زمانی
// ═══════════════════════════════════════════════════════════════════════

/**
 * مهلتِ پاسخِ مهمان به آفرِ لیستِ انتظار — و **دقیقاً** همان مدتی که یک
 * `tables.state = 'reserved'` بدونِ ردیفِ رزرو زنده می‌ماند.
 *
 * ⚠️ چرا اینجا و نه در `waitlist.ts`: این عدد دو مصرف‌کننده دارد. `waitlist.ts`
 * با آن `offer_expires_at` را می‌نویسد، و `availability.ts` با آن حساب می‌کند
 * یک هولد حداکثر تا کِی می‌تواند به رزرو تبدیل شود. نوشتنِ عددِ ۵ در دو فایل
 * یعنی دو ثابتِ قابلِ‌واگرایی برایِ یک مفهوم — همان کلاسی که سه تخصیص‌دهنده‌ی
 * میز را با سه فیلترِ متفاوت به جا گذاشت. `table-occupancy.ts` برگ است (به
 * هیچ ماژولِ دامنه‌ای وابسته نیست)، پس تنها جایی است که هر دو می‌توانند بدونِ
 * چرخه‌ی import از آن بخوانند.
 */
export const OFFER_TTL_MINUTES = 5;

/**
 * وضعیت‌هایِ نقطه‌ای که یعنی «همین حالا یک مهمانِ فیزیکی مدعیِ این میز است».
 *
 * `maintenance` عمداً **بیرون** است و این استثنا معنادار است: «خارج از سرویس»
 * یک بازه‌ی کوتاهِ قابلِ‌افق نیست، تا وقتی کارکنان برش‌گردانند ادامه دارد. پس
 * در `availability.tableFitsParty` به‌صورتِ **زمان‌ناوابسته** رد می‌شود و
 * افقی به آن چسبانده نمی‌شود. اگر اینجا هم می‌آمد، یک قاعده در دو جا نوشته
 * می‌شد و می‌توانست واگرا شود.
 */
export const TABLE_STATES_BLOCKING_NOW = ['reserved', 'occupied', 'cleaning'] as const;

export function tableStateBlocksNow(state: string | null | undefined): boolean {
  return (TABLE_STATES_BLOCKING_NOW as readonly string[]).includes(state ?? '');
}

/**
 * ═══════════════════════════════════════════════════════════════════════
 *  «یک رزرو چقدر میز را می‌بندد؟» — **یک** تعریف، نه پنج نسخه
 * ═══════════════════════════════════════════════════════════════════════
 *
 * تا ۲۰۲۶-۰۹-۰۵ همین یک جمله در **چهار** جا مستقل نوشته شده بود:
 *   • `reservation-helpers.computeRanges` — تولیدکننده‌ی واقعیِ `block_end`
 *   • `availability.computeSlots`         — بلاکِ هر سانس در نمایش
 *   • `reservations.createWalkin`         — بلاکِ ذخیره‌شده‌ی walk-in
 *   • `holdHorizonMinutes` (همین فایل)    — افقِ هولدِ آفر
 * و تنها چیزی که آن‌ها را به هم گره می‌زد یک **کامنت** بود («همان فرمولِ
 * computeRanges»). کامنت گارد نیست: یک ترمِ تازه در یکی از آن‌ها بی‌صدا سه
 * تای دیگر را واگرا می‌کرد.
 *
 * پیامدِ همین کلاس، همین امروز، در `promoteNextTx` زنده بود — با این تفاوت
 * که آن‌جا حتی transcription هم نبود، یک **ورودیِ رده‌اشتباه** بود:
 * `AVG_DINING_MINUTES` (آمارِ «مردم چقدر می‌نشینند») جایِ بلاک (قراردادِ «این
 * رزرو تا کِی میز را می‌بندد») نشسته بود. گارد:
 * `tests/waitlist-promotion-horizon.test.mts`.
 *
 * ⚠️ چرا در این فایل و نه کنارِ `computeRanges`: این ماژول عمداً **برگ** است
 * (فقط به `reservation-status` وابسته است). اگر تعریف در `reservation-helpers`
 * می‌نشست، این فایل باید آن را import می‌کرد و برگ‌بودنش — که تنها دلیلِ
 * دسترسیِ هم‌زمانِ `waitlist` و `availability` به `OFFER_TTL_MINUTES` است —
 * از بین می‌رفت.
 */
export interface BlockTiming {
  slotMinutes: number;
  cleaningMinutes: number;
  bufferMinutes: number;
}

/**
 * دنباله‌ی بلاک: مدتی که **بعد از** پایانِ سانس هم میز در دسترس نیست
 * (نظافت + بافرِ ایمنی). همان چیزی که در ستونِ `block_buffer_minutes` ذخیره
 * می‌شود و `block_end` را از `slot_end` جلوتر می‌برد.
 */
export function blockTailMinutes(cfg: Pick<BlockTiming, 'cleaningMinutes' | 'bufferMinutes'>): number {
  return cfg.cleaningMinutes + cfg.bufferMinutes;
}

/**
 * کلِ مدتی که یک رزرو میز را از دسترس خارج می‌کند = `block_end − slot_start`.
 *
 * `durationOverride` همان معنایِ `computeRanges` را دارد: اگر رزرو مدتِ
 * صریح داشته باشد جایِ `slotMinutes` می‌نشیند و دنباله همچنان اضافه می‌شود.
 */
export function reservationBlockMinutes(cfg: BlockTiming, durationOverride?: number): number {
  return (durationOverride ?? cfg.slotMinutes) + blockTailMinutes(cfg);
}

/**
 * افقِ یک پرچمِ نقطه‌ای: تا چند دقیقه‌ی آینده می‌تواند بر رزروپذیری اثر بگذارد؟
 *
 * ── اشتقاق، نه انتخاب ──
 * یک میزِ `reserved` یعنی آفری زنده است. مهمان تا آخرین ثانیه‌ی
 * `OFFER_TTL_MINUTES` حق پذیرش دارد، و `acceptOffer` رزروی می‌سازد که از
 * **همان لحظه** شروع می‌شود (`waitlist.ts:646-659` تاریخ/ساعتِ «الان» را به
 * `createReservation` می‌دهد). آن رزرو `slotMinutes + cleaningMinutes +
 * bufferMinutes` طول می‌کشد. پس دیرترین لحظه‌ای که این هولد می‌تواند میز را
 * اشغال کند:  now + TTL + بلاکِ سرویس. هر سانسی که بلاکش با
 * [now, now+افق) هم‌پوشانی دارد، اگر مهمان بپذیرد واقعاً غیرقابلِ‌رزرو می‌شود.
 *
 * ⚠️ دو خطایی که این تابع عمداً مرتکب نمی‌شود:
 *  • **افق را حذف نکن.** بدونِ افق، پرچم رویِ هر بازه‌ی آینده اعمال می‌شود و
 *    میز از رزروِ هفته‌ی بعد هم پنهان می‌ماند.
 *  • **افق را به TTL تقلیل نده.** با افقِ ۵دقیقه‌ای و شبکه‌ی ۳۰دقیقه‌ایِ
 *    سانس‌ها، تقریباً هیچ سانسی داخلِ افق نمی‌افتد و رفع عملاً هیچ‌چیز
 *    نمی‌سنجد. جمله‌ی غالب، مدتِ نشستن است؛ TTL فقط تأخیرِ پذیرش را می‌پوشاند.
 *
 * ⚠️ **مدتِ سرکوب ≠ افق.** افق تعیین می‌کند *کدام* سانس‌ها، نه *چند وقت*.
 * سرکوب دقیقاً تا وقتی زنده است که خودِ پرچم زنده باشد (حداکثر TTL + جاروبِ
 * ۲دقیقه‌ای)؛ به‌محضِ `state='free'` شدن، محاسبه‌ی بعدی همه‌ی سانس‌ها را
 * برمی‌گرداند.
 */
/*
 * ⚠️ دومین مصرف‌کننده از ۲۰۲۶-۰۹-۰۵: `promoteNextTx` (waitlist.ts) — نویسنده‌ی
 * همین پرچم. نویسنده و خواننده حالا **یک** افق دارند. اگر واگرا شوند، صف میزی
 * را آفر می‌دهد که availability همان لحظه قادر به میزبانی‌اش نمی‌داند — یعنی
 * دقیقاً وعده‌ای که در لحظه‌ی پذیرش می‌شکند.
 */
export function holdHorizonMinutes(cfg: BlockTiming): number {
  return OFFER_TTL_MINUTES + reservationBlockMinutes(cfg);
}

/**
 * شماره‌ی میزهایی که **همین حالا** مدعی دارند — از `tables.state`، نه از رزروها.
 *
 * 🚨 این تابع هیچ بازه‌ای نمی‌گیرد و هیچ بازه‌ای برنمی‌گرداند، و این عمدی است.
 * خروجی‌اش یک عکسِ لحظه‌ای است. **هرگز** آن را مستقیم به یک تصمیمِ بازه‌ای
 * وصل نکن؛ اول با `holdHorizonMinutes` یک افق به آن بچسبان، وگرنه یک هولدِ
 * ۵دقیقه‌ای را به «اشغالِ همیشگی» ترجمه کرده‌ای.
 *
 * چرا نامِ جدا و نه گشاد‌کردنِ `getOccupiedTableNumbers`: قراردادِ آن تابع
 * درست است و مشکل نبودِ نام برایِ سؤالِ دوم بود. با یک نامِ صریح، مصرف‌کننده‌ی
 * بعدی **انتخاب** می‌کند به‌جایِ اینکه نقطه‌ی کورِ تنها تابعِ موجود را به ارث ببرد.
 *
 * مصرف‌کننده‌ها (۲۰۲۶-۰۹-۰۵ — این فهرست را با هر فراخوانِ تازه به‌روز کن):
 *   • availability از مسیرِ `holdsFromTables` همین طبقه‌بندی را رویِ ردیف‌هایی
 *     که از قبل خوانده اعمال می‌کند (بدونِ کوئریِ اضافه). این تابع برایِ
 *     مسیرهایی است که ردیفِ میز را در دست ندارند.
 *
 * ⚠️ عمداً رویِ `is_active` فیلتر **نمی‌کند**: میزِ غیرفعالی که `occupied`
 * است هم واقعاً مدعی دارد. «قابلِ فروش بودن» سؤالِ دیگری است و
 * `tableFitsParty` جواب می‌دهد؛ قاطی‌کردنشان همین تابع را به یک جوابِ
 * نصفه‌ی بی‌اعلام تبدیل می‌کرد.
 */
export async function getHeldTableNumbers(
  tx: Pick<PrismaClient, '$queryRaw'>,
  restaurantId: string,
): Promise<Set<number>> {
  // فهرستِ وضعیت‌ها از همان ثابتِ بالا می‌آید، نه یک کپیِ درون‌SQL — وگرنه
  // این کوئری و `tableStateBlocksNow` می‌توانستند بی‌صدا واگرا شوند.
  const states = [...TABLE_STATES_BLOCKING_NOW];
  const rows = await tx.$queryRaw<{ num: number }[]>`
    SELECT number AS num
    FROM tables
    WHERE restaurant_id = ${restaurantId}::uuid
      AND state::text = ANY(${states})
  `;
  return new Set(rows.map((r) => Number(r.num)));
}
