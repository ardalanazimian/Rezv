{
  "name": "rezervno-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "تست‌های E2E اپ کاستومر رزرونو (Playwright، موبایل‌محور)",
  "scripts": {
    "test": "playwright test",
    "test:mobile": "playwright test --project=mobile-safari --project=mobile-chrome",
    "test:desktop": "playwright test --project=desktop-chrome",
    "test:booking": "playwright test booking",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "audit": "node ux-audit.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "pngjs": "^7.0.0",
    "serve": "^14.2.0"
  }
}
