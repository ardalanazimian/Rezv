# ── Override محلی (برای توسعه‌ی بومی API) ──
# postgres و redis را به پورت‌های هاست باز می‌کند تا API روی Node هاست به آن‌ها
# برسد. فقط برای توسعه‌ی محلی. (این فایل در git نیست)
version: "3.9"

services:
  postgres:
    ports:
      - "5432:5432"
  redis:
    ports:
      - "6379:6379"
