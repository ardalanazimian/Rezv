# رزرونو — راهنمای عملیاتی لانچ

این سند آخرین گام قبل از لانچ واقعی است: راه‌اندازی HTTPS، دفاع DDoS، بک‌آپ، و چک‌لیست نهایی. کد آماده است؛ این‌ها کار سرور و زیرساخت‌اند.

> ⚠️ **اصلاح‌شده (۲۰۲۶-۰۸-۱۲):** پیش‌نیازِ قبلی («بسته‌ی `rezervno-full-deploy.zip`
> روی سرور») از دورانی مانده که کد به‌صورتِ zip تحویل داده می‌شد — رجوع کن به
> `DEPLOY-COMMANDS.md`. واقعیتِ فعلی: ریپو رو `git clone` کن
> (`github.com/ardalanazimian/Rezv`)، سپس طبقِ `README.md`ِ ریشه یا `CLAUDE.md`
> با `docker compose --profile http up -d --build` (تست محلی) یا
> `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build`
> (تولید با HTTPS) بالا بیار. بخش‌هایِ ۱ و ۳ زیر هم اصلاح شدن — این ریپو الان
> Caddy و بک‌آپ رو **خودش دوکرایز و آماده** داره؛ نصبِ دستیِ جداگانه نه فقط
> لازم نیست، بلکه با سرویسِ dockerized تداخل می‌کنه (هر دو رویِ پورتِ
> ۸۰/۴۴۳ بالا میان).

---

## ۱. HTTPS / SSL — ضروری 🔒

بدون HTTPS، توکن‌های ورود و کدهای OTP روی شبکه قابل شنود هستند. **هیچ‌وقت بدون این لانچ نکن.**

> ⚠️ **اصلاح‌شده (۲۰۲۶-۰۸-۱۲):** این بخش قبلاً نصبِ دستیِ Caddy رویِ خودِ
> سرور (`apt install caddy`) رو پیشنهاد می‌کرد — از دورانی که هنوز
> `docker-compose.prod.yml` و `deploy/caddy/` وجود نداشتن. امروز این ریپو
> خودش یک سرویسِ Caddyِ کاملاً dockerized داره (با ریت‌لیمیت، هدرهایِ
> امنیتی، و روتینگِ زیردامنه‌ایِ از قبل تنظیم‌شده — رجوع کن به
> `deploy/caddy/Caddyfile`). نصبِ دستیِ Caddy یا certbot **رویِ همون سرور
> با این سرویس تداخل می‌کنه** (هر دو پورتِ ۸۰/۴۴۳ می‌خوان).
>
> **راهِ درستِ فعلی، کاملاً مستندشده در `HTTPS-SETUP.md`:** فقط چهار
> رکوردِ DNSِ زیردامنه (`api.`/`app.`/`business.`/`admin.`) رو تنظیم کن،
> `DOMAIN=` رو در `.env` بذار، و:
> ```bash
> docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
> ```
> همین. `HTTPS-SETUP.md` رو برایِ DNS/عیب‌یابیِ کامل بخون — این بخش دیگه
> جزئیاتِ نصبِ دستی رو تکرار نمی‌کنه تا با اون سند واگرا نشه.

---

## ۲. دفاع DDoS — CDN/WAF جلوی دامنه 🛡️

ریت‌لیمیت اپلیکیشن (که داری) لایه‌ی آخر است، نه اول. برای دفاع واقعی، یک CDN/WAF جلوی دامنه بگذار.

### ArvanCloud (ایرانی، پیشنهادی برای کاربر ایرانی)

1. در پنل ArvanCloud دامنه را اضافه کن
2. nameserverهای دامنه را به ArvanCloud تغییر بده
3. در بخش امنیت: **محافظت DDoS** و **فایروال** را فعال کن
4. قوانین rate-limit در سطح شبکه بگذار (مثلاً حداکثر N درخواست در ثانیه per IP)
5. **CDN** را برای فایل‌های استاتیک (فرانت‌اندها) فعال کن — سرعت بارگذاری چند برابر می‌شود

### جایگزین: Cloudflare (پلن رایگان کافی است)

مشابه بالا. "Under Attack Mode" برای مواقع حمله، و WAF rules برای مسدودکردن الگوهای بد.

> **چرا هر دو لازم است:** CDN/WAF حملات حجمی (لایه شبکه) را می‌گیرد؛ ریت‌لیمیت اپ سوءاستفاده‌ی منطقی (مثلاً اسپم OTP) را. یکی جایگزین دیگری نیست.

---

## ۳. بک‌آپ خودکار دیتابیس 💾

داده‌ی مشتری ارزشمندترین دارایی است.

> ⚠️ **اصلاح‌شده (۲۰۲۶-۰۸-۱۲):** این بخش قبلاً یک اسکریپتِ دستیِ
> `/opt/rezervno/backup.sh` + crontabِ خودِ سرور پیشنهاد می‌کرد — از
> دورانی که هنوز سرویسِ `backup` در `docker-compose.yml` وجود نداشت.
> امروز این ریپو خودش یک سرویسِ بک‌آپِ کاملاً dockerized داره (تشخیصِ
> بک‌آپِ خالی/خراب، آپلودِ اختیاری به S3-compatible، بازیابیِ یک‌دستوره) —
> نوشتنِ اسکریپتِ دستیِ موازی هم اضافی است هم بی‌فایده (سرویسِ dockerized
> خودش هر شب اجرا می‌شه، نیازی به crontabِ سرور نیست).
>
> **راهِ درستِ فعلی، کاملاً مستندشده در `BACKUP-GUIDE.md`:** فقط
> `BACKUP_CRON`/`BACKUP_KEEP`/`BACKUP_ON_START` رو در `.env` تنظیم کن؛
> سرویسِ `backup` با همون `docker compose up -d` بالا میاد. برایِ
> بک‌آپِ فوری/بازیابی/آبجکت‌استوریج، `BACKUP-GUIDE.md` رو بخون.

---

## ۴. متغیرهای محیطی نهایی

قبل از لانچ، `.env` را کامل کن:

```bash
# دیتابیس — رمز قوی
POSTGRES_PASSWORD=$(openssl rand -base64 24)

# کلیدهای JWT — تصادفی و بلند
JWT_SECRET=$(openssl rand -base64 48)
JWT_REFRESH_SECRET=$(openssl rand -base64 48)

# tenant مدیر پلتفرم (از خروجی seed کپی کن)
PLATFORM_ADMIN_TENANT_ID=<uuid-از-seed>

# پیامک: ملی‌پیامک (مهاجرت از کاوه‌نگار، ۲۰۲۶-۰۸-۲۶)
# ⚠️ الگوها با «شناسه‌ی عددی» (bodyId) کار می‌کنند، نه با نام. bodyId را بعد از
#    تأییدِ متنِ الگو از پنلِ ملی‌پیامک بردار. خالی‌ماندنِ هرکدام = ارسال‌نشدنِ
#    همان نوع پیامک (که صریح لاگ می‌شود، بی‌صدا رد نمی‌شود).
MELIPAYAMAK_USERNAME=<نام‌کاربری>
MELIPAYAMAK_PASSWORD=<رمز>
MELIPAYAMAK_BODYID_OTP=<شناسه>
MELIPAYAMAK_BODYID_BOOKING=<شناسه>
# فقط برای کمپینِ متن‌آزاد لازم است (خطِ اختصاصی):
MELIPAYAMAK_FROM=<خطِ اختصاصی>

# ⚠️ مهم‌ترین متغیرِ این فهرست — دامنه‌هایی که اجازه‌ی صداکردنِ API را دارند.
# هر دامنه‌ای که فرانت از آن سرو می‌شود باید اینجا باشد: اپِ مشتری، پنلِ
# رستوران، پنلِ شرکت، و وب‌سایت.
ALLOWED_ORIGINS=https://rezervno.ir,https://www.rezervno.ir,https://app.rezervno.ir,https://business.rezervno.ir

# محافظتِ /api/metrics — بدونِ این، متریک‌های داخلی عمومی‌اند.
METRICS_TOKEN=$(openssl rand -hex 32)
```

### ⚠️ `ALLOWED_ORIGINS` — تنها متغیری که خرابیِ کاملاً خاموش می‌سازد

اگر این مقدار **غلط** باشد (نه خالی)، هیچ‌چیز شبیهِ خرابی به‌نظر نمی‌رسد: API
بالاست، لاگ تمیز است، صفحه بدونِ خطا باز می‌شود — ولی مرورگر هر درخواستِ اپ را
بلاک می‌کند و اپِ مشتری صادقانه به دادهٔ **نمونه** برمی‌گردد و هر کارت را
«نمونه» برچسب می‌زند. یعنی همه‌ی بازدیدکننده‌ها به‌جای رستورانِ واقعی، محتوایِ
آزمایشی می‌بینند. (این با تستِ زنده‌ی مرورگر در ممیزیِ ۲۰۲۶-۰۸-۱۹ تأیید شد.)

قاعده‌ی طلایی: هدرِ `Origin` که مرورگر می‌فرستد **همیشه** دقیقاً
`scheme://host[:port]` است. پس:

| ❌ غلط | ✅ درست | چرا |
|---|---|---|
| `rezervno.ir` | `https://rezervno.ir` | بدونِ scheme هرگز مچ نمی‌شود |
| `https://rezervno.ir/` | `https://rezervno.ir` | مرورگر اسلشِ پایانی نمی‌فرستد |
| `https://rezervno.ir/app` | `https://rezervno.ir` | Origin مسیر ندارد |
| `*` | فهرستِ صریحِ دامنه‌ها | رد می‌شود |
| فقط `https://rezervno.ir` | + `https://www.rezervno.ir` | `www` یک originِ جداست |
| فقط `https://rezervno.ir` | + هر زیردامنه‌ی فرانت | هر زیردامنه origin جداست |

از ۲۰۲۶-۰۸-۱۹ سرور در `production` این موارد را خودش می‌گیرد و با پیامِ صریح
بالا نمی‌آید (fail-fast) — به‌جای اینکه بی‌صدا به دادهٔ نمونه سقوط کند. ولی
بهتر است از اول درست بنویسی.

**تأییدِ سریع بعد از استقرار** (باید نامِ همان دامنه را برگرداند؛ خروجیِ خالی
یعنی پیکربندی غلط است):

```bash
curl -sD - -o /dev/null -H "Origin: https://app.rezervno.ir" \
  https://api.rezervno.ir/api/v1/restaurants | grep -i access-control-allow-origin
```

---

## ۵. ذخیره‌سازی عکس

**به‌روزرسانی (۲۰۲۶-۰۸-۰۷): این بخش منسوخ بود.** آپلودِ عکس/لوگو دیگر سمتِ
مرورگر/base64 نیست — از مدت‌ها پیش با یک مسیرِ واقعی جایگزین شده:
`POST /api/v1/restaurant/photos` (چندبخشی/multipart، `api/src/lib/media.ts`
برایِ sniffing/اعتبارسنجی، صفِ بازبینیِ انسانی قبل از انتشارِ عمومی، ذخیره روی
volumeِ `uploads` در `docker-compose.yml`). لوگو هم از همین مسیر با
`category='logo'` رد می‌شود. رجوع کن به `SECURITY-AUDIT.md` بخشِ File Uploads
برایِ جزئیاتِ امنیتی.

اگر هدف مهاجرت از volumeِ محلی به آبجکت‌استوریج (آروان/S3) است (برای
مقیاسِ چند-سرور)، آن هنوز کار نشده — ولی endpoint/اعتبارسنجی/صفِ بازبینی
همه از قبل آماده‌اند؛ فقط لایه‌یِ ذخیره‌سازیِ فیزیکی باید عوض شود.

---

## ✅ چک‌لیست نهایی قبل از لانچ

کد:
- [x] سه فرانت‌اند به API وصل (انجام‌شده)
- [x] احراز هویت مشتری (OTP) — انجام‌شده
- [x] احراز هویت کارمند و مدیر — انجام‌شده
- [x] ریت‌لیمیت روی همه‌ی endpointها — انجام‌شده
- [ ] seed کامل اجرا شده (`docker compose exec api npx prisma db seed`)
- [x] `sms.ts` به ملی‌پیامک مهاجرت کرد (۲۰۲۶-۰۸-۲۶) — دو مسیر: الگو (bodyId) و متنِ آزاد
- [x] ذخیره‌سازی عکس — انجام‌شده (بخشِ ۵ بالا؛ فقط مهاجرت به آبجکت‌استوریج برایِ مقیاسِ چند-سرور باقی مانده، بلاکرِ لانچ نیست)

زیرساخت (⚠️ اصلاح‌شده ۲۰۲۶-۰۸-۱۲ — Caddy و بک‌آپ کد/کانفیگشون کاملِ و
dockerized توی خودِ ریپوعه؛ کارِ باقی‌مونده فقط تنظیمِ DNS/`.env` و
`docker compose up`ه، نه نصبِ دستی):
- [ ] HTTPS فعال — `docker-compose.prod.yml` (رجوع کن به `HTTPS-SETUP.md` برایِ DNSِ زیردامنه‌ای)
- [ ] CDN/WAF جلوی دامنه (ArvanCloud/Cloudflare) — این یکی واقعاً بیرونِ ریپوعه، دستیه
- [ ] بک‌آپ خودکار دیتابیس — سرویسِ `backup` در `docker-compose.yml`، فقط `BACKUP_CRON`/S3 رو در `.env` تنظیم کن (`BACKUP-GUIDE.md`)
- [x] پورت دیتابیس از بیرون بسته (فقط شبکه‌ی داکر — در compose از قبل این‌طور است)
- [ ] `.env` با رمزها و کلیدهای قوی و تصادفی
- [x] `.env` در git نیست (در .gitignore هست)
- [ ] **`ALLOWED_ORIGINS` با هر چهار دامنه‌ی فرانت** — و بعد از استقرار با
      دستورِ `curl` بخشِ ۴ تأیید شده که هدر واقعاً برمی‌گردد. اگر این را رد کنی،
      سایت بالا می‌آید ولی همه دادهٔ «نمونه» می‌بینند و هیچ خطایی نمی‌بینی.
- [ ] `METRICS_TOKEN` تنظیم شده (وگرنه `/api/metrics` عمومی است)

مهاجرتِ دیتابیس — ⚠️ یک پیش‌نیازِ **مسدودکننده** دارد:

- [ ] **پیش از اعمالِ مهاجرتِ ۰۷۹ روی هر محیطِ واقعی، این کوئری اجرا و
      نتیجه‌اش ثبت شود:**

      ```sql
      SELECT phone, count(*), array_agg(id ORDER BY created_at)
      FROM staff WHERE role='owner'
      GROUP BY phone HAVING count(*) > 1;
      ```

      **نتیجه‌ی موردِ انتظار: صفر ردیف.**

      مهاجرتِ `079-staff-owner-phone-unique.sql` یک ایندکسِ یکتای جزئی روی
      `staff (phone) WHERE role = 'owner'` می‌سازد. اگر دو ownerِ هم‌شماره از
      قبل در دیتابیس باشند، `CREATE UNIQUE INDEX` با پیامِ روشنِ
      «Key (phone)=(…) is duplicated» **بلند شکست می‌خورد** — این عمدی است، نه
      نقص.

      **اگر ردیفِ تکراری پیدا شد:** هیچ حذف یا ادغامِ خودکاری مجاز نیست.
      ownerِ دوم ممکن است اعتبارنامه‌ی username/password از مهاجرتِ ۰۷۴ داشته
      باشد. فهرست برای تصمیمِ مالک می‌رود و یک اسکریپتِ reconciliationِ
      جداگانه **پیش از** ۰۷۹ نوشته و اجرا می‌شود؛ بعد ۰۷۹ دوباره اجرا شود
      (idempotent است).

      وضعیتِ ثبت‌شده تا امروز: هر دو DBِ قابل‌دسترسِ توسعه صفر ردیف
      (`docs/reports/PRE-MERGE-VERIFY-2026-08-27.md`). **محیطِ
      production/staging هرگز سنجیده نشده** — چون در دسترس نبوده. این تیک
      تا اجرای واقعیِ کوئری روی همان محیط باز می‌ماند.

امنیت:
- [x] فرانت‌اندها XSS-safe (تابع esc)
- [ ] escape سمت سرور هم اضافه شده (چون مهاجم می‌تواند مستقیم به API بزند)
- [ ] تست نفوذ ساده (حداقل: تلاش ورود با شماره غیرمجاز، اسپم OTP، رزرو هم‌زمان)

عملیات:
- [ ] مانیتورینگ لاگ خطاها و نرخ ۴۲۹
- [ ] یک شماره/ایمیل پشتیبانی برای مشکلات کاربران
- [ ] صفحه‌ی وضعیت (status page) برای زمان‌های قطعی

---

## جمع‌بندی: از اینجا تا لانچ

**آماده است:** کل کد (سه اپ + بک‌اند + ورود + ریت‌لیمیت)، همه تست‌شده.

**کار تو (روی سرور):** تنظیمِ DNSِ زیردامنه‌ای + `.env` و `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d` (HTTPS + بک‌آپ خودشون با همین یک دستور دوکرایز میان)، CDN جلوی دامنه، اجرای seed، گذاشتن نام‌کاربری/رمزِ ملی‌پیامک. این‌ها چند ساعت کارند، نه چند هفته.

**فاز ۴ (بعد از لانچ، به‌روزرسانیِ ۲۰۲۶-۰۸-۰۷):** ذخیره‌سازیِ عکس ساخته شده (این‌ها منسوخ شدند)؛ آنچه واقعاً باقی مانده: مهاجرتِ اختیاریِ ذخیره‌سازی به آبجکت‌استوریج برایِ مقیاسِ چند-سرور، صفِ پیامکِ عمومی‌تر (BullMQ در صورتِ نیاز)، و بهبودهای تدریجی بر اساسِ بازخوردِ کاربرانِ واقعی. رجوع کن به `LAUNCH-GAPS.md` برایِ فهرستِ زنده‌یِ شکاف‌ها.

موفق باشی! 🌿
