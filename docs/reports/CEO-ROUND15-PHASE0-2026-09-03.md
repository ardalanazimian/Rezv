# گزارشِ CEO — دورِ ۱۵، فازِ ۰: حقیقتِ زمین + Wave 0

**تاریخ:** ۲۰۲۶-۰۹-۰۳ · **شاخه:** `feat/admin-totp-login` (`dbec99f`) · **PR:** #81 · **برنامه:** `CEO_LAUNCH_AUDIT_PROMPT.md` (§۱۰: PLAN + فازِ ۰ + mandateهای A1–A3+A6 + گزارشِ فارسی)

> **خلاصه در پنج خط:** (۱) PR #81 قابلِ merge نبود — jobِ `test` روی کدِ کامیت‌شده تا **۳۶۱ دقیقه** گیر بود؛ ریشه پیدا و رفع شد و همان job حالا در **۲.۶ دقیقه** سبز می‌شود؛ **#81 روی `a2cf39d` کاملاً سبز است**. (۲) درِ پشتیِ OTP که عاملِ سوم را دور می‌زد، ۱۳ ساعت commit‌نشده مانده بود؛ کامیت و push شد — و رگرسیونِ e2e که همین کار ساخت، پیدا و با تستِ falsifiable رفع شد. (۳) سرشماریِ فازِ ۱ روی هر سه اپ کامل و پذیرفته شد (۳۲۵ ردیف؛ ۹ FAKE، ۵۶ PARTIAL، ۲ blocker) — یکی از دو blocker (ورودِ پنلِ بیزنس با رمز که از ۰۸-۲۶ `ReferenceError` می‌داد) همین‌جا رفع شد. (۴) صفر پروژه در Vercel و Sentry؛ drift/RLS زنده هنوز «نمی‌دانیم». (۵) A6 (یکپارچگیِ تست) به‌خاطرِ session limit هنوز اجرا نشده.

---

## ۰. تأییدِ پیش‌فرض‌ها — فکت‌ها بر ادعاها (§۲.۱ خودِ برنامه)

| برنامه می‌گفت | مخزن (verify شده) | عمل |
|---|---|---|
| repo `ardalanaz/RezervnoOS`، Next 14 | `ardalanazimian/Rezv`، Next 16 | مخزن |
| مهاجرت در `prisma/migrations/manual/` | `api/prisma/sql/NNN-*.sql` با `apply-sql.sh`؛ CLAUDE.md: `manual/` وجود ندارد | CLAUDE.md |
| PR #79 = ۱۲/۱۲ | ۱۳ چک (Sourcery skipped)؛ #81 در جریان | ثبت |
| Supabase `zmyuvtpbchytqvtgyewt` در دسترس | MCP می‌بیندش (تنها ACTIVE) ولی pg auth ×۲ رد شد، بعد «hibernated»، بعد کلاسیفایر retry را بست | **تصمیمِ شما** |

---

## ۱. ابزارها و کانکتورها — چه چیزی واقعاً کار کرد

| ابزار | نتیجه |
|---|---|
| **Supabase MCP** | `list_projects` ✓ — پروژه‌ی مصوب ACTIVE_HEALTHY؛ `Kikiz` (مسموم) INACTIVE. `execute_sql`/`list_tables`/`get_advisors` ✗ (auth → hibernated → classifier). **drift و RLS زنده مسدود.** |
| **Vercel MCP** | تیم `team_gLTPq1IJB0ayoC3NsDE4yoXg` (hobby) → **۰ پروژه** |
| **Sentry MCP** | org `rezvno` (de) → **۰ پروژه** → DSN ممکن نیست |
| **graphify** | مخزن ایندکس‌شده، ۳٬۵۹۷ گره — برای فازِ ۱ |
| GitHub | `gh` باطل (۴۰۱)؛ API عمومی خواندنی (یک ساعت rate-limit) |
| Docker محلی | PG 55432 ✓ · Redis روی 56500 (56379 داخلِ بازه‌ی رزروِ Hyper-V) |

---

## ۲. CI — چهار سرِ متوالی

| SHA | چه بود | `test` | `e2e` | نتیجه |
|---|---|---|---|---|
| `660456b` | وضعِ اولیه‌ی PR #81 | ✖ **cancelled بعد از ۳۶۱ دقیقه** (سقفِ ۶ ساعتِ GitHub) | ✅ ۱۱.۷ | ۱۱/۱۳ |
| `dbec99f` | Wave 0 (فلگِ OTP + ۵ رفعِ تست) | ✅ **۲.۸ دقیقه** — hang رفع شد | ✖ cancelled ۲۴.۴ دقیقه (رگرسیونِ P0-013) | ۱۱/۱۳ |
| `a2cf39d` | رفعِ رگرسیونِ e2e | ✅ ۲.۶ | ✅ ۱۵.۲ | **۱۲/۱۳ سبز + ۱ skipped، صفر شکست — PR #81 سبز** |
| `580cf7f` | رفعِ A3-001 + A2-001 | ✅ ۲.۸ | ✅ ۱۴.۲ | **۱۲/۱۳ سبز + ۱ skipped، صفر شکست — سرِ فعلیِ PR، آماده‌ی merge** |

`ci.yml` هیچ `concurrency:` ندارد → runِ گیرکرده‌ی قدیمی شش ساعتِ کامل runner سوزاند. jobِ `test` هم `timeout-minutes` ندارد (فقط `e2e`: ۲۵). هر دو → تصمیمِ شما (بندِ ۸).

---

## ۳. سبزِ واقعیِ محلی — روی DBِ صفر، مثلِ CI

```
tests 1528 · pass 1528 · fail 0 · REAL_NPM_TEST_EXIT=0 · 148 s
```

DB با همان سه گامِ CI ساخته شد (`db push` → `apply-sql.sh` → `test-schema-fixups.sql`؛ انتظار `staff=0 tables=72 idx079=1`). شش اجرا لازم شد و هر شکست یک درسِ واقعی بود:

| # | نتیجه | ریشه‌ی واقعی |
|---|---|---|
| ۱ | ۱۱ شکست | DBِ آلوده از اجرای دیروز (P2002 روی شماره‌های ثابتِ owner + ایندکسِ ۰۷۹) |
| ۲ | **hang ۱۷ دقیقه** | همان P0-005ِ CI — بلافاصله بعد از شکستِ file-levelِ `provision-slug-validation` |
| ۳ | ۲۹ شکست | بازمانده‌ی دو اجرای ناتمامِ خودم (`code`/`authority`/`phone` یکتا) |
| ۴ | ۱۱ شکست | ۸ از `.env` (`ADMIN_LOGIN_ENABLED=true`) · ۲ shared-state · ۱ relationِ ناموجود |
| ۵ | نامعتبر | خودم DB را زیرِ سوئیتِ پس‌زمینه‌ی خودم DROP کردم |
| ۶ | **۱۵۲۸/۱۵۲۸** | foreground + DBِ تازه |

گیت‌های دیگر (همه با کدِ خروج): `tsc` 0 · `lint` 0 · `sync-design-system --check` 0 · `build-standalone --check` 0 · `xss-escaping-regression` 0 · `xss-sink-audit --check` 0 (خطِ پایه‌ی unsafe ۶۵ دست‌نخورده).

---

## ۴. Wave 0 — چه کامیت و push شد

| کامیت | محتوا |
|---|---|
| `d1e7365` | فلگِ `admin_otp_login_enabled` (DEFAULT_OFF): `auth/admin/request\|verify` خاموش → **۴۰۴** پیش از ریت‌لیمیت و پارس (نه ۴۰۳)؛ `GET /auth/admin/login` پرچمِ `otp_login_enabled`؛ پنلِ شرکت دکمه‌ی «ورود با پیامک» را فقط با آن می‌سازد (نه `display:none`). ۱۱ فایل + تستِ ۹ ادعایی؛ جهشِ ۴۰۴→۴۰۳ → `EXIT=1` (گیت واقعاً می‌سنجد). |
| `dbec99f` | پنج رفعِ تست (بندِ ۵) + `audit/round-15/` (PLAN، ground-truth.json، چهار mandate) + گزارشِ راستی‌آزماییِ raceِ owner. |
| `a2cf39d` | **رفعِ رگرسیونِ خودم (P0-013):** `d1e7365` دکمه‌ی «ورود با پیامک» را مشروط کرد ولی ۶ specِ e2e (۹ محلِ ورود) بدونِ mockِ `GET /auth/admin/login` واردِ پنلِ شرکت می‌شدند → `#adminPhone` هرگز ساخته نمی‌شد → ۶۰ ثانیه × ۲ retry → jobِ e2e cancelled در ۲۴.۴ دقیقه. helperِ مشترکِ `mockAdminOtpFlag(page, enabled)` + یک تستِ منفی (با `enabled=false` دکمه در DOM نیست). اسموک ۱۱ passed؛ جهش → EXIT=1، بازگردانی → EXIT=0. |
| `37b2a82` | **A3-001 (یافته‌ی عاملِ A3، تأییدِ من):** سوییچِ فلگِ تازه هرگز رندر نمی‌شد — نقشه‌ی برچسبِ کلاینت ۶ کلید داشت، سرور ۷. برچسب اضافه شد؛ overrideهای XSS که به شماره‌ی خط پین بودند (۸۶۲→۸۶۵، ۳۱۶۱→۳۱۶۴) دوباره پین شدند؛ `xss --check` ۰ روی ۶۵. |
| `580cf7f` | **A2-001 (blockerِ عاملِ A2، تأییدِ من):** ورودِ پنلِ بیزنس با رمز — تنها مسیرِ تولید — از `6301ff1` (۰۸-۲۶) `ReferenceError` می‌داد: سه فراخوانی به `enterStaffPanel` که هیچ‌جا تعریف نشده؛ هیچ e2eای این مسیر را نمی‌زد. رفع + `business-password-login.spec.ts` (mockِ `POST /auth/staff/login` → overlay بسته، بدونِ pageerror). spec EXIT=0؛ جهش → EXIT=1؛ بازگردانی → EXIT=0. |

pushها: `660456b..dbec99f` → `..a2cf39d` → `..580cf7f`، همه exit 0. کامیتِ `426ff3b` (تحقیقِ باشگاه، فقط سند) هم که push نشده بود همراهِ اولی رفت.

---

## ۵. یافته‌های فازِ ۰ (شاهدِ کامل در `audit/round-15/ground-truth.json`)

| ID | شدت | یافته | وضعیت |
|---|---|---|---|
| **P0-001** | blocker | مسیرِ OTPِ ادمین principalِ platform-admin را **بدونِ TOTP** صادر می‌کرد (`verify/route.ts:23,30`)؛ رفعش ۱۳ ساعت commit‌نشده در worktree | ✅ `d1e7365` |
| **P0-005** | blocker | `npm test` hang — محلی ۱۷ دقیقه، CI ۲۶۴+ دقیقه؛ jobِ `test` بدونِ timeout | ✅ ریشه رفع (P0-002)، **تأیید در CI: ۲.۸ دقیقه**؛ گارد هنوز نیست → A6 |
| **P0-008** | blocker | Vercel ۰ پروژه · Sentry ۰ پروژه | ❌ باز → A10 |
| P0-002 | major | `provision-slug-validation.after()` روی relationِ ناموجودِ `AuditLog.restaurant` می‌مرد → `$disconnect()` هرگز اجرا نمی‌شد → **همین علتِ hang** | ✅ `dbec99f` |
| P0-003 | major | شماره‌های ثابتِ owner + ایندکسِ ۰۷۹ → هر اجرای ناتمام اجرای بعدی را می‌کشد | ✅ `dbec99f` |
| P0-004 | major | آینه‌ی دستیِ `DEFAULT_OFF_KEYS` فاقدِ `admin_otp_login_enabled` — تست کارش را کرد | ✅ `dbec99f` |
| P0-007 | major | `platform_settings` **plaintext** (`platform-settings.ts:23-24`؛ صفر cipher در `src/lib`) — برخلافِ تصمیمِ §۳ | ❌ باز → A5 |
| P0-011 | major | سوئیت هرمتیک نبود: `api/.env` با `ADMIN_LOGIN_ENABLED=true` ۲۲ تست را با ۴۰۱ می‌کشت، CI (بی‌`.env`) سبز بود | ✅ `dbec99f` (pin در `test-env.mts` + `password-login`) |
| P0-012 | major | runnerِ تک‌پروسه هوک‌های root-levelِ فایل‌ها را در هم می‌بافد (`clearFlags()` فلگِ فایلِ دیگر را پاک می‌کرد) | ✅ `dbec99f` (`beforeEach`) → A6 sweep |
| P0-006 | minor | کلاینتِ Prisma محلی کهنه‌تر از schema → خطاهایی که CI نمی‌بیند | ✅ محلی → A6 گارد |
| P0-009 | minor | برنامه به `prisma/migrations/manual/` ارجاع می‌دهد؛ وجود ندارد | ثبت |
| P0-010 | minor | کارِ تکراری بینِ سشن‌های موازی (modifiersJson دو بار در یک فایل) | ✅ حذف؛ قاعده در PLAN |

### چهار blockerِ منتقل‌شده از دورهای قبل

| | وضعیت | شاهد |
|---|---|---|
| S2 پاسخِ null = موفقیت | ✅ رفع | `sms.ts:101-106` (`meliAccepted(null)=false`)، `:296` |
| P2002→409 | ✅ رفع + **امروز قرمزشدنش اثبات شد** (`DROP INDEX` → EXIT=1) | `provisioning.ts:192` و دو مسیرِ دیگر |
| رمزنگاریِ اعتبارنامه‌ها | ❌ plaintext | P0-007 |
| چرخشِ کلیدِ ملی‌پیامک | ؟ از مخزن قابلِ تأیید نیست | فقط شما |

### تصحیحِ گزارشِ قبلی
ردیفِ `+989265500665` که به‌عنوانِ «شاهدِ زنده‌ی یک نفر در دو تنانت» آورده بودم، بازمانده‌ی فیکسچرِ تست بود. استدلالِ دامنه‌ای (قیدِ جزئی `WHERE role='owner'`) سرِ جایش است؛ شاهدش نبود.

---

## ۶. سه سشنِ موازی — وضعیتِ هماهنگی

| سشن | شاخه | آخرین ردِ واقعی |
|---|---|---|
| TOTP | `feat/admin-totp-login` → #81 | کارِ ۱۴ فایلی‌اش ۰۹-۰۲ ۱۷:۵۱ رها شده بود → امروز کامیت شد |
| Intelligence/ML | `claude/rezervno-intelligence-platform-w7wcty` | ۰۹-۰۳ ۰۱:۳۴ push؛ `docs/ml/` هنوز untracked در همین پوشه |
| XSS tools | `fix/xss-audit-tool-windows-paths` (پوشه‌ی `Rezv`) | آخرین کامیت یک `Revert` |

پیام‌رسانیِ بین‌سشنی خاموش است؛ نظارت فقط از روی مخزن. قاعده‌ی هماهنگی (در PLAN): پیش از ویرایشِ هر سندِ مشترک `git log --all -- <file>`.

---

## ۷. فازِ ۱ — سرشماریِ واقعیتِ قابلیت‌ها (دورِ ۱۶) — گیت گذشت

اولین راه‌اندازیِ عامل‌ها (~۰۷:۳۰) با **session limit (429)** مُرد؛ دومی ۱۰:۰۳. سه سرشماری کامل شدند؛ A6 (یکپارچگیِ تست) دو بار مُرد و هنوز اجرا نشده (ریستِ بعدی ۱۴:۴۰). روش: ردیابیِ ایستا در `dbec99f` (بدونِ API زنده — DB در اختیارِ A6 بود). آرتیفکتِ گیت: `audit/round-16/feature-census.json` + `PHASE1-REPORT.md`؛ گزارشِ هر عامل: `audit/round-16/A{1,2,3}-REPORT.md`.

| اپ | پوشش | REAL | PARTIAL | DEMO | FAKE | DEAD | blocker | major | minor | نمونه‌گیریِ من |
|---|---|---|---|---|---|---|---|---|---|---|
| customer (A1) | ۱۰۱/۱۰۱ | ۷۲ | ۱۹ | ۱ | ۴ | ۵ | ۰ | ۱۱ | ۱۷ | ۵ نمونه: ۴ تأیید، ۱ محتمل، ۰ رد |
| business (A2) | ۲۴۵/۲۴۵ | ۱۰۳ | ۳۰ | ۰ | ۵ | ۵ | **۲** | ۱۶ | ۱۷ | ۵ نمونه: ۵ تأیید، ۰ رد |
| company (A3) | ۸۱/۸۱ | ۷۰ | ۷ | ۱ | ۰ | ۳ | ۰ | ۸ | ۶ | ۵ نمونه: ۵ تأیید، ۰ رد |
| **جمع** | ۳۲۵ ردیف | ۲۴۵ | ۵۶ | ۲ | ۹ | ۱۳ | **۲** | ۳۵ | ۴۰ | هر سه پذیرفته |

**دو blocker — هر دو در پنلِ بیزنس:**

- **A2-001** ورود با رمز `ReferenceError` می‌داد (`enterStaffPanel` تعریف‌نشده، از ۰۸-۲۶ روی main) → **رفع شد `580cf7f`** با تستِ falsifiable.
- **A2-002** دکمه‌های ردیفِ رزرو با `RES[i]` (فهرستِ امروزِ داشبورد) کار می‌کنند در حالی که ردیف‌ها از آرایه‌ی fetch‌شده‌ی جدا رندر می‌شوند → در تب‌های فردا/آینده/گذشته **رزروِ اشتباه** تغییرِ وضعیت می‌گیرد (`reservations.js:54-58,65,179,200,205` — تأییدِ من). **باز** → Wave 1.

**چند یافته‌ی majorِ نمونه (فهرستِ کامل در گزارش‌ها):** SWِ اپِ مشتری پاسخِ GETهای احرازشده (`/me/*`) را کش می‌کند (A1-001) · نوارِ «پیشنهاد هوشمند برای تو» هاردکد با `openRest(6)` و بدونِ هیچ منطق (A1-003 — نقضِ قانونِ صداقتِ AI) · وعده‌ی «۵۰۰ امتیاز برای هر دعوت» که بک‌اند هرگز نمی‌دهد (A1-005) · دیتای دمو بعد از هر شکستِ fetch در پنلِ بیزنس به‌عنوانِ واقعی رندر می‌شود — waitlist/staff/tables (A2-006/7/8) · «افزودنِ شعبه» که پیام‌های ۴۰۹ به آن ارجاع می‌دهند وجود ندارد؛ `adminCreateBranch` صفر caller (A3-006) · رمزِ کسب‌وکار در پنلِ شرکت `type="text"` (A3، نشتِ راز روی صفحه) · رمزِ اولیه‌ی مالک نه system-generated است نه shown-once/forced-change (A3-007، برخلافِ §۳).

`src-v2`: از قبل در `95e95f1` حذف شده — متنِ برنامه کهنه بود.

---

## ۸. تصمیم‌های شما (فقط تصمیم — فکت‌ها بالا verify شده‌اند)

1. **merge #81 روی `580cf7f`** — سرِ فعلیِ PR با هر ۱۲ چک سبز (e2e سه پروفایل ۱۴.۲ دقیقه، test ۲.۸ دقیقه). من نمی‌توانم (`gh` باطل). همه‌ی شرط‌هایی که گذاشتید برقرار است: محلی ۱۵۲۸/۱۵۲۸ روی DBِ صفر، CI بدونِ hang، e2e سه پروفایل.
2. **Supabase:** پروژه را بیدار کنید یا اجازه‌ی `execute_sql` بدهید، یا خودتان دو کوئریِ داخلِ `ground-truth.json` را بزنید. تا آن موقع drift/RLS «نمی‌دانیم» است، نه «سالم».
3. **چرخشِ کلیدِ ملی‌پیامک** — انجام شده یا نه؟
4. **`timeout-minutes` روی `test` + بلاکِ `concurrency`** روی CI — runِ گیرکرده‌ی قدیمی شش ساعتِ کامل سوزاند تا GitHub خودش کشتش. A6 پیشنهادِ دقیق و اثباتِ قرمزشدن می‌دهد؛ اعمالش تصمیمِ شماست.
5. **A2-002 (رزروِ اشتباه در تب‌های غیرِ امروز)** — blockerِ باز؛ توصیه: اولین کارِ Wave 1، با تستی که ردیفِ tabِ «فردا» را می‌زند و ID درست را در PATCH می‌سنجد.

**ترتیبِ موج‌ها:** Wave 1 = A6 (بعد از ۱۴:۴۰) ‖ رفعِ A2-002 ‖ A10 برای Vercel/Sentry (مستقل از کد، همین حالا شروع‌شدنی) → Wave 2 = A5 (رمزنگاریِ `platform_settings`، رمزِ اولیه‌ی مالک، `type="text"`) ‖ A4 → Wave 3 = A10 بقیه (SMS pattern، backup drill، load) → A7/A8/A9 (با ۹ FAKE و ۵۶ PARTIALِ فازِ ۱ به‌عنوانِ ورودی) → scorecard.
