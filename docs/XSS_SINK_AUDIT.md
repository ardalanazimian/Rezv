# XSS Sink Audit

> Generated by `tools/xss-sink-audit.mjs` on 2026-09-11T09:56:39.371Z. Auditor tone — this is a heuristic scan (regex-based, not real dataflow analysis), not a formal proof.

Scanned: apps/customer, apps/business, apps/company, shared/js (enforced — non-zero `unsafe` fails the script). Report-only (not enforced): demo-mvp, standalone.

## Summary

| Classification | Count |
|---|---|
| `dom_api_safe` | 89 |
| `safe_static` | 50 |
| `unsafe` | 42 |
| `escaped` | 25 |
| `review` | 18 |
| **Total sinks found** | **224** |

- `safe_static` — sink argument is a literal/constant string, no interpolated variable.
- `escaped` — the sink expression routes through `esc(` before use.
- `dom_api_safe` — line uses `textContent`, not an HTML sink, or is a benign adjacent match.
- `payload_not_captured` — **the scanner never saw the inserted content.** The expression extractor stops before the payload argument (every `insertAdjacentHTML` sink), so no verdict about that content has been earned. It is NOT a finding and NOT a clearance: it marks a sink a human must read. Previously these were labelled `safe_static`, which claimed something the tool had not checked.
- `unsafe` — a sink interpolates a variable without going through `esc(`. **Must be zero for this script to exit 0.**
- `review` — `eval`/`new Function` calls; always flagged for manual review regardless of content.

## Findings requiring attention

| File:Line | Kind | Classification | Snippet |
|---|---|---|---|
| `apps/customer/js/auth.js:238` | innerHTML | `review` | `document.getElementById('sheetBody').innerHTML=html;` |
| `apps/customer/js/data/booking.js:160` | innerHTML | `unsafe` | `sel.innerHTML=(r.slots.length?r.slots:FALLBACK_TIMES).map(s=>`<option value="${s}">${s} (نمونه)</option>`).join('');` |
| `apps/customer/js/data/booking.js:183` | innerHTML | `review` | `sel.innerHTML=open.map(s=>`<option value="${esc(s.time)}">${faTime(s.time)}</option>`).join('');` |
| `apps/customer/js/data/booking.js:595` | innerHTML | `review` | `sheetBody.innerHTML=`` |
| `apps/customer/js/data/detail.js:158` | innerHTML | `review` | `document.getElementById('page-rest').innerHTML=`` |
| `apps/customer/js/data/discover.js:64` | innerHTML | `unsafe` | `if(box) box.innerHTML = slotsHTML(r);` |
| `apps/customer/js/data/discover.js:203` | innerHTML | `unsafe` | `f.innerHTML=list.map(()=>`<div class="rc" style="opacity:1;transform:none"><div class="rc-img sk" style="border-radius:0"></div><div class="rc-body"><div class=` |
| `apps/customer/js/data/discover.js:206` | innerHTML | `unsafe` | `f.innerHTML=list.map(cardHTML).join('') + moreBtnHTML(list);` |
| `apps/customer/js/data/discover.js:248` | innerHTML | `unsafe` | `document.getElementById('feedTitle').innerHTML=v==='all'?icon('flame',{size:16,fill:true})+' محبوب امشب':esc(el.textContent.trim());` |
| `apps/customer/js/data/discover.js:294` | innerHTML | `unsafe` | `el.innerHTML=withDist.map(({r,km})=>hCardHTML(r,` |
| `apps/customer/js/data/discover.js:301` | innerHTML | `unsafe` | `el.innerHTML=[...R].sort((a,b)=>(b.rt\|\|0)-(a.rt\|\|0)).slice(0,6).map(r=>hCardHTML(r,'')).join('');` |
| `apps/customer/js/data/discover.js:327` | innerHTML | `unsafe` | `el.innerHTML=trend.map((r,i)=>hCardHTML(r,i<2?`${icon('flame',{size:12,fill:true})} داغ`:'')).join('');` |
| `apps/customer/js/data/discover.js:361` | innerHTML | `unsafe` | `el.innerHTML=eventsHtml(events,false);` |
| `apps/customer/js/data/discover.js:365` | innerHTML | `unsafe` | `el.innerHTML=`<div class="empty-state"><div class="empty-state-icon">${icon('alert',{size:40})}</div><div class="empty-state-title">رویدادها بارگذاری نشد</div><` |
| `apps/customer/js/data/discover.js:368` | innerHTML | `unsafe` | `el.innerHTML=eventsHtml(SAMPLE_EVENTS,true);` |
| `apps/customer/js/features/chat.js:52` | innerHTML | `unsafe` | `body.innerHTML = `<div class="chat-empty">${msg}</div>`;` |
| `apps/customer/js/features/chat.js:58` | innerHTML | `review` | `body.innerHTML = items.map(t => `` |
| `apps/customer/js/features/economy.js:116` | innerHTML | `unsafe` | `root.innerHTML=`<div class="wrap section">` |
| `apps/customer/js/features/food-dna.js:63` | innerHTML | `unsafe` | `document.getElementById('dnaProgress').innerHTML = Array.from({length:nSlides},()=>'<div class="dna-progress-bar"><i></i></div>').join('');` |
| `apps/customer/js/features/food-dna.js:88` | innerHTML | `review` | `wrap.innerHTML = slides.map((s,i)=>`` |
| `apps/customer/js/features/food-dna.js:191` | innerHTML | `review` | `document.getElementById('page-profile').innerHTML=`<div class="wrap section">` |
| `apps/customer/js/features/food-dna.js:261` | outerHTML | `unsafe` | `host.outerHTML =` |
| `apps/customer/js/features/loyalty.js:69` | innerHTML | `review` | `root.innerHTML=`<div class="wrap section">` |
| `apps/customer/js/features/notifications.js:150` | innerHTML | `unsafe` | `tabs.innerHTML = Object.entries(CATS).map(([k,v])=>`<button class="notif-tab ${k===_filter?'on':''}" role="tab" aria-selected="${k===_filter}" data-k="${k}">${v` |
| `apps/customer/js/features/notifications.js:160` | innerHTML | `review` | `list.innerHTML = items.map(n=>{` |
| `apps/customer/js/features/palette.js:99` | innerHTML | `unsafe` | `list.innerHTML = _items.length ? _items.map(itemHTML).join('') : '<li class="cmdk-empty">چیزی پیدا نشد</li>';` |
| `apps/customer/js/features/rewards.js:136` | innerHTML | `unsafe` | `el.innerHTML=`<div class="gift-check ${d.valid?'valid':'invalid'}">` |
| `apps/customer/js/features/trips.js:105` | innerHTML | `unsafe` | `box.innerHTML = `<div style="background:#fff;border-radius:8px;padding:8px;display:inline-block">` |
| `apps/customer/js/reservation.js:22` | innerHTML | `unsafe` | `empty.style.display='none';grid.innerHTML=list.map(cardHTML).join('');grid.querySelectorAll('.rc').forEach(c=>c.classList.add('in'));` |
| `apps/customer/js/reservation.js:128` | innerHTML | `unsafe` | `listEl.innerHTML=skTrip.repeat(3);` |
| `apps/customer/js/reservation.js:152` | innerHTML | `review` | `listEl.innerHTML=summary+trips.map(t=>{` |
| `apps/customer/js/user-profile.js:85` | innerHTML | `review` | `now.innerHTML = ready` |
| `apps/customer/js/waitlist.js:61` | innerHTML | `review` | `sheetBody.innerHTML=`` |
| `apps/customer/js/waitlist.js:92` | innerHTML | `review` | `sheetBody.innerHTML=`` |
| `apps/business/js/crm.js:1094` | innerHTML | `unsafe` | `document.getElementById('ct-campaign').innerHTML=`` |
| `apps/business/js/crm.js:1153` | innerHTML | `unsafe` | `if(chatHtml){ el.innerHTML=chatHtml+`<div id="aiCards"><div style="text-align:center;padding:30px;color:var(--t2)">در حال بارگذاری پیشنهادها...</div></div>`; }` |
| `apps/business/js/loyalty.js:42` | innerHTML | `unsafe` | `document.getElementById('v-loyalty').innerHTML=`` |
| `apps/business/js/menu.js:73` | innerHTML | `unsafe` | `el.innerHTML = `` |
| `apps/business/js/menu.js:291` | innerHTML | `unsafe` | `box.innerHTML = `` |
| `apps/business/js/overview.js:172` | innerHTML | `review` | `document.getElementById('v-overview').innerHTML = `` |
| `apps/business/js/overview.js:293` | innerHTML | `unsafe` | `document.getElementById('peakChart').innerHTML=slots.map(h=>{` |
| `apps/business/js/overview.js:368` | innerHTML | `unsafe` | `document.getElementById('insights').innerHTML=insights.slice(0,4).map(i=>`` |
| `apps/business/js/reservations.js:92` | innerHTML | `unsafe` | `el.innerHTML=`<div class="empty-state"><div class="empty-state-icon">${resDate==='past'?icon('inbox',{size:40}):icon('search',{size:40})}</div><div style="font-` |
| `apps/business/js/reservations.js:107` | innerHTML | `unsafe` | `el.innerHTML=demoNote+`` |
| `apps/business/js/staff-system.js:56` | innerHTML | `unsafe` | `document.getElementById('v-staff').innerHTML=(isDemo?`<div class="cash-note">${icon('info',{size:13})} فهرستِ زیر نمونه است، کارکنانِ واقعیِ تو نیست.</div>`:'')` |
| `apps/business/js/staff-system.js:127` | innerHTML | `review` | `document.getElementById('modalBody').innerHTML = html;` |
| `apps/business/js/staff-system.js:232` | innerHTML | `unsafe` | `document.getElementById('popupIc').innerHTML=iconName?icon(iconName,{size:18}):'';` |
| `apps/business/js/staff-system.js:292` | innerHTML | `unsafe` | `document.getElementById('v-cashback').innerHTML=`` |
| `apps/business/js/staff-system.js:578` | innerHTML | `unsafe` | `document.getElementById('loginCard').innerHTML = `` |
| `apps/business/js/waitlist.js:45` | innerHTML | `unsafe` | `document.getElementById('v-waitlist').innerHTML=wlDemoNote+`` |
| `apps/business/js/waitlist.js:488` | innerHTML | `unsafe` | `document.getElementById('toastMsg').innerHTML=msg+` <button class="undo-btn" onclick="${undoFn}();document.getElementById('toast').classList.remove('show')">باز` |
| `apps/company/js/intelligence.js:353` | innerHTML | `review` | `document.getElementById('v-aihealth').innerHTML=`` |
| `apps/company/js/intelligence.js:584` | innerHTML | `unsafe` | `document.getElementById('v-security').innerHTML=(mqRes.ok&&mqRes.data?moderationQueuePanelHTML(mqRes.data):unavailablePanelHTML('خلاصه‌ی صفِ نظارت خوانده نشد — ` |
| `apps/company/js/intelligence.js:667` | innerHTML | `unsafe` | `document.getElementById('v-security').innerHTML=unavailablePanelHTML('صفحه‌ی امنیت بارگذاری نشد');` |
| `apps/company/js/intelligence.js:719` | innerHTML | `unsafe` | `box.innerHTML=renderCustomer360(res.data);` |
| `apps/company/js/intelligence.js:865` | innerHTML | `unsafe` | `document.getElementById('loginCard').innerHTML = `` |
| `apps/company/js/intelligence.js:983` | innerHTML | `unsafe` | `document.getElementById('loginCard').innerHTML = `` |
| `apps/company/js/overview.js:156` | innerHTML | `review` | `document.getElementById('modalBody').innerHTML = html;` |
| `apps/company/js/restaurant.js:76` | innerHTML | `unsafe` | `box.innerHTML = `` |
| `apps/company/js/sales.js:125` | innerHTML | `review` | `el.innerHTML = `` |

## Manually reviewed (not auto-classified safe)

The regex classifier flagged these as `unsafe`/`review`; each was read by hand and reclassified with a justification (see `MANUAL_REVIEW_OVERRIDES` in `tools/xss-sink-audit.mjs`). Each override is keyed on a hash of the sink expression itself, so moving code keeps the review attached and CHANGING the sink drops it (the gate then goes red). The line number below is where the sink sat when the report was generated — it is a pointer for the reader, not the key.

| File:Line | Justification |
|---|---|
| `apps/customer/js/features/chat.js:134` | bubble(m) دو درج دارد: esc(m.body) — escaperِ واقعیِ [&<>"'] — و faTime(m.created_at) که new Date(iso).toLocaleTimeString(fa-IR) است و برای هر ورودی فقط رقم/جداکننده می‌دهد (ورودیِ نامعتبر: catch → رشته‌ی خالی). هیچ مسیری برای markup نیست. خوانده‌شده ۲۰۲۶-۰۹-۰۵. |
| `apps/customer/js/features/live-strip.js:49` | out فقط از pill(fmtFa(عدد)) ساخته می‌شه — بدونِ متنِ کاربر/API. هر سه درج پشتِ گاردِ `Number(d.x) > 0` هستند، پس رشته‌ای که `<` داشته باشد NaN می‌شود و اصلاً رندر نمی‌شود؛ fmtFa هم `n.toLocaleString('fa-IR')` است که فقط رقم/جداکننده تولید می‌کند. ۲۰۲۶-۰۹-۰۴: کلید از :38 به :49 جابه‌جا شد چون راندِ ۲۰ شاخه‌ی res.ok را افزود؛ خودِ سینک دوباره دستی خوانده شد، نه فقط شماره‌خطِ جدید جایگزین. |
| `apps/customer/js/features/loyalty.js:25` | perksBlock() از PERKS محلی (shared/js seed) می‌سازه؛ tier فقط esc(b.name) بعدِ رفعِ این PR. |
| `apps/business/js/assistant.js:41` | assistantAppend(html) یک پارامترِ خام می‌گیرد، پس قاعده‌ی شناسه‌ی خام درست review می‌دهد. هر شش فراخوان خوانده شد: :51 esc(message) · :57 pendingId که ap+Date.now() است · :65 رشته‌ی literal · :69 esc(d.answer) · :85 esc(res.data.answer) · :74 chips که عمداً خام است چون فقط از jsq(d.log_id)، jsq(s.intent) و esc(s.label) ساخته شده — و jsq خودش esc(JSON.stringify(String(v))) است. خوانده‌شده ۲۰۲۶-۰۹-۰۵ (و مستقلاً توسطِ دو نشستِ دیگر). |
| `apps/business/js/chat.js:24` | chatEsc در همه‌ی فیلدها استفاده شده؛ t.id ستونِ Postgres UUID (فرمت تضمین‌شده). |
| `apps/business/js/chat.js:75` | bizBubble(m) قرینه‌ی bubble است: chatEsc(m.body) همان پنج کاراکتر را escape می‌کند و chatTime(m.created_at) همان toLocaleTimeString است. خوانده‌شده ۲۰۲۶-۰۹-۰۵. |
| `apps/business/js/crm.js:132` | loadErrorBlock(title, retry) — هر دو آرگومان literalِ خودِ کدند؛ title با esc() می‌گذره و retry عمداً یک رشته‌ی **کد** برایِ onclick است (نه دیتا). |
| `apps/business/js/crm.js:136` | گالریِ عکس. تنها درجِ خامش `${g.emoji}` است و آن همیشه تهی است: ردیف‌های گالری در crm.js:79-80 از API با `emoji:''` ساخته می‌شوند. url/label/caption همگی از esc() رد می‌شوند؛ بقیه fa() روی شمارنده، ترنریِ رشته‌ی ثابت، و اندیسِ حلقه است. |
| `apps/business/js/crm.js:427` | همان loadErrorBlock مثلِ crm.js:132 — آرگومان‌ها literalِ کدند. |
| `apps/business/js/crm.js:441` | کارتِ نظرهای مهمان. متنِ نوشته‌ی مهمان و نامش در همین قالب esc() دارند؛ دو درجِ بازمانده یکی درصدِ محاسباتی (`d.count/totalCount*100`) و دیگری `Array.from({length:5},…icon('star'))` است — هیچ‌کدام متن حمل نمی‌کنند. |
| `apps/business/js/crm.js:582` | کارتِ سگمنت‌ها. همه‌ی درج‌ها fa() روی شمارنده/درصدِ محاسبه‌شده‌اند (`segTotal`، `s.count`، `p`) به‌علاوه icon()؛ نامِ سگمنت از نگاشتِ ثابتِ درون‌فایل می‌آید. |
| `apps/business/js/crm.js:660` | هوشِ مشتری. fa()/faOrDash() روی شمارنده‌ها، و `cards.slice(0,3).map(...)` که متنِ کارت‌ها را esc() می‌کند؛ شدت (`c.severity`) فقط با رشته‌های ثابت مقایسه می‌شود و به کلاسِ CSS تبدیل می‌شود. |
| `apps/business/js/crm.js:720` | فهرستِ پروفایلِ مشتری‌ها. `iqClr`/`iqFa`/`urgClr` نگاشت‌های ثابتِ درون‌فایل‌اند (crm.js:735-736)، `urg` از مقایسه‌ی عددیِ churn ساخته می‌شود، و نام/تلفنِ مشتری در همین قالب esc()/jsq() دارند. بقیه fa() روی امتیازهای عددی است. |
| `apps/business/js/crm.js:843` | ساعاتِ کاری. `dirtyBanner` (crm.js:838)، `hoursStatusBannerHTML()` (:817) و `formatHoursCompact()` (:808) هر سه داده‌شان را esc() می‌کنند؛ `HOURS_DOW_FA` نگاشتِ ثابت است و `d` کلیدِ روز از `HOURS_DOW_ORDER`. |
| `apps/business/js/crm.js:958` | سیاستِ کنسلی. `okBadge`/`waitBadge` دو رشته‌ی ثابتِ محلی با icon() هستند (crm.js:956-957)؛ بقیه ترنریِ رشته‌ی ثابت و fa() روی عدد است. |
| `apps/business/js/crm.js:1120` | لاگِ پیامک. `faRelative(l.created_at)` روی ISOِ سرور و `fa(l.recipients_count)` روی شمارنده؛ نامِ سگمنت از `SEG_FA` با esc() رد می‌شود و متنِ پیام esc() دارد. |
| `apps/business/js/crm.js:1166` | کارتِ پیشنهادهای هوشمند. `URG_FA` نگاشتِ ثابتِ درون‌فایل است (crm.js:1165) و `crmEffectivenessFa` خروجیِ عددی می‌دهد؛ عنوان و متنِ کارت esc() دارند. |
| `apps/business/js/marketing.js:56` | کوپن‌ها. `COUPON_KIND_FA[c.kind]\|\|c.kind` و `COUPON_SEG_FA[...]` روی enumهای دیتابیس‌اند — `CouponKind` = percent\|fixed\|free_item (schema.prisma:1133)، پس شاخه‌ی fallback هم فقط یک شناسه‌ی ثابت می‌دهد. کدِ کوپن esc() دارد و بقیه fa() است. |
| `apps/business/js/marketing.js:106` | قانونِ خودکار. `AUTOMATION_TRIGGER_FA[a.trigger]\|\|a.trigger` روی enumِ `AutomationTrigger` (schema.prisma:1186) است — پنج مقدارِ شناسه‌ای. `${k}`/`${v}` از همان نگاشتِ ثابت می‌آیند، `c.id` یک UUID در مقدارِ option است، و نامِ قانون esc() دارد. |
| `apps/business/js/marketing.js:387` | تحلیل‌ها. `trendChip` (marketing.js:384)، `dataSourceNote()` (data.js:653) و `rAnalyticsTail()` (:430) رشته‌های ساخته‌شده از icon()/ثابت‌اند؛ `d`/`l`/`p`/`v` عناصرِ آرایه‌های عددیِ محلیِ `A.*`‌اند و بقیه محاسبه‌ی درصد است. |
| `apps/business/js/menu.js:213` | برندینگِ منو. هیچ درجِ اثبات‌نشده‌ای ندارد: رنگ و نام از esc() رد می‌شوند و بقیه ثابت است. |
| `apps/business/js/menu.js:674` | گروه‌های افزودنی. نامِ گروه/آیتم esc() و شناسه‌ها jsq() دارند؛ `g.id` یک UUIDِ Prisma است، و `fa(g.min_select)`/`fa(g.max_select)`/`fnMoney(o.price_delta_toman)` عددی‌اند. |
| `apps/business/js/overview.js:334` | نقشه‌ی حرارتی. `html` در همان تابع (overview.js:326-335) از آرایه‌های محلیِ `days`/`slots` و `fa(v)` ساخته می‌شود — هیچ داده‌ی API‌ای در آن نیست. |
| `apps/business/js/overview.js:340` | مشتریانِ برتر. نام از esc() و از jsq() در onclick رد می‌شود، `c.seg==='vip'?…:''` ترنریِ ثابت است و `fa(c.visits)` عددی. |
| `apps/business/js/overview.js:389` | یادداشت‌های کارکنان. متنِ یادداشت `${esc(n.txt)}` است و `n.pinned?…:''` فقط یک attributeِ styleِ ثابت می‌گذارد. |
| `apps/business/js/reservations.js:25` | صفحه‌ی رزروها. `TONIGHT_FILTERS` آرایه‌ی ثابتِ درون‌فایل است (reservations.js:8) پس `f.l`/`f.v` رشته‌ی ثابت‌اند، onclick از jsq() رد می‌شود و بقیه icon() است. |
| `apps/business/js/reservations.js:121` | سرخطِ شمارشِ رزرو. `fa(list.length)`/`fa(seated)` عددی‌اند و عبارتِ جست‌وجوی کاربر `${esc(resQuery)}` است. |
| `apps/business/js/staff-system.js:210` | مرکزِ اعلان. `NOTIFS` آرایه‌ی محلی است (staff-system.js:172-)، `n.unread?…:''` ترنریِ ثابت، و `n.ic`/`n.emoji` از esc()/icon() رد می‌شوند. |
| `apps/business/js/staff-system.js:349` | قیمت‌گذاری هوشمند. `sugCard` (staff-system.js:331) رشته‌های آمده از API را عمداً esc() می‌کند (توضیحش همان‌جا ثبت است)، `dataSourceNote()` ثابت است و `toman(r.min_toman)` عددی. |
| `apps/business/js/waitlist.js:152` | پلانِ سالن. نام/برچسبِ میز از esc() رد می‌شوند و بقیه شمارنده‌ی اشغال و کلاسِ CSSِ ثابت است. |
| `apps/business/js/waitlist.js:333` | قالبِ چاپِ QRِ میز. `_tableQrSvg` خروجیِ `QRCode.toString(url,{type:'svg'})` از کتابخانه‌ی qrcode است که از endpointِ احرازشده‌ی `GET restaurant/tables/:id/qr` می‌آید (بازبینیِ ۲۰۲۶-۰۹-۰۵ که با تغییرِ هویتِ سینک بی‌لنگر شده بود؛ دوباره خوانده و تأیید شد). برچسبِ کنارش esc() دارد. |
| `apps/business/js/waitlist.js:429` | قالبِ چاپِ QRِ میز دو درج دارد: esc(_tableQrLabel) و _tableQrSvg خام. دومی از GET restaurant/tables/:id/qr می‌آید که withRestaurantAuth دارد و صریح table.restaurantId را با ctx.restaurant.id می‌سنجد. محتوایش خروجیِ QRCode.toString(url,{type:svg}) از کتابخانه‌ی qrcode است — هندسه‌ی ماژول‌ها، نه متنِ کاربر؛ و خودِ url هم سمتِ سرور از codeِ اختصاصیِ assignQrCode ساخته می‌شود، پس حتی ورودی‌اش هم مهاجم‌پذیر نیست. اسکنر نمی‌تواند داخلِ کتابخانه را ببیند، پس این ثبتِ دستی لازم است. ⚠️ اگر روزی منبعِ svg عوض شود (کتابخانه‌ی دیگر، یا svg از دیتای کاربر)، این درج یک تزریقِ مستقیم است — و چون خطِ سینک عوض نمی‌شود، این override همچنان اعمال می‌شود. منبع را عوض کردی، این را هم بازبینی کن. |
| `apps/company/js/badges.js:21` | نشان‌های پلتفرم. `b.isActive?'':'…'` ترنریِ ثابت است؛ نام و توضیحِ نشان esc() دارند. |
| `apps/company/js/hours.js:122` | صفِ تأییدِ ساعتِ کاری. شاخه‌های loading/demo رشته‌ی ثابت‌اند و `renderHoursChangeQueue()` (hours.js:153) داده‌اش را esc() می‌کند؛ `fa(HCHANGE_BADGE)` عددی است. |
| `apps/company/js/intelligence.js:22` | KPIهای هوشِ پلتفرم. همه‌ی درج‌ها fa() روی شمارنده‌اند و نامِ رستوران در نگاشت‌های تودرتو esc() دارد. |
| `apps/company/js/intelligence.js:57` | اشتراک‌ها. `PLAN_LABEL`/`STATUS_LABEL` نگاشت‌های ثابتِ data.js:23-24‌اند، `badgeCls` از مقایسه با رشته‌ی ثابت ساخته می‌شود، و بقیه fa() روی عدد است. |
| `apps/company/js/intelligence.js:205` | نوارِ وضعیتِ رستوران. `STATUS_LABEL[r.status]\|\|''` — fallbackش رشته‌ی تهی است، نه کلیدِ خام — و درجِ دیگر ترنریِ ثابت است. |
| `apps/company/js/intelligence.js:234` | سلامتِ سیستم. `healthMeta` جدولِ ثابتِ درون‌خطی است (intelligence.js:233) و بقیه fa() روی شمارنده‌های صف/وبهوک است. |
| `apps/company/js/missions.js:21` | ماموریت‌های پلتفرم. عنوان و شناسه esc() دارند؛ `m.status` فقط با رشته‌های ثابت مقایسه می‌شود و در یک `data-status` می‌نشیند، و بقیه fa() روی پاداش/هدفِ عددی است. |
| `apps/company/js/overview.js:37` | نمای کلیِ شرکت. `r.logo`/`r.grad` هرگز از API نمی‌آیند — `mapAdminRestaurant` (company/js/api.js:137-138) آن‌ها را فقط از ردیفِ نمونه‌ی محلی پر می‌کند و در همان‌جا «تزئینی» علامت خورده‌اند. `healthMeta`/`clvLine` محلی‌اند، نامِ رستوران esc() دارد و بقیه fa()/محاسبه‌ی درصد است. ⚠️ این سینک روی سقفِ ۴۰۰۰ کاراکتری می‌ایستد، پس ۷۸۱ کاراکترِ آخرش را طبقه‌بند اصلاً ندیده و من **مستقیم از فایل** خواندمش: ردیف‌های هشدارِ اشتراک‌اند با `esc(r.name)`، `fa(r.daysLeft)`، `icon()` و `jsq(r.id)` در onclick — هیچ درجِ خامی ندارد. |
| `apps/company/js/overview.js:125` | فهرستِ رستوران‌ها. همان نگاشتِ api.js:137-138 برای logo/grad؛ `statusText` از `STATUS_LABEL` و شاخه‌های ثابت ساخته می‌شود و نامِ رستوران esc() دارد. |
| `apps/company/js/photos.js:136` | صفِ بازبینیِ عکس. `renderPhotoQueue()` (photos.js:167) نشانی و کپشن را esc() می‌کند، `PHOTO_CATEGORY` نگاشتِ ثابت است و شاخه‌های loading/demo ثابت‌اند. |
| `apps/company/js/restaurant.js:11` | جزئیاتِ رستوران. logo/grad همین‌جا هم از esc() رد می‌شوند و `PLAN_LABEL` نگاشتِ ثابت است؛ بقیه fa() روی عدد. |
| `apps/company/js/restaurant.js:142` | باشگاهِ رستوران. `PLAN_LABEL[p]\|\|p` روی کلیدهای پلن (`apiR.plan\|\|'free'`) و بقیه fa() روی شمارنده‌هاست. |
| `apps/company/js/restaurant.js:176` | btn.innerHTML = label که خودش چهار خط بالاتر از همان دکمه خوانده شده (ذخیره/بازگرداندنِ برچسبِ دکمه حینِ لودینگ) — رفت‌وبرگشتِ markupِ خودِ عنصر، بدونِ ورودِ هیچ دادهٔ بیرونی. |

## Report-only paths (demo-mvp, standalone)

Not enforced (per scope: no mass rewrite of generated/duplicate frontends). 107 unsafe/review sink(s) found there — see `tools/xss-sink-audit-report.json` → `report_only_hits` for the full list.

## Re-running

```sh
node tools/xss-sink-audit.mjs
```

Deterministic given the same source tree. Regenerates both this file and `tools/xss-sink-audit-report.json`.
