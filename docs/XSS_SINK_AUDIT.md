# XSS Sink Audit

> Generated by `tools/xss-sink-audit.mjs` on 2026-08-26T08:57:44.236Z. Auditor tone — this is a heuristic scan (regex-based, not real dataflow analysis), not a formal proof.

Scanned: apps/customer, apps/business, apps/company, shared/js (enforced — non-zero `unsafe` fails the script). Report-only (not enforced): demo-mvp, standalone.

## Summary

| Classification | Count |
|---|---|
| `unsafe` | 66 |
| `safe_static` | 54 |
| `dom_api_safe` | 53 |
| `escaped` | 35 |
| `review` | 5 |
| **Total sinks found** | **213** |

- `safe_static` — sink argument is a literal/constant string, no interpolated variable.
- `escaped` — the sink expression routes through `esc(` before use.
- `dom_api_safe` — line uses `textContent`, not an HTML sink, or is a benign adjacent match.
- `unsafe` — a sink interpolates a variable without going through `esc(`. **Must be zero for this script to exit 0.**
- `review` — `eval`/`new Function` calls; always flagged for manual review regardless of content.

## Findings requiring attention

| File:Line | Kind | Classification | Snippet |
|---|---|---|---|
| `apps/customer/js/auth.js:238` | innerHTML | `review` | `document.getElementById('sheetBody').innerHTML=html;` |
| `apps/customer/js/data/booking.js:116` | innerHTML | `unsafe` | `sel.innerHTML=(r.slots.length?r.slots:FALLBACK_TIMES).map(s=>`<option value="${s}">${s} (نمونه)</option>`).join('');` |
| `apps/customer/js/data/booking.js:139` | innerHTML | `unsafe` | `sel.innerHTML=open.map(s=>`<option value="${s.time}">${faTime(s.time)}</option>`).join('');` |
| `apps/customer/js/data/booking.js:366` | innerHTML | `unsafe` | `party.innerHTML=Array.from({length:PARTY_MAX},(_,i)=>i+1)` |
| `apps/customer/js/data/detail.js:120` | innerHTML | `unsafe` | `document.getElementById('page-rest').innerHTML=`` |
| `apps/customer/js/data/discover.js:64` | innerHTML | `unsafe` | `if(box) box.innerHTML = slotsHTML(r);` |
| `apps/customer/js/data/discover.js:205` | innerHTML | `unsafe` | `f.innerHTML=list.map(()=>`<div class="rc" style="opacity:1;transform:none"><div class="rc-img sk" style="border-radius:0"></div><div class="rc-body"><div class=` |
| `apps/customer/js/data/discover.js:208` | innerHTML | `unsafe` | `f.innerHTML=list.map(cardHTML).join('') + moreBtnHTML(list);` |
| `apps/customer/js/data/discover.js:259` | innerHTML | `unsafe` | `document.getElementById('feedTitle').innerHTML=v==='all'?icon('flame',{size:16,fill:true})+' محبوب امشب':esc(el.textContent.trim());` |
| `apps/customer/js/data/discover.js:305` | innerHTML | `unsafe` | `el.innerHTML=withDist.map(({r,km})=>hCardHTML(r,` |
| `apps/customer/js/data/discover.js:312` | innerHTML | `unsafe` | `el.innerHTML=[...R].sort((a,b)=>(b.rt\|\|0)-(a.rt\|\|0)).slice(0,6).map(r=>hCardHTML(r,'')).join('');` |
| `apps/customer/js/data/discover.js:338` | innerHTML | `unsafe` | `el.innerHTML=trend.map((r,i)=>hCardHTML(r,i<2?`${icon('flame',{size:12,fill:true})} داغ`:'')).join('');` |
| `apps/customer/js/data/discover.js:372` | innerHTML | `unsafe` | `el.innerHTML=eventsHtml(events,false);` |
| `apps/customer/js/data/discover.js:376` | innerHTML | `unsafe` | `el.innerHTML=`<div class="empty-state"><div class="empty-state-icon">${icon('alert',{size:40})}</div><div class="empty-state-title">رویدادها بارگذاری نشد</div><` |
| `apps/customer/js/data/discover.js:379` | innerHTML | `unsafe` | `el.innerHTML=eventsHtml(SAMPLE_EVENTS,true);` |
| `apps/customer/js/features/chat.js:52` | innerHTML | `unsafe` | `body.innerHTML = `<div class="chat-empty">${msg}</div>`;` |
| `apps/customer/js/features/food-dna.js:63` | innerHTML | `unsafe` | `document.getElementById('dnaProgress').innerHTML = Array.from({length:nSlides},()=>'<div class="dna-progress-bar"><i></i></div>').join('');` |
| `apps/customer/js/features/food-dna.js:263` | outerHTML | `unsafe` | `host.outerHTML =` |
| `apps/customer/js/features/notifications.js:150` | innerHTML | `unsafe` | `tabs.innerHTML = Object.entries(CATS).map(([k,v])=>`<button class="notif-tab ${k===_filter?'on':''}" role="tab" aria-selected="${k===_filter}" data-k="${k}">${v` |
| `apps/customer/js/features/palette.js:99` | innerHTML | `unsafe` | `list.innerHTML = _items.length ? _items.map(itemHTML).join('') : '<li class="cmdk-empty">چیزی پیدا نشد</li>';` |
| `apps/customer/js/features/rewards.js:122` | innerHTML | `unsafe` | `el.innerHTML=`<div class="gift-check ${d.valid?'valid':'invalid'}">` |
| `apps/customer/js/features/trips.js:104` | innerHTML | `unsafe` | `box.innerHTML = `<div style="background:#fff;border-radius:8px;padding:8px;display:inline-block">` |
| `apps/customer/js/reservation.js:22` | innerHTML | `unsafe` | `empty.style.display='none';grid.innerHTML=list.map(cardHTML).join('');grid.querySelectorAll('.rc').forEach(c=>c.classList.add('in'));` |
| `apps/customer/js/reservation.js:101` | innerHTML | `unsafe` | `listEl.innerHTML=skTrip.repeat(3);` |
| `apps/customer/js/user-profile.js:85` | innerHTML | `review` | `now.innerHTML = ready` |
| `apps/customer/js/waitlist.js:92` | innerHTML | `unsafe` | `sheetBody.innerHTML=`` |
| `apps/business/js/crm.js:390` | innerHTML | `unsafe` | `document.getElementById('pt-reviews').innerHTML=`` |
| `apps/business/js/crm.js:531` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/crm.js:606` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/crm.js:666` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/crm.js:778` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/crm.js:1002` | innerHTML | `unsafe` | `document.getElementById('ct-campaign').innerHTML=`` |
| `apps/business/js/crm.js:1028` | innerHTML | `unsafe` | `el.innerHTML=logs.length?`<table class="tbl"><thead><tr><th>سگمنت</th><th>تاریخ</th><th>گیرنده</th><th>پیام</th></tr></thead><tbody>` |
| `apps/business/js/crm.js:1061` | innerHTML | `unsafe` | `if(chatHtml){ el.innerHTML=chatHtml+`<div id="aiCards"><div style="text-align:center;padding:30px;color:var(--t2)">در حال بارگذاری پیشنهادها...</div></div>`; }` |
| `apps/business/js/crm.js:1071` | innerHTML | `unsafe` | `cardsEl.innerHTML=`` |
| `apps/business/js/loyalty.js:42` | innerHTML | `unsafe` | `document.getElementById('v-loyalty').innerHTML=`` |
| `apps/business/js/marketing.js:56` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/marketing.js:106` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/marketing.js:341` | innerHTML | `unsafe` | `document.getElementById('v-analytics').innerHTML=`` |
| `apps/business/js/menu.js:60` | innerHTML | `unsafe` | `el.innerHTML = `` |
| `apps/business/js/menu.js:257` | innerHTML | `unsafe` | `box.innerHTML = `` |
| `apps/business/js/overview.js:159` | innerHTML | `unsafe` | `document.getElementById('v-overview').innerHTML = `` |
| `apps/business/js/overview.js:280` | innerHTML | `unsafe` | `document.getElementById('peakChart').innerHTML=slots.map(h=>{` |
| `apps/business/js/overview.js:321` | innerHTML | `review` | `document.getElementById('heatmap').innerHTML=html;` |
| `apps/business/js/overview.js:355` | innerHTML | `unsafe` | `document.getElementById('insights').innerHTML=insights.slice(0,4).map(i=>`` |
| `apps/business/js/reservations.js:91` | innerHTML | `unsafe` | `el.innerHTML=`<div class="empty-state"><div class="empty-state-icon">${resDate==='past'?icon('inbox',{size:40}):icon('search',{size:40})}</div><div style="font-` |
| `apps/business/js/reservations.js:106` | innerHTML | `unsafe` | `el.innerHTML=demoNote+`` |
| `apps/business/js/reservations.js:120` | innerHTML | `unsafe` | `el.innerHTML=demoNote+`<div style="font-size:13px;color:var(--t2);margin-bottom:14px;font-weight:600">${fa(list.length)} رزرو · ${fa(seated)} مهمان رسیده${resQu` |
| `apps/business/js/staff-system.js:56` | innerHTML | `unsafe` | `document.getElementById('v-staff').innerHTML=(isDemo?`<div class="cash-note">${icon('info',{size:13})} فهرستِ زیر نمونه است، کارکنانِ واقعیِ تو نیست.</div>`:'')` |
| `apps/business/js/staff-system.js:127` | innerHTML | `review` | `document.getElementById('modalBody').innerHTML = html;` |
| `apps/business/js/staff-system.js:232` | innerHTML | `unsafe` | `document.getElementById('popupIc').innerHTML=iconName?icon(iconName,{size:18}):'';` |
| `apps/business/js/staff-system.js:265` | innerHTML | `unsafe` | `document.getElementById('v-cashback').innerHTML=`` |
| `apps/business/js/staff-system.js:322` | innerHTML | `unsafe` | `document.getElementById('v-pricing').innerHTML=`` |
| `apps/business/js/staff-system.js:532` | innerHTML | `unsafe` | `document.getElementById('loginCard').innerHTML = `` |
| `apps/business/js/waitlist.js:145` | innerHTML | `unsafe` | `document.getElementById('v-floor').innerHTML=`` |
| `apps/business/js/waitlist.js:481` | innerHTML | `unsafe` | `document.getElementById('toastMsg').innerHTML=msg+` <button class="undo-btn" onclick="${undoFn}();document.getElementById('toast').classList.remove('show')">باز` |
| `apps/company/js/hours.js:122` | innerHTML | `unsafe` | `el.innerHTML = `` |
| `apps/company/js/intelligence.js:57` | innerHTML | `unsafe` | `document.getElementById('v-billing').innerHTML=`` |
| `apps/company/js/intelligence.js:205` | innerHTML | `unsafe` | `document.getElementById('v-support').innerHTML=`` |
| `apps/company/js/intelligence.js:234` | innerHTML | `unsafe` | `document.getElementById('v-systemhealth').innerHTML=`` |
| `apps/company/js/intelligence.js:353` | innerHTML | `unsafe` | `document.getElementById('v-aihealth').innerHTML=`` |
| `apps/company/js/intelligence.js:581` | innerHTML | `unsafe` | `document.getElementById('v-security').innerHTML=(mqRes.ok&&mqRes.data?moderationQueuePanelHTML(mqRes.data):unavailablePanelHTML('خلاصه‌ی صفِ نظارت خوانده نشد — ` |
| `apps/company/js/intelligence.js:664` | innerHTML | `unsafe` | `document.getElementById('v-security').innerHTML=unavailablePanelHTML('صفحه‌ی امنیت بارگذاری نشد');` |
| `apps/company/js/intelligence.js:716` | innerHTML | `unsafe` | `box.innerHTML=renderCustomer360(res.data);` |
| `apps/company/js/intelligence.js:915` | innerHTML | `unsafe` | `document.getElementById('loginCard').innerHTML = `` |
| `apps/company/js/overview.js:37` | innerHTML | `unsafe` | `document.getElementById('v-overview').innerHTML=`` |
| `apps/company/js/overview.js:155` | innerHTML | `review` | `document.getElementById('modalBody').innerHTML = html;` |
| `apps/company/js/photos.js:136` | innerHTML | `unsafe` | `el.innerHTML = `` |
| `apps/company/js/restaurant.js:74` | innerHTML | `unsafe` | `box.innerHTML = `` |
| `apps/company/js/restaurant.js:140` | innerHTML | `unsafe` | `document.getElementById('v-analytics').innerHTML=`` |
| `apps/company/js/sales.js:114` | innerHTML | `unsafe` | `el.innerHTML = `` |

## Manually reviewed (not auto-classified safe)

The regex classifier flagged these as `unsafe`/`review`; each was read by hand and reclassified with a justification (see `MANUAL_REVIEW_OVERRIDES` in `tools/xss-sink-audit.mjs`). Re-review if the cited line ever changes.

| File:Line | Justification |
|---|---|
| `apps/customer/js/features/economy.js:106` | missionCard(m) دیگه esc(m.title)/esc(m.description) داره (رفع‌شده در همین PR). |
| `apps/customer/js/features/live-strip.js:38` | out فقط از pill(fmtFa(عدد)) ساخته می‌شه — بدونِ متنِ کاربر/API. |
| `apps/customer/js/features/loyalty.js:25` | perksBlock() از PERKS محلی (shared/js seed) می‌سازه؛ tier فقط esc(b.name) بعدِ رفعِ این PR. |
| `apps/customer/js/features/loyalty.js:64` | badges.map دیگه esc(b.name) داره (رفع‌شده در همین PR)؛ tier.emoji/tier.name از enumِ ثابتِ سطحِ باشگاهه. |
| `apps/business/js/chat.js:24` | chatEsc در همه‌ی فیلدها استفاده شده؛ t.id ستونِ Postgres UUID (فرمت تضمین‌شده). |
| `apps/business/js/crm.js:89` | logoPhoto.url دیگه esc شده (رفع‌شده در همین PR). |
| `apps/business/js/waitlist.js:38` | wlCard(w,i) از قبل esc(w.name) داشت — بررسی شد. |
| `apps/company/js/badges.js:21` | BADGES_LIST.map از قبل esc(b.name)/esc(b.description)/... داشت — بررسی شد. |
| `apps/company/js/intelligence.js:22` | fa(d.guests.total_clv_toman) + rfm_distribution.map — اعداد/توابعِ trusted. |
| `apps/company/js/missions.js:21` | MISSIONS_LIST.map از قبل esc(m.title) داشت — بررسی شد. |

## Report-only paths (demo-mvp, standalone)

Not enforced (per scope: no mass rewrite of generated/duplicate frontends). 84 unsafe/review sink(s) found there — see `tools/xss-sink-audit-report.json` → `report_only_hits` for the full list.

## Re-running

```sh
node tools/xss-sink-audit.mjs
```

Deterministic given the same source tree. Regenerates both this file and `tools/xss-sink-audit-report.json`.
