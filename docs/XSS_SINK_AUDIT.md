# XSS Sink Audit

> Generated by `tools/xss-sink-audit.mjs` on 2026-08-22T17:48:15.735Z. Auditor tone — this is a heuristic scan (regex-based, not real dataflow analysis), not a formal proof.

Scanned: apps/customer, apps/business, apps/company, shared/js (enforced — non-zero `unsafe` fails the script). Report-only (not enforced): demo-mvp, standalone.

## Summary

| Classification | Count |
|---|---|
| `dom_api_safe` | 79 |
| `safe_static` | 53 |
| `unsafe` | 38 |
| `escaped` | 33 |
| **Total sinks found** | **203** |

- `safe_static` — sink argument is a literal/constant string, no interpolated variable.
- `escaped` — the sink expression routes through `esc(` before use.
- `dom_api_safe` — line uses `textContent`, not an HTML sink, or is a benign adjacent match.
- `unsafe` — a sink interpolates a variable without going through `esc(`. **Must be zero for this script to exit 0.**
- `review` — `eval`/`new Function` calls; always flagged for manual review regardless of content.

## Findings requiring attention

| File:Line | Kind | Classification | Snippet |
|---|---|---|---|
| `apps/customer/js/data/booking.js:86` | innerHTML | `unsafe` | `sel.innerHTML=(r.slots.length?r.slots:FALLBACK_TIMES).map(s=>`<option value="${s}">${s} (نمونه)</option>`).join('');` |
| `apps/customer/js/data/booking.js:109` | innerHTML | `unsafe` | `sel.innerHTML=open.map(s=>`<option value="${s.time}">${faTime(s.time)}</option>`).join('');` |
| `apps/customer/js/data/booking.js:277` | innerHTML | `unsafe` | `party.innerHTML=Array.from({length:PARTY_MAX},(_,i)=>i+1)` |
| `apps/customer/js/data/discover.js:112` | innerHTML | `unsafe` | `f.innerHTML=list.map(()=>`<div class="rc" style="opacity:1;transform:none"><div class="rc-img sk" style="border-radius:0"></div><div class="rc-body"><div class=` |
| `apps/customer/js/data/discover.js:114` | innerHTML | `unsafe` | `f.innerHTML=list.map(cardHTML).join('');` |
| `apps/customer/js/data/discover.js:156` | innerHTML | `unsafe` | `document.getElementById('feedTitle').innerHTML=v==='all'?icon('flame',{size:16,fill:true})+' محبوب امشب':esc(el.textContent.trim());` |
| `apps/customer/js/data/discover.js:200` | innerHTML | `unsafe` | `el.innerHTML=withDist.map(({r,km})=>hCardHTML(r,` |
| `apps/customer/js/data/discover.js:207` | innerHTML | `unsafe` | `el.innerHTML=[...R].sort((a,b)=>(b.rt\|\|0)-(a.rt\|\|0)).slice(0,6).map(r=>hCardHTML(r,'')).join('');` |
| `apps/customer/js/data/discover.js:225` | innerHTML | `unsafe` | `el.innerHTML=trend.map((r,i)=>hCardHTML(r,i<2?`${icon('flame',{size:12,fill:true})} داغ`:'')).join('');` |
| `apps/customer/js/data/discover.js:252` | innerHTML | `unsafe` | `el.innerHTML=eventsHtml(events,false);` |
| `apps/customer/js/data/discover.js:256` | innerHTML | `unsafe` | `el.innerHTML=`<div class="empty-state"><div class="empty-state-icon">${icon('alert',{size:40})}</div><div class="empty-state-title">رویدادها بارگذاری نشد</div><` |
| `apps/customer/js/data/discover.js:259` | innerHTML | `unsafe` | `el.innerHTML=eventsHtml(SAMPLE_EVENTS,true);` |
| `apps/customer/js/features/chat.js:49` | innerHTML | `unsafe` | `body.innerHTML = `<div class="chat-empty">${msg}</div>`;` |
| `apps/customer/js/features/trips.js:104` | innerHTML | `unsafe` | `box.innerHTML = `<div style="background:#fff;border-radius:8px;padding:8px;display:inline-block">` |
| `apps/customer/js/reservation.js:71` | innerHTML | `unsafe` | `listEl.innerHTML=skTrip.repeat(3);` |
| `apps/business/js/crm.js:512` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/crm.js:587` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/crm.js:647` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/crm.js:759` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/crm.js:953` | innerHTML | `unsafe` | `document.getElementById('ct-campaign').innerHTML=`` |
| `apps/business/js/crm.js:979` | innerHTML | `unsafe` | `el.innerHTML=logs.length?`<table class="tbl"><thead><tr><th>سگمنت</th><th>تاریخ</th><th>گیرنده</th><th>پیام</th></tr></thead><tbody>` |
| `apps/business/js/crm.js:1012` | innerHTML | `unsafe` | `if(chatHtml){ el.innerHTML=chatHtml+`<div id="aiCards"><div style="text-align:center;padding:30px;color:var(--t2)">در حال بارگذاری پیشنهادها...</div></div>`; }` |
| `apps/business/js/crm.js:1022` | innerHTML | `unsafe` | `cardsEl.innerHTML=`` |
| `apps/business/js/marketing.js:56` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/marketing.js:106` | innerHTML | `unsafe` | `el.innerHTML=`` |
| `apps/business/js/marketing.js:285` | innerHTML | `unsafe` | `document.getElementById('v-analytics').innerHTML=`` |
| `apps/business/js/menu.js:60` | innerHTML | `unsafe` | `el.innerHTML = `` |
| `apps/business/js/menu.js:257` | innerHTML | `unsafe` | `box.innerHTML = `` |
| `apps/business/js/reservations.js:91` | innerHTML | `unsafe` | `el.innerHTML=`<div class="empty-state"><div class="empty-state-icon">${resDate==='past'?icon('inbox',{size:40}):icon('search',{size:40})}</div><div style="font-` |
| `apps/business/js/reservations.js:99` | innerHTML | `unsafe` | `el.innerHTML=demoNote+`` |
| `apps/business/js/reservations.js:109` | innerHTML | `unsafe` | `el.innerHTML=demoNote+`<div style="font-size:13px;color:var(--t2);margin-bottom:14px;font-weight:600">${fa(list.length)} رزرو · ${fa(seated)} مهمان رسیده${resQu` |
| `apps/business/js/staff-system.js:410` | innerHTML | `unsafe` | `document.getElementById('loginCard').innerHTML = `` |
| `apps/business/js/waitlist.js:442` | innerHTML | `unsafe` | `document.getElementById('toastMsg').innerHTML=msg+` <button class="undo-btn" onclick="${undoFn}();document.getElementById('toast').classList.remove('show')">باز` |
| `apps/company/js/hours.js:117` | innerHTML | `unsafe` | `el.innerHTML = `` |
| `apps/company/js/intelligence.js:331` | innerHTML | `unsafe` | `document.getElementById('v-aihealth').innerHTML=`` |
| `apps/company/js/intelligence.js:523` | innerHTML | `unsafe` | `document.getElementById('v-security').innerHTML=moderationQueuePanelHTML(mq)+featureFlagsPanelHTML(flags)+`` |
| `apps/company/js/intelligence.js:654` | innerHTML | `unsafe` | `box.innerHTML=renderCustomer360(res.data);` |
| `apps/company/js/intelligence.js:811` | innerHTML | `unsafe` | `document.getElementById('loginCard').innerHTML = `` |

## Manually reviewed (not auto-classified safe)

The regex classifier flagged these as `unsafe`/`review`; each was read by hand and reclassified with a justification (see `MANUAL_REVIEW_OVERRIDES` in `tools/xss-sink-audit.mjs`). Re-review if the cited line ever changes.

| File:Line | Justification |
|---|---|
| `apps/customer/js/auth.js:209` | openSheet(html) — فراخوان‌ها بررسی شدن (bookStep2/3 با esc(r.n)، rewards.js/trips.js با قالبِ استاتیک) — امن. |
| `apps/customer/js/features/economy.js:106` | missionCard(m) دیگه esc(m.title)/esc(m.description) داره (رفع‌شده در همین PR). |
| `apps/customer/js/features/food-dna.js:59` | Array.from محلی — نوارِ پیشرفتِ ثابت، بدونِ دیتا. |
| `apps/customer/js/features/food-dna.js:233` | f/l در همون تابع با esc(USER.firstName/lastName) تعریف شدن — بررسی شد. |
| `apps/customer/js/features/live-strip.js:38` | out فقط از pill(fmtFa(عدد)) ساخته می‌شه — بدونِ متنِ کاربر/API. |
| `apps/customer/js/features/loyalty.js:25` | perksBlock() از PERKS محلی (shared/js seed) می‌سازه؛ tier فقط esc(b.name) بعدِ رفعِ این PR. |
| `apps/customer/js/features/loyalty.js:64` | badges.map دیگه esc(b.name) داره (رفع‌شده در همین PR)؛ tier.emoji/tier.name از enumِ ثابتِ سطحِ باشگاهه. |
| `apps/customer/js/features/notifications.js:93` | Object.entries(CATS) — آبجکتِ محلیِ ثابت. |
| `apps/customer/js/features/palette.js:94` | itemHTML(it,i) از قبل esc(it.t)/esc(it.sub) داشت — بررسی شد. |
| `apps/customer/js/features/rewards.js:108` | d.valid ترنری با icon(...)؛ d.balance_toman از fmtFa می‌گذره. |
| `apps/customer/js/reservation.js:21` | cardHTML() دیگه esc(r.n) داره (رفع‌شده در همین PR). |
| `apps/customer/js/waitlist.js:55` | isOffered ترنری رویِ HTMLِ استاتیک. |
| `apps/business/js/chat.js:24` | chatEsc در همه‌ی فیلدها استفاده شده؛ t.id ستونِ Postgres UUID (فرمت تضمین‌شده). |
| `apps/business/js/crm.js:89` | logoPhoto.url دیگه esc شده (رفع‌شده در همین PR). |
| `apps/business/js/crm.js:379` | dist فقط {star:number,count:number} — بدونِ رشته. |
| `apps/business/js/loyalty.js:22` | Array.from({length:31}) — تقویمِ محلیِ عددی. |
| `apps/business/js/overview.js:153` | liveStatusBadge()/dashboardUsingDemoData() توابعِ محلیِ trusted‌اند. |
| `apps/business/js/overview.js:274` | pct محاسبه‌ی محلیِ عددیه (Math.round). |
| `apps/business/js/overview.js:315` | heatmapِ html فقط از slots/days (محلیِ ثابت) + fa(v) (عدد) ساخته می‌شه. |
| `apps/business/js/overview.js:349` | RHSِ زنجیره‌ای (.slice().map().join()) — بررسیِ دستی: esc(i.c)/icon(i.ic)/esc(i.t)/esc(i.d) هرچهارتا امن‌اند. |
| `apps/business/js/staff-system.js:35` | isDemo ترنری + fa(STAFF_LIST.length) — عدد. |
| `apps/business/js/staff-system.js:91` | openModal(html) — فراخوان‌هایِ نمونه‌گیری‌شده (data.js changeStatus با esc(r.name)) امن بودن. |
| `apps/business/js/staff-system.js:192` | iconName?icon(...):"" — ترنریِ icon، رجکسِ ابزار پارسش نکرد ولی امنه. |
| `apps/business/js/staff-system.js:225` | cards آرایه‌ی literal محلیه (تعریف‌شده دو خط بالاتر در همون تابع). |
| `apps/business/js/staff-system.js:282` | sugCard از PR#13 قبلاً esc(s.reason) داره؛ r.label هم در همین PR با esc() رفع شد. |
| `apps/business/js/waitlist.js:38` | wlCard(w,i) از قبل esc(w.name) داشت — بررسی شد. |
| `apps/business/js/waitlist.js:115` | floorEdit ترنریِ بولینِ محلی رویِ HTMLِ استاتیک. |
| `apps/company/js/badges.js:21` | BADGES_LIST.map از قبل esc(b.name)/esc(b.description)/... داشت — بررسی شد. |
| `apps/company/js/intelligence.js:22` | fa(d.guests.total_clv_toman) + rfm_distribution.map — اعداد/توابعِ trusted. |
| `apps/company/js/intelligence.js:50` | RESTAURANTS.map — فیلدهایِ نمایش‌داده‌شده اعداد/enumِ status‌اند. |
| `apps/company/js/intelligence.js:183` | متنِ ثابتِ توضیحی + fa(needsAttention.length). |
| `apps/company/js/intelligence.js:212` | healthMeta از یک لوکاپِ محلیِ ثابت؛ d.jobs.dead عدد. |
| `apps/company/js/missions.js:21` | MISSIONS_LIST.map از قبل esc(m.title) داشت — بررسی شد. |
| `apps/company/js/overview.js:26` | lowBalanceCount عددِ محلیه. |
| `apps/company/js/overview.js:129` | openModal(html) — همون الگویِ staff-system.js:91؛ فراخوان‌هایِ نمونه‌گیری‌شده امن بودن. |
| `apps/company/js/photos.js:131` | ['pending','approved','rejected','all'].map — آرایه‌ی literal محلی. |
| `apps/company/js/restaurant.js:62` | Object.entries(planDist) — شمارشِ محلیِ اعداد. |
| `apps/company/js/sales.js:105` | openInquiries عددِ محلیه. |

## Report-only paths (demo-mvp, standalone)

Not enforced (per scope: no mass rewrite of generated/duplicate frontends). 135 unsafe/review sink(s) found there — see `tools/xss-sink-audit-report.json` → `report_only_hits` for the full list.

## Re-running

```sh
node tools/xss-sink-audit.mjs
```

Deterministic given the same source tree. Regenerates both this file and `tools/xss-sink-audit-report.json`.
