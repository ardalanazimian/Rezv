# BRIEF — ۲۰۲۶-۰۹-۱۳ · Launch Engineer

- **نشست:** `rezv-48 [a55e94]` · sessionId `2bffd0d9-1ff0-4ca6-ae02-0f22879b30e8` — نشستِ تازه، نه ادامه‌ی `rezv-d8`. نقش را مالک با زبانِ خودش داد: «YOU ARCE LUNCH ENGINIER , FIX EVERY PROBLEMS BEFORE LUNCHING THE APP».
- **مقصد:** CEO (امروز زنده نیست — `ListAgents` + رجیستری: فقط Red Team `rezv-25` و طراح `rezv-ba`) · مالک · بازبین
- **آنچه از خواننده می‌خواهد:** §۵ — یک خط برای مالک.

---

## ۱. آنچه روی `main` نشست (با تأییدِ صریحِ مالک برای ادغامِ #86)

| | اثر | اثبات |
|---|---|---|
| **CIِ `main` اولین حکمِ کامل را گرفت** | ۱۳ job سبز؛ e2e در هر سه پروژه **تمام شد** (نه cancelled): ۱۸۸ سبز / ۶ قرمز | run `34734916852` |
| **`landing` سبز** (قرمز از ۰۹-۱۱) | گاردهای لندینگ حالا در سوئیتِ لندینگ‌اند؛ دو سوراخِ گاردِ پرده بسته | FIX-050 |
| **پیامکِ رستوران** | یک پیام دیگر دو اعتبار نمی‌خورد؛ پیامِ ردشده اعتبار نمی‌خورد | FIX-049 · قرمز: موجودی 8 به‌جای 9 |
| **PR #86** | ۳۱ رفعِ امنیت/پول/waitlist | Red Team: «ادغام کن»؛ درختِ ادغام 1844/1844 روی DBِ تازه |
| **e2e B-01** | قرمزِ شبانه‌ی ۳٫۵ ساعته رفت؛ تست حالا نقصِ ۰۴۵ را واقعاً می‌بیند | FIX-E2E-B01 |
| **CI ظرفیت/حکم** | سقفِ هر job، `main` بی‌لغو، e2e سه job | FIX-051 |

## ۲. آنچه submitted است و روی شاخه منتظر (این batch)

- **RT-13** — پرداختِ تکراریِ واقعیِ بیعانه حالا متریک، آلارمی که روی یک رویداد فایر می‌شود، و ردِ حسابرسی دارد. FIX-RT-13.
- **RT-14** — گاردِ «امتیاز منقضی نمی‌شود» حالا کلِ `api/src` را می‌بیند، و وعده را در هر فایلِ سطحِ کاربر پیدا می‌کند. FIX-RT-14.
- **دوباره‌زنیِ Red Team (RETEST-2026-09-13):** RT-14 دامنه HOLDS؛ RT-13 **PARTIAL** — آلارم گیرنده ندارد (بی‌alertmanager)، پس تا C-3 تنها تشخیص کوئریِ دستی است؛ و **RT-18** تازه: همان گارد با منفیِ hoisted دور می‌خورد.
- **RT-18** — ضامن از regex به دیتابیس رفت: قیدِ CHECKِ مهاجرتِ ۰۸۸، کسرِ امتیاز فقط با بازخرید یا برگشتِ کش‌بک. ⚠️ یعنی «کسرِ دستیِ ادمین» روی DB ممکن نیست — تصمیمِ محصولیِ ضمنی، در FIX-RT-18 §۴. FIX-RT-18.

## ۳. آنچه مسدودم کرد یا عمداً نکردم

- **۶ تستِ e2e و نقصِ قرصِ «کِی و چند نفر»** — رفعش روی شاخه‌ی طراح است و طراحِ زنده همان‌جا کار می‌کند. رفعِ دوباره روی `main` تعارضِ قطعی بود. به `rezv-ba` پیام دادم (msg `023f5472`) و این‌جا ثبت است.
- **همین رفعِ ۰۵۰ یک بار پیش‌تر نوشته شده بود** — Launch Engineerِ پیشین آن را در `wt-rezv-a0` staged گذاشته و هرگز کامیت نکرده بود. دیر دیدم. کامیتش نکردم؛ مقایسه در FIX-050 §۸.
- **دامنه** — هنوز `ENOTFOUND`. هر ردیفِ «روی دامنه‌ی واقعی» پارک است.
- **آلارم‌ها به کسی پیج نمی‌شوند** — `BACKLOG` C-3 (بی‌alertmanager) هنوز باز است. RT-13 فایر می‌شود، ولی ممکن است کسی نبیند.

## ۴. دو درسِ روش، با هزینه‌ی واقعی

1. **سبزِ در انزوا ≠ سبزِ در سوئیت.** تستِ تازه‌ی پیامک ۶/۶ در انزوا، ۵ قرمز در `npm test` — `before`ِ ریشه‌ی همه‌ی فایل‌ها پیش از هر تستی اجرا می‌شود و فایلِ دیگری env را پاک می‌کرد. کنترل‌های «موضوعِ تست غایب است» گرفتند؛ بدونشان سبزِ توخالی بود.
2. **هر شاخه سبز، ادغام قرمز.** `docs/DECISIONS.md:349` (از #86) به فایلی اشاره می‌کرد که شاخه‌ی من جابه‌جا کرده بود. فقط اجرای گاردها روی **درختِ ادغام** نشانش داد.

## ۵. تابلو

`docs/audit/fixes/LAUNCH-READINESS.md`. تغییرِ امروز: «CI حکم می‌دهد» → submitted؛ پولِ بیعانه و پیامک → submitted؛
سفرهای طلایی **هنوز UNKNOWN** — هیچ‌کدام روی محیطِ واقعی سرتاسر اجرا نشده.

**یک خط برای مالک:**

> `main` برای اولین بار حکمِ کاملِ CI دارد: همه سبز جز ۶ تستِ e2e که رفعشان روی شاخه‌ی طراح است؛ ۳۱ رفعِ #86 و ۴ رفعِ Launch Engineer روی `main` نشستند. برای لانچ هنوز لازم است: خریدِ دامنه، و یک اجرای سرتاسریِ سفرِ رزرو با OTP و پیامکِ واقعی.
