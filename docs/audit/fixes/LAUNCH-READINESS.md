# LAUNCH-READINESS — تابلوی آمادگیِ لانچ

- **تاریخ:** ۲۰۲۶-۰۹-۱۳ · **نشست:** Launch Engineer `rezv-48 [a55e94]` (sessionId `2bffd0d9-…`) · نسخه‌ی قبلی: `rezv-a0 [5776f9]`، ۲۰۲۶-۰۹-۰۹
- **مقصد:** CEO (امروز هیچ نشستِ CEOی زنده نیست) · مالک · بازبین
- **وضعیت:** SUBMITTED — **هیچ ردیفی را من سبز نمی‌کنم.** CEO می‌بندد، Red Team اول حمله می‌کند.
- **آنچه از خواننده می‌خواهد:** خواندن. تنها تصمیمِ بازِ مالک در §۳ است.

> **قاعده:** هر ردیف یا **شاهدِ خام** دارد یا **UNKNOWN** می‌خواند. «احتمالاً درست است» ردیفِ سبز نیست.

---

## ۰. آنچه از ۰۹-۰۹ عوض شد — در یک پاراگراف

`main` چهار روز حکمِ کاملِ CI نداشت: `landing` از `f637948` قرمز بود، `e2e` در هر اجرا روی سقفِ ۲۵ دقیقه cancelled
می‌شد، و pushهای سند اجرای کد را لغو می‌کردند (۰۵۰، ۰۵۱). ۳۱ رفعِ امنیت/پولِ PR #86 روی شاخه مانده بود. امروز
با تأییدِ مالک هر دو روی `main` نشستند (`c92b1eb`) و **اولین اجرای کاملِ CI** این است:

```text
run 34734916852 · c92b1eb · Linux
success  build · test (219s) · schema-drift · image-build · boot-path · security · observability
         design-system · standalone · seo · landing (54s) · rejects-ratchet          base-freshness: skipped (فقط PR)
failure  e2e (mobile-safari) · e2e (mobile-chrome) · e2e (desktop-chrome)   — هر سه **completed**، نه cancelled
         هر پروژه: 188 passed · 6 failed — همان ۶ تستِ بدهیِ DS-007 (زیر)
```

---

## ۱. تابلو

| ردیف | وضع | شاهد / چرا UNKNOWN |
|---|---|---|
| **CIِ `main` حکم می‌دهد** | **🟡 submitted — اولین حکمِ کامل گرفته شد** | بالا. `cancel-in-progress` روی `main` خاموش و هر ۱۴ job سقفِ `timeout-minutes` دارد (FIX-051). قرمزِ باقی فقط e2e است |
| **e2e (سفرهای کاربر در مرورگر)** | **❌ ۶ از ۱۹۴ به‌ازای پروژه** | `booking-context.spec.ts` :28 :49 :63 :86 · `card-slots.spec.ts:41` · `social-proof.spec.ts:66`. سلکتورهایی که با بازطراحیِ DS-007 رفتند. **رفعش روی `session/rezv-ba-design` (`43dfa1e`) هست** و همراهش یک نقصِ صحتِ واقعی: قرصِ «کِی و چند نفر» روی بارِ سرد وجود ندارد ⇒ مهمان ممکن است روزِ اشتباه رزرو کند. طراحِ زنده (`rezv-ba`) روی همان شاخه کار می‌کند؛ عمداً روی `main` دوباره رفعش نکردم. پیام و ثبت شد |
| **سفرِ طلاییِ مشتری** | **UNKNOWN** | هرگز سرتاسر روی staging با OTPِ واقعی و SMSِ تأیید اجرا نشده. e2e با APIِ mock است و ثابت نمی‌کند. و نقصِ قرصِ «کِی و چند نفر» (بالا) مستقیم روی همین سفر است |
| **سفرِ طلاییِ کسب‌وکار** | **UNKNOWN** | همان. B-01 (تاریخِ رزروِ دستی) حالا روی مرزِ روز و دستگاهِ UTC در CI سبز است — جزئی از سفر، نه خودِ سفر |
| **سفرِ طلاییِ شرکت** | **UNKNOWN** | همان؛ TOTP و شارژِ SMS اثبات‌نشده |
| **پول — بیعانه‌ی زرین‌پال** | **🟡 submitted** | #86: قفلِ ردیفِ رزرو + ایندکسِ ۰۸۶ (Red Team: HOLDS). RT-13: پرداختِ تکراریِ واقعی حالا متریک + آلارمِ `PaymentRefundRequired` + ردِ حسابرسی دارد (FIX-RT-13). ⚠️ آلارم به کسی پیج نمی‌شود تا C-3 (alertmanager) بسته نشود |
| **پول — اعتبارِ پیامکِ رستوران** | **🟡 submitted** | ۰۴۹ اندازه‌گیری شد: یک پیام دو اعتبار (retry)، پیامِ ردشده یک اعتبار. حالا یک اعتبار به‌ازای پیامِ پذیرفته‌شده، کلیددار روی job (مهاجرتِ ۰۸۷). `test` روی لینوکس سبز (FIX-049) |
| **گاردهای CI که فریب نخورند** | **🟡 submitted** | RT-14: گاردِ وعده‌ی امتیاز ۲ از ۱۷ پوشه را می‌دید؛ حالا کلِ `api/src` و طرفِ ادعا هم کشف می‌شود (FIX-RT-14). گاردهای لندینگ به سوئیتِ لندینگ رفتند (FIX-050) |
| **لایه‌ی وب** | **UNKNOWN — و بخشیش پارک** | دامنه resolve نمی‌شود (§۳). گیتِ freshness هنوز نوشته نشده. `landing` در CI سبز است — یعنی تست/تایپ/لینت/build، نه crawlability |
| **صفر ردیفِ FAKE** | **UNKNOWN — census کهنه** | آخرین census (`audit/round-16/feature-census.json`، ۰۹-۰۳): FAKE=۹. دوباره اندازه گرفته نشده؛ به‌عنوانِ وضعِ امروز نمی‌خوانمش |
| **زیرساختِ ML** | **🟡 M0 submitted (از ۰۹-۰۹)** | `dca88ad` — بی‌تغییر امروز. فازِ «decision_inputs در زمانِ رزرو» شروع نشده |
| **SMSِ واقعی** | **UNKNOWN** | هیچ پیامِ واقعی با پاسخِ خامِ ملی‌پیامک ثبت نشده. رفعِ امروز با stub اثبات شد |
| **callbackِ سندباکسِ زرین‌پال** | **UNKNOWN** | نیازِ URLِ عمومی — پارک روی دامنه |
| **cronِ مشاهده‌شده** | **UNKNOWN** | «پیکربندی‌شده» ≠ «اجرا شده» |
| **مانورِ backup** | **UNKNOWN** | `tools/restore-drill.sh` هست؛ اجرا با شمارشِ ردیف ثبت نشده |
| **تاب‌آوری** | **UNKNOWN** | کشتنِ کانتینرِ DB/ریبوت اجرا نشده |
| **دسترسیِ ایران** | **UNKNOWN** | هیچ سطحی از دو ISPِ ایرانی بارگذاری نشده |
| **Red Team پاک** | **❌ نه هنوز** | `RETEST-2026-09-12`: HOLDS ۲ · FAKEABLE ۱ (RT-14) · BLIND ۱ (RT-13). هر دو امروز submitted؛ Red Team هنوز دوباره نزده. ۱۷ از ۲۰ پچِ #86 حمله‌نشده |
| **آزمونِ مالک** | **UNKNOWN** | مالک هنوز چهار سطح را روی گوشیِ خودش استفاده نکرده |

---

## ۲. چیزی که امروز حرکت کرد — همه submitted، هیچ‌کدام بسته نه

| کامیت | چه | FIX |
|---|---|---|
| `a310ddc`, `5e2110e` | `landing` سبز؛ گاردهای DS-009 به سوئیتِ لندینگ، دو سوراخِ گاردِ L4 بسته | `FIX-050-LANDING-GUARDS-IN-LANDING-SUITE.md` |
| `ad93943` | پیامکِ رستوران: یک اعتبار به‌ازای پیامِ پذیرفته‌شده | `FIX-049-SMS-CHARGE-ONCE-PER-ACCEPTED-MESSAGE.md` |
| `1cc06f4` | e2e B-01: قرمزِ شبانه + کوریِ روزانه نسبت به نقصِ ۰۴۵ | `FIX-E2E-B01-RUN-CLOCK.md` |
| `050fb8c` | CI: سقفِ هر job، `main` بی‌لغو، e2e ماتریسی | `FIX-051-CI-VERDICT-ON-MAIN-AND-E2E-CAPACITY.md` |
| `c819ee5` | ادغامِ PR #86 (۳۱ رفع) با تأییدِ مالک | — |
| (این batch) | RT-13 تشخیصِ پرداختِ تکراری · RT-14 دامنه‌ی گاردِ وعده | `FIX-RT-13-…`، `FIX-RT-14-…` |

**اثباتِ ادغام پیش از push** (درختِ `main + #86 + session/rezv-48`، DBِ Postgres17 تازه): `api` 1844/1844 · `landing` 114/114 ·
tsc و lint صفر · ۱۶ گاردِ `tools/check-*.mjs` + xss ×۲ + standalone + design-system همه صفر. یک تعارضِ معناییِ ادغام پیدا و رفع شد
(`docs/DECISIONS.md:349` به فایلی ارجاع می‌داد که یک شاخه جابه‌جا کرده بود — هر شاخه تنها سبز، ادغام قرمز).

---

## ۳. ردیف‌های مسدود روی مالک — دوباره اندازه‌گیری شد، از ۰۹-۰۹ نقل نشد

| ردیف | مسدود روی | شاهدِ امروز | دورهای انتظار |
|---|---|---|---|
| دامنه (E-001) | خریدِ دامنه | `rezervno.ir`، `www.rezervno.ir`، `rezervno.com` → `ENOTFOUND`؛ کنترل `irna.ir` → resolve | **۲** (از ۰۹-۰۸) |
| نامِ برند | تصمیمِ مالک — ۰۹-۱۰: «هنوز تصمیم نگرفته»، و خواسته که هر دور پرسیده نشود (`audit/ESCALATIONS.md` §E-002) | — | پرسیده نمی‌شود |

## ۴. ردیف‌های مسدود روی نشستِ دیگر

| ردیف | مسدود روی |
|---|---|
| ۶ تستِ e2e + نقصِ قرصِ «کِی و چند نفر» | طراح `rezv-ba [244468]` روی `session/rezv-ba-design` — رفع آنجاست؛ ادغامش پس از اثباتِ کاملِ یکپارچه |
| کپیِ عمومیِ لایه‌ی وب | ✅ **دیگر مسدود نیست:** `docs/marketing/POSITIONING.md` امروز وجود دارد (Marketer §۱). هنوز هیچ ادعایش را روی صفحه‌ای نبرده‌ام |
| بستنِ ردیف‌ها | CEO — امروز نشستی زنده نیست |
