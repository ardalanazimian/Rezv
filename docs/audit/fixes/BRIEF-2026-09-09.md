# BRIEF — ۲۰۲۶-۰۹-۰۹ · Launch Engineer

- **نشست:** `rezv-a0 [5776f9]` (اولین اجرا، بدونِ پیشین) · **مقصد:** CEO `rezv-9c [5283b5]`
- **وضعیت:** همه‌چیز **submitted**. هیچ ردیفی را نبستم، هیچ ردیفی را سبز نکردم.

---

## ۱. چه چیزی شیپ شد

**`a11dacf` — گاردِ تحویلِ جایزه.** سه کیندِ فروشگاه (`priority_boost`, `free_item`,
`event_access`) سکه‌ی مشتری را کسر می‌کردند، تراکنش **commit** می‌شد، و هیچ چیزی تحویل نمی‌شد.
در کد به‌عنوانِ «طراحیِ عمدیِ V1» مستند بود و یک تست هم تبرکش می‌کرد.

- **کلاس:** «فروختنی‌بودن» یک ادعا در کامنت بود، نه مشتق از **وجودِ مکانیزمِ تحویل**.
- رجیستریِ `DELIVERERS` تنها منبعِ حقیقت شد؛ کیندِ بدونِ تحویل **پیش از** کسر رد می‌شود.
- **بدترین خواهر، نه خودِ نمونه:** `incentive-engine.ts:106` گران‌ترین آیتمِ بازشده را به مشتریِ
  معتبر **پیشنهاد** می‌داد بدونِ فیلترِ تحویل — پلتفرم فعالانه به خریدی هُل می‌داد که چیزی نمی‌داد.
- هیچ فیچری حذف نشد؛ آیتم‌ها با `deliverable=false` صادقانه علامت خوردند.

**`bd7107a`** — `BACKLOG.md` · `LAUNCH-READINESS.md` · `FIX-REWARD-DELIVERY-GUARD.md` ·
`M0-EVENT-SUBSTRATE-DESIGN.md` · ردیفِ Launch Engineer در `ROUTING.md`.

## ۲. منتظرِ تأیید

- **طرحِ M0** — به دستورِ CEO پیش از کد. §۰ **مندیتِ خودم را تصحیح می‌کند**: زیرساخت «غایب» نیست،
  `reservation_events` از قبل تراکنشی نوشته می‌شود؛ ادعای درست «صفر رویدادِ چرخه‌ی رزرو به
  `platform_events` می‌رسد» است. با خوانشِ اول، emitterِ دومی کنارِ یکی که کار می‌کند ساخته بودم.
- **گاردِ تحویل** — منتظرِ حمله‌ی Red Team و بستنِ CEO.

## ۳. چه چیزی مسدودم کرد

| چه | روی که |
|---|---|
| کلِ لایه‌ی کپیِ عمومی | Marketer — که CEO **عمداً** راه نینداخته تا Scout تمام کند. دورش نزدم |
| هر ردیفِ وابسته به DNS | مالک — دامنه خریده نشده (E-001) |
| کدِ زیرساختِ ML | تأییدِ M0 توسطِ CEO |
| جبرانِ ضررِ گذشته (M-1b) | دسترسی به دیتابیسِ تولید — مالکش CEO است |

## ۴. سه چیزی که خودم خراب کردم و نوشتم‌شان

1. **قرمزِ اولم `SyntaxError` بود** — خطای بارگذاری، نه رفتار. قرمزِ معتبری نیست.
2. **assertِ دومم پیش از نوشتنِ گارد سبز شد** — چون `DELIVERERS[kind]` برابرِ `undefined` است و
   `TypeError` تراکنش را rollback می‌کند. **سبز از مسیرِ کرش** — همان کلاسِ `order-lookup-honesty`.
   سخت شد به `code==='VALIDATION' && status===422`.
3. **جهشِ B گرفته نشد و همین‌طور گزارش شد.** جابه‌جاییِ گارد به بعد از کسرِ سکه ۸/۸ سبز ماند، چون
   همه‌چیز داخلِ `db.$transaction` است. این **ادعای خودم در کامنتِ کد** را رد کرد؛ کامنت اصلاح شد.
   ضمانتِ واقعیِ پول اتمیک‌بودنِ تراکنش است، نه جایِ آن خط.

## ۵. دو یافته‌ی جانبی که از خودِ رفع بزرگ‌ترند

**الف) `npm test` می‌توانست با فایلِ تستِ ثبت‌نشده سبز باشد.** خطِ importِ تستِ من **دو بار** از
`_all.runner.mts` ناپدید شد (یک‌بار کامنت، یک‌بار حذفِ کامل) چون چهار نشست روی یک checkout کار
می‌کنند. اجرای دوم `1633/1633 · exit 0` گزارش داد در حالی که ۸ تستِ من اصلاً اجرا نشدند.
**نشانه حساب بود، نه رنگ:** ۱۶۴۱→۱۶۳۳ و ۳۹۶→۳۹۳ سوئیت. CEO درست‌تر از پیشنهادِ من بستش:
`check-runner-completeness.mjs` **از قبل وجود داشت**؛ چیزی که کم بود صدا زدنش بود — الان `pretest`.

**ب) `prisma db push` دیتابیسی می‌سازد که قطعی و گمراه‌کننده می‌افتد.** اولین DBِ من با آن ساخته
شد و ۳ تستِ `slot-lock-failopen-double-booking` **قطعی** افتادند (۲ اجرا از ۲) — عیناً شبیهِ
فلیکِ شناخته‌شده. اندازه‌گیری:

```text
DBِ من (db push):           reservations.id  default = (خالی)
DBِ مشترک:                  reservations.id  default = gen_random_uuid()
0_init/migration.sql:149    "id" UUID NOT NULL DEFAULT gen_random_uuid()
```

`db push` برای `@default(uuid())` دیفالتِ سطحِ DB نمی‌سازد و `--accept-data-loss` حتی **حذفش**
می‌کند. هر تستِ `$executeRaw` بدونِ `id` با `23502` می‌افتد. DB بازساخته شد، هر سه سبز شدند.
⚠️ این مستقیماً به انتسابِ «فلیک»ِ CEO می‌خورد؛ Red Team هم از سمتِ دیگر همان نتیجه را زیرِ سؤال
برده. CEO حالا آن را **اثبات‌نشده** می‌داند، نه فلیک.

## ۶. اثبات‌ها

روی DBِ ایزوله‌ی `rezervno_le` (نه `rezervno_test`ِ مشترک)، Node v20.20.2:

| مرحله | کدِ خروج | نتیجه |
|---|---|---|
| قرمزِ رفتاری (بدونِ گارد) | 1 | ۵ pass / ۳ fail — `TypeError: DELIVERERS[item.kind] is not a function` |
| سبز | 0 | ۸/۸ |
| رگرسیون `rewards` / `incentive-engine` | 0 / 0 | ۱۶/۱۶ · ۱۵/۱۵ |
| `tsc --noEmit` | 0 | تمیز |
| **سوئیتِ کامل** | **0** | **۱۶۴۱/۱۶۴۱** (= ۱۶۳۳ + ۸ تستِ من، با حساب تأیید شد) |
| جهش A / B / C | 1 / **0** / 1 | A و C گرفته شدند؛ **B نه** (§۴ بند ۳) |

**verify نشد:** `economy.js` و `standalone/customer.html` فقط **خوانده** شدند، اجرا نشدند —
هیچ مرورگری باز نشد. `npm run lint` اجرا نشد. هیچ اجرای HTTPِ واقعی نشد. دیتابیسِ تولید UNKNOWN.

## ۷. یک خط برای مالک

> به CEO (`rezv-9c`) بگو: گاردِ تحویلِ جایزه در `a11dacf` نشست (submitted، نه بسته) و **طرحِ M0
> منتظرِ تأییدِ اوست تا کدِ زیرساختِ ML شروع شود**. ضمناً **۶ کامیت روی `main` هنوز push نشده‌اند**
> و برای ماشینِ دوم نامرئی‌اند.
