# DS-006 — اپِ مشتری از دیدِ یک ۲۲ساله: آنچه دیده شد، با محل

| | |
|---|---|
| **تاریخ** | ۲۰۲۶-۰۹-۱۰ |
| **نویسنده** | `rezv-e6 [a3c0f0]` — Designer، مأموریتِ بصری (ابلاغِ CEO به نقل از مالک، همین روز) |
| **برایِ** | CEO (`rezv-cf [97a8f9]`) · اجرایِ `apps/**`: `rezv-a0 [e6c5ba]` |
| **وضعیت** | **submitted** |
| **روش** | ۱۰ اسکرین‌شات از `standalone/customer.html` در ۳۹۰×۸۴۴ (‎@2x)، Playwright/Chromium، آنبوردینگ رد‌شده — **بعد** هر مورد در کد پیدا شد، و برای cascade به `getComputedStyle` رجوع شد نه به ترتیبِ فایل‌ها. موردی که در کد پیدا نشد «محل: تأییدنشده» دارد |

---

## ۰. یک‌خط

**استخوان‌بندی خوب است — کارت‌ها، رنگ، تایپ، و حرکتِ پایه.** آنچه از دیدِ یک ۲۲ساله غلط است،
**ساختاری** است نه بصری: محتوا زیرِ fold است، ۴۰٪ ناوبریِ اصلی خارج‌شده به دیوارِ ورود می‌خورد،
و یک صفحه تقریباً خالی است. همه در `apps/**` — لِینِ مهندسِ لانچ. **این سند بیشتر spec است تا
تغییر.**

دو پیشفرضِ ابلاغ با اندازه‌گیری نخواند و هر دو را اینجا تصحیح می‌کنم:

- «۲۶ ارجاعِ حرکتی در کلِ سیستم» فقط `shared/css` را شمرده بود. `app.css` **۱۵** `@keyframes`،
  **۶۷** `transition`، **۲۹** `animation` دارد، به‌اضافه‌ی `.reveal` روی اسکرول با
  `IntersectionObserver` (`init.js:25`). حرکت **هست**.
- و فرضِ خودِ من هم غلط بود: نوشته بودم «`shared/css` روی اپِ مشتری کنترل ندارد». `getComputedStyle`
  خلافش را نشان داد (§۳).

---

## ۱. ساختار: چه چیزی اول دیده می‌شود

| # | دیده شد | از دیدِ ۲۲ساله | محل |
|---|---|---|---|
| S1 | صفحه با **شعار + بَجِ AI + فرمِ سه‌فیلدی + دکمه‌ی جست‌وجو** باز می‌شود؛ اولین کارتِ رستوران در **۹۵۴px** از بالا (اندازه‌گیری: `getBoundingClientRect().top + scrollY` بعد از آنبوردینگ) — **بیش از یک صفحه‌ی کاملِ ۸۴۴px**. تخمینِ اولم «~۷۰۰» کم بود | تیک‌تاک/اینستاگرام با **محتوا** باز می‌شوند، نه با فرم و شعار. این هیرو زبانِ لندینگ است داخلِ اپ | `index.html:100` (`<section class="hero">`) پیش از `:190` (`#feed`) |
| S2 | دکمه‌ی تغییرِ تم (☀) **شناور روی هیرو**، بالا-چپ، `position:fixed` | شبیهِ کنترلِ دیباگ؛ باارزش‌ترین ناحیه‌ی صفحه را یک ابزارِ فرعی گرفته | `app.css:131` `.theme-fab`، `index.html:99` |
| S3 | ناوبریِ پایین ۵ تب: کشف · علاقه · رزروها · باشگاه · اعتبار | «اعتبار» و «باشگاه» برچسبِ مفهوم‌اند نه عمل؛ **خارج‌شده، هر دو + پروفایل به «هنوز وارد نشدی» می‌رسند** — ۴۰٪ تب‌های اصلی بن‌بست | `loyalty.js`, `economy.js:68-80`, پروفایل |
| S4 | صفحه‌ی «اعتبار» خارج‌شده: آیکون + یک جمله + یک دکمه، و **~۷۰٪ صفحه گرادیانِ خالی** | صفحه‌ی خالی یعنی «اینجا چیزی نیست»؛ یک ۲۲ساله برنمی‌گردد | `economy.js:70-77` — زیرِ دکمه هیچ چیزی رندر نمی‌شود |
| S5 | سه صفحه‌ی خارج‌شده یک قالبِ تکراری با inline style | سه کپی از یک صفحه؛ `foundation.css` یک `.empty-state` دارد که استفاده نشده | `economy.js:71-76`؛ `foundation.css` `.empty-state*` |

**همه‌ی پنج مورد `apps/**` هستند.** spec در §۵.

---

## ۲. نقص‌های دیدنی

| # | دیده شد | محل (اندازه‌گیری‌شده) | لِین |
|---|---|---|---|
| V1 | **«undefined تومان»** به کاربر نشان داده می‌شود در منویِ رستورانِ نمونه — **و بدتر از آنچه اول نوشتم** | **تصحیح (بازتولیدشده در DOM، ۲۰۲۶-۰۹-۱۰):** علتِ اولیه‌ی من («۶ آیتمِ بی‌قیمت در `seed.js`») **غلط بود** — آن grep آرایه‌های `tags` را شمرده بود؛ هر ۲۴ آیتمِ منو سه‌عنصری‌اند. علتِ واقعی: مسیرِ آفلاین/نمونه رکوردهای خام را به `R` می‌دهد (`init.js:12` `R = R_SAMPLE`، و `loadRestaurants` هنگامِ offline `return R_SAMPLE`) و `normalizeMenuEntry` فقط **یک** صداکننده دارد، `mapApiRestaurant` (`api.js:316`) که روی مسیرِ API است. پس آیتم‌ها به `menuItemHTML` (`detail.js:121-130`) به‌شکلِ **آرایه‌ی خام** می‌رسند و `m.e`, `m.n`, `m.p` هر سه `undefined`اند — در DOM: ایموجی خالی، نام خالی، «undefined تومان»، روی **هر ۴** آیتم. رفعِ درست **نرمال‌سازی در مرزِ seed→R** است، نه گاردِ قیمت در `:128` — گاردِ قیمت فقط آیتمِ خالی را پنهان می‌کند. مسیرِ زنده (`api.js:464`) سالم است؛ این فقط باندلِ آفلاین و fallbackِ «بک‌اند در دسترس نیست» را می‌گیرد | LE |
| V2 | **دو حلقه‌ی فوکوس** روی `<select>` تاریخ در شیتِ رزرو | **تشخیصِ اولِ من غلط بود** (cascade). `getComputedStyle` نشان داد: حلقه‌ی داخلی `--focus-ring`ِ توکنی است روی خودِ select (درست)، و حلقه‌ی بیرونی `app.css:734` `.bw-field:focus-within{border-color:var(--blue)}` — **دو نشانگرِ عمدیِ فوکوس، روی هم**. حکمِ طراحی: **یک نشانگر برای هر فوکوس.** یا کادر، یا کنترل — نه هر دو | LE (`app.css:734`) |
| V3 | چیپِ «Wi-Fi» تنها به سطرِ دوم افتاده (۳ + ۱ یتیم) | `detail.js` بلوکِ امکانات؛ `flex-wrap` بدونِ توزیعِ متعادل | LE |
| V4 | کارتِ رزرو در «رزروها»: ایموجی از لبه‌ی بالایِ نوارِ سبز بیرون زده و بریده | **محل تأییدنشده** — با نام‌های حدسی پیدا نشد؛ از عکس معلوم، از کد نه | LE |
| V5 | `[DEMO]` و «نمونه» داخلِ متنِ کارت | داده‌ی آفلاین، نه باگ | — |

---

## ۳. لایه‌ی بصریِ مشترک — چه چیزی را **واقعاً** کنترل می‌کند (اندازه‌گیری‌شده)

ترتیبِ لود (`index.html`): `fonts → tokens → theme → foundation → app → ds-bridge`.
از ترتیب نمی‌شود نتیجه گرفت؛ `getComputedStyle` روی باندلِ واقعی:

| اولیه | مقدارِ سنجیده روی اپِ مشتری | منبعِ برنده | یعنی |
|---|---|---|---|
| فشارِ `.btn` | `scale(.98)`، `130ms`، `cubic-bezier(.4,0,.2,1)` | **`ds-bridge.css:50`** (مشترک) — نه `app.css:158/532` | **مشترک کنترل دارد** |
| فوکوسِ `.inp` | `outline:none` + `--focus-ring` | **`foundation.css:21`** (مشترک) — `app.css:514` اعمال **نشد** | **مشترک کنترل دارد** |
| `.reveal` روی اسکرول | `0.3s`، transform با `--ease-spring`، opacity با `--ease-out` | `app.css` برای فاصله/مقیاس؛ **توکن‌های مشترک** برای منحنی (از راهِ `--spring`/`--ease` که `ds-bridge:28-29` نگاشت می‌کند) | مشترک **منحنی** را کنترل می‌کند، نه هندسه |
| کادرِ فیلد در فوکوس | `border-color: brand-400` | `app.css:734` | اپ |

**پس ابلاغِ امروز ابزارِ واقعی می‌دهد:** حسِ فشار و منحنی‌های حرکت از `shared/css` قابلِ تغییرند و
`app.css` رویشان نمی‌نشیند. آنچه مشترک کنترل **نمی‌کند**، ساختار و چیدمان است (§۱) — که همان‌جا
بیشترین ارزش برای یک ۲۲ساله نشسته.

> اعتراف: نسخه‌ی اولِ همین بخش از ترتیبِ لود استدلال کرده بود و به نتیجه‌ی **وارونه** رسیده بود
> («app برنده است»). یک تغییرِ بی‌اثر روی همان فرض زدم، قبل/بعدش پیکسل‌به‌پیکسل یکی درآمد، و
> برگرداندمش. cascade را باید **اندازه گرفت**، نه حدس زد.

---

## ۴. اولین تغییرِ بصری — کوچک، در لِینِ خودم، سنجیده

**فشارِ دکمه با فنر.** در `ds-bridge.css` (فایلی که §۳ ثابت کرد برنده است):

| | قبل | بعد |
|---|---|---|
| `transform` هنگامِ فشار | `scale(.98)` | `scale(.96)` |
| منحنیِ transform | `--ease-out` | `--ease-spring` (cubic-bezier(.34,1.56,.64,1)) |
| مدت | `130ms` | `130ms` — **دست‌نخورده** |
| رنگ/سایه/شفافیت | `--ease-out` | `--ease-out` — دست‌نخورده |

**الگویِ برداشته‌شده و چرا:** بازخوردِ لمسیِ دکمه در iOS/Instagram — فروترفتنِ محسوس هنگامِ فشار
و برگشتِ فنری با کمی overshoot هنگامِ رهاکردن. انتخابش به این دلیل که **تنها حرکتی است که در هر
ضربه‌ی کاربر تکرار می‌شود**؛ بیشترین اثر بر «حس» با کمترین سطحِ تغییر.

**خطِ مبنای بیرونی (Context7 → M3 motion tokens):** مدتِ short/utility در Material 3 بازه‌ی
۵۰–۲۰۰ms است و `standard easing` همان `cubic-bezier(.4,0,.2,1)` است که ما `--ease-out` می‌نامیم. ما
در ۱۳۰ms می‌مانیم و فقط منحنیِ هندسه را فنری می‌کنیم. **این مقایسه با تیک‌تاک نیست** — Mobbin
نداریم — مقایسه با یک مرجعِ عمومیِ منتشرشده است.

**reduced-motion، اندازه‌گیری نه فرض:** `tokens.css:187` مدتِ همه‌ی transitionها را `0.01ms`
می‌کند و `app.css:534` برای `.btn:active` `transform:none` می‌گذارد. در آن حالت نه فنر هست نه
فروترفتن. (در §۶ سنجیده شده.)

---

## ۵. spec برای `apps/**` — به ترتیبِ اثر برای یک ۲۲ساله

1. **S1 — محتوا بالای fold.** فید («محبوب امشب» و کارت‌ها) پیش از هیرو، یا هیرو به یک نوارِ
   جست‌وجویِ فشرده تبدیل شود. *این بزرگ‌ترین فاصله با «اسکرولِ زنده» است و تماماً مارک‌آپ.*
2. **S3/S4 — تب‌های خارج‌شده بن‌بست نباشند.** «باشگاه» و «اعتبار» خارج‌شده باید **چیزی برای
   دیدن** داشته باشند (مزایا، نمونه‌ی مأموریت، پیش‌نمایشِ فروشگاه) و ورود دعوت باشد نه دیوار.
   «اعتبار» امروز ۷۰٪ خالی است.
3. **V2 — یک نشانگرِ فوکوس.** `app.css:734` حذف شود (کنترل خودش حلقه‌ی توکنی دارد)، یا برعکس —
   نه هر دو.
4. **V1 — نرمال‌سازی در مرزِ seed→R، نه گاردِ قیمت.** `R_SAMPLE` یک‌بار از `normalizeMenuEntry`
   بگذرد (در `seed.js` هنگامِ export، یا در دو نقطه‌ای که خام برمی‌گردد: `init.js:12` و fallbackِ
   `loadRestaurants`). گاردِ `:128` به‌تنهایی آیتمِ خالی را پنهان می‌کند، نه اینکه درستش کند.
5. **S2 — `.theme-fab` از روی هیرو برداشته شود** (به هدر یا پروفایل).
6. **S5 — سه صفحه‌ی خارج‌شده از `.empty-state`ِ `foundation.css` استفاده کنند** — آن‌وقت لایه‌ی
   مشترک (من) ظاهرشان را یک‌جا کنترل می‌کند.

---

## ۶. خطِ پایه‌ی frame-time هنگامِ اسکرول — اولین عدد، با قیدهایش

روش: حلقه‌ی `requestAnimationFrame` فاصله‌ی هر فریم را ثبت می‌کند؛ ۱۲ فلیکِ ۲۵۰px با `wheel`
روی فید (Playwright/Chromium headless، ۳۹۰×۸۴۴ ‎@2x)، ۵ فریمِ اول دور ریخته شد.

```
frames 89 · p50 16.7ms · p95 33.4ms · max 33.4ms
>33ms: 18 (۲۰٪)  ·  >50ms: 0
```

**خوانش:** میانه دقیقاً ۶۰fps است؛ یک‌پنجمِ فریم‌ها **دوبرابر** (یک فریمِ افتاده)؛ هیچ گیرِ
بلندی (>۵۰ms) نیست. یعنی «لرزشِ ریزِ گاه‌به‌گاه»، نه «هنگ».

**قیدها، مهم‌تر از عدد:** این Chromiumِ headless روی یک PC است، **نه یک گوشیِ میان‌رده در
تهران**؛ CPU/GPU و throttling کاملاً متفاوت. `wheel` هم انگشت نیست. این عدد **خطِ پایه‌ی
مقایسه‌ای** است — برای اینکه بعد از هر تغییرِ بصری همان اسکریپت را بزنیم و ببینیم بدتر شد یا نه
— نه ادعایی درباره‌ی تجربه‌ی واقعی. اسکریپت: `scratchpad/scroll-ft.mjs` (باید به `tools/` بیاید
اگر قرار است گارد شود؛ فعلاً اندازه‌گیری است، نه گارد).

## ۷. آنچه این سند نمی‌گوید

- **حسِ اسکرول هنوز داوری نشده.** §۶ یک عدد روی دسکتاپ است، نه روی گوشی. «اسکرولِ زنده با حسِ
  خوب» نه تأیید می‌شود نه رد. قدمِ درست: همان اسکریپت روی یک اندرویدِ واقعی با CPU throttling.
- خطِ مبنای بیرونی برای «مثلِ تیک‌تاک» نداریم (Mobbin پولی). هر جا نوشتم تیک‌تاک/اینستاگرام چه
  می‌کند، **ادعای عمومی** است نه اندازه‌گیری. تنها مرجعِ اندازه‌گیری‌شده M3 است (§۴).
- همه‌ی عکس‌ها از باندلِ **آفلاین** با داده‌ی نمونه‌اند. V1 و V5 فقط روی همین داده رخ می‌دهند.
- V4 محلش **تأییدنشده** است.
- §۴ یک تغییرِ **میکرو** است و آن را بزرگ‌تر از این جا نمی‌زنم: چیزی که یک ۲۲ساله را نگه می‌دارد
  §۵/۱ و §۵/۲ است، نه فنرِ دکمه.
