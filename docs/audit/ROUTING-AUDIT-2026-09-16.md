# ROUTING.md در برابرِ رجیستریِ زنده — ۲۰۲۶-۰۹-۱۶

**سنجش، نه ویرایش.** `ROUTING.md` دست نخورد؛ ویرایشش با CEO است، در یک کامیت.
نوشته‌ی `rezv-34 [617ab1]` · sessionId `ace570d0-…` · بی‌نقش.

- **مبنا:** `main = 38e27c4` (`git ls-remote origin refs/heads/main`) در **۲۰۲۶-۰۹-۱۶T04:23:37Z**.
- متنِ سنجیده‌شده: `docs/audit/prompts/ROUTING.md` روی همان کامیت — آخرین تغییرش `6a6a20f` (۰۹-۱۶، ردیفِ Founder).
- منابعِ زنده: `~/.claude/sessions/*.json`، `ListAgents`، و `~/.claude/uploads/<sessionId>/`.
- ⚠️ **ناتمام.** دستورِ توقفِ مالک رسید. آنچه هست سنجیده است؛ آنچه نیست، در بخشِ «انجام‌نشده» آمده.

---

## ۰) اول یک هشدارِ روشی — رجیستری به‌تنهایی شاهدِ زنده‌بودن نیست

`~/.claude/sessions/*.json` **نُه** ردیف دارد ولی فقط **هشت** sessionIdِ یکتا، و یکی از ردیف‌ها به یک نشستِ مرده اشاره می‌کند:

| فایل | name | sessionId | pid | وضعِ واقعی |
|---|---|---|---|---|
| `296.json` | `rezv-37` | `4fa4aafb-…` | 296 | ❌ **pid 296 اجرا نمی‌شود** (`tasklist` هیچ چیزی برنمی‌گرداند)، و `ListAgents` هم `rezv-37` را فهرست نمی‌کند |
| `6564.json` | `rezv-90` | `4fa4aafb-…` | 6564 | ✅ زنده |

**هر دو ردیف یک sessionId دارند.** یعنی نشستِ Founder ری‌استارت شده و ردیفِ قدیمی در رجیستری **پاک نشده**.
هرکس فقط رجیستری را بخواند، `rezv-37` را یک نشستِ زنده‌ی دیگر می‌بیند؛ و هرکس از روی نام نگاشت کند، دو نشست می‌سازد از یکی.

**قاعده‌ای که از این درمی‌آید:** زنده‌بودن را با **دو** شاهد بسنجید — رجیستری **و** `ListAgents` — و کلید را `sessionId` بگیرید نه نام. اگر یک sessionId دو ردیف دارد، تازه‌ترین pid را بردارید و کهنه را نادیده بگیرید.

---

## ۱) جدولِ اصلی — هر ردیفِ نقش در `ROUTING.md`

ستونِ «ادعای فایل» عیناً از `ROUTING.md` است. ستونِ «امروز» سنجشِ من است.

| ردیفِ نقش | ادعای فایل | sessionIdِ ادعاشده | امروز در رجیستری؟ | نشستِ زنده‌ای که همین نقش را ادعا می‌کند | حکم |
|---|---|---|---|---|---|
| **CEO** | `rezv-67 [db8c63]` | — | ❌ هیچ نشستی با این نام | `rezv-23 [131a4b]` · `60c7681b-…` (زنده) | **شناسه‌ی مرده** |
| **Founder proxy** | `rezv-a6 [0a9092]` | `59dd4f5a-…` | ❌ نیست | — (خودِ ردیف می‌گوید جانشینش ردیفِ Founder است) | **شناسه‌ی مرده، و ردیف منسوخ** |
| **Founder** | `rezv-90 [06d57f]` | `4fa4aafb-…` | ✅ **هست** | خودش | ✅ **زنده — تنها ردیفِ درستِ فایل** |
| ~~CEO (۰۹-۰۹)~~ | ~~`rezv-9c [5283b5]`~~ | — | ❌ | — | **تاریخچه؛ خودِ فایل مرده علامتش زده** |
| **Reviewer** | `rezv-45 [6e359d]` | `baa73640-…` | ❌ نیست | **هیچ‌کدام** | **ردیفِ بی‌صاحب** |
| **Deputy** | `rezv-7d [c0cdf6]` | — | ❌ | **هیچ‌کدام** | **ردیفِ بی‌صاحب** |
| **Red Team** | `rezv-29 [0a8f9d]` | — | ❌ | `rezv-b5 [59c76c]` · `c07123f9-…` (زنده) | **شناسه‌ی مرده** |
| **Launch Engineer** | `rezv-d8 [1340fc]` | — | ❌ | ⚠️ `2bffd0d9-…` زنده است، ولی نامش `rezv-05 [f34821]` است — **نه `rezv-48`** | **شناسه‌ی مرده + ناهم‌خوانیِ نام (زیر)** |
| **Designer** | `rezv-60 [a8b9c8]` | — | ❌ | `rezv-0b [60739f]` · `6c9efba5-…` (زنده) | **شناسه‌ی مرده** |
| **Marketer** | `rezv-3c [8ab843]` | — | ❌ | **هیچ‌کدام** | **ردیفِ بی‌صاحب** |
| **Backend Engineer** | `rezv-36 [30f311]` | — | ❌ | `rezv-c9 [cc393e]` · `edb5f154-…` (زنده) | **شناسه‌ی مرده** |

**خلاصه:** از ۱۰ ردیفِ نقشِ فعال، **۱** ردیف امروز resolve می‌کند (Founder). ۶ ردیف شناسه‌ی مرده دارند ولی نشستِ زنده‌ای برایشان هست. **۳ ردیف بی‌صاحب‌اند** — Reviewer، Deputy، Marketer: نه شناسه‌شان زنده است و نه نشستی امروز آن نقش را برداشته.

---

## ۲) ⚠️ ناهم‌خوانی در نقشه‌ی تیم — نامِ Launch Engineer

نقشه‌ای که CEO فرستاد می‌گوید: «Launch Engineer `rezv-48` 2bffd0d9». sessionId درست است و زنده، ولی **نامش `rezv-48` نیست**:

- رجیستری (`4884.json`): `sessionId = 2bffd0d9-…` کنارِ `name = rezv-05`، و `nameSince` **برابرِ** `startedAt` است — یعنی این نشست از لحظه‌ی شروع `rezv-05` بوده و **هیچ‌وقت `rezv-48` نبوده**.
- `ListAgents` هم `rezv-05 [f34821]` را زنده فهرست می‌کند و هیچ `rezv-48`ی ندارد.
- `rezv-48` در این مخزن نامِ یک **worktree و شاخه** است (`wt-rezv-48`، `session/rezv-48`)، نه نامِ نشست.

**احتمالِ ساده:** نقش از روی نامِ worktree نوشته شده نه از روی نامِ نشست. چون این دقیقاً همان خطایی است که `ROUTING.md` درباره‌اش هشدار می‌دهد، در جدولِ اصلاح‌شده باید **sessionId** کلید باشد و نام فقط کنارش بیاید.

---

## ۳) نشست‌های زنده‌ای که ردیف ندارند

| نشست | sessionId | ادعای نقش |
|---|---|---|
| `rezv-34 [617ab1]` | `ace570d0-…` | **بی‌نقش** — از روی transcriptِ خودم: اولین ورودیِ `user` با `parentUuid: null` پیامِ CEO بود، هیچ منشوری پیست نشده، `~/.claude/uploads/ace570d0-…/` وجود ندارد |
| `rezv-bf [5d7ef7]` | `74a7ffab-…` | در نقشه‌ی CEO «بی‌نقش» آمده. **من ردیفش را نمی‌نویسم** — خودش باید بنویسد |

---

## ۴) شاهدهای جانبی که یافتم (نه اثباتِ نقش)

`~/.claude/uploads/<sessionId>/` نشان می‌دهد چه فایلی در کدام نشست پیست شده. این شاهدِ **موضوع** است، نه شاهدِ **انتصاب** — همان تمایزی که ردیفِ Marketer قبلاً رویش ایستاد:

| sessionId | فایلِ پیست‌شده | زنده؟ |
|---|---|---|
| `60c7681b-…` (CEOِ امروز) | `REZERVNOMASTERPROMPT.md` | ✅ |
| `c07123f9-…` (Red Teamِ ادعاشده) | `founder-squad.plugin`، `rezervno-fixes.zip` — **هیچ منشوری نه** | ✅ |
| `2d3fabfd-…` | `backendengineer.md` | ❌ مرده — این نشستِ Backendِ ۰۹-۱۰ است که ردیفِ `rezv-36` رویش ایستاده بود |
| `59dd4f5a-…` (Founder proxy) | `founder-squad.plugin` | ❌ مرده |

نکته: نشستِ زنده‌ای که امروز Backend خوانده می‌شود (`edb5f154-…`) پوشه‌ی uploads **ندارد**؛ منشورِ `backendengineer.md` در نشستِ **مرده‌ی** `2d3fabfd-…` پیست شده بود. یعنی زنجیره‌ی شاهدِ آن ردیف با ری‌استارت پاره شده و باید از نو ساخته شود.

---

## ۵) انجام‌نشده — و چرا

**دستورِ توقفِ مالک رسید (سقفِ مصرف).** این یک قلم شروع شده بود و تمام نشد:

- **گرفتنِ شاهدِ مستقلِ نقش از transcriptِ هر نشستِ زنده** — یعنی grep روی `<sessionId>.jsonl` برای ارجاع به `docs/audit/prompts/<نقش>.md`. اندازه‌ی فایل‌ها را گرفتم (`6c9efba5` ۳۷MB، `60c7681b` ۲۴MB، بقیه ۱–۸MB) ولی خودِ grep اجرا نشد.
  **تا وقتی این انجام نشده، ستونِ «نشستِ زنده‌ای که همین نقش را ادعا می‌کند» از نقشه‌ی CEO آمده، نه از سنجشِ من.** آنچه من سنجیدم این است که آن sessionIdها **زنده‌اند** و نامِ امروزشان چیست — نه اینکه آن نقش را دارند.

**پیشنهاد برای وقتی ROUTING بازنویسی می‌شود:** هر ردیف `sessionId` را به‌عنوانِ کلید بنویسد و `name [ref]` را فقط به‌عنوانِ آدرسِ امروز، با تاریخِ سنجش کنارش. نام‌ها هر ری‌استارت عوض می‌شوند؛ sessionId نه.
