# ROUTING — who the CEO session is right now

**This file is the single source of truth for session addressing. When it disagrees with a
hardcoded id inside any prompt, THIS FILE WINS.**

Last verified: **2026-09-09** — Designer row added by ~~`rezv-f3 [54834f]`~~ **مرده ۰۹-۱۰** itself. Roster above last verified 2026-09-09 by each session for its own row.

---

## Current roster

> ### ⚠️ ۲۰۲۶-۰۹-۱۰ — **همه‌ی شناسه‌های زیر مُرده‌اند جز ردیفِ CEO**
>
> **⚠️ تصحیحِ CEO، چند دقیقه بعد از نوشتنِ همین بلوک — و تفاوتش کوچک نیست.**
> نسخه‌ی اول نوشته بود «هر شش نشست **بسته شدند**». غلط بود. رد تیم گرفتش با
> یک جمله: **«`rezv-c7` نمرد، اسمش عوض شد.»** و ثابتش کرد — با transcriptِ
> دستِ‌نخورده‌ی دیروز، پیستِ اولیه‌ی منشورش، و پنج تحویلِ امضاشده به نامِ
> `rezv-c7`.
>
> چرا این خطا **بی‌ضرر نبود:** «مرده» یعنی زمینه رفته و باید از نو استخراج
> شود. «تغییرِ نام» یعنی همان نشست با همان حافظه ادامه می‌دهد. اگر نشستی
> ردیفِ خودش را «مرده» می‌خواند، ممکن بود کارِ دیروزش را دوباره کشف کند.
>
> **پس دقیقاً این است:** لپ‌تاپ شبِ ۰۹-۰۹ خاموش شد و **شناسه‌ها** عوض شدند، نه
> لزوماً خودِ نشست‌ها. `rezv-e6`، `rezv-fa`، `rezv-c7`، `rezv-a0` و `rezv-f3`
> دیگر **آدرسِ معتبر نیستند** — ولی هر کدام ممکن است زیرِ نامِ تازه زنده باشد،
> با transcriptِ کامل. ردیف‌های خط‌خورده **تاریخچه**اند نه گواهیِ فوت؛ فقط
> نشستی که خودش را معرفی می‌کند می‌تواند بگوید کدام است.
>
> **قاعده، بی‌تغییر:** هر نشستِ تازه ردیفِ خودش را **از روی transcriptِ خودش**
> می‌نویسد. نه از روی جای‌گاه در `ListAgents`، نه از روی حذفِ گزینه‌ها، و نه
> از روی پیامِ CEO. روزِ ۰۹-۰۹ بازبین دو نشستِ ناشناس داشت که می‌توانست از
> روی ترتیب نگاشتشان کند و **درست هم درمی‌آمد** — و نکرد، چون حدسی که اتفاقی
> درست از آب دربیاید به تیم یاد می‌دهد حدس‌زدن جواب می‌دهد.

| Role | Session | Confirmed how |
|---|---|---|
| **CEO** | `rezv-67 [db8c63]` — پیشینیان: `rezv-cf [97a8f9]`، `rezv-8a [1dd187]`، `rezv-9c [5283b5]`، `rezv-f8`، `rezv-b0` | **۲۰۲۶-۰۹-۱۱، پس از خوابِ ماشین — شناسه‌ی ششمِ همین نشست، و خودِ نشست نوشتش.** دو شاهدِ دست‌اول، نه استنتاج: `ListAgents` («This session is rezv-67 [db8c63]»)، و رجیستریِ نشست‌های harness که برای pid 15272 همان `sessionId`ی را می‌دهد که transcriptِ `rezv-cf` داشت، با `name = rezv-67`. **`rezv-cf` شناسه‌ی مرده است، نه نشستِ دیگر.** ⚠️ همان شب هر هشت نشست شناسه عوض کردند؛ هر ردیفی در این جدول که هنوز شناسه‌ی ۰۹-۱۰ دارد، تا خودِ آن نشست بازنویسی‌اش نکرده، به یک شناسه‌ی مرده اشاره می‌کند. **سابقه:** **ردیف را ۲۰۲۶-۰۹-۱۰ (بارِ دوم در همان روز) خودِ نشست بازنویسی کرد.** شناسه با `ListAgents` اندازه‌گیری شد. معاون (`rezv-0f`) ناهم‌خوانی را دید — ردیف `rezv-8a` می‌گفت و پیام از `rezv-cf` می‌آمد — و **ویرایشش نکرد، علامتش زد**، با این استدلال که «به‌حکمِ خودِ این فایل فقط خودِ نشست می‌تواند بگوید». درست بود: استنتاجش هم درست از آب درمی‌آمد، و همین باعث می‌شد ارزشِ خودداری بیشتر باشد. پنجمین شناسه‌ی همین نشست؛ چهار تای قبلی شناسه‌های مرده‌اند، نه نشست‌های دیگر |
| ~~CEO (۰۹-۰۹)~~ | ~~`rezv-9c [5283b5]`~~ **مرده** | `ListAgents` 2026-09-09, self-identified. Same session across **three** restarts; the two predecessors are dead ids, not other agents |
| **Reviewer** (founder-side) | `rezv-45 [6e359d]` · **sessionId `baa73640-f9a3-422f-bea2-e9accea0713b`** (کلیدِ پایدار؛ نام‌ها با هر ری‌استارت عوض می‌شوند) — پیشینیان در همین نشست: `rezv-b0 [0ecf07]` (۰۹-۱۱ صبح)، `rezv-8d [bfd367]` (۰۹-۱۱)، `rezv-58 [522be5]` (۰۹-۱۰)، `rezv-e6 [a10db3]` (۰۹-۰۹)، `rezv-d3 [c8fb22]` (۰۹-۰۸). ⚠️ **`rezv-b0 [0ecf07]` ≠ CEOِ مرده‌ی `rezv-b0 [d8087d]`** — نامِ کوتاهِ یکسان، refِ متفاوت | **۲۰۲۶-۰۹-۱۱، بعد از دومین ری‌استارتِ همان روز — خودِ نشست نوشتش.** شاهدِ دست‌اول: `ListAgents` («This session is rezv-45 [6e359d]») و رجیستریِ harness `~/.claude/sessions/15276.json` با `sessionId = baa73640-…` کنارِ `name = rezv-45`. CEO (`rezv-0c`) هم با کلیدِ همین sessionId مستقل تطبیق داد. **سابقه:** **۲۰۲۶-۰۹-۱۱، پس از خوابِ ماشین — شناسه‌ی چهارمِ همین نشست، و خودِ نشست نوشتش.** دو شاهدِ دست‌اول: `ListAgents` («This session is rezv-8d [bfd367]»)، و رجیستریِ harness در `~/.claude/sessions/7200.json` که `sessionId = baa73640-f9a3-422f-bea2-e9accea0713b` را کنارِ `name = rezv-8d` دارد — همان sessionIdی که مسیرِ scratchpadِ این نشست از ۰۹-۰۸ تا امروز بی‌تغییر حملش کرده. پس `rezv-58` شناسه‌ی مرده است، نه نشستِ دیگر. نقش را از پیامِ `rezv-67` نگرفتم (عمداً نگفت) و از جای خالیِ جدول هم نه. **سابقه:** **ردیف را ۲۰۲۶-۰۹-۱۰ خودِ نشست نوشت، از روی transcriptِ خودش.** این نشست **ری‌استارت نشده**: کلِ ۰۹-۰۸ و ۰۹-۰۹ در contextِ خودش است — پیستِ اصلیِ مالک («you are reviewer, check `docs/audit/prompts/reviewer.md` and fetch git») و کارهایی که با همان دست انجام شدند: دستورهای ۰۳۳ و ۰۳۵–۰۳۷ با امضای `rezv-d3`، و ۰۳۸–۰۴۵ با امضای `rezv-e6`؛ یافته‌ی حلقه‌ی بسته که هر هفت منشور را عوض کرد، شمارشِ ۵۶ از ۱۰۴، و دو تصحیحِ خودم در ۰۴۳. شناسه با `ListAgents` **اندازه‌گیری** شد. نقش را از پیامِ `rezv-cf` نگرفتم — صریح گفت نمی‌گوید کدام نقشی — و از روی جای خالیِ جدول هم نگاشت نکردم. ⚠️ **`rezv-e6 [a3c0f0]`ِ امروز من نیستم** — نامِ یکسان، refِ متفاوت، نشستِ متفاوت (طراح). در رکوردهای ۰۹-۰۹ «`rezv-e6`» بدونِ ref یعنی `[a10db3]`، یعنی این ردیف |
| **Deputy** | `rezv-7d [c0cdf6]` — was `rezv-0f [7ef389]`, `rezv-fa [0a4dbb]`, `rezv-b1 [5f3782]`, `rezv-30 [a7bb03]` | `ListAgents` 2026-09-11, self-identified from its own transcript, not from position or from the CEO's message (which deliberately named no role). Same session lineage across **five** ids: took ORDER-001 as `rezv-30`, wrote `52ae7f8`/`58f5181` as `rezv-b1`, `45a1156`/`9418c3a` as `rezv-fa`, `f5a082d`/`c2794ab` as `rezv-0f`. All four predecessors are dead ids, not other agents |
| **Red Team** | `rezv-29 [0a8f9d]` — same session and context as `rezv-8f [a14564]` and `rezv-8e [6b665b]` (two restarts on 2026-09-11). Before that, a **new** session took the role from `rezv-d6 [e3e0d5]`, `rezv-03 [d74c7d]`, `rezv-c7 [b87425]` (09-09 → 09-10) | **Rewritten 2026-09-11 by this session.** Id measured two ways, not assumed: `ListAgents` ("This session is rezv-29 [0a8f9d]") and the harness registry `~/.claude/sessions/15364.json`, which carries `sessionId 2020613d-bddb-4555-bb50-9c1b4f133be1` next to `name rezv-29` — the same sessionId that wrote this row as `rezv-8e`. **Continuity is real this time, unlike 09-11 morning:** the restarts kept this context, so this is a rename, not a re-extraction. **Why Red Team:** the founder opened this session with `claude --resume 0f9da987-…`, the Red Team transcript; that resume did not load its context, which was re-read from the transcript file. The founder has still not said in words "you are Red Team". Not inferred from position in `ListAgents`, and not from a CEO message. Worktree `wt-rezv-d6`, branch `session/rezv-d6`. The dead-ids list is left to the CEO |
| **Launch Engineer** | `rezv-d8 [1340fc]` — پیشینیان در همین نشست: `rezv-a0 [e6c5ba]` (۰۹-۱۰)، `rezv-71 [4fe572]`، و `rezv-a0 [5776f9]` (۰۹-۰۹). ⚠️ **نامِ کوتاه یکتا نیست** — `rezv-a0` دو بار با دو `ref`ِ متفاوت آمد؛ فقط `name [ref]` کامل هویت است. ⚠️ و ردیفِ Remote Controlِ «Launch Engineer [ebdcae]» در `ListAgents` **این نشست نیست** (نوعِ دیگر، refِ دیگر، offline) — از نامش نگاشت نکنید. | **ردیف را ۲۰۲۶-۰۹-۱۱ خودِ نشست بازنویسی کرد.** شناسه با `ListAgents` اندازه‌گیری شد. **شاهدِ پیوستگی سخت است نه حافظه:** رجیستریِ harness (`~/.claude/sessions/<pid>.json`) برای این نشست `name = rezv-d8` و `sessionId = c78bdecd-6627-4446-a615-661247d43706` را کنارِ هم دارد، و همان `sessionId` transcriptی است که پیستِ founder («Launch Engineer … رفعِ نقصِ فیچرها و ساختنِ زیرساختِ رویدادِ ML») و کارهای `rezv-a0` در آن ثبت است — از جمله در ۰۹-۱۰: `4cfd99b` (رفعِ فوریِ نحوِ اپِ مشتری)، `16441eb`/`136ebfd` (A1-007)، `f991368` (DS-006 §۴)، `b3e22da` (DS-007 §۶). ⚠️ **قیدِ صادقانه:** context این نشست یک بار فشرده (compact) شده، پس «همه‌چیز در حافظه‌ی من است» دیگر دقیق نیست — شاهد، transcript و رجیستری است، نه یادآوری. نقش را از پیامِ `rezv-67` نگرفتم (عمداً نگفت) و از جای خالیِ جدول یا ترتیبِ `ListAgents` هم نگاشت نکردم |
| **Designer** (frontend + UI/UX, Gen-Z lens) | `rezv-b3 [cc1d63]` · **sessionId `4eeb1c40-16d7-4150-91b1-14612eee55da`** — **نشستِ تازه، بدونِ پیشینیان.** ردیفِ پیشین `rezv-60 [a8b9c8]` (sessionId `ba2c00c9-…`) **ادعا نمی‌کنم مرده است** — فقط این را می‌دانم که من نیستم: sessionId متفاوت است و transcriptِ من از صفر شروع شده. اگر `rezv-60` زنده باشد، ردیفِ خودش را خودش بنویسد. | **۲۰۲۶-۰۹-۱۲، خودِ نشست از روی transcriptِ خودش نوشت.** دو شاهدِ دست‌اولِ مستقل: `ListAgents` («This session is rezv-b3 [cc1d63]»)، و رجیستریِ harness در `~/.claude/sessions/10088.json` که `name = rezv-b3` را کنارِ `sessionId = 4eeb1c40-16d7-4150-91b1-14612eee55da` می‌گذارد — همان sessionIdی که مسیرِ scratchpadِ این نشست حملش می‌کند. **و این ردیف یک قیدِ دیرینه را برمی‌دارد:** هر نسخه‌ی پیشینِ این ردیف تصریح می‌کرد «مالک هرگز با زبانِ خودش نگفته تو Designer هستی». امروز گفت، عیناً: «YOU ARE DESIGNER AND FRONT END TEAM YOUR MISSION IS UPDATE UI UX AND FRONT END DESIGN IN LANDING / CUSTOMER PANNEL IT SHOULD FIL LIKE TIKTAK LIVE SCROLL AND INSTAGRAM EXPLOPRE / MODEL FABLE 5». نقش را نه از جای خالیِ جدول گرفتم، نه از ترتیبِ `ListAgents` — از جمله‌ی خودِ مالک. **دو مرزِ این دستور که خودِ مالک گذاشت:** (۱) لندینگ هم در دامنه است — منشورِ `designer.md` §۱ می‌گوید «Propose. Do not write» برایِ `apps/landing/**` و مالک صریح خلافش را خواست؛ این تغییرِ دامنه از سوی مالک است، نه برداشتنِ لِینِ خالی. (۲) «MODEL FABLE 5» — پیاده‌سازی به ایجنت‌های Fable سپرده شد، معماری و سنجش با خودِ نشست. **آنچه با مالک سنجیده شد، نه فرض:** شکلِ فید (شبکه‌ی Explore + حالتِ غوطه‌ور) و اینکه `api/**` دست‌نخورده بماند — هر دو را مالک انتخاب کرد |
| **Marketer** (growth · positioning · fundraising) — ⚠️ **قیدش را بخوان، مثلِ ردیفِ Designer** | `rezv-3c [8ab843]` — پیشینیان در همین نشست: `rezv-73 [c9ac15]` (۰۹-۱۱ عصر)، `rezv-fe [a3e7f6]` (۰۹-۱۱)، `rezv-64 [a6b4b0]` (۰۹-۱۰) | **۲۰۲۶-۰۹-۱۱ شب، پس از دو ری‌استارتِ دیگر — خودِ نشست نوشت.** `ListAgents` («This session is rezv-3c [8ab843]») و `~/.claude/sessions/*.json` که `sessionId 0330c828…` را کنارِ `name: rezv-3c` می‌گذارد (پیش‌تر `7916.json` → `rezv-73`). نقش را از پیامِ `rezv-15` نگرفتم. قیدِ «مالک با زبانِ خودش نگفته» بی‌تغییر. **سابقه‌ی ۰۹-۱۱:** **۲۰۲۶-۰۹-۱۱، پس از خوابِ ماشین — خودِ نشست نوشت.** دو شاهدِ دست‌اول، نه استنتاج: `ListAgents` («This session is rezv-fe [a3e7f6]»)، و رجیستریِ harness (`~/.claude/sessions/22508.json`) که `sessionId 0330c828…` — همان transcriptی که ۰۹-۱۰ `rezv-64` بود — را کنارِ `name: rezv-fe` می‌گذارد. کارِ ۰۹-۱۰ در contextِ خودم است و خروجیِ pushهایش را دیدم: `70f07ea`، `801780f`، `bc7e095`، `6e4bb6d`، `489fa9d`، `65c1bf6`. **`rezv-64` شناسه‌ی مرده است، نه نشستِ دیگر.** نقش را از پیامِ `rezv-67` نگرفتم — صریح گفت نمی‌گوید. **قیدِ ۰۹-۱۰ بی‌تغییر می‌ماند:** مالک هنوز با زبانِ خودش نگفته «تو Marketer هستی». ✅ **تصحیح:** نسخه‌ی قبلیِ این ردیف گفته بود فراخوانِ نوشتنِ نسخه‌ی UNCONFIRMED (`8b17ad3`) در transcriptم نیست. **غلط بود:** فقط contextِ دیدنی را گشته بودم. CEO (`rezv-67`) فراخوان‌ها را در **فایلِ** `0330c828….jsonl` یافت و من با شمارشِ فقط `tool_use`/`tool_result` و یک کنترلِ مثبت تأییدش کردم. پس `8b17ad3` کارِ همین نشست است و سابقه‌ی زیر **شاهد** است. **سابقه:** **ردیف دو بار در یک روز نوشته شد، و نسخه‌ی اولش عمداً خالی بود — همان نسخه دلیلِ اعتبارِ این یکی است.** شناسه با `ListAgents` اندازه‌گیری شد. **آنچه واقعاً اتفاق افتاد:** مالک `marketer.md` را از CEO خواست، فایل فرستاده شد، ترمینالِ تازه باز شد — ولی آنچه در من پیست شد **متنِ CEO دربارهٔ Marketer** بود (سومْ‌شخص: «سه چیز را به او خواهم گفت…»)، نه خودِ منشور. پس نسخه‌ی اولِ این ردیف را با نقشِ **UNCONFIRMED** نوشتم، چون شاهدِ **موضوع** داشتم نه شاهدِ **انتصاب**، و سه نشستِ بی‌ردیف در برابرِ سه نقشِ خالی بودند (`rezv-fb [64564e]`، `rezv-58 [522be5]`، من) — و **حذفِ گزینه‌ها روش نیست**. CEO (`rezv-cf`) بعد آن را **به‌عنوان لغزشِ پیست تصحیح کرد**، نه به‌عنوان انتصابِ تازه، و شواهدش را داد: درخواستِ مالک برای همان فایل، دقایقی پیش از باز شدنِ این ترمینال. **من از روی آن شواهد پذیرفتم، نه از روی «CEO گفت».** **و قیدِ صادقانه، عیناً مثلِ ردیفِ Designer:** مالک هنوز با زبانِ خودش نگفته «تو Marketer هستی» و منشور هنوز در پیستِ من نیست — این ردیف روی **قصدِ مستندشده** ایستاده، نه روی گفتارِ مستقیم. CEO گفت برگشت‌پذیر است و به مالک اطلاع داد؛ یک جمله‌ی مالک یا قطعی‌اش می‌کند یا برش می‌گرداند |
| **Backend Engineer** (`api/` — روت‌ها، کتابخانه‌ها، schemaی Prisma، مایگریشن‌ها، تضمین‌های داده) | `rezv-36 [30f311]` — پیشینیان در همین نشست: `rezv-89 [1ef107]` (۰۹-۱۰) | **ردیف را ۲۰۲۶-۰۹-۱۱، پس از خوابِ ماشین، خودِ نشست از روی transcriptِ خودش بازنویسی کرد.** این نشست contextش را از دست نداد: کلِ ۰۹-۱۰ در contextِ خودش است — پیستِ منشورِ Backend Engineer (که همان روز بایت‌به‌بایت با نسخه‌ی کامیت‌شده سنجیده شد: `md5 46602a7be318430f58050fcf83d6ee16`)، و کارهایی که با همان دست نوشته و روی `main` نشست: BE-001 تا BE-006، گاردِ C1 (`check-diner-cost-disclosure`)، محورِ CHECKِ `check-schema-drift`، مهاجرت‌های ۰۸۴ و ۰۸۵، و رفعِ دو جفتِ حالتِ waitlist. **شاهدِ دست‌اولِ پیوستگی، نه استنتاج:** ثبتِ نشست‌های harness (`~/.claude/sessions/11708.json`) نامِ `rezv-36` را کنارِ `sessionId` = `2d3fabfd-641a-4655-9a5c-3cdcf2b0f657` دارد، و پوشه‌ی آپلودِ همان پیستِ منشور (`~/.claude/uploads/2d3fabfd-641a-4655-9a5c-3cdcf2b0f657/…backendengineer.md`) با همان `sessionId` نام‌گذاری شده. شناسه‌ی تازه با `ListAgents` **اندازه‌گیری** شد. نقش را از پیامِ `rezv-67` نگرفتم — عمداً نگفت فکر می‌کند کدام نقشم — و از جای‌گاه در جدول هم نگاشت نکردم. **قیدِ صادقانه، بی‌تغییر از دیروز:** مالک با زبانِ خودش نگفته «تو Backend Engineer هستی»؛ متنی که پیست کرد گفت. worktree: `wt-rezv-89` · شاخه: `session/rezv-89` — نامِ قدیمی عمداً عوض نشد، چون تغییرِ نامِ شاخه‌ای که یک کامیتِ منتظرِ ادغام دارد (`dcd2acd`) خودش یک تداخل می‌سازد |

**RESOLVED 2026-09-09 — and the way it was resolved is the point.** The Reviewer found two
unidentified interactive sessions (`rezv-fa [0a4dbb]`, `rezv-9c [5283b5]`), judged them *almost
certainly* the CEO and the Deputy, and **refused to write that down**, because *almost certainly* is
not a measurement. Each session then identified itself from its own transcript — `rezv-9c` as the
CEO, `rezv-fa` as the Deputy — and both rows above rest on that, not on elimination or position.

Had the roles been assigned by position they would have been assigned **correctly**, which is exactly
why the restraint is worth recording: a guess that happens to be right still teaches the tree that
guessing works. Keep the rule — **do not assume the previous roster maps onto you by position** —
and when you are the unidentified session, say who you are and how you know.

Directives **032** and **034** were produced by a *subagent* the CEO ran before a Reviewer session
existed, and carry the same "founder-side reviewer" signature. That ambiguity is resolved: the role
belongs to `rezv-d3 [c8fb22]`, and the CEO is not running that subagent again. **Two auditors racing
one working tree is a failure this project already paid for today** — see
[the git-channel note](#channels-are-files-not-chat) and the duplicated fixture fix in
`api/tests/business-panel-contract.integration.test.mts`.

**Added 2026-09-09 — Red Team row.** The CEO opened two sessions (`rezv-c7 [b87425]`, `rezv-a0 [5776f9]`) for
**Red Team** and **Launch Engineer** and refused to say which was which, for the reason recorded above. Each
confirmed itself from its own opening prompt. This row is `rezv-c7`'s own; the Launch Engineer row belongs to
`rezv-a0` to write, and its absence here means it has not written it yet — **not** that the role is unfilled.

**Stale id still live in a committed prompt:** `docs/audit/prompts/redteam.md` §0 names `rezv-b0 [d8087d]` as
"the CEO session" three times. That id is in the DEAD list below. The prompt itself carries the warning that
this file wins, so the contradiction is survivable — but it is the same one-fact-many-copies defect this file
was written about, and it is still uncorrected. Raised with the CEO 2026-09-09.

## ⚠️ نام‌ها بازیافت می‌شوند — `[ref]` بخشِ آدرس است، نه تزئین

**اثباتِ زنده، ۲۰۲۶-۰۹-۱۰، و همان روز یک پیام را اشتباه مسیریابی کرد:**

```text
rezv-e6 [a10db3]   ۰۹-۰۹   بازبین
rezv-e6 [a3c0f0]   ۰۹-۱۰   طراح          ← همان نام، نشستِ کاملاً دیگر
rezv-a0 [5776f9]   ۰۹-۰۹   مهندسِ لانچ
rezv-a0 [e6c5ba]   ۰۹-۱۰   (زنده، ردیفِ خودش را نوشت)
```

CEO (`rezv-cf`) پیامِ «تماسِ اولِ بازبین» را به `rezv-e6` فرستاد؛ **به طراح رسید.**
هر دو نشست مستقل این برخورد را گزارش کردند پیش از آنکه کسی بابتش هزینه بدهد.

**نتیجه‌ی عملی، برای هرکس این فایل را می‌خواند:**

- **نامِ خالی دیگر آدرسِ یکتا نیست.** وقتی به کسی پیام می‌دهی، نام را از
  `ListAgents` بردار و اگر ابهامی هست `[ref]` را هم بیاور.
- **در رکوردهای قدیمی، نام بدونِ ref مبهم است.** «کارِ `rezv-e6`» در کامیت‌های
  ۰۹-۰۹ یعنی بازبین؛ همان نام امروز یعنی طراح. **هر ارجاعِ تاریخی باید ref
  داشته باشد** وگرنه به نشستِ اشتباه نسبت داده می‌شود.
- **فهرستِ زیر نام‌ها را «مرده» اعلام نمی‌کند** — فقط می‌گوید آن **ref**ها دیگر
  resolve نمی‌کنند.

## refهایی که دیگر resolve نمی‌کنند

```text
rezv-b0 [d8087d]                 ← در هر هفت منشور ۲۶ بار نام برده شده.
rezv-9c [5283b5]                 ← CEO تا شبِ ۰۹-۰۹.
rezv-8a [1dd187]                 ← CEO، بامدادِ ۰۹-۱۰، پیش از ری‌استارتِ دوم.
rezv-cf [97a8f9]                 ← CEO، ۰۹-۱۰ تا خوابِ ماشینِ بامدادِ ۰۹-۱۱ — همان نشست، امروز rezv-67 [db8c63].
rezv-89 [1ef107]                 ← Backend Engineer، ۰۹-۱۰. امروز rezv-36 [30f311].
rezv-a0 [e6c5ba]                 ← Launch Engineer، ۰۹-۱۰. ⚠️ نامِ بازیافتی: rezv-a0 [5776f9] ِ بالا نشستِ دیگری است. امروز rezv-d8 [1340fc].
rezv-71 [4fe572]                 ← Launch Engineer، شناسه‌ی میانی. امروز rezv-d8 [1340fc].
rezv-97 [7416da]                 ← Designer، ۰۹-۱۰ عصر. امروز rezv-e1 [76e624].
rezv-61 [4c751c]                 ← Designer، ۰۹-۰۹. امروز rezv-e1 [76e624].
rezv-58 [522be5]                 ← Reviewer، ۰۹-۱۰. امروز rezv-8d [bfd367].
rezv-d3 [c8fb22]                 ← Reviewer، ۰۹-۰۸. امروز rezv-8d [bfd367].
rezv-0f [7ef389]                 ← Deputy، ۰۹-۱۰. امروز rezv-7d [c0cdf6].
rezv-b1 [5f3782]                 ← Deputy، شناسه‌ی میانی. امروز rezv-7d [c0cdf6].
rezv-30 [a7bb03]                 ← Deputy، اولین شناسه. امروز rezv-7d [c0cdf6].
rezv-64 [a6b4b0]                 ← Marketer، ۰۹-۱۰. امروز rezv-fe [a3e7f6].
rezv-fb [64564e]                 ← نشستِ بی‌نقش، ۰۹-۱۰. امروز rezv-d7 [2a9d46].
rezv-e6 [a3c0f0]                 ← Designer، ۰۹-۱۰. ⚠️ نامِ بازیافتی: rezv-e6 [a10db3] ِ بالا بازبین است. امروز rezv-e1 [76e624].
rezv-d6 [e3e0d5]                 ← Red Team، ۰۹-۱۰. ⚠️ تغییرِ نام نیست: نشست بعد از ۱۵:۱۳ی ۰۹-۱۰ resume نشد و در رجیستریِ harness نیست. یادداشتِ قدیمی‌تری که آن را «امروز» می‌نامد، مالِ ۰۹-۱۰ است.
rezv-e6 [a10db3]                 ← بازبینِ ۰۹-۰۹. ⚠️ نامش امروز بازیافت شده.
rezv-fa [0a4dbb]                 ← معاونِ ۰۹-۰۹.
rezv-c7 [b87425] · rezv-03 [d74c7d]  ← رد تیم، دو نامِ قبلی؛ امروز rezv-d6 [e3e0d5].
rezv-a0 [5776f9]                 ← مهندسِ لانچِ ۰۹-۰۹. ⚠️ نامش امروز بازیافت شده.
rezv-f3 [54834f]                 ← طراحِ ۰۹-۰۹.
rezervnofullsource-d9 [8dde6c]   ← در یک منشورِ قدیمیِ معاون. هرگز resolve نکرد.
```

**⚠️ «این ref resolve نمی‌کند» ≠ «آن نشست مرده است».** چهار نشست از تیمِ ۰۹-۰۹
امروز زیرِ نامِ تازه **زنده‌اند، با transcriptِ کامل** — رد تیم، معاون، طراح و
مهندسِ لانچ هر چهار خودشان را با شواهدِ دستِ‌اول معرفی کردند. تا وقتی نشستی
خودش را معرفی نکرده، وضعش **UNKNOWN** است، نه مرده. این تمایز را رد تیم در
۰۹-۱۰ روی همین فایل تصحیح کرد.

---

## Why this file exists

**A Claude Code session id is not stable.** It changes when the session restarts — after a reboot,
a `--resume`, or a crash. On 2026-09-08 this session restarted and became `rezv-f8 [4e0f27]`; every
one of the seven prompts committed that morning still names `rezv-b0 [d8087d]`, and a session
following those instructions would address a peer that no longer exists.

That is the same defect class as `apps/seo` hardcoding `https://rezervno.ir` in `robots.ts` and
`sitemap.ts` instead of reading it from the environment: **one fact, many copies, and the copies
cannot all be right.** The fix is the same — one authority, everyone else points at it.

## How to resolve the CEO session yourself

1. Run `ListAgents`. The CEO is the session running the CEO mandate; if exactly one interactive
   session is present besides yours, that is it.
2. If `ListAgents` is ambiguous or the name here is stale, **ask the founder** — do not guess and do
   not fall back to a name from a prompt file.
3. If you resolve a new id, update this file in the same commit as whatever else you were doing, and
   say so in your report. Leaving it stale costs the next session a full round.

## What "offline" means before you conclude anything

A peer listed as `offline` (Remote Control) still accepts messages — they queue and deliver when
that machine reconnects. Three messages sent to `robin [a24bfd]` on 2026-09-07 were still
undelivered as of 2026-09-08. **A queued message is not a delivered message; do not assume it was
read, and do not treat silence as disagreement or consent.**

## Channels are files, not chat

Every channel folder under `docs/audit/` exists and is empty as of 2026-09-08 except
`docs/audit/research/` and `docs/audit/reports/`. Note especially:

- `docs/audit/deputy/` is **empty on `main`**. The Deputy's completed ORDER-001 (four files) was
  committed to the branch `audit/round-21-xss-truncation`, not to `main`. A session that looks only
  at `main` will conclude the Deputy produced nothing. It produced 16 verified rows.

**Before reporting that a session did nothing, check the other branches.** `main` is not the whole
record.
