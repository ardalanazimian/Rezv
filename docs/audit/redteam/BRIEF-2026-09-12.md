# BRIEF — ۲۰۲۶-۰۹-۱۲ · رد تیم

- **نشست:** `rezv-25 [2f4e5c]` · sessionId `c07123f9-16de-4e21-ba6c-1e602d96db91` — نشستِ تازه، بدونِ پیشینیان. نقش را **مالک با زبانِ خودش** داد («ACT AS A RED TEAM»)، نه از جای خالیِ جدول.
- **هدفِ حمله:** شاخه‌ی `fix/verified-findings-2026-09` @ `5ea7631` — **نه `main`**. این شاخه هنوز ادغام نشده؛ `main` روی `923a20a` است.
- **گزارشِ کامل:** `docs/audit/redteam/RETEST-2026-09-12.md`

---

## توصیه، اول

**شاخه را ادغام کن — ولی نه به‌عنوانِ «پولِ دوباره‌کسرشده بسته شد».** پیشگیری واقعی است و
خودم دیدم که شلیک می‌کند. چیزی که نیست، **تشخیص** است.

---

## عدد

| | |
|---|---|
| ادعا/گیتی که واقعاً حمله شد | **۴** |
| HOLDS (مقاومت کرد) | **۲** |
| FAKEABLE (شکست) | **۱** |
| BLIND (گیت کار می‌کند، ولی حالتِ عبورکرده دیده نمی‌شود) | **۱** |
| آزمایش‌نشده | ۱۷ پچ از ۲۰، و ۱۶ حمله از ۱۸ فهرستِ منشور |

**«همه‌چیز سالم بود» نمی‌گویم و نگفتم.** سه حمله زدم، نه بیشتر. ادعای تیترِ README
(«۱۸۳۹ از ۱۸۳۹ سبز») را **بازتولید نکردم** — فقط فایلِ پرداخت را زدم: ۲۰ از ۲۰، خروج ۰.

---

## بدترین چیزی که پیدا کردم (RT-13)

**بعد از این پچ، اگر پولِ یک مهمان واقعاً دوبار کم شود، هیچ‌جا دیده نمی‌شود.**

پیش از پچ، دو ردیفِ `success` می‌ماند و کوئریِ مستندشده پیدایش می‌کرد. بعد از پچ، ردیفِ
دوم `failed` + `REFUND_REQUIRED` نوشته می‌شود (کدِ `route.ts:128`، و تست خودش همین شکل را
پین می‌کند). نتیجه:

1. کوئریِ تشخیصی که **در هدرِ خودِ مهاجرتِ ۰۸۶ نوشته شده** روی همین حالت `(0 rows)` می‌دهد — اجرا کردم، خروجیِ خام در گزارش است.
2. و بعد از اعمالِ ۰۸۶ آن کوئری **هرگز** نمی‌تواند ردیفی بدهد، چون خودِ ایندکس دو `success` را غیرممکن می‌کند.
3. `audit()` در آن روت نیست.
4. `REFUND_REQUIRED` در کلِ مخزن **هیچ مصرف‌کننده‌ای ندارد** (با کنترلِ مثبت سنجیده شد).
5. تنها آلارمِ پرداخت روی نرخِ ۴xx/۵xx است، و این مسیر **۳۰۲** برمی‌گرداند — پس اصلاً نمی‌تواند شلیک کند.

تنها ردِ باقی‌مانده یک خطِ `log.error` است.

**یعنی پچ خرابی را کم نکرد، ساکتش کرد.** این یک تصمیمِ محصولی است، نه یک باگ: کسی باید
بگوید چه چیزی در production این حالت را می‌بیند. پیشنهادِ من (اجرا نکردم، چون رد تیم
تعمیر نمی‌کند): یک `audit()` روی همان بلوک، و یک آلارم روی `fail_reason LIKE 'REFUND_REQUIRED%'`.

---

## دومی (RT-14) — یک خطِ کد، ولی گاردِ CI را کور می‌کند

`tools/check-loyalty-promise.mjs:111` فقط **۲ پوشه از ۱۷ پوشه‌ی** `api/src/app/api/v1/` را
می‌خواند. یک روتِ واقعیِ «انقضای امتیاز» را در `api/src/app/api/v1/admin/` گذاشتم:

- همان فایل در `api/src/lib/` → گارد **قرمز، خروج ۱** ✅
- همان فایلِ بایت‌به‌بایت در `admin/` → گارد **سبز، خروج ۰** ❌
- و هر ۱۶ گاردِ `check-*.mjs` هم‌زمان سبز ماندند.

هر نشستی که فردا «ادمین بتواند امتیازهای کهنه را پاک کند» را بسازد، دقیقاً همین‌جا می‌نویسد
و CI سبز می‌ماند — در حالی که اپِ مشتری می‌گوید «امتیازها منقضی نمی‌شن».

---

## چیزهایی که **مقاومت کردند** (مهم است که گفته شود)

- **ایندکسِ مهاجرتِ ۰۸۶** واقعاً شلیک می‌کند — با `duplicate key value violates unique constraint` روی دیتابیسِ واقعی دیدمش. قوی‌ترین چیزِ این شاخه است.
- **`check-runner-completeness.mjs`** وقتی یک فایلِ تست را از runner حذف کردم، دقیقاً همان فایل را نام برد و قرمز شد (خروج ۱).

---

## یک خطِ کپی‌پیست برای CEO

> رد تیم (`rezv-25 [2f4e5c]`) شاخه‌ی `fix/verified-findings-2026-09` @ `5ea7631` را زد: ایندکسِ ۰۸۶ و گاردِ runner مقاومت کردند، ولی **RT-13** (بعد از پچ، کسرِ دوباره‌ی پولِ مهمان هیچ ردِ قابلِ‌پایشی ندارد — کوئریِ تشخیصیِ خودِ مهاجرت ساختاراً مرده است، `audit()` نیست، آلارم روی ۳۰۲ شلیک نمی‌کند) و **RT-14** (گاردِ وعده‌ی امتیاز فقط ۲ از ۱۷ پوشه را می‌خواند؛ یک روتِ انقضای واقعی در `admin/` از CI رد می‌شود) باز است. گزارش: `docs/audit/redteam/RETEST-2026-09-12.md` روی شاخه‌ی `redteam/retest-2026-09-12`. ادغامِ شاخه مانعی ندارد، ولی «دوباره‌کسری بسته شد» را **بدونِ تصمیم درباره‌ی تشخیص** ثبت نکن.

---

## قیدِ صادقانه

هیچ CEOی زنده نیست. `ListAgents` پنج همتا نشان می‌دهد که همه offline یا cloud-idle‌اند، و
رجیستریِ محلی فقط یک فایل دارد — همین نشست. پس این گزارش هاب ندارد و مسیریابی‌اش با
مالک است.

درختِ کاری در پایانِ کار **تمیز** است؛ هر خرابکاری برگردانده شد و خروجِ سبزش دوباره
اندازه‌گیری شد. `main` و `fix/verified-findings-2026-09` اصلاً checkout نشدند.
