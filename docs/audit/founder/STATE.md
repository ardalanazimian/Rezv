# میزِ Founder — STATE

- **تاریخ:** ۲۰۲۶-۰۹-۱۶ (نوبتِ سوم، پس از گزارشِ پیشرفتِ CEO) · **نشست:** Founder `rezv-90 [06d57f]` · sessionId `4fa4aafb-4888-45f5-a658-00544f8e7d07` (نام‌های پیشینِ همین نشست: `rezv-02 [6be6e8]` ۰۹-۱۳ · `rezv-37` ۰۹-۱۵)
- **مقصد:** خودِ نشستِ Founder پس از resume · مالک · CEO (وقتی زنده شد) · بازبین
- **آنچه از خواننده می‌خواهد:** اگر resume شده‌ای، §۰ را اجرا کن و بعد §۲ را بخوان. اگر مالکی، فقط §۴.

> این فایل **میز** است، نه گزارش. هر نوبتِ کاری با آن تمام می‌شود. ردیفی که اینجا نیست، برای نشستِ
> بعدی وجود ندارد — و ردیفی که یک روز کهنه باشد یک دورِ کامل هزینه دارد.

---

## ۰. هویت — چطور اندازه گرفته شد، نه فرض

| شاهد | مقدار (۰۹-۱۶) |
|---|---|
| `ListAgents` | «This session is rezv-90 [06d57f]» |
| رجیستریِ harness `~/.claude/sessions/6564.json` | `name = rezv-90` · `sessionId = 4fa4aafb-…` · `entrypoint = claude-vscode` |
| ردیفِ کهنه‌ی رجیستری `296.json` | `name = rezv-37` · همان sessionId · pid مرده (`tasklist` خالی) — یعنی resumeِ ۰۹-۱۵ که سرِ سقفِ هفتگی ایستاد |
| transcript `…/4fa4aafb-….jsonl` | اولین پیامِ کاربر = جمله‌ی واگذاریِ مالک؛ `custom-title = FOUNDER` (یکتا در کلِ پروژه) |
| worktree / شاخه | `wt-rezv-02` · `session/rezv-02-founder` (از `origin/main @ 42ba301`) |

**دو resume تا امروز، هر دو با همان sessionId:** نام عوض شد، حافظه نه. **درسِ ۰۹-۱۵:** سقفِ هفتگی
وسطِ نوبت خورد و هیچ‌چیز از دست نرفت، چون هر فایل روی دیسک بود — همان قیدِ مالک.

## ۱. نقشه‌ی زنده‌ها — لحظه‌ی نوشتن (از رجیستری، نه حدس)

**CEO زنده شد (۰۹-۱۶، بعد از خطِ §۴):** `rezv-23 [131a4b]` · sessionId `60c7681b-1b65-467a-88ca-e3e5c85bd33d` —
**همان نشستِ CEOی ۰۹-۱۱** (نویسنده‌ی `HANDOFF-2026-09-11-ceo.md`)، نام عوض شده نه شناسه. خودش معرفی کرد و من
مستقل در رجیستری سنجیدم (`name = rezv-23`، همان sessionId، `claude-vscode`). ردیفِ CEO در `ROUTING.md` را خودش
می‌نویسد؛ من دست نمی‌زنم.
**هشت نشست، به گزارشِ CEO (نوبتِ سوم):** هر نشست خودش را با شاهدِ دستِ اول معرفی کرده؛ من فقط جفتِ
نام↔sessionId را در رجیستری تأیید کردم، نقش را از CEO نقل می‌کنم نه اینکه خودم نگاشت کنم:
Launch Engineer `2bffd0d9` (CEO نوشت `rezv-48`؛ رجیستریِ همین لحظه `rezv-05` — نام باز عوض شده، شناسه نه) ·
Designer `rezv-0b [60739f]` = `6c9efba5` (همان `rezv-ba`) · Backend `rezv-c9 [cc393e]` = `edb5f154` ·
Red Team `rezv-b5 [59c76c]` = `c07123f9` · بی‌نقش: `rezv-34 [617ab1]` = `ace570d0`، `rezv-bf [5d7ef7]` = `74a7ffab`
(CEO به این دو **کارِ مشخص** داد، نه نقش — درست). ⚠️ نامِ `rezv-36` که ۰۹-۱۳ در فهرست بود بازیافتی بود و
Backend Engineerِ ۰۹-۱۱ نبود؛ Backendِ امروز `edb5f154` است.

## ۲. میزِ تصمیم — باز، به ترتیبِ هزینه‌ی تأخیر

| # | تصمیم | منبع | چه چیزی مسدودش کرده | مالکِ تصمیم |
|---|---|---|---|---|
| ۱ | ~~CEO زنده نیست~~ ✅ **بسته، ۰۹-۱۶** — مالک ترمینال را باز کرد؛ `rezv-23 [131a4b]` (§۱) | رجیستریِ ۰۹-۱۶ | — | — |
| ۱ب | ~~ادغامِ `session/rezv-02-founder`~~ ✅ **بسته، ۰۹-۱۶** — CEO fast-forward کرد؛ `origin/main = 1d03717` (سنجیده با `ls-remote`)، سه گاردِ سند صفر، هر سه ردیفِ ROUTING سرِ جا (به گزارشِ CEO؛ گاردها را من روی شاخه‌ی خودم هم صفر گرفته بودم) | `git ls-remote` | — | — |
| ۲ | ادغامِ `session/rezv-ba-design` (۱۱ جلو / ۴۳ عقب) — **آماده روی `ceo/merge-ba-design-2026-09-16 @ c1a3a9b`** (روی origin هست، سنجیده). سه تعارض حل شد (`sw.js` → v46، دو آرتیفکتِ XSS بازتولیدشده، `--check` صفر). **اثباتِ درختِ ادغام هنوز گرفته نشده و CEO ادعایش نمی‌کند** (worktreeِ او `node_modules` نداشت و اجرای تست کدِ خروجِ ۱۲۷ داد). طبقِ `FP-007` سنجش با Launch Engineer است | `BRIEF-2026-09-13` §۳ · `ls-remote` | عددِ سبز روی درختِ ادغام (api + landing + e2e با `CI=1` و پورتِ جابه‌جا) از Launch Engineer؛ رفعِ `ds009` را طراح جدا می‌نویسد. **main تا آن عدد جلو نمی‌رود** | **CEO** ← Launch Engineer |
| ۳ | ~~ادغامِ RT-13 / RT-14~~ **تصحیح (CEO، سنجیده):** `1a53e98` و `ec22ecf` از ۰۹-۱۳ روی main‌اند (`merge-base --is-ancestor` → YES هر دو)؛ `session/rezv-48` هم در `0eb5f32` ادغام شده و فقط شاخه‌اش پاک نشده — **وجودِ شاخه شاهدِ ادغام‌نشدن نیست.** باقی‌مانده: فقط `redteam/retest-2026-09-13 @ c6073af` (۲ کامیت) | `git merge-base` · `rev-list` | Red Team دوباره نزده | **CEO** (برداشت، ۰۹-۱۶) |
| ۴ | ~~`E-002` §۲ — نرخ~~ ✅ **بسته با `FP-008`، ۰۹-۱۶:** `TOMAN_PER_POINT = 2` می‌ماند؛ سه عددِ مالک فقط اگر هر سه «نرخ» خوانده شوند ناسازگارند — دومی کسبِ ۵٪ است و با کد دقیقاً می‌خواند، سومی قیمتِ یک جایزه است (که ۵٬۰۰۰ امتیاز می‌شود نه ۲۰۰) | `docs/DECISIONS.md` FP-008 | **باید عیناً به مالک برسد** — او خودش پارکش کرده بود؛ یک جمله برمی‌گرداند | Founder ✔ |
| ۵ | ~~`E-002` §۳ — واحدِ جوایز~~ ✅ **بسته با `FP-008`:** جوایزِ امتیازی به تومان تعریف می‌شوند، کد تبدیل می‌کند؛ سکه دست نمی‌خورد؛ خرج تا سه شرط خاموش می‌ماند | همان | همان | Founder ✔ |
| ۶ | ~~`E-003` — OTP در قطعیِ Redis~~ ✅ **از ۰۹-۰۹ بسته بود؛ ردیفِ من کهنه بود.** مالک گزینه‌ی «د» را انتخاب کرد (شمارنده به Postgres، مهاجرت ۰۸۳، ابطال‌پذیری با md5)؛ بخشِ «سؤالِ مالک» زیرِ سرآیندِ «✅ بسته شد» متنِ تاریخی است. همان تله‌ای که حافظه هشدارش را داده بود: بدنه‌ی ESCALATIONS کهنه می‌ماند، سرآیند را بخوان | `audit/ESCALATIONS.md:348-425` · `ratelimit.ts:87-140` | — | — |
| ۶ب | **`RT-18` روی main باز است** (Red Team، `RETEST-2026-09-13`): گاردِ `check-loyalty-promise.mjs` **املا** را تشخیص می‌دهد نه کسر را — **FAKEABLE**. **به‌روز ۰۹-۱۶ (نوبتِ چهارم):** ۰۸۸ (`CHECK`) به‌تنهایی نمی‌بندد — Red Team با کنترلِ مثبت نشان داد `DELETE`ِ ردیف‌های قدیمی و `UPDATE … SET delta = 0` موجودی را ۱۷۵ → ۲۵ می‌کنند بی‌آنکه ردیفِ منفی بنویسند. LE با ۰۸۹ (تریگرها) می‌بندد. **حکمِ Founder `FP-009`:** FK `RESTRICT` می‌ماند، **هیچ cascade**؛ سه تریگر (`UPDATE`/`DELETE`/`TRUNCATE` → `RAISE`)؛ ده جای تست که دفتر را پاک می‌کنند بازنویسی می‌شوند، بدونِ درِ فرار؛ حذفِ حساب روزی که فیچر شد = ناشناس‌سازی، نه حذفِ ردیفِ مالی | `RETEST-2026-09-13.md:14,52` · `FP-009` | ۰۸۹ طبقِ FP-009 + حمله‌ی مستقلِ Red Team (از جمله حذف/بازسازیِ کاربر و `TRUNCATE`) | CEO ← Launch Engineer · حمله: Red Team |
| ۷ | `C-3` — alertmanager: آلارمِ RT-13 فایر می‌شود ولی به کسی پیج نمی‌شود | `BRIEF-2026-09-13` §۳ | صاحبِ فنی: Launch Engineer | **CEO** (برداشت، ۰۹-۱۶) |
| ۸ | شاخه‌های ادغام‌نشده‌ی قدیمی (`session/rezv-89`، `rezv-d6`، `rezv-e6`، `rezv-bc` با `ORDER-BE-001`) — زنده یا مرده؟ | `HANDOFF-2026-09-11-ceo.md` · `E-002` تصمیمِ ۱ | باید روی درختِ ادغام سنجیده شود، نه از حافظه — و درسِ ردیفِ ۳: اول `merge-base --is-ancestor`، بعد قضاوت | **CEO** (برداشت، ۰۹-۱۶) |
| ۹الف | ~~منشورهایی که `FP-007` نسخ کرده~~ ✅ **اعمال شد، ۰۹-۱۶ (نوبتِ چهارم):** `launch-engineer.md` §۴ + `:10` (`web/` که وجود نداشت → `apps/landing`/`apps/seo`؛ «you own it, permanently» → «you verify it, the Designer writes it»)، `designer.md` `:57`، `:85` («the architect» = CEO)، §۶٫۴، `deputy.md` `:53`، `backend-engineer.md` §۳ (دو ردیف → سه ردیف + پاراگرافِ `standalone`). متن از پیشنهادِ `rezv-bf` (`docs/audit/proposals/FP-007-charter-conflicts-2026-09-16.md`، cherry-pick شده به همین شاخه) — هر شش بند **پذیرفته**، بی‌بازنویسی | همان فایل §۲ A–E | بازبین چک می‌کند؛ CEO به نشست‌های زنده‌ی طراح، LE، Backend یک خط با شماره‌ی کامیت می‌دهد (ویرایشِ main، contextِ نشستِ باز را عوض نمی‌کند) | Founder ✔ · اعلان: CEO |
| ۹ب | ~~`reviewer.md` §۰~~ ✅ **اعمال شد — ناسخش `FP-006` (d)(۲) است نه `FP-007`** (تصحیحِ `rezv-bf`، تأییدِ CEO، پذیرفته): هدفِ گزارش → Founder با رونوشتِ CEO؛ دامنه‌ی ممیزی + `docs/audit/founder/` و سریِ FP؛ خطِ رِله «give the founder the one line» → **the owner** (رِله نزدِ انسان می‌ماند — حکمِ Founder روی ابهامِ §۲ F)؛ `:42` خلاصه → به نشستِ Founder؛ شش موردِ ارجاع با یادداشتِ تقسیمِ `FP-006`؛ قاعده‌ی خواندنِ founder/owner یک بار در مقدمه. و جاروی بنرِ هر نُه منشور: «ask the founder» → «ask the Founder session (…; the owner only when none is alive)». `launch-engineer.md:218` «the founder decides» → «the Founder session decides» | همان فایل §۲ F + §۳ | همان | Founder ✔ · اعلان: CEO |

**تقسیمِ کار از ۰۹-۱۶:** CEO ردیف‌های ۱ب، ۲، ۳، ۷، ۸، ۹ را برداشت (۸ و ۹ را به دو نشستِ بی‌نقش به‌عنوانِ کارِ مشخص داد: سیاهه‌ی ۲۶ شاخه، و سیاهه‌ی بندهایی که `FP-007` نسخ کرده — **پیشنهاد**، اعمالش با Founder). ردیف‌های ۴–۶ با Founder بود و امروز بسته شد.

**تصحیحِ شرطِ ادغام (۰۹-۱۶، نوبتِ پنجم — از یک قرمزیِ واقعیِ main):** شرطِ «سه گاردِ سند» در ردیفِ ۱ب **زیرمجموعه** بود نه کل. `3112044` روی `check-doc-path-refs` قرمز شد (دو ارجاعِ من به `DS-011` که هنوز فقط روی شاخه‌ی طراح است) و CEO با همان سه گارد ادغام کرده بود. از این پس شرطِ هر ادغام روی main: **همه‌ی `tools/check-*` که `ci.yml` اجرا می‌کند** (۲۰ گارد، شمرده از `ci.yml`؛ آن‌هایی که DB/psql می‌خواهند با CI). و به تصحیحِ Red Team: `base-freshness` در `42ba301` **failure** بود و حالا **skipped** — jobِ skipped سبز نیست و در شمارشِ «۱۲ سبز» نمی‌نشیند.

**یک محدودیتِ مجوز که به مالک می‌رسد، نه به من:** classifierِ نشستِ Backend (`edb5f154`) دستورهای مسیرِ ادغام را مسدود می‌کند («Merge Without Review»). آن نشست درست رفتار کرد و از کسی نخواست دورش بزند؛ CEO ادغامِ شاخه‌اش را **به‌عنوانِ وظیفه‌ی خودش** انجام می‌دهد (ادغام لِینِ CEO است)، نه به‌جای او. اگر مالک بخواهد آن محدودیت عوض شود، تصمیمِ اوست؛ هیچ نشستی تنظیماتِ مجوزِ نشستِ دیگر را عوض نمی‌کند.

**قاعده‌ی این جدول:** ردیفِ ۴–۶ را Founder می‌تواند همین حالا ببندد و باید در `docs/DECISIONS.md`
(سریِ FP) بنویسد — نه اینجا. اینجا فقط اشاره است.

## ۳. مسدود روی مالک — با وابستگیِ دقیق، نه «منتظر»

| ردیف | وابستگی | دورهای انتظار |
|---|---|---|
| دامنه (`E-001`) | خریدِ `rezervno.ir` یا هر دامنه‌ی دیگر — `ENOTFOUND` در ۰۹-۱۳ | ۳+ |
| نامِ برند | مالک: «هنوز تصمیم نگرفته»، و نخواسته هر دور پرسیده شود | پرسیده نمی‌شود |
| پیامکِ واقعی | چرخاندنِ کلیدِ ملی‌پیامک + یک ارسالِ واقعی با پاسخِ خام | — |
| آزمونِ مالک روی گوشیِ خودش | چهار سطح | — |
| ~~CEO~~ | ✅ باز شد، ۰۹-۱۶ | — |

## ۴. برای مالک — یک خط

> **از تو فعلاً هیچ‌چیز لازم نیست جز همان دو ردیفِ همیشگی: خریدِ دامنه، و یک روز آزمونِ چهار سطح روی گوشیِ خودت.**
> دو چیز فقط برای اطلاع، هر دو با یک جمله‌ات برگشت‌پذیر: (۱) `FP-008` — نرخِ ۲ تومان به ازای هر امتیاز می‌ماند و جوایز به تومان تعریف می‌شوند؛ خرجِ امتیاز خاموش است و **بازارزش‌گذاریِ موجودی‌های قدیمی** (دو برابر شدن با نرخِ ۲) تصمیمِ توست، وقتی خواستی خرج باز شود. (۲) نشستِ Backend مجوزِ ادغام ندارد؛ CEO ادغام می‌کند. اگر می‌خواهی آن نشست خودش ادغام کند، فقط تو می‌توانی مجوزش را بدهی.

## ۵. تصمیم‌های ثبت‌شده‌ی این نشست

| شناسه | عنوان | تاریخ |
|---|---|---|
| `FP-006` | نشستِ Founder تأسیس شد؛ زنجیره‌ی گزارش: کارکنان ← CEO ← Founder ← مالک (+ پی‌نوشتِ بازبینیِ منشور، ۰۹-۱۶) | ۲۰۲۶-۰۹-۱۳ |
| `FP-007` | لِینِ لندینگ و اپِ مشتری: طراح می‌نویسد، مهندسِ لانچ می‌سنجد — حکم روی تعارضِ سه منشور با دستورِ ۰۹-۱۲ مالک | ۲۰۲۶-۰۹-۱۶ |
| `FP-008` | اقتصادِ امتیاز (E-002 §۲–۳): نرخِ ۲ تومان می‌ماند، جوایز به تومان، خرج خاموش تا سه شرط؛ بازارزش‌گذاریِ موجودی‌های قدیمی با مالک | ۲۰۲۶-۰۹-۱۶ |
| `FP-009` | دفترِ امتیاز تغییرناپذیر و حذف‌ناپذیر؛ هیچ cascade — FK `RESTRICT` می‌ماند، سه تریگر، تست‌ها بدونِ درِ فرار بازنویسی (RT-18 / مهاجرت ۰۸۹) | ۲۰۲۶-۰۹-۱۶ |

## ۶. بازبینیِ منشور — چه شد و چه نشد

بازبینیِ ۰۹-۱۳ (۲۰ بازبینِ سندمحور + ۲ منتقدِ کامل‌بودن، هر یافته با ۳ ردکننده) با سقفِ هفتگی قطع شد:
**۸ بازبین تمام شد، ۵۶ یافته، صفر ردکننده اجرا شد.** جدولِ کامل در transcriptِ workflow
(`…/subagents/workflows/wf_a2c124e3-221/journal.jsonl`). Founder خودش داوری کرد و هر ۵۶ را در نسخه‌ی ۲ی
منشور اعمال کرد — یعنی **این نسخه هنوز بازبینیِ مستقل ندارد**؛ ردیفِ ۹ی §۲ و خطِ بازبین در منشور §۵ برای همین است.
۱۲ سند بازبینی نشدند: `ROUTING.md`، `ceo.md`، `_TEAM.md`، `DECISIONS.md`، `ESCALATIONS.md`، `CLAUDE.md`،
دو مهارت، `LAUNCH-READINESS.md`، `SESSION-HANDOFF.md`، `STATE.md`، و `prelaunch-auditor.md` (خطا داد).

## ۷. چطور مرا برگردانی

`docs/audit/prompts/founder.md` §۷ — به فارسی، برای مالک. کوتاهش: **Session history → FOUNDER**، یا
`claude --resume 4fa4aafb-4888-45f5-a658-00544f8e7d07`، و اولین سؤال: «تو کی هستی و sessionId‌ات چیست؟».
نامِ کوتاه (`rezv-90`) با هر resume عوض می‌شود؛ شناسه نه.
