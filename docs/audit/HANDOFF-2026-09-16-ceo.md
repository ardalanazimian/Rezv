# تحویلِ CEO — ۲۰۲۶-۰۹-۱۶، توقف به‌خاطرِ سقفِ مصرف

- **مبنا:** `main = 38e27c4` · **ساعتِ اندازه‌گیری:** ۰۹-۱۶ ~۰۸:۰۰ محلی · درخت تمیز
- **نویسنده:** CEO `rezv-23 [131a4b]` · sessionId `60c7681b-…`
- **علت:** مالک گفت سقفِ مصرف دارد تمام می‌شود؛ همه متوقف و هر چیزی push شد.

> **قاعده‌ای که امروز ساخته شد و این سند خودش رعایتش می‌کند:** هر سندی که عددِ
> شاخه یا وضعیتِ گیت دارد باید shaی مبنا **و ساعتِ اندازه‌گیری** را روی خودش
> بنویسد. `main` امروز در یک ساعت چهار بار تکان خورد.

## وضعِ CI

`main` سبز است. **۱۸** ابزارِ `node tools/*.mjs` که `ci.yml` صدا می‌زند، همه صفر.

⚠️ در جاروی اولم فقط `check-*.mjs` را زدم (۱۶ تا) و دو تا از قلم افتاد:
`xss-sink-audit.mjs --check` و `xss-escaping-regression.mjs`. هر دو بعداً زده شد و صفرند.
**درس: الگوی جست‌وجو را با فهرستِ واقعیِ `ci.yml` بسنج، نه با پیشوندِ حدسی.**

## شاخه‌های آماده و ادغام‌نشده

| شاخه | جلو | حال |
|---|---|---|
| `session/rezv-ba-design @ 08e1491` | ۱۶ | ۰ عقب. منتظرِ اثباتِ درختِ ادغام از Launch Engineer. **CEO ادعای سبز ندارد** |
| `session/rezv-36-backend` | ۱۶ | ۹ رفعِ بک‌اند + ~۴۰ فرانت. نوبتش **بعد از** طراح |
| `session/rezv-bf @ 457b4e1` | ۱ | پیشنهادِ اصلاحِ منشورها؛ Founder محتوایش را cherry-pick کرد |
| `session/rezv-34 @ e32a687` | — | سیاهه‌ی شاخه‌ها + فهرستِ حذفِ ۲۲تایی |
| `redteam/rt18-mutation3` | — | پروب‌های RT-22/24/25 |
| `backup/launch-rc4` | — | **پشتیبان** — دو کامیتی که فقط روی دیسک بودند |

## کارهای باز، با صاحب

| # | کار | صاحب |
|---|---|---|
| ۱ | اثباتِ درختِ ادغامِ طراح (api روی DBِ تازه + لندینگ + e2e ×۳) → بعد CEO ادغام می‌کند | Launch Engineer |
| ۲ | مهاجرتِ `089`: سه تریگر `UPDATE`/`DELETE`/**`TRUNCATE`** روی `points_ledger`، **بدونِ استثنا**، و بازنویسیِ ده محلِ `deleteMany` در `api/tests` | Launch Engineer |
| ۳ | **`RT-25`:** همان حفره‌ی `TRUNCATE` روی `reservation_events` **همین حالا روی main زنده است** — بیرونِ دامنه‌ی ۰۸۹ | Launch Engineer + Backend |
| ۴ | `FP-009` §۵: محورِ `check-schema-drift.sh` که `confdeltype='r'` و هر سه تریگر را assert کند، با **ابطال‌پذیریِ چهارگانه** | Backend (محور) + LE (اثبات) |
| ۵ | `FP-009` §۶: گاردِ ایستا — `session_replication_role` هرگز در `api/src` نباشد، با کنترلِ مثبت | Backend |
| ۶ | 🔴 **ثبتِ staff بدونِ اثباتِ شماره** + `findStaffForLogin`ِ پلتفرم‌محور ⇒ تصاحبِ شماره‌ی مالکِ آینده. تستِ بازتولید **اول** | Backend |
| ۷ | بازسنجیِ باقیِ گزارشِ کهنه: `POST /waitlist`، شارژِ دوباره‌ی بیعانه، فلگِ سوءاستفاده، شکافِ بنِ مشتری | Red Team |
| ۸ | حمله به نسخه‌ی **نوشته‌شده‌ی** ۰۸۹ (TRUNCATE، حذف‌وبازسازیِ کاربر، بازگشتِ معافیت) | Red Team |
| ۹ | حذفِ ۲۲ شاخه‌ی مرده — فهرست آماده، هر خط خودش لحظه‌ی اجرا می‌سنجد و `STOP` می‌دهد | CEO، بعد از ادغام‌ها |
| ۱۰ | ردیف‌های `ROUTING.md` که به sessionIdهای مرده اشاره می‌کنند | CEO، یک کامیت |
| ۱۱ | `C-3`: آلارم‌ها فایر می‌شوند ولی alertmanager وجود ندارد — به کسی پیج نمی‌شود | Launch Engineer |

## چیزهایی که **بسته نیستند** و نباید بسته ثبت شوند

- **`RT-18`** — Red Team نبسته‌اش. ۰۸۹ هنوز نوشته نشده.
- **`E-001`** نام و دامنه · آزمونِ مالک روی گوشیِ خودش · پیامکِ واقعی — همه منتظرِ مالک.

## نقشه‌ی تیم (کلید = sessionId؛ نام با هر ری‌استارت عوض می‌شود)

Founder `4fa4aafb` · CEO `60c7681b` · Launch Engineer `2bffd0d9` · Designer `6c9efba5` ·
Backend `edb5f154` · Red Team `c07123f9` · بی‌نقش `ace570d0` و `74a7ffab`.

بعد از ری‌استارت: `grep -l <sessionId> ~/.claude/sessions/*.json` نامِ تازه را می‌دهد.
