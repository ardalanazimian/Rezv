# ممیزیِ فول‌استک — ماتریسِ جداسازیِ تنانت

> ۲۰۲۶-۰۹-۱۷ · دستهٔ ۲ · پایه: `main` @ `cf60b9c` (پین: `refs/snap/fsaudit-main`)
>
> **حکم: مرزِ تنانت در اجرا نگه داشت. صفر نشت در ۲۱۸ ردیف.**
> RLS بی‌اثر است (۰ policy روی ۷۳ جدول)، پس هرچه اینجا اندازه‌گیری شده **کلِ**
> مرز است، نه یک لایه از آن.

---

## ۰. هویتِ هارنس — چیزی که Red Team باید به آن حمله کند

| چیز | مقدار |
|---|---|
| worktree | `C:\Users\Ardalan\Desktop\rezv\wt-fsaudit` @ `audit/fullstack-2026-09-16` |
| commit پایه | `cf60b9c` (پین‌شده در `refs/snap/fsaudit-main`) |
| سرورِ API | `http://localhost:3916` — `next dev`، با envِ زیر |
| دیتابیس | کانتینرِ `rezv-fsaudit-pg` (postgres:17) روی `localhost:55916` |
| Redis | کانتینرِ `rezv-fsaudit-redis` (redis:7) روی `localhost:16916` |
| مالکیت | این دو کانتینر **فقط** برای همین ممیزی ساخته شده‌اند؛ هیچ نشستِ دیگری از آن‌ها استفاده نمی‌کند |
| اسکریپت‌ها | `scratchpad/iso-seed.mjs` · `iso-matrix.mjs` · `iso-gap.mjs` · `iso-gap2.mjs` · `iso-gap3.mjs` |
| نتایج خام | `scratchpad/iso-results.json` (۲۱۸ ردیف، status + بدنهٔ خام برای هر ردیف) |

### اثباتِ «این سرور دیتابیسِ من را می‌خواند»

منشور می‌گوید «چیزی روی پورت ۳۰۰۰ جواب می‌دهد» ≠ «سرورِ من جواب می‌دهد». پس
پیش از هر ردیف، یک sentinel:

```
GET /api/v1/restaurants/fsaudit-a1  → 200
{"id":"…","slug":"fsaudit-a1","name":"FSAUDIT A1","cuisine":"audit", …}
```

`fsaudit-a1` فقط در دیتابیسِ همین ممیزی وجود دارد و id‌اش با ردیفی که
`iso-seed.mjs` ساخت یکی است. ضمناً health خودش می‌گوید `{"db":"ok","redis":"ok"}`
و لاگِ همان سرور درخواست‌های من را نشان می‌دهد.

⚠️ **PLATFORM_ADMIN_TENANT_ID باید بعد از هر re-seed دوباره ست شود.** هر
truncate شناسه‌ی تنانتِ پلتفرم را عوض می‌کند؛ اگر سرور با شناسه‌ی کهنه بالا
بماند، **همهٔ ردیف‌های کنترلِ V4 دروغ می‌شوند** (ادمینِ واقعی هم ۴۰۳ می‌گیرد و
ماتریس به دلیلِ غلط «قبول» می‌شود). این را یک بار خوردم و سرور را ری‌استارت کردم.

---

## ۱. فیکسچر

سه تنانت در دیتابیسِ خودم: **P** (پلتفرم) · **A** (مهاجم) · **B** (قربانی).
A دو شعبه دارد (A1, A2) تا قفلِ شعبه داخلِ یک تنانت آزموده شود.

از **هر** نوع شیئی که در مسیرها به‌صورتِ `[param]` ظاهر می‌شود، در **هر دو**
طرف یک نمونه ساخته شد — رستوران، میز، رزرو، آیتم و دستهٔ منو، گروه و گزینهٔ
افزودنی، چت، عکس، لیستِ انتظار، مشتری، و پروفایلِ اقتصادی. دلیلش ساده است:
اگر شیءِ قربانی اصلاً وجود نداشته باشد، ۴۰۴ِ مهاجم معنایی ندارد.

---

## ۲. بردارها و نتیجه

| بردار | چه می‌پرسد | ردیف | نتیجه |
|---|---|---|---|
| **V1** | توکنِ owner‌ِ تنانت A + **شناسه‌ی شیءِ تنانت B در مسیر** | ۲۶ | ✅ **۲۶/۲۶ SCOPED** — همه با کنترلِ ۲xx |
| **V2** | توکنِ A + هدرِ `X-Restaurant-Id` = شعبهٔ تنانت B | ۹۴ | ✅ ۹۱ SCOPED · ۳ بررسی و **رد شد** (پایین) |
| **V3** | کارمندِ قفل‌شده به A1 + هدرِ A2 | ۱ | ✅ هدر **نادیده گرفته شد**، با کنترلِ مثبت |
| **V4** | ownerِ یک تنانتِ عادی روی `/v1/admin/*` | ۵۲ | ✅ **۵۲/۵۲ تفکیک‌کننده** (حمله ۴۰۳، کنترل ≠۴۰۳) |
| **V5** | توکنِ STAFF و توکنِ ادمینِ پلتفرم روی `/v1/me/*` | ۴۲ | ✅ **۴۲/۴۲ REJECTED** |
| **V6** | شناسه‌ی تنانت B در **بدنه** (نه مسیر) | ۲ | ✅ ۱ SCOPED · ۱ بی‌موضوع (پایین) |
| | **جمع** | **۲۱۷ + ۱ hویت** | **صفر نشت** |

### V1 — چرا «۲۶/۲۶» و نه «۲۶ ردیفِ سبز»

یک ردیف فقط وقتی چیزی ثابت می‌کند که **کنترلش موفق شده باشد**. اگر هم حمله و
هم کنترل ۴۰۴ بگیرند، مسیر به همه ۴۰۴ می‌دهد و ردیف هیچ‌چیز دربارهٔ تنانت
نمی‌گوید. در اجرای اول ۹ ردیف دقیقاً همین بودند و **علتش خودِ هارنس بود**:
ردیف‌های `DELETE` که به‌عنوانِ کنترل اجرا می‌شدند، فیکسچرِ کنترلِ ردیف‌های
بعدی را پاک می‌کردند (`itemA` تا آخرِ اجرا از دیتابیس رفته بود). هر ۹ ردیف با
فیکسچرِ تازه و **یک‌به‌یک** دوباره اجرا شدند.

۷ ردیفِ دیگر کنترلشان ۴۲۲ بود (بدنهٔ `{}` پیش از رسیدن به شیء رد می‌شد). آن‌ها
هم با بدنهٔ معتبر بازاجرا شدند. سه مورد از این‌ها خودشان چیزِ مفیدی نشان دادند:

| مسیر | چرا کنترل اول رد شد | معنی |
|---|---|---|
| `POST /menu/[id]/photo` | «عکس باید multipart باشد» → بعد «دستِ‌کم ۲۰۰×۲۰۰» | اعتبارسنجیِ تصویر واقعاً کار می‌کند (با `icon-512.png` شد ۲۰۱) |
| `PATCH /reservations/[code]/status` | `INVALID_STATUS_TRANSITION: confirmed → arrived` | **state machineِ چرخهٔ عمر واقعاً گاردِ انتقالِ غیرمجاز دارد** — `confirmed → checked_in` مجاز است و ۲۰۰ داد |
| `DELETE /tables/[id]` | میزِ دارای رزرو حذف نمی‌شود | قاعدهٔ کسب‌وکار، نه نتیجهٔ تنانت |

نتیجهٔ نهایی: هر ۲۶ ردیف حالا «حمله ۴۰۳/۴۰۴ · کنترل ۲۰۰/۲۰۱» است.

### V2 — سه ردیفی که «نشت؟» علامت خوردند و نبودند

طبقه‌بندیِ خودکارِ من فقط به status نگاه می‌کرد، پس هر ۲xx را مشکوک علامت زد.
هر سه روی `/v1/restaurant/staff*` بودند و **هر سه رد شدند**:

| ردیف | چرا ۲xx | شاهدِ رد |
|---|---|---|
| `GET /restaurant/staff` | مسیر **tenant-scoped** است نه branch-scoped؛ عمداً `withStaffAuth` دارد و `resolveStaffRestaurant` را صدا نمی‌زند (`route.ts:46-47`)، و کوئری‌اش `where:{tenantId: auth.tenantId}` است (`:100`) | پاسخِ کامل فقط شناسه‌های تنانت A را دارد (`ownerA`, `lockedA1`, `restA1`)؛ `ownerB` **در آن نیست** |
| `POST /restaurant/staff` | همان — کارمند را در تنانتِ **خودِ فراخوان** می‌سازد (`data:{tenantId: auth.tenantId}`, `:125`) | ردیفِ ساخته‌شده `tenant_id` = A دارد |
| `POST /restaurant/staff/password` | مسیر **self-service** است: `where:{id: auth.sub}` (`:42`). اصلاً فیلدِ `staff_id` در schema ندارد، پس کلیدِ ناشناسِ من را zod دور ریخت | کوئریِ دیتابیس: `ownerB.password_hash IS NULL` (تغییر نکرد) و `ownerA` رمز گرفت |

درسِ روش: **۲xx به‌تنهایی نشت نیست.** بدنه و وضعیتِ دیتابیس تصمیم می‌گیرند.

### V5 — سردرگمیِ نوعِ principal بسته شد

`authFromRequest` (`jwt.ts:104-108`) خودش `kind` را چک **نمی‌کند** و
`auth.sub` برای staff شناسه‌ی `staff.id` و برای customer شناسه‌ی `user.id`
است — هر دو uuid. یعنی اگر روتی `kind` را نبیند، توکنِ کارمند بی‌صدا به‌عنوانِ
مشتری قبول می‌شود.

هر ۲۱ روتِ `/v1/me/*` × ۲ نوعِ توکنِ مهاجم (STAFF و ادمینِ پلتفرم) = **۴۲ ردیف،
هر ۴۲ رد شدند**، در حالی که کنترلِ مشتری روی همان ردیف‌ها عبور می‌کند. این
همان چیزی است که در دستهٔ ۱ فقط REAL-STATIC بود.

---

## ۳. آنچه هنوز اندازه‌گیری نشده

| # | مورد |
|---|---|
| I-1 | هایجکِ شمارهٔ کارمند (`UNKNOWN.md` U-4) — این ماتریس آن را پوشش **نمی‌دهد**: آنجا مهاجم توکنِ تنانتِ دیگر ندارد، بلکه شماره‌ای ثبت‌نشده را پیش‌دستی برمی‌دارد |
| I-2 | نشتِ زمانی/پیام (آیا ۴۰۴ِ «مالِ تنانتِ دیگر» با ۴۰۴ِ «اصلاً وجود ندارد» فرق دارد؟) — بررسی نشد |
| I-3 | مسیرهای `/v1/waitlist/[id]/accept|decline` که توکن‌محورند نه tenant-محور |

---

## ۴. یک یافتهٔ جانبی — ریت‌لیمیت به هدرِ کلاینت اعتماد می‌کند

> **بازنگری‌شده: MEDIUM → MINOR.** رتبهٔ اولِ من زیادی بالا بود و دلیلی که
> برایش آوردم **غلط** بود. شواهدِ زیر را پس از اعتراضِ CEO خودم اندازه گرفتم.

برای اینکه ردیف‌های بعدی ۴۲۹ نشوند، هر probe یک `X-Real-IP` متفاوت فرستاد و
کار کرد: `clientIp()` (`ratelimit.ts:207-213`) وقتی `TRUST_PROXY_HEADERS !== 'false'`
باشد — پیش‌فرض — همان هدر را باور می‌کند و **اول از همه** می‌خواندش.

### چرا در توپولوژیِ واقعی کار نمی‌کند

| شاهد | نتیجه |
|---|---|
| `deploy/nginx/nginx.conf:125-126` | `X-Real-IP` **و** `X-Forwarded-For` هر دو بازنویسی می‌شوند (نه append)، با کامنتِ صریحِ ضدِ جعل |
| `deploy/caddy/Caddyfile:113,140` | `header_up X-Real-IP {remote_host}` — همان کار |
| `docker-compose.prod.yml` | **پورتِ کانتینرِ api اصلاً به هاست publish نمی‌شود**؛ فقط caddy روی ۸۰/۴۴۳ است |

آخری از دوتای اول مهم‌تر است: این یک قراردادِ پیکربندی نیست، یک **تضمینِ
ساختاری** است — API جز از پشتِ پراکسی قابلِ دسترسی نیست.

### و ادعای «brute-forceِ OTP» که خودم گفتم، غلط بود

نوشته بودم «ریت‌لیمیت تنها چیزی است که جلوی brute-forceِ OTP را می‌گیرد».
اندازه‌گیری خلافش را نشان می‌دهد:

* `verifyOtp` (`lib/otp.ts:219-221`) شمارندهٔ تلاش را **به‌ازای شماره و در
  Postgres** نگه می‌دارد: `attempts >= 5` → رد. کامنتِ `:116` صریح می‌گوید این
  شمارنده از دیرباز در Postgres است و هرگز به Redis منتقل نشد.
* کد فقط **۲ دقیقه** عمر دارد (`:176`) و `otpPerPhone` هم ۳ در ۱۰ دقیقه است.

هر سه **مستقل از IP**‌اند. پس چرخاندنِ IP کدِ OTP را brute-force نمی‌کند.
همین الگو برای ورود با رمز هم هست (per-IP **و** per-username).

### باقی‌مانده‌ی واقعی

چرخاندنِ IP فقط ابعادِ **حجمی** per-IP را دور می‌زند (`search`, `globalPerIp`,
`reservation`, `otpPerIp`, `otpVerify`) — یعنی سوءاستفادهٔ حجمی، **نه دور زدنِ
احراز هویت**.

پس: **MINOR + یک پیش‌شرطِ چک‌لیستِ A2**، نه تیکتِ رفع. یک تیزکردنِ پیشنهادی در
متنِ پیش‌شرط: خطر فقط «پراکسیِ نامطمئن» نیست، **نبودِ هر پراکسی‌ای که این هدر را
بنویسد** است — چون `clientIp` اول `X-Real-IP` را می‌خواند، اگر هیچ لایه‌ای آن را
ست نکند، هدرِ ساختهٔ کلاینت برنده می‌شود. مصداقش استقرارِ serverless است.

⚠️ **و چیزی که ادعا نمی‌کنم:** `docs/DEPLOY_API_VERCEL.md` یک مسیرِ استقرارِ
API روی Vercel را توصیف می‌کند (جایی که nginx/caddy وجود ندارند)، ولی خودِ آن
سند با بنرِ `ARCHIVED-SNAPSHOT` شروع می‌شود و می‌گوید عددهایش دیگر درست نیستند.
**من هیچ توپولوژیِ Vercelی را اندازه نگرفته‌ام** و نمی‌گویم آنجا می‌شکند — فقط
می‌گویم متنِ پیش‌شرط باید این حالت را هم پوشش بدهد.

## ۵. جمع‌بندی برای CEO

بلاکرِ **B-4** دستهٔ ۱ («ماتریس اجرا نشده، پس ۱۶۷ روتِ حساس REAL-STATIC‌اند»)
**بسته شد**. آن ۱۶۷ روت حالا REAL‌اند: ۹۴ روتِ رستوران و ۵۲ روتِ ادمین و ۲۱
روتِ `/me/*` با توکنِ متخاصمِ واقعی زده شدند و هیچ‌کدام نشت ندادند.

این یعنی مرزِ تنانت — تنها مرزی که وجود دارد، چون RLS خالی است — **در اجرا
سالم است**. سه بلاکرِ دیگر (`S-04` تلهٔ دمو · `S-02/S-03` دفترهای بدونِ تریگر ·
`S-01` e2e) سرِ جایشان‌اند.
