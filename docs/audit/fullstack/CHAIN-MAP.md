# ممیزیِ فول‌استک — نقشهٔ زنجیره (درز)

> ۲۰۲۶-۰۹-۱۶ · دستهٔ ۱ · پایه: `main` @ `cf60b9c` (پین: `refs/snap/fsaudit-main`)

---

## ۰. شمارشِ کلاس‌ها

⚠️ **این جدول هنوز کاملِ سطحِ محصول نیست.** دستهٔ ۱ درز را باز کرده و
زیرساختِ اندازه‌گیری را ساخته؛ ردیفِ «طبقه‌بندی‌نشده» عمداً بزرگ است و
پنهان نشده.

| کلاس | تعداد | توضیح |
|---|---|---|
| **REAL** (در runtime اثبات شد) | ۴ | پایین، §۱ |
| **FAKE** | ۱ | تلهٔ دمو — `FRONTEND.md` §۱ 🔴 |
| **PARTIAL** | ۱ | هایجکِ شمارهٔ کارمند (حلقهٔ شکسته نام‌گذاری شد، اثرش هنوز نه) |
| **REAL-STATIC** (در سورس هست، در اجرا دیده نشده) | ۱۶۷ روتِ حساس | ۹۴ `restaurant/*` + ۵۲ `admin/*` + ۱۷ `me/*` + ۴ auth-staff — **طبقِ منشور هرکدام روی مسیرِ پول/auth/رزرو تا اثباتِ runtime بلاکر است** |
| **DEMO-ONLY** | ۰ تأییدشده | — |
| **DEAD** | ۰ | `apps/business/src-v2/*.jsx` که منشور خواسته **وجود ندارد** (§۴) |
| **طبقه‌بندی‌نشده** | باقیِ ۱۶۴ روت × ۱۳۱ فراخوانیِ فرانت | دستهٔ ۲ |

**جمعِ سطحِ اندازه‌گیری‌شده:** ۱۶۴ مسیرِ یکتا / ۲۲۴ ردیفِ (مسیر × متد) در بک‌اند ·
۱۳۱ محلِ فراخوانی / ۶۸ مسیرِ یکتا در فرانت (customer 47 · business 22 · company 14 ·
standalone 48).

---

## ۱. REAL — آنچه واقعاً اجرا شد و دیده شد

| # | زنجیره | شاهد |
|---|---|---|
| R-1 | `GET /api/v1/restaurants` → رندرِ فیدِ مشتری با دادهٔ واقعیِ سرور | حالتِ C هارنسِ `demo-trap-proof.mjs`: رستورانِ کنترلی در DOM رندر شد، صفر `[DEMO]` |
| R-2 | `GET /api/health` → `{status:'ok', checks:{db:'ok', redis:'ok'}}` | `curl` علیه سرورِ خودم روی `3916`؛ هویت با لاگِ خودِ همان سرور تطبیق داده شد |
| R-3 | کاملیِ runnerِ تست | ۱۹۸ فایل روی دیسک = ۱۹۸ ایمپورت، صفر یتیم — با کنترلِ مثبت |
| R-4 | RLS **بی‌اثر است** (مرزِ تنانت کاملاً در لایهٔ اپ) | کوئریِ زندهٔ DBِ خودم: `SELECT count(*) FROM pg_policies WHERE schemaname='public'` → **۰**، روی ۷۳ جدول |

## ۲. FAKE — 🔴 بلاکر

| # | زنجیره | حکم |
|---|---|---|
| F-1 | `GET /api/v1/restaurants` → ۵۰۰ → `loadRestaurants()` → `R_SAMPLE` → شش کارتِ `[DEMO]` با سرآیندِ «۶ رستوران فعال» | در chromiumِ واقعی اثبات شد؛ هر دو کنترل سالم. جزئیات و جدولِ خامِ نتایج: `FRONTEND.md` §۱ |

## ۳. PARTIAL — حلقهٔ شکسته نام‌گذاری‌شده

| # | زنجیره | لینکِ شکسته |
|---|---|---|
| P-1 | `POST /v1/restaurant/staff` → `staff` row → `POST /v1/auth/staff/login` → نشست | **شماره تأیید نمی‌شود** (`restaurant/staff/route.ts:119-128` فقط `normalizePhone` می‌کند) و **جست‌وجوی ورود سراسری است** (`staff-helpers.ts:192-206`، بدونِ `tenantId`). مهاجرتِ ۰۷۲ فقط ترتیب را قطعی کرد («قدیمی‌ترین ثبت برنده»)، حفره را نبست — و کامنتِ خودِ فایل این باقی‌مانده را می‌پذیرد. **اثرِ واقعی هنوز در runtime بازتولید نشده** → `UNKNOWN.md` U-4 |

---

## ۴. تصحیحِ سه پیش‌فرضِ منشورِ خودم

منشور می‌گوید «زیروتراست: سند و ادعای قبلی CLAIM است». همین را روی خودِ منشور
هم اعمال کردم:

| پیش‌فرضِ منشور | واقعیت روی `cf60b9c` | شاهد |
|---|---|---|
| «`apps/business/src-v2/*.jsx` را طبقه‌بندی کن، هرگز حذف نکن» | **وجود ندارد.** در کلِ مخزن **صفر** فایلِ `.jsx` هست. در کامیتِ `95e95f1` («حذفِ کدِ مرده»، PR #59) حذف شده | `git ls-files \| grep -c '\.jsx$'` → ۰ |
| «`demo-mvp/` را با `apps/*` دیف کن» | **وجود ندارد** | `git ls-files \| grep -i demo` → فقط `prisma/seed/demo-*.sql` و `apps/landing/app/demo/page.tsx` |
| «`seed.js`» | ✅ وجود دارد: `apps/customer/js/data/seed.js` | — |
| گزارش به `rezv-b0 [d8087d]` | **شناسهٔ مرده.** `ROUTING.md:42` روی main صریحاً «CEOِ مرده‌ی `rezv-b0 [d8087d]`» می‌نویسد؛ `:73` می‌گوید یک منشورِ کامیت‌شده هنوز همین را صدا می‌زند و `:137` می‌گوید در هر هفت منشور ۲۶ بار تکرار شده — یعنی منشورِ من یکی از همان‌هاست | خودم روی main خواندم |

## ۵. ادعاهای نشستِ `rezv-87` — وضعیتِ تأیید

| ادعا | حکمِ من |
|---|---|
| `rezv-b0 [d8087d]` مرده است | ✅ **تأیید شد** از روی خودِ فایل |
| `rezv-87 [09dbab]` هابِ CEO این موج است | ⚠️ **اثبات نشد.** شاهدی که داد (`~/.claude/sessions/13140.json`) فقط پیوندِ نام↔sessionId را ثابت می‌کند؛ `ROUTING.md:38` روی main هنوز CEO را `rezv-67 [db8c63]` می‌نویسد و خودِ همان فایل می‌گوید فقط خودِ نشست می‌تواند ردیفش را بنویسد. با او کار می‌کنم، ولی ردیف را دستکاری نمی‌کنم — همان کاری که معاون در سابقهٔ `ROUTING.md:42` کرد: علامت زد، ویرایش نکرد |
| `INTEGRITY.md`ِ Red Team را cite کن | ❌ **آن فایل وجود ندارد** — در هیچ ref‌ای. `git log --all --diff-filter=A -- '*INTEGRITY*'` خالی است. پس U-10..U-12 نه انجام شده‌اند، نه قابلِ cite کردن‌اند |
| `BE-007-…-2026-09-13.md` روی `session/rezv-36-backend` هست، نه main | ✅ **تأیید شد** (`git ls-tree` هر دو ref). هنوز ردیف‌به‌ردیف تطبیق نشده → `CONTRACT-DRIFT.md` دستهٔ ۲ |
| مهاجرتِ ۰۸۹ روی main نیست | ⏳ تأیید نشده |
| e2e روی `cf60b9c` قرمز است | ⏳ خودم اجرا نکرده‌ام |

---

## ۶. تقسیمِ کار با subagentها (مکانیکی → haiku، قضاوت → من)

| کار | مدل | نتیجه | اعتبارسنجیِ من |
|---|---|---|---|
| شمارشِ ۱۶۴ روت × متد × wrapper | haiku | ۲۲۴ ردیف، «صفر روتِ بدونِ wrapper» | ⚠️ **سرتیترش گمراه‌کننده بود و منتشرش نکردم**: `withApiMetrics` (۱۲۹ ردیف) رصدپذیری است نه auth، و `requireAdmin` اصلاً در واژگانِ خروجی‌اش نبود. خودم دوباره جارو زدم |
| شمارشِ ۱۳۱ فراخوانیِ فرانت | haiku | ۶۸ مسیرِ یکتا؛ فقط endpointهای auth بدونِ توکن | ✅ نمونه‌گیری شد و پایهٔ URL هر سه اپ (`base + '/api/v1'`) مستقل تأیید شد |

جاروبِ دوم را خودم زدم و **اولین بارِ خودم هم غلط بود** — `withAdminAuth` را گرفتم
که وجود ندارد و ۴۰ روتِ ادمین دروغی «بدونِ auth» شدند. با واژگانِ درست
(`requireAdmin`) دوباره اجرا شد. عددِ نهایی: ۴۰ روت بدونِ هیچ primitiveِ auth،
که همگی سطحِ عمومیِ موجه‌اند (health, metrics, auth, مرورِ عمومی, callbackِ
پرداخت, cron, media, site) — و هیچ‌کدام هنوز با درخواستِ واقعی زده نشده‌اند
(`UNKNOWN.md` U-5).
