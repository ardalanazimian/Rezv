# STATE — 2026-09-18 · one measured picture

- **Author:** CEO `rezv-46 [786cf0]` · sessionId `a7284011-f041-4d87-8f09-9949b557226b`
- **Base:** `origin/main = ba878294cc76b8470971b47817b87ab6a426e7a0`, pinned as `refs/snap/ceo46-main` and measured against that ref throughout. Measured 2026-09-18 ~03:30–05:00Z.
- **Replaces as source of truth:** `docs/audit/reports/STATE-2026-09-16.md`. That document is now materially wrong in its headline rows; see §2.
- **Reader:** the owner · every live session in this wave · the Founder session when it next resumes.

Severity: **BLOCKER** = no launch while open. **MAJOR** = fix or explicitly risk-accept before real users. **MINOR** = debt.
Status: OPEN · SUBMITTED (a fix exists somewhere, unproven or not on `main`) · CLOSED.

> **Why this file exists.** The 09-16 desk called `S-01` ("no green CI since 09-08"), `S-02` and `S-03` open BLOCKERS. All three are closed. Every session that read that desk this morning was about to spend a round re-proving work that had already shipped.

---

## 1. The headline: the team-side blocker list is clear

**`main` CI is fully green.** Run `35222259099` on `ba87829`: 16 jobs — 15 `success`, 1 `skipped` (`base-freshness`, PR-only, so it did not run rather than passing). All three `e2e` jobs green (`desktop-chrome`, `mobile-chrome`, `mobile-safari`). Measured via the public Actions API by the CEO and independently re-measured by Launch Engineer `rezv-8c` and Red Team `rezv-18` on the same sha.

Per the Founder's own rule a skipped job is not a green job, so this is stated as **15 green, 1 not run** — not "16 green".

| # | 09-16 status | 09-18 verdict | Evidence |
|---|---|---|---|
| **S-01** e2e blind to booking-context since 09-08 | BLOCKER | **CLOSED** | all 3 e2e jobs `success` in run `35222259099` |
| **S-02** `points_ledger` not append-only | BLOCKER | **CLOSED** | `api/prisma/sql/089-points-ledger-append-only.sql:79,84,90` — `BEFORE UPDATE`/`DELETE`/`TRUNCATE` on `main`, not on a candidate branch |
| **S-03** other ledgers unprotected | BLOCKER | **CLOSED — no gap** | `090-append-only-ledgers.sql` covers all 8 required tables with statement-level TRUNCATE; UPDATE on 7 (`reservation_events` already had one from `082`); DELETE unconditional on 5, retention-scoped on `audit_logs`/`platform_events` |
| **S-04** demo-trap on customer home | BLOCKER | **CLOSED** | on `main` since `8b63e61` |
| **S-05** provider credentials in plaintext | BLOCKER | **CLOSED** | `api/src/lib/secret-box.ts:1,81,107` real AES-256-GCM, fail-closed (`openSecret` throws `not_sealed`); wired at `platform-settings.ts:3,30,36` |
| **S-06** no staging on a real domain | BLOCKER | **OPEN — owner** | domain bought; name and DNS UNKNOWN; hosting unpaid |
| **S-07** real SMS transport unproven | BLOCKER | **OPEN — owner** | no raw provider send response in any ref |

**Five of seven BLOCKERS are closed. The two that remain are both owner-gated and neither can be routed around by the team.**

Falsifiability of the append-only work is being proven, not assumed: Red Team `rezv-18` is injecting a real break into `090` on a scratch branch against a fresh pg17 DB and recording baseline/attack/revert exit codes. Until that lands, "the trigger is in the `.sql` file" and "CI would go red if it were removed" remain two different claims.

## 2. Where the 09-16 desk is wrong

Recorded so the next session does not inherit the errors, and because two of them were mine to catch:

1. **S-01/S-02/S-03 called open.** All closed (§1). The desk's own tail line still reads "NO-GO (S-01 … S-07 open)" while its S-01 row says green — the file contradicts itself.
2. **"The self-serve trial was removed; `/v1/site/trial` returns 410."** **False on `main`.** The route is a live handler calling `createTrialAccount` and returning 201/200 — no 410. `apps/landing/app/demo/page.tsx` exists. See §3, F-1.
3. **M-08 downgraded to MINOR** because `resolvePolicy` is dead code. The dead-code half is true and independently confirmed (`git grep "from '@/lib/cancellation-policy'"` in `api/src` → 0, against 5 real importers of sibling `fraud.ts` as positive control). **But the downgrade is wrong:** `hasActiveAbuseFlag` also gates `incentive-engine.ts:83`, which is live via `api/src/app/api/v1/me/incentives/route.ts:3`. Clearing the flag platform-wide still has a live cross-restaurant effect. **Severity restored to MAJOR.**
4. **"The parked checkout is ~45 behind main."** It is **152** behind, 0 ahead. I repeated the 45 from the desk before measuring it; Sync Auditor `rezv-45` and `rezv-76` both measured the real number and corrected me.

## 3. Open findings, re-measured against `ba87829`

### The paid launch is not enforced in code

The owner decided the business model on 09-17. Almost none of it exists in code. This is now the largest team-controlled gap.

| # | Finding | Verdict | Evidence |
|---|---|---|---|
| **F-1** | **The self-serve free funnel is fully live, contradicting the PAID-launch decision.** `POST /v1/site/trial` creates a real Tenant + Restaurant + owner Staff, idempotent on phone, returns 201. `/demo` is a complete 30-day-trial marketing page; ~20 files under `apps/landing` reference it | **OPEN** | `api/src/app/api/v1/site/trial/route.ts:5,40,60`; `apps/landing/app/demo/page.tsx`; `site-orders.ts:262` |
| **F-2** | **`plan === 'free'` is never branched on.** `SubscriptionPlan.free` is the default for every provisioned tenant (`schema.prisma:13,29`), and a free tenant has byte-for-byte the same booking and listing capability as a paying one. The paid/free distinction does not exist at the data-model level — it must be built, not enabled | **OPEN** | public listing `restaurants/route.ts:41,49` filters only `isOpen` + heartbeat |
| **F-3** | **Subscription expiry is not enforced anywhere.** All 28 `planExpiresAt` reads are admin reporting, order activation, or the applicant's own status echo. Zero in booking or panel paths. `ClubMember` has no lock/status field at all. No 15-day renewal notice exists | **OPEN** (was M-21) | `admin/overview/route.ts:49,57`; `admin/restaurants/[id]/control/route.ts:76-93`; `site-orders.ts:573-624` |
| **F-4** | **Sold prices in code are wrong.** Seed says 18M / **34M** / **65M**; the owner decided 18M / **33M** / **60M** | **OPEN** | `api/prisma/seed/site-content.json:10,29,49` |
| **F-5** | **No VAT anywhere.** The owner decided all prices exclude VAT and every surface shows the +10% line. `vat`/`tax` → 0 hits in `api/`+`apps/`; one generic Persian sentence at `pricing/page.tsx:150` with no rate; order amount is `plan.priceToman` verbatim, no multiplier | **OPEN** | `site-orders.ts:439` |
| **F-6** | **Price lock has no mechanism.** `priceAtPurchase`/`lockedPrice`/`pricingSnapshot` → 0 in `schema.prisma`. `SiteOrder.amountToman` is an audit snapshot, not a guarantee tied to the tenant; nothing stops a renewal being billed at a changed price | **OPEN** | `schema.prisma:1946` |
| **F-7** | **Pricing page contradicts itself and sells a switched-off feature.** It claims every plan is the full product at `pricing/page.tsx:131-132` while `PlanCards.tsx:61-65` renders per-plan feature lists that genuinely differ. The m12 plan exclusively sells smart price rules, whose engine states in its own header that it is not AI. The m6 plan advertises points, cashback and gift cards while `gift_card_purchase_enabled` and `points_redemption_enabled` are both in `DEFAULT_OFF` | **OPEN** (was M-12) | `feature-flags.ts:70-74`; `pricing.ts:1-9`; `seed/site-content.json:38,58` |
| **F-8** | **No diner can redeem a coupon.** The API accepts and atomically redeems `coupon_code`; restaurants create coupons in the panel today; the customer booking body has no coupon key (0 hits in a 682-line file, positive-controlled) | **OPEN** (was M-20) | `reservations/route.ts:42,140`; `lib/reservations.ts:616-621`; `apps/customer/js/data/booking.js:494-501`; `apps/business/js/marketing.js:99` |

Marketing has already written the corrected Persian copy for F-7 on `session/rezv-c6-marketing` (`PRICING.md` §6, nine rows of live-vs-proposed). It needs routing, not re-writing.

### Security and money

| # | Finding | Verdict | Evidence |
|---|---|---|---|
| **M-01** | **Staff phone hijack.** `POST /v1/restaurant/staff` still creates a staff row from any phone with zero proof of ownership. `findStaffForLogin` now prefers the `owner` row first, then falls back platform-wide to oldest-row-wins. Owner phones are protected by the partial-unique index from migration `079`; **plain staff are not.** The function's own comment admits the residual | **PARTIAL — the most serious open security defect** | `restaurant/staff/route.ts` POST ~103-124; `staff-helpers.ts:172-190,192-208` |
| **M-05** | **Reward redemption debits the wallet with no economy-ledger entry.** Raw `UPDATE … wallet_balance`; `economyLedgerEntry` in that file → 0. `EconomyLedgerKind.wallet_spend` exists with a comment naming the Reward Marketplace — the ledger kind was designed for this write and the write was never added | **OPEN** | `rewards.ts:159-160`; control: `economy.ts:212,339,347` |
| **M-08** | **Abuse flag clears platform-wide** and still gates live incentive ranking across every restaurant | **OPEN — MAJOR** (severity restored, §2.3) | `fraud.ts:342-345`; `incentive-engine.ts:83`; `me/incentives/route.ts:3` |
| **m-14** | **OTP attempt-cap TOCTOU.** Read, compare, then a separate increment — still two statements, no `WHERE attempts < 5` guard | **OPEN** | `otp.ts:216-222` |
| **M-18** | **Webhook SSRF** | **CLOSED** | three ordered layers: literal check `events.ts:113`, awaited DNS pre-check `:141`, and pinned-connect delivery via `postWebhookPinned` `:157-172` using `http(s).request({ lookup: safeLookup })`. No `fetch()`, so no redirect following by construction. IPv4-mapped IPv6 decoded and blocked in `security.ts` |
| **m-13** | **Rate limit trusts `X-Real-IP` by default** | **CLOSED as designed** | both shipped proxies overwrite it at every API location block: `nginx.conf:125,146,165,177`; `Caddyfile:113,140` |
| **M-09** | **Weak `assert.rejects` matchers** | **PARTIAL** | the cited instance is fixed; the ratchet blocks only NEW weak matchers, leaving 112 of 191 (59%) across 19 files as legacy debt |

### Repo hygiene — one live exposure

| # | Finding | Verdict | Evidence |
|---|---|---|---|
| **X-1** | **The 09-17 private-repo migration removed nothing.** `592bfca` has one parent; its diff is 11 files, all `M`, zero `D`. Each was replaced by a ~400-byte pointer; the originals are intact in public history at `47aa3f0`, which is on `origin/main`'s ancestry. 111,506 bytes became 4,352. Anyone who clones the public repo can read the full attack catalogue and the 12,923-byte investor document | **OPEN — owner decision** | Sync Auditor `rezv-45`, `docs/audit/sync/SYNC-2026-09-18.md`. `INVESTORS.md` on `main` states the residual honestly in its own pointer text |
| **X-2** | **A session branch of a public repo is public.** `session/rezv-c6-marketing` is pushed and its tree carries `docs/marketing/BUSINESS-PLAN.md`, `PRICING.md` and the fundraising folder. Proven by unauthenticated fetch, not by reasoning: `raw.githubusercontent.com/ardalanazimian/Rezv/session/rezv-c6-marketing/docs/marketing/BUSINESS-PLAN.md` → **HTTP 200, 18,784 bytes**, the capital ladder present at line 180; `api.github.com/repos/ardalanazimian/Rezv` → `"private": false`; negative control 0. So the exposed count is **two files, not one**: `PAYMENT-PATHS.md:67` via `main` and `BUSINESS-PLAN.md:180-181` via the branch. ⚠️ A check that looked only at `main` and at local worktrees concluded "one file" — the optimistic error. The file is not on `main` **and is public anyway** | **OPEN — owner decision** | Marketing `rezv-aa` (self-corrected its own earlier, smaller claim); CEO confirmed the branch tree with `git ls-tree -r origin/session/rezv-c6-marketing` → `d1464b8` |
| **X-5** | **`tools/check-doc-path-refs.mjs` is blind to `docs/audit/reports/`** — the directory holding every STATE and CEO report, which carries more cross-file references than any other. `HISTORICAL_DIRS` (`:62`) excludes `docs/audit/(directives\|reports\|redteam\|round-\|deputy\|fixes\|design\|research)/`. **Proven, not assumed:** a deliberately broken reference injected into this very file left the guard at `EXIT=0` with its live-doc count unchanged at 183 before and after `git add`. The exclusion is deliberate and documented at `:30` ("historical documents"), but `reports/` is where the *current* desk lives, so the scope no longer matches reality | **OPEN — MINOR** | `tools/check-doc-path-refs.mjs:30,62`; falsifiability probe run by the CEO on this document |
| **X-3** | **All ten charters in the working checkout differ from `main`**, because that tree is 152 commits behind. Every session reading a charter off disk gets pre-`FP-007` text naming dead session ids. This is the mechanism behind the recurring dead-id problem | **OPEN** | Launch Engineer `rezv-8c` (found it), Sync `rezv-45` and `rezv-76` (measured all ten) |
| **X-4** | **The 8 permission deny-rules never load.** Sessions launch from `…\Desktop\rezv`; the project's `.claude/` is one level down in `rezv\Rezv\.claude\`. The settings file that actually loads contains only `enabledPlugins` and zero permission rules | **OPEN — owner, 30 seconds** | diff of the two `settings.json` files, verified by the CEO on his own session |

**Work rescued this pass.** Three local branches carried commits absent from `main` and from their own remotes. Pushed unchanged to new backup refs — nothing forced, nothing moved — and verified with `git ls-remote` rather than push output:

```
backup/reclaim-0918/fix-rezv-1b-features            739f56d   7 commits absent from main
backup/reclaim-0918/impl-rezv-85-p1-4a              4100f76   2
backup/reclaim-0918/fix-redteam-findings-2026-09-12 888324e   1   (had no remote at all)
```

`fix/rezv-1b-features` is the one that mattered: seven commits from 09-17 fixing shipped, customer-visible behaviour — no-show rendering as cancelled while the SMS said no-show, the admin's internal note being delivered to the banned user, and a compensating cron tick after an outage. It existed only on this disk.

## 4. What I decided this pass

| Decision | Call |
|---|---|
| The ownerless P0/P1 queue from the 09-16 desk | **Not reassigned as-is.** It was built from a desk whose top three rows were wrong. It is re-derived here as §3 |
| Launch Engineer's queue | Staging readiness for `S-06`, with `LAUNCH-READINESS.md` rebuilt against `ba87829` as its scoreboard — not a separate round |
| Mirroring `permissions.deny` into the launch directory | **Declined.** Permission configuration is the owner's control surface, and adding deny-rules while eight sessions are mid-flight can block legitimate work. Issued a binding CEO order covering the same operations instead, which binds on intent rather than on an exact string |
| M-08 severity | **Restored to MAJOR** over the 09-16 downgrade (§2.3) |
| At-risk local commits | Pushed to backup refs immediately rather than waiting for a consolidation plan |

## 5. What the owner blocks

Every row stops a gate nobody else can open.

| Row | Blocks | Since |
|---|---|---|
| **Buy hosting and point the domain** | `S-06` → gate A2 → GO | 2026-09-08, **≥5 rounds** |
| **Rotate the Melipayamak key and do one real send, keeping the raw response** | `S-07` → gate A4 → the real-OTP journey | ≤2026-09-13 |
| **Test the four surfaces on your own phone** | GO | ≤2026-09-13 |
| **Register the Melipayamak pattern for `booking_late`; export the template bodies** for `booking_noshow`, rejected, cancelled, expired | automatic no-show; SMS wording checks | 09-17 |
| **Decide on X-1**: the attack catalogue and investor document are readable in public history. Rewriting history is irreversible, needs a force-push, and still leaves GitHub's unreferenced objects and every existing clone and fork. The cheaper path is to rotate whatever those documents expose and accept the residual | — | 09-18 |
| **Counsel on Iranian advertising-SMS rules** before any marketing SMS | — | 09-17 |
| **Who makes the sales calls** | Marketing Phase 1 | 09-17 |
| **Merge one block into `rezv\.claude\settings.json`** so the 8 deny-rules load | X-4 | 09-18 |

**GO/NO-GO: the scorecard says NO-GO** — `S-06` and `S-07` are open. No GO is requested. Per the charter the CEO does not certify the launch he built.

## 6. Roster — 2026-09-18 wave

Eight live sessions. Every role below was read from the **owner's own first message in that session's transcript**, never from position in `ListAgents` and never by elimination. Each session writes its own `ROUTING.md` row; only the CEO row is written.

| Session | Role | sessionId |
|---|---|---|
| `rezv-46 [786cf0]` | **CEO** (this session) | `a7284011` |
| `rezv-8c [6a4279]` | Launch Engineer | `fa3d4091` |
| `rezv-18 [9da8c9]` | Red Team | `e527ee6c` |
| `rezv-1b [1b2595]` | Full-Stack Auditor ⚠️ recycled name | `8f274032` |
| `rezv-45 [cf2be6]` | Sync Auditor ⚠️ recycled name | `6839e90d` |
| `rezv-aa [52177b]` | Marketing / business model | `f9f256d4` |
| `rezv-47 [93d956]` | Folder and worktree consolidation | `8147eb17` |
| `rezv-76 [ed2727]` | Session capability audit | `4a31c085` |

No session from the 09-16 wave is in the harness registry. The Founder session `4fa4aafb` is **not** resumed; that is UNKNOWN, **not** a transfer of authority, and its desk at `docs/audit/founder/STATE.md` stands.

## 7. برای مالک — کوتاه

**پیشنهاد اول:** تیم دیگر گلوگاه نیست. پنج بلاکر از هفت‌تا بسته شد و CI روی `main` سبزِ کامل است. دو بلاکرِ باقی‌مانده هر دو دستِ توست: **میزبانی و دامنه**، و **یک ارسالِ واقعیِ پیامک**. تا این دو نباشد، هیچ مقدار کارِ تیم دروازه‌ی A2 را باز نمی‌کند.

**یک چیز که باید بدانی و خوشایند نیست:** تصمیمِ «لانچِ پولی» در کد اجرا نشده. مسیرِ ثبت‌نامِ رایگانِ خودکار هنوز زنده است و حسابِ واقعی می‌سازد، و رستورانِ رایگان دقیقاً همان توانِ رزروِ رستورانِ پولی را دارد. قیمت‌ها هم در کد ۱۸/۳۴/۶۵ مانده، نه ۱۸/۳۳/۶۰ که تو گفتی. این‌ها را تیم می‌بندد، ولی تا بسته نشده، صفحه‌ی قیمت چیزی می‌گوید که محصول انجام نمی‌دهد.

**یک تصمیمِ تو:** فایل‌های حمله و سندِ سرمایه‌گذاری از نوکِ ریپو رفتند ولی **در تاریخچه‌ی عمومی دست‌نخورده‌اند**. پاک‌کردنشان بازنویسیِ تاریخ و force-push می‌خواهد، برگشت‌ناپذیر است، و باز هم در کلون‌ها و forkها می‌ماند. پیشنهادِ من: هرچه آن اسناد لو می‌دهند را **بچرخان** و باقی را بپذیر. این تصمیمِ توست، نه من.
