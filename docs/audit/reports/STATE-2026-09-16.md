# STATE — 2026-09-16 · one merged picture

- **Author:** CEO `rezv-87 [09dbab]` · sessionId `66f43af4-dd36-417d-a984-810a35f4a455`
- **Base:** `origin/main = cf60b9c` · **measured at:** 2026-09-16 ~14:20Z (rule §4i: every number here carries its sha and time)
- **Replaces as source of truth:** `docs/audit/HANDOFF-2026-09-16-ceo.md`. Its to-do list was kept after re-measurement. Two of its claims were wrong (C-1, C-2 below).
- **Companion:** `COORDINATION-2026-09-16.md` (inbox, spot-checks, decisions, roster)
- **Reader:** the owner · every live session in this wave · the Founder session when it next resumes

Severity: **BLOCKER** means no launch while it is open. **MAJOR** means it must be fixed or explicitly risk-accepted before real users. **MINOR** is debt.
Status: OPEN · SUBMITTED (a fix exists somewhere, unproven or not on `main`) · MITIGATED · CLOSED. The CEO closes nothing in this report.

---

## 1. Findings — deduplicated

Where one defect was found by several sessions, it is one row with several sources.

### BLOCKER

| # | Finding | Evidence (measured this pass) | Owner | Sources | Status |
|---|---|---|---|---|---|
| **S-01** | **`main` CI has had no fully green run since `3e22075` (2026-09-08).** Since `42ba301` (09-13) only the 3 e2e jobs are red, with "6 failed". Root cause for `booking-context.spec.ts`: `b3e22da` (09-10, DS-007 §6) removed `#sWhen/#sParty`, and the spec still reads them 8×. **e2e has not covered the booking-context flow for 6 days.** | CI API: run `35055753423` on `cf60b9c`: 13 success, e2e ×3 failure, `base-freshness` skipped. `git grep -c sWhen` in `apps/customer` on main = 0, in the spec = 8 | Implementation `rezv-85` (P0-1) | CEO (CI API), Red Team old RT-23, Launch Engineer (branch LAUNCH-READINESS), verification agent, Full-Stack `rezv-75`, Implementation `rezv-85` | OPEN on main · SUBMITTED in candidate `99065a7` (specs moved to `#ctxWhen`), unproven |
| **S-02** | **The money ledger `points_ledger` is not append-only.** A balance (`SUM(delta)`, `loyalty.ts:165`) can be erased by `DELETE`, a zeroing `UPDATE` or `TRUNCATE`. The guard `check-loyalty-promise.mjs` is FAKEABLE (hoisted negation) | `git grep` for triggers on points_ledger in `api/prisma` on main = 0, positive control `reservation_events` = 2. `088` + `089` exist only on the candidate lineage | Implementation `rezv-85` (P0-1) · attack: Red Team `rezv-31` | RT-18, RT-22 (old Red Team) · AO-1 (Red Team `rezv-31`) · FP-009 · FIX-RT-18 (Launch Engineer) · Full-Stack `rezv-75` | OPEN on main · SUBMITTED `089` @ `adc8c8f`. **Red Team `rezv-31` verdict (~15:00Z, `origin/redteam/retest-2026-09-16 @ fcd31a5`): 089 HOLDS for `points_ledger`.** UPDATE, DELETE and TRUNCATE are all blocked on a fresh DB, and the RT-24 door is closed (FK flipped to CASCADE, `DELETE FROM users` still blocked, because the delete trigger is unconditional). Residual: `session_replication_role=replica`, superuser only, guarded statically by FP-009 §6. CEO spot-check 4/4 against candidate source: unconditional `points_ledger_no_delete`; `rejectsAppendOnly` matches the trigger's own message (`:34-40`); alerts bind to `sms.ts:229/236/251`. The candidate still needs M-09, a fresh-DB api run and named e2e reds before it can merge |
| **S-03** | **The other append-only tables have no enforcement.** `audit_logs` (the refund-debt evidence RT-13 relies on), `economy_ledger_entries`, `sms_transactions`, `campaign_logs`, `platform_events`, `coupon_redemptions` and `reward_redemptions` each have 0 triggers. The one protected table, `reservation_events`, can be emptied by `TRUNCATE` (row triggers do not fire on it) | Per-table trigger count via `git grep` on main: all 0; `reservation_events` 2 (row-level only); `BEFORE TRUNCATE\|FOR EACH STATEMENT` repo-wide → exit 1 | Implementation `rezv-85` (P0-2) · attack: Red Team | RT-25 (old Red Team, `rezv-31` re-confirmed) · AO-2, AO-3 (`rezv-31`) · B-2 (`rezv-75`) | OPEN on main · **Red Team verdict GO (`rezv-31`, local `ee5d3f5`): 27 triggers counted in the DB, TRUNCATE blocked on 9/9, conditional DELETEs exact, UPDATE blocked (age-then-delete closed), SET NULL parent deletes rejected, §5/§6 gates falsifiable; residuals are DDL/superuser only. Merged into `origin/ceo/main-ready-0917 @ 2b65897` (19 guards 0 on the merged tree).** · SUBMITTED `origin/impl/rezv-85-090-append-only @ 553ecf4` (090 + FP-009 §5/§6 gates, 3 commits on `8cdc6e9`; CEO checked ancestry, the FOREACH trigger loops at `090:103-136`, the conditional guards `090:77` (audit_logs, 1 year) and `:90` (platform_events, `ingested_at`, 90 days), and §6 wired into `ci.yml`). Per the builder: new test 27/32 red → 32/32; idempotent twice; triggers 5 → 27; api suite 1891/1891; `check-schema-drift.sh` DRIFT_EXIT=0 (run before the comment-only commit `eeff902`). **Awaiting Red Team.** Earlier note, still true for main: **merging the candidate alone does NOT close it:** trigger census on `99065a7` (`rezv-31`) is `points_ledger` = DEL+UPD+TRUNC, `reservation_events` = DEL+UPD (no TRUNCATE), every other table none. Exposure, not active loss: no app code path updates or deletes these rows (`rezv-75`, positive control). "089 shipped" must not be read as "append-only done" |
| **S-04** | **Demo trap on the customer home feed:** an HTTP 500 from `/restaurants` renders six `[DEMO]` restaurants as «۶ رستوران فعال», with no error UI | `apps/customer/js/api.js:370` returns `R_SAMPLE` for any `!res.ok`; `api-core.js:50-52` marks HTTP errors without `offline`. Runtime proof by `rezv-75` (500 / 200-empty / 200-real). The candidate does not change it | Implementation `rezv-85` (P0-0) · re-verify: `rezv-75` | Full-Stack B-1 | OPEN on main · **SUBMITTED `origin/impl/rezv-85-p0-0-demo-trap @ b496991`** (parent `99065a7`): samples only under `isOfflineDemo()` (file://); 500/503/network loss show an error state with retry; the boot paint of `R_SAMPLE` was removed (a sibling, visible on any network slower than 280 ms); `renderEvents` and `doSearch` siblings fixed. Proof per `rezv-85`: RED 15 failed on `99065a7` → GREEN 33 passed; 3 mutations red; full e2e 603/0 failed/4 flaky; 18 guards 0. CEO verified the gate diff in `api.js` and `init.js`. **Policy D-19:** samples are allowed only in the file:// demo, and my own looser wording was overruled by the builder, correctly. **Re-verified by `rezv-75` (`d889a8e`): ACCEPTED.** Five cases on both trees: the candidate shows samples in 3 of 5 (500, network loss, cold boot at 400 ms); the fix shows 0 of 5, with an error state and retry. The file:// standalone demo is still intact, and samples were identified by name from each tree's `seed.js`, not by the `[DEMO]` prefix. **Flake measured de-confounded (`rezv-75`, workers=1, 480×2): 3.96% vs 2.29%, p=0.138, pre-existing and unrelated → merge unblocked. Merged into `origin/ceo/main-ready-0917 @ 623af56` (with 090; 19 guards 0 on the merged tree), awaiting the owner's push.** Earlier wording: merge waits on a de-confounded measurement of the `cancel-window-disclosure.spec.ts:84` [mobile-safari] flake (1/48 on the candidate vs 8/78 on the fix, confounded by overlapping DB builds; the trace shows a tap landing mid-transition, which may also be a real-user mis-tap). **Siblings classified (`rezv-75`):** PARTIAL, labelled → STATE m-15 |
| **S-05** | **Provider credentials in `platform_settings` are stored in plaintext.** No encryption at rest exists anywhere in `api/src`. That breaks the standing decision of round 15 §3 | `git grep -il 'createCipheriv\|aes-256\|encryptSecret\|kms'` in api/src = 0 files; positive control `createHash` = 5 | Implementation `rezv-85` (P1, after P0) | P0-007 (round 15) = A3-008 (round 16) = `rezv-31` batch 1 | OPEN since 2026-09-03. **Raised from MAJOR to BLOCKER** this pass (D-12) |
| **S-06** | **No domain, so no staging on the real domain.** Gate A2 (four golden journeys) cannot pass | `audit/ESCALATIONS.md` E-001; `tools/gate-deploy.mjs` RED (Launch Engineer agent, `DECISIONS.md:33`) | **Owner** (money) | E-001, Launch Engineer scorecard | OPEN since 2026-09-08 |
| **S-07** | **Real SMS transport is unproven.** Needs key rotation plus one real send with the raw provider response; gate A4 and the customer journey's real OTP depend on it | Founder `STATE.md` §3; no raw send response in any ref | **Owner** (credentials) | Founder desk, Launch Engineer | OPEN |

### MAJOR

| # | Finding | Evidence | Owner | Sources | Status |
|---|---|---|---|---|---|
| **M-01** | **Staff phone hijack:** `POST /v1/restaurant/staff` accepts any phone with no proof, and `findStaffForLogin` is platform-wide (owner row first, then oldest). A real staff member of restaurant X whose number has no owner row can be captured by a tenant that registered the number first: they log into the wrong tenant, and are locked out of their own | `api/src/app/api/v1/restaurant/staff/route.ts:124`; `api/src/lib/staff-helpers.ts:192-206`, whose own comment at `:176-185` admits the residual. No repro test in any ref (verification agent) | Implementation `rezv-85` (P1-4, the design slice runs on opus) | CEO handoff #6, Founder desk, verification agent | OPEN. **Package delivered (`origin/impl/rezv-85-p1-4-package @ feea34b`)** with a live red repro: tests 5, fail 4. R1 attacker adds the victim's phone as manager, and the victim's first login lands in the attacker's tenant. R2 the victim's own trial signup is refused. R3 the attacker's older row wins. **R4 the public no-OTP trial form creates an owner row for any phone and blocks real provisioning of it.** **D-23: Option A** (consent at login: `staff.accepted_at`, accepted rows only, consent/chooser, 079 uniqueness on accepted owners only), migration 093; interim I-1 approved (narrow `site-orders.ts:288` to owner rows, as `provisioning.ts:121` already does). Option B rejected (depends on unproven SMS, and adds a phishing surface). Delivered to the owner in words on 09-16 |
| **M-02** | **RT-19/20/21 are marked "FIXED" but the fixes never reached `main`:** no `SmsUnchargedSends`/`SmsDuplicateSend` alert, and `test-env-up.sh` has no `prisma generate` (a stale client fails open on money) | `observability/alerts.yml`: main 0 / candidate 2; `prisma generate` in `tools/test-env-up.sh`: main absent / candidate 3 | Implementation (lands with P0-1) | old Red Team RETEST-09-13, verification agent (new) | SUBMITTED in candidate |
| **M-03** | **Alarms page no one:** there is no alert receiver (C-3), so the RT-13 refund alarm fires into nothing. `observability/metrics-token` is gitignored and absent while the compose file and `prometheus.yml` reference it, so that stack cannot start | Launch Engineer BACKLOG C-3 still unassigned; Sync §3 | Implementation (P2) | CEO handoff #11, Launch Engineer, Sync `rezv-7a` | OPEN |
| **M-04** | **9 real backend fixes are not on `main`,** including B1 (staff `restaurant_id` taken from the auth context, not the body, which is a tenant boundary). The merge has 5 conflicts | `git merge-tree` main × `485fda3`: exit 1, 5 conflicts (`apps/business/js/marketing.js`, `waitlist.js`, `docs/XSS_SINK_AUDIT.md`, `standalone/business.html`, `tools/xss-sink-audit-report.json`) | Implementation (P1-5) → CEO merge | Backend `edb5f154`, verification agent | SUBMITTED on branch |
| **M-05** | **Reward redemption debits the wallet with no economy-ledger entry** | `api/src/lib/rewards.ts:159-162` raw `UPDATE … wallet_balance`; `economyLedgerEntry` in that file = 0 (exit 1), positive control elsewhere | Implementation (P2) | CEO report 09-09 (F15), LOYALTY-ECONOMY-DESIGN §4.6, verification agent | OPEN since 09-09 |
| **M-06** | **FP-009 §5/§6 guards are not on `main`** (confdeltype + triggers axis; `session_replication_role` static guard) | They existed only uncommitted in `wt-rezv-c9`; rescued verbatim at `backup/rescue-0916/fp009-guards-wt-rezv-c9 @ 5ae45a2` | Implementation (P0-3). **Scope must widen to S-03's full table set and `BEFORE TRUNCATE`** (`rezv-31`) | Backend, verification agent, Red Team `rezv-31` | SUBMITTED (rescued, unreviewed) |
| **M-07** | **Fake-green guard gaps FG-10 / FG-11 are still open** (doc-staleness check-3 is blind to several forms; `loyalty-constant-binding` checks set membership, not position) | verification agent against `cf60b9c` source | Implementation (P2) · re-attack: Red Team | old Red Team LEDGER-ADDITIONS | OPEN |
| **M-08** | **Customer abuse flag can be cleared platform-wide** by any restaurant the user ever visited. `customer_economy_profiles` has no restaurant column | `api/src/lib/fraud.ts:342-345` `updateMany({ where: { userId } })`; `schema.prisma:2001-2017` | **CEO decision owed** (parked on a 09-06 package) | old Red Team RT-26 #2 · Feature Verification `rezv-1b` | OPEN · CEO debt. **Impact downgraded to MINOR (~09-16 21:10Z):** the policy layer the flag was said to switch off, `resolvePolicy` (`cancellation-policy.ts:134`), has **0 call sites**. It is dead code (`rezv-1b`; I re-grepped: definition plus comments only). The only live reader left is `incentive-engine.ts:83` (nudge suppression). New MINOR: the cancellation-policy engine exists but is not reachable (constitution §4h). `rezv-75` classifies which code actually decides deposit and auto-confirm |
| **M-16** | **Tenant isolation, measured at runtime: 218 control-gated rows, 0 leaks** across 6 vectors: path ids, `X-Restaurant-Id` cross-branch, branch-lock, owner → `/v1/admin/*`, STAFF/PLATFORM-ADMIN → `/v1/me/*`, and body ids. RLS is 0 policies, so this is the only tenant boundary | `rezv-75` `origin/audit/fullstack-2026-09-16 @ 1ead846` (ISOLATION-MATRIX). CEO static spot-check 3/3: `jwt.ts:104-108` has no kind check, while `me/route.ts:20,38` rejects `kind !== 'customer'`; `restaurant/staff` is tenant-scoped | Red Team `rezv-31` (attacks #6/#7 plus a ≥20% re-run) | Full-Stack B-4 · Red Team `ISOLATION-RETEST-2026-09-16-rt31` (`77c6dbd`) | **ACCEPTED: HOLDS at handler level (D-2 satisfied).** Red Team: #6 N/A (the harness is in-process with an identity sentinel) · #7 cannot flip a 403/404 (isolation is decided before rate limiting) · trap 3 disproven (V2 rejects in `resolveStaffRestaurant` before any path lookup; CEO checked `staff-helpers.ts`, where the branch lookup is `findFirst where { id: requestedId, tenantId: auth.tenantId }`, and branch-locked staff use `staff.restaurantId` before the header is read) · trap a not triggered (control distribution has 0 403s) · 48/218 rows (22%, seed 31, V2-weighted) re-judged from raw status+body, 0 leaks. **Two limits, kept visible:** Red Team re-judged the raw evidence and did not re-execute all 218 rows against a fresh server; the harness calls handlers directly, so `middleware.ts` and the full HTTP pipeline are UNKNOWN |
| m-14 | **OTP attempt cap has a TOCTOU race:** `verifyOtp` reads `attempts`, compares, then increments separately (`lib/otp.ts:219-221`). 10 concurrent guesses beat the cap of 5 | Red Team `rezv-31` (live, 10 concurrent); CEO static: read → check → `update increment` are three separate statements | Implementation (P2): an atomic conditional increment (`UPDATE … SET attempts = attempts + 1 WHERE phone = $1 AND attempts < 5 RETURNING …`) | Red Team `rezv-31` | OPEN. MINOR: bounded by the per-IP `otpVerify` limit, which the shipped proxies make trustworthy (m-13) |
| m-13 | **Rate-limit client IP trusts `X-Real-IP` by default** (`TRUST_PROXY_HEADERS !== 'false'`, `ratelimit.ts:208-212`); the verify routes are IP-keyed only. **Both shipped proxies overwrite the header** (`deploy/nginx/nginx.conf:125`, `deploy/caddy/Caddyfile:113,140`), so it is safe only behind them | as cited; the bypass was reproduced without a proxy by `rezv-75` | A2 checklist, worded to cover serverless: **"the API is reachable only through a layer that OVERWRITES `X-Real-IP`; if no layer writes it (including serverless), `TRUST_PROXY_HEADERS=false`"**, because `clientIp` reads `X-Real-IP` first and a client-supplied value wins when nothing sets it | Full-Stack side finding (re-rated MINOR by its author, `956412c`) | OPEN (deploy precondition). **The brute-force question is resolved: it is not an auth bypass.** The OTP attempt counter is per phone in Postgres (`lib/otp.ts:219-221`, `attempts >= 5`), a code lives 2 min (`:175-176`), and new codes are capped per phone (`otpPerPhone` 3/10 min), all independent of IP. `docker-compose.prod.yml` publishes only caddy's 80/443; the `api` service has no `ports:`. Rotation bypasses only the volumetric per-IP limits (abuse). CEO spot-checked both points. `docs/DEPLOY_API_VERCEL.md` (an ARCHIVED snapshot) describes a proxy-less topology that nobody has measured |
| **M-17** | **Staff can mark `no_show` days before the slot, with the full penalties** (cashback reversal, strike, reliability). The status route has no time check, the transition map allows `confirmed → no_show`, and the panel offers «نیومد» on every upcoming row | `api/src/app/api/v1/restaurant/reservations/[code]/status/route.ts:41` → `transitionReservation`; `api/src/lib/lifecycle.ts:40-44`; `apps/business/js/reservations.js:165` (on `c234479`) | `rezv-1b` (with F001). **D-20 (c):** staff no-show only at `now ≥ slotStart + grace (+ extension)`, otherwise 409 with the deadline; cancel stays available | Feature Verification (sibling of F001) | OPEN |
| **M-18** | **Webhook SSRF: delivery does no DNS check at all.** `deliverWebhook` (`events.ts:128`) uses the synchronous `assertSafeWebhookUrl` (`events.ts:81-120`), which checks only literal IPs and hostnames. A hostname whose A record points to `169.254.169.254` passes, no rebinding needed. An IPv4-mapped IPv6 literal (`[::ffff:a9fe:a9fe]`) also passes. `startsWith('fc'\|'fd')` blocks legitimate hostnames. The DNS-checking `assertPublicHttpUrl` (`security.ts:94-110`) exists but delivery doesn't use it (§4h). `redirect: 'manual'` is present | CEO static read of main; Red Team `rezv-31` raised it as "latent DNS rebinding" | **`rezv-31`** (owner's direct order to fix; branch `fix/rt31-ssrf-pinned-lookup`) · independent verify: `rezv-75` | Red Team · CEO | OPEN |
| **M-13** | **A late guest becomes a no-show in one cron tick with no warning.** confirmed → running_late → no_show at +17 min: 0 SMS before, `booking_noshow` after, cashback −40, strike +1, reliability 75 → 0, and `canTransition(no_show → seated) = false`. `lateGraceMinutes` has readers only (default `?? 15`) | runtime on a fresh pg17 with the production cron functions (`rezv-1b`, `origin/audit/features-2026-09-16 @ d606df1`, not re-run by me); static: `api/src/lib/lifecycle.ts:403-407` | **`rezv-1b` (owner's direct order, ~09-16 22:00Z)**; attack: Red Team. Numbers: D-18. **Design approved with amendments (D-20):** `late_warned_at` is set only when the provider accepts the `booking_late` SMS or the guest calls `/eta`; no auto no-show without it; `/eta` is customer-kind only and owner-of-reservation only; migration 091 after 090. **Owner-held dependency:** the Melipayamak pattern for `booking_late` must be registered, and until then auto no-show is effectively off | Feature Verification F001 | OPEN, design approved |
| **M-14** | **The banned user receives the admin's internal note.** `Err.userBanned(user.bannedReason)` puts it in `details.reason` of the JSON response. It is not rendered, but it is delivered. The login screen also shows the ban as a wrong-code toast | `api/src/lib/errors.ts:50`; `api/src/app/api/v1/auth/otp/verify/route.ts:35`; refresh `:76`; `apps/customer/js/auth.js:121-125` | **`rezv-1b`** (owner's order); re-verify: `rezv-75`; schema-drift run: `rezv-85` | Feature Verification F003 (the UX half; the exposure was reframed by the CEO) | **SUBMITTED `origin/fix/rezv-1b-features @ cddf476`** (migration 091). CEO checked: `userBanned(reasonKey, bannedAt)` (`errors.ts:53`, the note is no longer a parameter); verify/refresh select `bannedReasonKey`; the admin ban route requires the `reason_key` enum. Per the builder: runtime repro of the leaked note → the raw body no longer carries it (verify, refresh, assertUserNotBanned, legacy null key); api suite 1876/0; e2e 90/0; 15 mutations caught; new `ban-reason-copy-binding` guard. **NOT RUN: `check-schema-drift.sh`** (no psql; CI does not run on `fix/*` branches, so the builder's "CI runs it" was wrong) |
| **M-15** | **No-show, rejected and expired all render as «لغوشده»** on the trip card while the SMS says no-show | `apps/customer/js/reservation.js:29` (status map), `:159` (label) | **`rezv-1b`** (owner's order); re-verify: `rezv-75` | Feature Verification F002 | **SUBMITTED `origin/fix/rezv-1b-features @ ad693ba`** (one commit on `c234479`; CEO checked ancestry and the 10-file diff). Per the builder: API red 4/4 → green 8/8; 5/5 server and 5/5 UI mutations caught; full api suite 1864/0 on a fresh DB; guards and artifact gates 0 on a clean LF checkout. Named WebKit flakes at workers=2: `cancel-window-disclosure.spec.ts:98`, `:118`, `customer-recovery-batch18.spec.ts:165` (pass at workers=1). **This tree does not contain `b496991`, so the same describe block flakes without the demo-trap fix** (a hypothesis for `rezv-75`'s measurement, not a count). Awaiting `rezv-75` re-verification. Merge note: `sw.js` v50 here vs v49 on the demo-trap branch; keep the larger |
| **M-09** | **(Downgraded to MINOR by Red Team's attack, but it still blocks the merge, because CI's `rejects-ratchet` goes red.) The FK-RESTRICT delete check in 089's own test has a string where a matcher belongs; the `confdeltype='r'` assert backs it up. The three append-only assertions use a real matcher (`rejectsAppendOnly`).** Original wording: `assert.rejects(fn, '<string>')`, so any rejection passes. `rejects-ratchet` will be red on the candidate | `node tools/check-rejects-matcher.mjs` on `99065a7` → exit 1 at `api/tests/points-ledger-append-only.integration.test.mts:134` | Implementation (P0-1) · attack: `rezv-31` | CEO (guard run) | OPEN on candidate |
| **M-11** | **Deposits settle to one platform-wide Zarinpal merchant, and no settlement or payout path exists.** If `paymentEnabled` is turned on for a restaurant, the platform holds that restaurant's money with no code to pass it on | `api/src/lib/platform-settings.ts:33-38` (`zarinpal_merchant_id`, single); `git grep -ilE 'settlement\|payout\|تسویه'` in api/src = 0 files, positive control `zarinpal` = 7 | **Owner** (money + legal); options drafted by Marketing `rezv-c6` | Marketing `rezv-c6` | OPEN. **Blocks turning deposits on for any restaurant** |
| **M-12** | **Pricing page contradicts itself and AI-washes.** It says "every plan is the full product" while the cards tier features; it sells «قواعدِ قیمتِ هوشمند» for a rule engine that says it is not AI; it advertises a gift card while the purchase flag is off | `apps/landing/app/pricing/page.tsx:134-137`; `api/prisma/seed/site-content.json:38,58`; `api/src/lib/pricing.ts:1-10`; `api/src/lib/feature-flags.ts:70-73` | Implementation (P1, honesty class; Designer lane per FP-007) | Marketing `rezv-c6` | OPEN. Also: the 6-month card advertises «امتیاز» while spending points is OFF (FP-008). Earning is real, spending is not, so the copy must say which |
| **M-10** | **Work at risk on this machine:** commits and uncommitted work reachable from no origin ref (089, a staged merge resolution, FP-009 guards, four worktrees). The morning's "everything pushed" was false | `git log --branches --not --remotes=origin`; per-worktree `git status` | CEO | Sync `rezv-7a`, CEO | MITIGATED: 8 backup refs (COORDINATION §e D-3). Adoption pending |

### MINOR

| # | Finding | Owner | Status |
|---|---|---|---|
| m-15 | **Company and business panels show LABELLED sample data while the backend is live** (500 / degraded shape): `[DEMO]` rows plus a banner, or a «دادهٔ نمونه» badge plus a note; well-formed payloads render real rows (controls proved it). On a 500 the company banner says «اتصال به سرور برقرار نیست» although the server answered (`apps/company/js/api.js:188` `reachable = !!(res && res.ok)`). INFO: `dashboardUsingDemoData` over-labels via `_tablesLoaded`/`_wlLoaded` | Implementation (P3). **D-21:** D-19's letter applies to the customer app; the panels' target is the same rule (error state with retry on http(s)) | OPEN |
| m-16 | **The documented retention procedure silently orphans ledger references.** `DROP TABLE reservations_old` with CASCADE (the path in `011:128-137` and `002:12,44`) drops the `economy_ledger_entries` FK, and `reservation_id` survives with its parent gone. Triggers don't fire on DDL. A month-partition DROP succeeds without touching the ledger. Not automated anywhere | measured on a scratch DB by `rezv-85` (FIX-BE-02 §partition); the comments are now updated to say so | CEO: re-decide retention against FP-009/090 before anyone uses it · attack: Red Team | OPEN |
| m-17 | **Module-level `beforeEach`/`afterEach` in api tests run before EVERY test** in the one-process runner (one broke 1829 tests against 090); 15 files, 2 fixed | `rezv-85` | Implementation (P2): a static guard with a self-test | OPEN |
| m-18 | **The «لغو» click on the trip card is silently dropped in ~2–4% of runs** (`cancel-window-disclosure.spec.ts` :75/:84/:98/:118/:132 fail on both trees; the text-only :49/:57/:64 never do). The DOM snapshot at failure shows the page settled and no sheet open: the click did nothing. The earlier reading "a wrong sheet («جزئیات رزرو») opens" was disproven; the match came from Playwright's embedded source listing, not the DOM | `rezv-75`: 19/480 and 11/480 failures, with DOM sections checked; inline onclick and the swipe-pad bail (`swipe-actions.js:82`) ruled out | `rezv-75` until the mechanism is known (`--trace=on`), then a builder | OPEN |
| m-01 | `ROUTING.md` CEO row pointed to a dead session. `:73` claimed `redteam.md` still names a dead id; fixed by `5936589` on 09-09. The four sessions opened on 09-16 have no rows | CEO (done on my branch; each session writes its own row) | SUBMITTED |
| m-02 | Ban check missing on `reservations/[code]/cancel`, `arrive` and `pay` (inside `lib/ban.ts`'s declared scope), plus `me/chats` and `me/referral`. Window ≤15 min (`jwt.ts:41`) | Implementation (P2) | OPEN |
| m-03 | BE-007 artifact is corrupt: duplicated §9, and the second copy has stripped backticks and leaked `Volume in drive C:` shell output. Its §7 conflict prediction is wrong (M-04) | Implementation (P1-5) · `rezv-75` re-verifies rows | OPEN |
| m-04 | `docs/audit/fixes/LAUNCH-READINESS.md` on main is one revision behind the branch (RT-13 → PARTIAL, RT-18 FAKEABLE) | lands with candidate | SUBMITTED |
| m-05 | `ESCALATIONS.md` E-003 body read "باز" under a closed header; one verifier took it as open | CEO | Annotated this pass |
| m-06 | Directive 046 residual: `apps/business/js/overview.js:129` renders a failed fetch as loading | Implementation (P3) | OPEN |
| m-07 | `shared/js` drift: 4 of 5 files differ in the business/company copies | Implementation (P3) | OPEN |
| m-08 | Scout proposals 001–007: all still "proposed", never ruled on | CEO debt | OPEN |
| m-09 | Deputy ORDER-003/004: SUBMITTED, never reviewed. (The Deputy's constitution proposal of 09-11 is **applied**, COORDINATION D-15) | CEO debt | OPEN (orders) |
| m-10 | Designer asks: a cover-photo field in the `/v1/restaurants` select; a stale comment ("app never calls `/restaurants/{slug}`") is wrong on main too; E-004 (feed text search has no API route) exists only on the design branch | Implementation (P3) / CEO after merge | OPEN |
| m-12 | **Merge order and numbering, ruled ~15:15Z:** (1) the candidate lineage is canonical for migrations (088, 089), so the next number is **090**. (2) Order: the candidate lineage (`impl/rezv-85-desk` strictly contains it, and every link of the chain was checked with `merge-base --is-ancestor`) → the backend rescue `ea1f264` (clean into main, but merged only after `rezv-85` reviews its 13 novel blobs) → the `rezv-36-backend` residue (3 commits, 1 conflicted path: BE-007). That order turns **5** conflicted paths into 1. (3) `fix/auth-headers-eslint-seo` (08-05, 678 behind, 31 conflicted paths) and `fix/launch-demo-sms-reviews` (08-19, 626 behind, 7 paths; its migration `048` is byte-identical on main, blob `a3877c8a`) are declared **SUPERSEDED/ABANDONED**: not pending, not merged, deletion waits on PR state (UNKNOWN, no `gh`) | CEO (ruling) · Implementation (execution) | Source: Sync `MERGE-BACKLOG-2026-09-16`. Its conflict counts (23/106/23) did not match unique-path measurement (5/31/7); correction requested |
| m-11 | Housekeeping: `audit/launch-hardening` is 321 behind and fully merged, yet still named as PR base in pasted prompts · `integration/launch-rc4` upstream mis-set · the `relay-wt` worktree is emptied (1209 deletions, nothing lost) · a remote branch literally named `origin` · 22-branch delete list (verified safe) | CEO | Deferred (COORDINATION D-9) |

---

## 2. Contradictions and how each was settled

| # | Claim A | Claim B | Settled by | Verdict |
|---|---|---|---|---|
| C-1 | CEO handoff 09-16: «`main` سبز است» | CI API: no fully green run since 09-08 | run `35055753423` jobs + annotations | **B.** A measured 18 local guards, not CI. Promoted to constitution §4i |
| C-2 | Founder desk / stop order: «همه‌چیز push شده», «۰۸۹ هنوز نوشته نشده» | `adc8c8f` (089) committed 07:59:06, pushed to no origin ref | `git log --branches --not --remotes=origin` | **B.** The Founder's desk was written at 07:55, 3 minutes before 089 existed, so its line is STALE, not wrong. The stop order's "everything pushed" was wrong |
| C-3 | RETEST-09-13: RT-19/20/21 FIXED | main has none of the fixes | `git grep` main vs candidate | Fixed **on branch only**; the report named its branch, later readers dropped that |
| C-4 | My verification agent: "089 exists nowhere; 082 nowhere" | `git show adc8c8f:api/prisma/sql/089-points-ledger-append-only.sql`; `082-reservation-events-ml-substrate.sql` on main | source | **B.** The agent searched `api/prisma/migrations/`. That agent's report is rejected (COORDINATION §b) |
| C-5 | Me (CEO) to three peers: dead id at `ROUTING.md:103` | Red Team `rezv-31`: `:103` is blank on main; the id is at `:73/:137/:175` | `git grep -n d8087d origin/main` | **B.** I had cited the stale main checkout (`session/rezv-b3-design`). Corrected to all three |
| C-6 | Me: "main red 8 days" | Implementation `rezv-85`: "e2e red since 09-10, 6 days" | CI history | **Both, for different claims:** no fully green run for 8 days; the booking-context e2e cause is 6 days old |
| C-7 | Me: `relay-wt` 1209 dirty entries "likely line-endings" | Sync: genuine deletions | `--ignore-cr-at-eol` still 1209 (Sync) | **B.** I had marked it unverified; it was wrong |
| C-8 | Sync §2.1: mergewt is "in Temp; ordinary cleanup destroys it" | the index lives at `Rezv/.git/worktrees/mergewt/index` | `git rev-parse --git-dir` | **Partly B:** loss needs temp cleanup, then `worktree prune`, then gc. Moot after rescue `ea1f264` |
| C-9 | Sync §2.3: tag `wip-before-rebuild` holds "a CI gate that does not exist on GitHub" | `git cherry origin/main 2266fe4` → all three "-"; the gate is on main (401 lines, called by ci.yml) | `git cherry`, blob compare | **B.** Nothing in the tag is lost work. Correction requested |
| C-10 | BE-007 §7: no business-JS overlap on merge | `git merge-tree`: `marketing.js` and `waitlist.js` conflict | merge-tree | **B** |
| C-11 | Pasted prompts (Full-Stack, Implementation): PR base `audit/launch-hardening` | branch 321 behind main, fully merged, on the verified delete list | `rev-list --count`, `merge-base --is-ancestor` | **Base = main.** Delete deferred (D-9) |
| C-12 | `rezv-75`: "INTEGRITY.md does not exist in any ref" | `origin/redteam/retest-2026-09-16 @ e32e200` has it | ref timing | **Both true at their times.** Red Team pushed after Full-Stack measured |
| C-13 | Red Team `rezv-31`: RT-25 is a blocker | old Red Team: "latent gap, not active loss" | `git grep -i truncate` in api/src, api/tests, tools: only a string helper | Folded into S-03 (class BLOCKER, harder on us). The single table on its own would be MAJOR |

---

## 3. Critical path

```
OWNER ──► domain (E-001) ──► staging on the real domain ──► A2: four golden journeys ──► GO/NO-GO
OWNER ──► SMS key rotation + one real send ──► A4 ──► journey 1 (real OTP) ─────────────┘

TEAM ──► P0-0  S-04 demo trap fix ──────────────────────────────────────────────────────┐
TEAM ──► P0-1  prove candidate 99065a7 ──(Red Team 089 verdict · M-09 fixed · api suite on a
               fresh DB · e2e reds named)──► CEO merges ──► main e2e covers booking again (S-01),
               088/089 (S-02), M-02, DS-011/012
                        └──► P1-5 merge rezv-36-backend (M-04: 9 fixes, B1 tenant boundary)
                                   └──► rezv-75 isolation matrix measured on the tree that ships
TEAM ──► P0-2  triggers for the S-03 table set (090+) ──► P0-3 FP-009 gate covering all of them
TEAM ──► P1    S-05 encryption at rest (key in env ⇒ deploy config ⇒ touches A2) · M-01 staff hijack
```

- **The single thing whose absence blocks the most:** **the domain (S-06).** No amount of team work passes A2 without it, and it is the only blocker the team cannot route around. It has been open since 2026-09-08.
- **The team-controlled single thing:** **P0-1, the merge-candidate proof.** Five findings land with it (S-01, S-02, M-02, m-04, the DS work), every later merge queues behind it, and until it lands every auditor is measuring a tree that will not ship.

---

## 4. What each session owes

"Rounds waited" counts coordination passes since the ask was made. Today's asks are round 1.

| Session | Owes | Rounds |
|---|---|---|
| Implementation `rezv-85 [27467f]` | P0-0 S-04 · P0-1 candidate proof + M-09 · P0-2 S-03 triggers · P0-3 FP-009 gate (widened) · P1-4 M-01 · P1-5 M-04 + m-03 · P1 S-05 · P2 M-03, M-05, M-07, m-02 · P3 m-06, m-07, m-10 | 1 |
| Red Team `rezv-31 [e816f0]` | ~~RT-24/RT-25 on 089~~ **delivered: HOLDS for `points_ledger`** · ~~D-13 guard~~ **delivered: MINOR, tightened** · gift-card double-spend **delivered: HOLDS under a live race** (runtime claim, not re-run by me) · still owed: 090 when pushed · M-09 attack · payment-callback replay · coupon/gift/points double-spend · attacks #6/#7 on `rezv-75`'s harness · RT-26 #4 (deposit double-charge) and #5 (leaked `reservation_code` cashback), inherited from the old Red Team | 1 |
| Full-Stack `rezv-75 [dd9771]` | batch 2 isolation matrix (167 routes currently REAL-STATIC, counted as blocking) · BE-007 row verdicts · journeys · re-verify S-04 after the fix | 1 |
| Sync `rezv-7a [1f856a]` | correct §2.3 (C-9) and §2.1 (C-8), then done | 1 |
| Marketing `rezv-c6 [897f2f]` | `docs/marketing/BUSINESS-MODEL.md` first; pricing numbers stay marked placeholder | 1 |
| Feature Verification `rezv-1b` | which folder and model tier; its lane is limited to missing-capability proposals | 1 |
| Old Launch Engineer `2bffd0d9` · Backend `edb5f154` · Designer `6c9efba5` · Red Team `c07123f9` | nothing. Not running; their queues moved to the rows above | — |

## 5. What the CEO owes (my debt, not anyone else's)

| Item | Since | Rounds |
|---|---|---|
| M-08 abuse-flag clearing: read the 09-06 package and decide, or send the owner a package if it changes an ADR decision | 2026-09-06 | ≥3 |
| m-08 rule on Scout proposals 001–007 | 09-05 … 09-11 | ≥3 |
| m-09 review Deputy ORDER-003/004 | 2026-09-11 | ≥2 |
| Merge the candidate once its gate passes; then `rezv-36-backend` | today | 1 |

## 6. What the owner blocks

Stated without softening: every row below stops a launch gate that nobody else can open.

| Row | Blocks | Since | Rounds |
|---|---|---|---|
| **Buy a domain** (E-001) | S-06 → A2 → GO | 2026-09-08 | **≥4** (the Founder desk counted 3+ on 09-13; this pass adds one) |
| **SMS: rotate the Melipayamak key and do one real send, raw response kept** | S-07 → A4 → journey 1 | ≤2026-09-13 | not counted before today; counted from 1 now |
| **Test the four surfaces on your own phone** | GO | ≤2026-09-13 | not counted before today; counted from 1 now |
| ~~Pricing 18/34/65M toman: real or placeholder?~~ **DECIDED by the owner in `rezv-c6` (09-17): ۱۸M / ۳۳M / ۶۰M, «کمی زیرِ SmartX، هم‌تراز»** (supersedes the A/B package). Still owner-held: VAT and price-lock terms. Earlier: placeholders; change them per the marketing research → `rezv-c6` writes `PRICING.md` with evidence → CEO verifies → Implementation edits `site-content.json` plus the M-12 honesty copy → the owner sees the final numbers before his push to main | Marketing business model | 09-16 | closed as a question |
| **Register the Melipayamak pattern for `booking_late`** (and send its bodyId). Without it, automatic no-show stays off by design (D-20 a) | M-13 auto no-show | today | 1 |
| **Export the provider template bodies** for `booking_noshow`, rejected, cancelled and expired. The SMS sentences live only in the Melipayamak panel; the repo holds only the label tokens (`lifecycle.ts:69`) | M-15 wording check (D-22); S-07 from the other end | today | 1 |
| ~~**Deposits:** one platform merchant with no payout path (M-11)~~ **DECIDED by the owner (09-17): deposits OFF at launch.** `paymentEnabled @default(false)` (`schema.prisma:184`); no copy may promise online deposits. M-11 stays open for the day it is re-decided | turning on `paymentEnabled` | 09-16 | decided |
| Brand name | — | — | not asked, per the owner's instruction |
| FP-008 revaluation of old point balances | spending points (feature is OFF) | 2026-09-16 | parked by the owner, not asked |
| **Create an empty PRIVATE GitHub repo** (suggested name `Rezv-security`, no README) and send its URL. Owner ruling 09-17: attack files leave the public repo. The CEO then copies `docs/audit/redteam/**` from main and the 5 `redteam/*` branches, verifies the copy, replaces the public files with tombstones, and deletes the public `redteam/*` branches. **Residual:** GitHub history and old SHAs still expose what's already public; only making `Rezv` private removes that | attack material in a public repo | 09-17 | 1 |

GO/NO-GO: **the scorecard says NO-GO** (S-01 … S-07 open). No GO is requested.
