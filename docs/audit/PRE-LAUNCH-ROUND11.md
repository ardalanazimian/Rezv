# ممیزیِ پیش از لانچ — دورِ یازدهم (بستنِ دور)

> ۲۰۲۶-۰۸-۲۹ · **merge شد**: `c0327c2` روی `main` · PR #79 بسته · شاخه حذف شد
> دورهای پیشین: `PRE-LAUNCH-AUDIT.md` · `-SUMMARY.md` · `-ROUND3.md` تا `-ROUND10.md`
> · `GATE-FALSIFIABILITY.md`

---

## ۱. S2 — پاسخِ غیرقابلِ‌پارس هرگز «ارسال شد» نیست ✅

**حکم: سوراخِ واقعی.**

`sendSmsNow` بدنه‌ی پاسخ را با `res.json().catch(() => null)` می‌خواند، پس هر
پاسخِ غیر-JSON به `null` می‌رسد. جهشِ `meliAccepted` — `if (!d) return false`
به `return true` — از **هر ۷ فایلِ تستی که ماژولِ sms را لمس می‌کنند** سالم رد
می‌شد، چون همه JSONِ **معتبر** stub می‌کنند و آن شاخه اصلاً اجرا نمی‌شود.

`tests/sms-unparsable-response.test.mts` سه شکلِ واقعیِ بدنه‌ی خراب می‌سازد و
از رویِ دفترِ ثبت (شمارنده‌های `metrics`) قضاوت می‌کند:

| ورودی | ادعا |
|---|---|
| بدنه‌ی کاملاً خالی | `smsSent` تکان نمی‌خورد · `smsFailed{reason=rejected}` +۱ |
| HTMLِ صفحه‌ی خطای پروکسی (۵۰۲) | همان |
| JSONِ ناقص | همان |
| **کنترلِ مثبت** — پاسخِ معتبرِ `RetStatus: 1` | `smsSent` +۱ و `smsFailed` بدونِ تغییر |

کنترلِ مثبت عمدی است: بدونش، ترانسپورتی که **همیشه** رد کند هم سه ادعای بالا
را پاس می‌کرد.

**اثبات:** تزریقِ دوباره‌ی جهش → `exit=1` با **۸ تستِ قرمز**؛ پس از بازگردانی
`exit=0`.

---

## ۲. P2002 → ۴۰۹ — کد به وعده‌ی کامنت رسید ✅

### یک تصحیحِ گزارشِ دورِ دهم

نوشتم «هیچ ترجمه‌ی P2002 در کد نیست». **دقیق‌تر:** ترجمه وجود داشت، ولی فقط
برای شماره‌ی مالک (`isOwnerPhoneUniqueViolation`) — نه برای نامِ کاربری.
کامنتِ `provisioning.ts` برای نیمی از موضوع درست بود.

`isUsernameUniqueViolation` اضافه شد (هم‌الگویِ خواهرش، رویِ ایندکسِ
`staff_username_key` از مهاجرتِ ۰۷۴) و به همان `catch` وصل شد.

`tests/provision-username-conflict.integration.test.mts` هر دو مسیر را می‌بندد:

| مسیر | نتیجه |
|---|---|
| ترتیبی (پیش‌بررسی) | ۴۰۹ · `details.reason = username_taken` · بدونِ نشتِ متنِ Prisma |
| **مسابقه‌ی همزمان** | دقیقاً یک برنده و یک بازنده؛ بازنده هم ۴۰۹ می‌گیرد نه P2002ِ خام |
| کنترلِ مثبت | دو نامِ کاربریِ متفاوت هر دو پذیرفته می‌شوند |

> **نکته‌ی روشی:** ادعای اولم `e.reason` بود و افتاد — قرارداد واقعی
> `e.details.reason` است (`Err.conflict` مقدار را داخلِ `details` می‌گذارد).
> ادعا غلط بود، نه کد.

### نتیجه‌ی ظریفِ جهش‌آزمایی پس از رفع

| جهش | پیش از رفع | پس از رفع |
|---|---|---|
| برداشتنِ پیش‌بررسیِ `if (taken)` | SURVIVED | **همچنان SURVIVED** |
| برداشتنِ ترجمه‌ی `isUsernameUniqueViolation` | — | **CAUGHT** (exit=1، ۴ قرمز) |

و این درست است: حالا هر دو مسیر همان ۴۰۹ را می‌دهند، پس پیش‌بررسی **واقعاً**
یک بهینه‌سازیِ افزونه است، نه نگهدارنده‌ی قرارداد. گارد از لایه‌ای که «گاهی»
اجرا می‌شد به لایه‌ای که **همیشه** اجرا می‌شود منتقل شد — و تست همان لایه را
می‌سنجد.

---

## ۳. V2 — الگویِ slug ✅

**حکم: سوراخِ واقعی، نه افزونگی.**

### لنگر یکتا شد

خطِ `SLUG_RE.test` دو بار در فایل تکرار شده (`provisionBusiness` و
`createBranch`). لنگر حالا در **زمانِ اجرا** از خطِ SLUG_RE به‌همراهِ خطِ بعدی
ساخته می‌شود (`slugTaken` در برابرِ `taken`) که آن دو را از هم جدا می‌کند.

نتیجه: **SURVIVED** — و طبقِ قاعده اول دنبالِ دلیل گشتم:

| لایه‌ی احتمالی | یافته |
|---|---|
| CHECK constraint رویِ slug در DB | **وجود ندارد** (فقط یکتایی) |
| شِیمِ routeِ ادمین (`admin/restaurants/route.ts:68`) | فقط `.trim().max(40)` — **نه charset، نه حداقلِ طول** |

یعنی با آن جهش `BadSlug`، `ab`، `bad slug`، `../etc` پذیرفته می‌شدند و در DB
می‌نشستند. slug در URLِ عمومیِ رستوران می‌آید، پس این فقط زشتی نیست.

`tests/provision-slug-validation.integration.test.mts` هر بُعدِ
`/^[a-z0-9-]{3,40}$/` را **جدا** می‌شکند — حروفِ بزرگ · کوتاه‌تر از ۳ · فاصله ·
کاراکترِ مسیر · زیرخط · حروفِ غیرلاتین — تا جهشِ **جزئی** هم گرفته شود، نه فقط
برداشتنِ کاملِ گارد. به‌علاوه‌ی کنترلِ مثبتی که ثابت می‌کند slugِ معتبر ذخیره
می‌شود و بی‌صدا بازنویسی نمی‌شود.

**اثبات:** تزریقِ جهش → `exit=1` با **۱۴ تستِ قرمز**؛ بازگردانی → `exit=0`.

> **نکته‌ی روشی دوم:** ادعای اولم `status: 400` بود و هر شش افتادند در حالی که
> کنترلِ مثبت سبز بود — نشانه‌ی روشنِ «ادعا غلط است، نه setup».
> `Err.validation` وضعیتِ **۴۲۲** می‌دهد.

---

## ۴. merge ✅

### توکنِ `gh` منقضی بود

```
HTTP 401: Requires authentication
X Failed to log in to github.com account — The token in default is invalid.
```

پس merge از طریقِ API ممکن نبود. محلی با `--no-ff` انجام شد که **دقیقاً همان
merge commit** را می‌سازد (نه squash):

```
c0327c2 parents: 768f7b4 656c6ac
```

دو والد، و هر ۲۷ commitِ شاخه دست‌نخورده در تاریخچه — تاریخچه سند است.

### CI روی main پس از merge

```
build ✓  test ✓  design-system ✓  e2e ✓  image-build ✓  standalone ✓
security ✓  schema-drift ✓  landing ✓  seo ✓  boot-path ✓
base-freshness — skipped
```

**۱۱ سبز + ۱ skip.** `base-freshness` شرطِ `if: github.event_name ==
'pull_request'` دارد (`ci.yml:481`) — رویِ pushِ مستقیم به main اصلاً موضوعیت
ندارد. این **skipِ طراحی‌شده** است، نه شکست.

### حذفِ شاخه

پیش از حذف تأیید شد: `git rev-list audit/pre-launch ^main` → **۰ commit**.
سپس از remote و local حذف شد.

---

## ۵. مرورگرهای Playwright ✅ — «برگشت‌ناپذیر» برگشت‌پذیر شد

```
PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright
npx playwright install chromium      →  MIRROR_EXIT=0
```

`chromium-1234`، `chromium_headless_shell-1234`، `ffmpeg-1011`، `winldd-1007`
نصب شدند. E2E محلی اجرا شد: **۱۷۸ passed، exit=0**.

آنچه در دورِ دهم «برگشت‌ناپذیر» ثبت شده بود دیگر برقرار نیست. راهِ حل یک خط
env است، نه VPN.

---

## دو دامی که در همین دور **خودم** ساختم

`_all.runner.mts` همه‌ی فایل‌ها را در **یک پروسه** import می‌کند، پس هوکِ
سطحِ فایل عملاً **سراسری** می‌شود:

۱. `afterEach`ِ `sms-transport-failclosed` اعتبارنامه‌ی ملی‌پیامک را پاک
   می‌کند. چون سراسری است، بعد از تست‌های فایلِ تازه هم اجرا می‌شد — و از
   تستِ دوم به بعد `sendSmsNow` پیش از `fetch` برمی‌گشت و شمارنده‌ها تکان
   نمی‌خوردند. تست **بی‌صدا بی‌معنا** می‌شد؛ دقیقاً قاعده‌ی ۵ِ CLAUDE.md.

۲. رفعِ اولم — یک `beforeEach` سطحِ فایل که `globalThis.fetch` را بازمی‌گرداند
   — خودش سراسری شد و stubِ `sms-queue-fallback-balance` را پاک کرد و آن تست
   شکست. **در کلِ سوئیت دیده شد، نه در اجرای تکیِ فایل.**

حالا همه‌ی هوک‌ها **داخلِ** `describe` اسکوپ شده‌اند و هر دو دلیل در خودِ فایل
ثبت است.

---

## راستی‌آزمایی

| گیت | نتیجه |
|---|---|
| CI روی `656c6ac` (شاخه) | **۱۲ / ۱۲ سبز** |
| CI روی `c0327c2` (main، پس از merge) | **۱۱ سبز + ۱ skipِ طراحی‌شده** |
| `npm test` | **۱۴۷۸ / ۱۴۷۸** |
| `tsc --noEmit` · `npm run lint` | exit 0 |
| پنج گاردِ ابزاری | همه exit 0 |
| E2E محلی (desktop-chrome) | ۱۷۸ passed، exit 0 |
| سه جهشِ تازه × تزریقِ دوباره | ۸ / ۴ / ۱۴ قرمز، سپس exit 0 |

---

## جمع‌بندیِ یازده دور

| سنجه | آغاز | پایان |
|---|---|---|
| jobهای CI | ۱۱ | **۱۲** (`image-build`) |
| تست‌های بک‌اند | ۱۴۲۸ | **۱۴۷۸** |
| خطِ پایه‌ی sinkِ `unsafe` / `review` | ۶۶ / ۲۲ | **۶۵ / ۲۰** |
| sinkهای دارای درجِ برهنه | ۲۰ | **۱** (SVGِ QR، عمدی) |
| ماژول‌های نمونه‌گیری‌شده‌ی جهش | ۰ | **۹** (۳۲ جهش) |
| قواعدِ ثبت‌شده در CLAUDE.md | ۳ | **۶** |

**بزرگ‌ترین یافته:** ایمیجِ تولید دو ماه ساخته نمی‌شد و هیچ‌کس نمی‌فهمید، چون
هیچ jobی نمی‌ساختش.

**درسِ روشیِ تکرارشونده:** زنده‌ماندنِ جهش همیشه یعنی «سوراخ» نیست. سه بار
(R4 · V1 · و V2 که برعکسش بود) تفاوتِ «افزونگی» و «سوراخ» فقط با گشتنِ دنبالِ
لایه‌ی دوم روشن شد — نه با پذیرفتنِ نتیجه‌ی خام.

---

## آنچه هنوز راستی‌آزمایی نشده

۱. **شش مرحله‌ی دودِ دستی** — بسته بر اعتبارنامه‌ی ملی‌پیامک و شماره‌ی دریافتِ
   کد. تنها بخشی که هیچ تحلیلِ ایستایی جایگزینش نمی‌شود.
۲. **اجرای واقعیِ استکِ رصدپذیری** رویِ سرور با `docker-compose.observability.yml`.
۳. **هشت فیچر از شانزده‌تای فازِ ۵** و **شکلِ فیلدِ ~۲۵ endpointِ پنل**.
۴. **۶۵ sinkِ `unsafe` و ۲۰ `review`** — حالا همه پشتِ `esc()`اند و ratchet
   قفلشان کرده، ولی تریاژِ تک‌به‌تکشان انجام نشده.
۵. **توکنِ `gh`** — منقضی است؛ تا تمدید نشود عملیاتِ PR از CLI ممکن نیست.
