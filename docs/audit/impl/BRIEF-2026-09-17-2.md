# Implementation Team — BRIEF 2026-09-17 (batch 2)

- **Date:** 2026-09-17 ~00:45 UTC · **Session:** Implementation Team `rezv-85 [27467f]` (sessionId `61edcb5d`)
- **Target:** CEO `rezv-87 [09dbab]`
- **What it needs from its reader:** send `553ecf4` to the Red Team (090 + gates), and rule on P1-4's shape
  when the package arrives.

## Recommendation first

Merge order stays: candidate `99065a7` + `8cdc6e9` (BE-01) → `b496991` (FE-06, after rezv-75) →
`553ecf4` (090 + gates, after the Red Team). 090 is the change with the widest blast radius; its two
deliberate side effects (SET NULL parents undeletable; the documented partition DROP orphans ledger
references) are measured and written down, not discovered later.

## Shipped this batch (submitted, not closed)

| ID | What | Branch @ sha | Proof headline |
|---|---|---|---|
| BE-02 | Migration 090: TRUNCATE/UPDATE/DELETE guards on 8 ledgers and logs; D-16; retention-matched guards | `impl/rezv-85-090-append-only @ 247d103` + `eeff902` | RED 27/32 → GREEN 32/32; idempotent ×2; full api suite 1891/1891 on a fresh clone |
| BE-03 | FP-009 §5/§6 gates over 9 tables + 2 FKs; the §6 guard widened and put into CI | same branch `@ 553ecf4` | 6 DB mutations → exit 1; end-to-end drift run DRIFT_EXIT=0; 3 code injections → exit 1 |

Earlier and still waiting: BE-01 `8cdc6e9` (accepted as submitted), FE-06 `b496991` (accepted as submitted, merge
waits on rezv-75).

## What this batch taught (and changed)

- A module-level `beforeEach` in one test file (fraud) fires before every test in the one-process runner.
  One trigger hit turned into 1829 red tests. Per-file runs on fresh clones found the real 13; the class
  is BE-14.
- The Bash tool turned `\b` into a backspace byte in a guard pattern. The guard's own self-test caught it
  (exit 2), which is exactly what the founder's rule about regexes and file tools is for.
- A haiku sweep said "0 test cleanup paths" on the ledger tables; `git grep` found 33.

## Blocked, and on whom

- SEO-B1: domain (owner).
- Linux evidence for every `impl/*` branch: CI does not run on them, and there is no `gh`.

## Delegation

Haiku (Explore): the ledger write-path sweep and the demo-fallback sweep. Every row was re-measured before
it became a decision. Root cause, the SET NULL decision, the partition probe, the rewrites and all
verification stayed in this session (Opus).

## Next

BE-14 guard (after 090 lands) → P1-4 as a **package** (red repro + options + recommendation: first login
needs a consent step for unproven memberships, which changes SPEC-B's auto-accept) → P1-5 via the rescue
merge → S-05 → m-14.

## One line for the founder to give the CEO

> rezv-85: ۰۹۰ و گیت‌هایش روی `553ecf4` تحویل شد — ۱۸۹۱/۱۸۹۱ تست روی DBِ تازه، شش جهشِ گیت قرمز، drift سبز؛ Red Team حالا حمله کند.
