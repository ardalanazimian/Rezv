# Implementation Team — BRIEF 2026-09-17 (batch 1)

- **Date:** 2026-09-17 · **Session:** Implementation Team `rezv-85 [27467f]` (sessionId `61edcb5d`)
- **Target:** CEO `rezv-87 [09dbab]`
- **What it needs from its reader:** send both submissions to the Red Team; take the merge candidate
  to the owner for the `main` push (neither my session nor the CEO's can push `main`).

## Recommendation first

The merge candidate is proven on Windows: api 1858/1858 on a fresh DB, e2e 578 passed / 0 failed /
2 named flakes, and all 18 node guards green with BE-01 applied. **Merge it**; it ends main's 6-day
red e2e. FE-06 (fake restaurants on a server error) sits on top of it and should follow once rezv-75
re-verifies.

## Shipped (submitted, not closed)

| ID | What | Branch @ sha | Proof |
|---|---|---|---|
| BE-01 | FK-RESTRICT test accepted any failure. The matcher is now `P2003` on `points_ledger_user_id_fkey`, which un-reds CI's rejects-ratchet | `impl/rezv-85-p0-1-candidate @ 8cdc6e9` | gate 1→0; wrong-reason mutation red on the new matcher, green on the old; 18/18 guards |
| FE-06 | HTTP 500 showed six `[DEMO]` restaurants as live; boot painted samples before the API answered; events showed samples offline | `impl/rezv-85-p0-0-demo-trap @ b496991` | RED 15 failed → GREEN 33 passed; 3 mutations each red; full e2e 603/0 failed/4 flaky |

## In progress

Migration 090, append-only triggers on 8 tables, per CEO rulings D-16 (`sms_transactions` FK → RESTRICT)
and the 90-day floor guard on `platform_events.ingested_at`. A contradiction with the ruling's column
name was raised and accepted.

## Blocked, and on whom

- SEO-B1 (outside crawl, rich results) needs a domain, which is on the owner (`E-001`).
- Linux proof of anything on `impl/*`: CI does not run on those pushes, and there is no `gh` to open a
  PR. It needs a merge or a PR opened by someone who can.

## UNKNOWN (never passing)

23 rows in `UNKNOWN.md`. Most important: main's own fresh-DB suite numbers are **not** measured. My
baseline run was invalidated by my own mistake (I edited the setup script while `sh` was executing it)
and discarded. Design is almost entirely unmeasured.

## Contradictions raised (source won)

1. `audit/launch-hardening`, named as the PR base, is 321 commits behind `main`, so work was based on
   the candidate instead.
2. "Add the cancelled statuses to the active set" would make cancelled bookings block tables; the
   right invariant is "every status classified once".
3. "Samples only when the backend is unreachable" is too loose: production network loss is a broken
   path, and D-19 adopted `file://`-only.
4. The `platform_events` floor goes on `ingested_at`; there is no `created_at` column.
5. "Business panel has zero tests" is wrong: there are 17 e2e specs.
6. "shared/js drift" did not reproduce: the sync check exits 0.

## Delegation

Mechanical sweeps went to **haiku** (5 Explore agents: api routes/auth, frontend demo/XSS/contract,
SEO structure, design mechanics, ledger write paths). Every finding they fed was re-measured before it
became a row. One haiku result was **wrong**: it reported 0 test-cleanup deletes on the ledger tables,
and there are 33. Root cause, concurrency, fix design and every verification stayed in this session
(Opus).

## One line for the founder to give the CEO

> rezv-85: کاندیدای ادغام روی ویندوز اثبات شد (api ۱۸۵۸/۱۸۵۸، e2e بدونِ شکست) و BE-01 و B-1 روی `8cdc6e9` و `b496991` تحویل شدند — ادغامِ `main` با مالک است، ۰۹۰ در کار است.
