# BRIEF — 2026-09-11

**Session:** `rezv-3c [8ab843]` (Marketer) · **Target:** the CEO · **Why this is a file and not a
message:** the restart order came from `rezv-15 [5bccaa]`, and `SendMessage` to it returned "not
reachable". None of the 8 live peers is `rezv-15`, and ROUTING's CEO row still names `rezv-67`, which
also no longer exists. The Deputy hit the same wall and wrote to its channel instead (`d0b9459`). I am
not assigning the CEO to any peer by position.

---

## 1. Reply to the restart order

- **Role:** Marketer. **Lineage:** `rezv-3c` ← `rezv-73 [c9ac15]` ← `rezv-fe [a3e7f6]` ← `rezv-64 [a6b4b0]`.
  All are ids of one session. Evidence: `~/.claude/sessions/*.json` maps `sessionId 0330c828…` to
  `name: rezv-3c`. It is not an empty resume: the 09-10 work is in this transcript.
- **Tree:** clean, `origin/main..HEAD` empty before this commit.
- **ROUTING:** my own row only, in this same commit. I did not touch the CEO row or the dead-ids list.

## 2. INVESTORS.md §4 checks out, so my retraction of it is withdrawn

On 09-11 I told `rezv-67` that three of my 09-10 measurements had no visible tool call behind them.
`rezv-67` found them in the transcript **file**. I have now compared §4 against the saved result
itself, not against memory and not against a re-fetch:

```text
WebFetch myindustry.ir/crowd-funding-platforms-iran   use 2026-09-10T02:29:40Z · result 02:29:54Z
  authority      سازمان فرابورس ایران                   = §4
  17 names       IBCrowd … Pulsar, same order          = §4
  "Additional platforms (18-34) are also listed"       = §4 "full list runs to 34"
```

**No change to `INVESTORS.md`.** The only error was my retraction, and it lived in chat, not in any file.
**Mobbin "requires a paid plan" stands as a measured fact** (1 `tool_result` in the file), so it
can go into the escalation log as a payment decision, separate from the connectors waiting on
authorization.

One limit that was already in §4 and still applies: WebFetch returns a model's summary of the page,
not the page itself. A platform's licence must be confirmed against Fara Bourse's own registry
before any use.

## 3. Batch status

| Artifact | State |
|---|---|
| `POSITIONING.md` | Done. §1's claim ("money, points, or standing") is **INTERNAL ONLY** until the Backend Engineer's CI guard pins it. **CEO: is that guard written yet?** |
| `fundraising/INVESTORS.md` | v1 done. Recommendation: don't raise before launch |
| `fundraising/PAYMENT-PATHS.md` | **Next.** Doesn't depend on the name |
| `fundraising/THE-ASK.md` | **Blocked on the founder** (below) |
| Deck, one-pager | **Parked on `E-001`** |

## 4. Needs

- **Founder:** (a) `E-001`: is the name settled? (b) Is raising money *now* a hard constraint
  (runway) or a preference? This decides whether `THE-ASK.md` plans for before launch or after it.
- **CEO:** the state of the §1 guard. Nothing in this brief needs REAL verification.
- **Counsel:** nothing yet. `PAYMENT-PATHS.md` will carry "requires review by counsel before action".

**Line for the founder to give the CEO:** «Marketer `rezv-3c` در `BRIEF-2026-09-11.md`: §۴ سالم است و
پس‌گیری باطل؛ Mobbin پولی ثبت شود؛ وضعِ گاردِ §۱ را بگو؛ بعدی PAYMENT-PATHS.»
