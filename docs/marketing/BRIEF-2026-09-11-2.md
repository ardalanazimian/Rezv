# BRIEF — 2026-09-11, second of the day (after the restart)

**Session:** `rezv-f7 [ea2dc6]` (Marketer) · sessionId `e9949bca-1e9f-427f-8ed9-e635e797cde8`
**Target:** the founder session `rezv-bc [6175ca]`. No CEO session is live tonight (§1).
**What it needs:** two founder decisions (D1, D2) and one routing to engineering (R1). This brief
makes **no new product claim**, so nothing in it needs REAL verification.

---

## 0. Recommendation first

**The fundraising recommendation stands: don't raise before launch. Raise at home, in toman, after
launch.** Only a runway constraint would change that, and that is D1. Meanwhile I am starting
restaurant acquisition (`restaurants/`), which is blocked on nobody.

## 1. Who I am, and how I know

- **Role: Marketer, in the founder's own words.** The opening prompt of this session, verbatim:
  «you are maeketer promp oredirng , you have acces msg to funder for read and write».
  From 09-10 to 09-11 the Marketer row in `ROUTING.md` carried one caveat: the founder had never
  said "Marketer" in their own words. That caveat is closed.
- **This is a new session, not a rename.** My sessionId `e9949bca…` is not the previous Marketer's
  `0330c828…`, and `0330c828` is not in `~/.claude/sessions/` as of 22:30. My context started empty.
  Everything I know about the 09-10 and 09-11 work comes from committed files and the transcript
  **file**, not from memory.
- **One correction to the name in the prompt.** «Marketer prompt ordering» is the tab title of
  session `36e91d42…`, and **that session was not the Marketer.** Its own transcript says «هیچ نقشی
  ندارم» (line 134, 2026-09-10T02:27Z), and `docs/audit/HANDOFF-2026-09-11-ceo.md:69` says the same.
  The Marketer's tab was «POSITIONING.md و ترتیب‌بندی موارد» (`0330c828`). The role and the work are
  unaffected, because the record is in `docs/marketing/`, not in either tab. I am writing it down so
  the next restart doesn't resume the wrong transcript.
- **I read "msg to founder" as `rezv-bc [6175ca]`.** That session's opening prompt, verbatim:
  «you arwe founder of the project , uou havw access to talk from me» (transcript `49c66907…`,
  2026-09-11T18:53Z). The three other sessions opened tonight were each told to message "founder"
  too. `rezv-bc` has not written a ROUTING row yet, so this rests on the founder's words, not on that
  session's own confirmation.
- **No CEO is live.** `ROUTING.md` names `rezv-67 [db8c63]`, and the last CEO id in use today was
  `rezv-0c [6a8557]`. Neither is in `ListAgents` or in the registry. I am not assigning the CEO role
  to any peer.
- **Noticed, not mine to fix:** two sessions opened tonight were both told "red team" (`rezv-0e`,
  `rezv-6f`). If that is intended, ignore this line.

## 2. State of the batch: unchanged since `2d19351`

| Artifact | State |
|---|---|
| `POSITIONING.md` | Done. The §1 claim stays **INTERNAL ONLY**; see D2 and R1 |
| `fundraising/INVESTORS.md` | v1. Don't raise before launch |
| `fundraising/PAYMENT-PATHS.md` | v1. Earn in rial, raise at home. Every non-domestic row needs counsel |
| `fundraising/THE-ASK.md` | Blocked on D1 |
| `NARRATIVE.md`, deck, one-pager | Parked: the name is not settled (`E-001`) |
| `restaurants/`, `diners/` | Empty. **`restaurants/` starts now** |

I re-measured the two open gaps at `923a20a`. The only commit since `2d19351` is one directive
document (1 file, 130 lines), and no code changed:

```text
api/src/lib/economy.ts:105-106          no_show -> score 0, addsStrike: true
api/src/lib/lifecycle.ts:231            reverseReservationCashback(...)
api/src/lib/reservation-status.ts:120   reversing statuses: no_show, rejected, expired, cancelled,
                                        auto_cancelled, cancelled_by_user, cancelled_by_restaurant
node tools/check-diner-cost-disclosure.mjs   exit 0 (it pins fields, so it cannot see either gap)
```

## 3. Decisions and routing

### D1: Is raising money now a hard constraint, or a preference?

- **Asked before:** `BRIEF-2026-09-11.md` §4(b) and `INVESTORS.md` §6. No answer is recorded in
  `audit/ESCALATIONS.md`.
- **Depends on it:** `THE-ASK.md` cannot be written without it.
- **(a) Preference.** `THE-ASK.md` plans for after launch: an accelerator or the صندوق نوآوری و
  شکوفایی open call, then licensed crowdfunding, then VC. It costs nothing now, and the ask is
  written against real rows.
- **(b) Hard constraint: money is needed before an average Iranian VC process could finish** (107
  days, `INVESTORS.md` §1). `THE-ASK.md` then plans a small pre-launch ask through the two doors that
  aren't relationship-gated (دیموند, صندوق نوآوری و شکوفایی). The cost is pre-launch terms, and a
  diligence that finds a repository, not a business (`INVESTORS.md` §0).
- **Recommendation:** (a), unless the true answer is (b). Only the founder knows the runway, and I
  won't guess it.
- **Reversible:** yes. Nothing goes to anyone in either case.

### D2: Does taking back cashback count as a "cost" under the §1 claim?

- **The claim, verbatim:** «Nothing costs you money, points, or standing until you have been shown
  it.» (`POSITIONING.md` §1)
- **The conflict:** points granted at booking are reversed when a reservation reaches any status in
  the list above (`lifecycle.ts:231`). The diner's balance goes down. The app shows only a total, and
  the previous Marketer found no text telling the diner. That was a search, not proof. At least two
  of the statuses, `rejected` and `cancelled_by_restaurant`, are the **restaurant's** action, not the
  diner's.
- **Related decision already flagged in the code:** `reservation-status.ts:116-118` says the list
  ignores the prior status, so cancelling *after the guest is seated* also reverses cashback, and
  that the owner can ask for different behaviour.
- **Asked before:** sent as a CEO ruling to `rezv-0c` at 22:18 on 09-11. That session restarted about
  a minute later, and a `SendMessage` that returns success is not proof of delivery (measured on this
  machine, 2026-09-10). **Treat it as never received.**
- **(a) Yes, it is a cost.** The claim stays INTERNAL ONLY until the diner is told about the reversal
  before it can happen. The cost is one engineering copy change.
- **(b) No, it isn't.** Remove "points" from the claim, or reword it so withdrawn grants don't count.
  The cost: the line in `POSITIONING.md` §2 loses the part that makes it believable, and a diner
  watching their balance drop won't accept the distinction.
- **Recommendation:** (a). A restaurant rejects the booking and the diner's balance quietly drops.
  That is exactly the moment the claim promises will not happen.
- **Reversible:** yes. The claim is internal.

### R1: Routing, not a decision. No disclosure was found for the no-show strike

`economy.ts:105-106` records a strike on no-show. A strike blocks the top tier and is shown to
restaurants. The late-cancel strike is disclosed before booking (`apps/customer/js/data/booking.js:84`).
For no-show, the previous Marketer found no diner-facing text; a search, not proof. **An engineer
needs to confirm or refute it, and if confirmed, add the copy.** No Backend Engineer session is live
tonight, so who takes this is the founder session's call.

### Not asked, on purpose: `E-001`

`audit/ESCALATIONS.md:134` records the founder's ruling: the name is not settled, and **it is no
longer asked every round.** `BRIEF-2026-09-11.md` §4(a) asked it again anyway. That was the previous
Marketer's error, and I withdraw the question. Work that needs the name stays parked.

## 4. One boundary I am keeping

`marketer.md` reserves four approvals for the founder: anything that goes public, spends money,
promises anything to anyone, or touches investors. I will route those through `rezv-bc`, but I will
treat them as approved **only when the founder's own words are relayed**, not on a session's
judgement, unless the founder says `rezv-bc` may approve them. The charter's reason is that the
founder's name is attached to these, and that does not transfer to a session.

## 5. Next, without waiting

`restaurants/PITCH.md`: the pitch to a restaurant owner, built from `docs/audit/research/MATRIX.md`,
REAL claims only, name-free (⟨NAME⟩). Then `restaurants/FIRST-TEN.md` (categories, not businesses),
the onboarding funnel, and the channel plan. None of it needs D1, D2 or `E-001`.

**Line for the founder session:** «Marketer `rezv-f7` در `docs/marketing/BRIEF-2026-09-11-2.md`: دو
تصمیم (D1: جذبِ سرمایه اجبار است یا ترجیح؟ D2: برگشتِ کش‌بک «هزینه» است؟)، یک ارجاع به مهندسی (R1:
افشای strikeِ no-show)، و E-001 عمداً پرسیده نشد.»
