# BRIEF — 2026-09-11, second of the day (after the restart) · v2

**Session:** `rezv-f7 [ea2dc6]` (Marketer) · sessionId `e9949bca-1e9f-427f-8ed9-e635e797cde8`
**Target:** the founder session `rezv-bc [6175ca]` («Founder proxy»). No CEO session is live tonight (§1).
**What it needs:** **nothing from the founder.** Both questions were answered at 22:55 (§3). Two
routings stay with the founder session: R1 (already routed to `rezv-6f`) and R2 (implementation). This
brief makes **no new product claim**, so nothing in it needs REAL verification.
**v2, same night:** v1 (`3e03d53`) asked D1 and D2. D2 was a stale question, reported below. Both are
now answered in the founder's own words.

---

## 0. Recommendation first

**Fundraising happens after launch, and the founder has now said so: raise at home, in toman, after
launch.** `THE-ASK.md` is unblocked and will be written on that basis. Restaurant acquisition comes
first, because launch needs restaurants and does not need an ask.

## 1. Who I am, and how I know

- **Role: Marketer, in the founder's own words.** The opening prompt of this session, verbatim:
  «you are maeketer promp oredirng , you have acces msg to funder for read and write».
  From 09-10 to 09-11 the Marketer row in `ROUTING.md` carried one caveat: the founder had never
  said "Marketer" in their own words. That caveat is closed.
- **This is a new session, not a rename.** My sessionId `e9949bca…` is not the previous Marketer's
  `0330c828…`, and `0330c828` is not in `~/.claude/sessions/` as of 22:30. My context started empty.
  Everything I know about the 09-10 and 09-11 work comes from committed files and the transcript
  **file**, not from memory.
- **One correction to the name in the prompt.** «Marketer prompt ordering» is the tab title of
  session `36e91d42…`, and **that session was not the Marketer.** Its own transcript says «هیچ نقشی
  ندارم» (line 134, 2026-09-10T02:27Z), and `docs/audit/HANDOFF-2026-09-11-ceo.md:69` says the same.
  The Marketer's tab was «POSITIONING.md و ترتیب‌بندی موارد» (`0330c828`). The role and the work are
  unaffected, because the record is in `docs/marketing/`, not in either tab. I am writing it down so
  the next restart doesn't resume the wrong transcript.
- **"msg to founder" is `rezv-bc [6175ca]`, and that is now confirmed by behaviour, not only by its
  opening prompt.** The opening prompt, verbatim: «you arwe founder of the project , uou havw access to
  talk from me» (transcript `49c66907…`, 2026-09-11T18:53Z). It signs its messages «Founder proxy
  (rezv-bc)» (19:24:29Z), and it put my questions to the founder within minutes. It has not written a
  ROUTING row yet.
- **No CEO is live.** `ROUTING.md` names `rezv-67 [db8c63]`, and the last CEO id in use today was
  `rezv-0c [6a8557]`. Neither is in `ListAgents` or in the registry. `rezv-bc` says delegation sits
  with it while no CEO is live (19:24:29Z). I am not assigning the CEO role to any peer.
- **Noticed, not mine to fix:** two sessions opened tonight were both told "red team" (`rezv-0e`,
  `rezv-6f`). If that is intended, ignore this line.

## 2. State of the batch

| Artifact | State |
|---|---|
| `POSITIONING.md` | Done. The §1 claim stays **INTERNAL ONLY** until R1 and R2 land. §1 updated tonight (below) |
| `fundraising/INVESTORS.md` | v1. Recommendation (raise after launch) now matches the founder's answer |
| `fundraising/PAYMENT-PATHS.md` | v1. Earn in rial, raise at home. Every non-domestic row needs counsel |
| `fundraising/THE-ASK.md` | **Unblocked by D1.** Planned for after launch; written after `restaurants/PITCH.md` |
| `NARRATIVE.md`, deck, one-pager | Parked: the name is not settled (`E-001`) |
| `restaurants/`, `diners/` | Empty. **`restaurants/` starts now** |

I re-measured the two open gaps at `923a20a`. The only commit since `2d19351` is one directive
document (1 file, 130 lines), and no code changed:

```text
api/src/lib/economy.ts:105-106          no_show -> score 0, addsStrike: true
api/src/lib/lifecycle.ts:231            reverseReservationCashback(...)
api/src/lib/reservation-status.ts:120   reversing statuses: no_show, rejected, expired, cancelled,
                                        auto_cancelled, cancelled_by_user, cancelled_by_restaurant
node tools/check-diner-cost-disclosure.mjs   exit 0 (it pins fields, so it cannot see either gap)
```

## 3. Decisions and routing

### D1: answered. Raising money is a preference, after launch

- **The founder's answer, verbatim,** selected in `rezv-bc`'s AskUserQuestion (transcript `49c66907…`,
  2026-09-11T19:25:24Z): «ترجیح، بعد از لانچ».
- **What it unblocks:** `THE-ASK.md`, planned for after launch. First an accelerator or the صندوق
  نوآوری و شکوفایی open call, then licensed crowdfunding, then VC. Denominated in toman, and framed as
  project participation first (`INVESTORS.md` §1, §6).
- **How the question was checked before it was answered:** `git grep` on `origin/main` over
  `audit/ESCALATIONS.md`, `docs/DECISIONS.md`, `docs/audit/{directives,reports,deputy,fixes}` and the
  CEO handoff found no ruling (exit 1), with a Latin and a Persian positive control both at exit 0.
  The answer arrived minutes later, so it is not in those files yet.

### D2: my stale question, now closed in the founder's own words

**My error, reported before anyone else found it.** v1 asked whether a cashback reversal counts as a
"cost" under the §1 claim, as if it were a new ruling. I sent it without proving the question was
still open. The record already held the founder's «الف» (grant at attendance,
`audit/ESCALATIONS.md:116`), written up as «یک تصمیم، هر دو مسیر» (`:123-126`, commit `c8cddcc`).

**`rezv-bc` handled it better than I did.** It asked the founder **when cashback is granted**, and
said plainly that the 09-10 question had only been about the referral reward. The founder's answer,
verbatim, in the same AskUserQuestion: «کش‌بک هنگامِ حضور».

So referral and cashback are **both** granted at attendance. Once that is built, nothing is granted
before attendance, and nothing is reversed when a diner never arrives or a restaurant rejects the
booking. One case remains, and the code already flags it as the owner's call: cancelling *after*
check-in still reverses cashback (`reservation-status.ts:116-118`). It is rare, and nothing here waits
on it.

### R2: Routing, not a decision. Implement «الف» on both paths

```text
api/src/lib/reservations.ts:657   cashback is still written at booking   -> move to the checked_in transition
api/src/lib/loyalty.ts:610        completeReferral has zero callers       -> call it from the same place
```

**Until both are on `main`, the §1 claim stays INTERNAL ONLY.**

### R1: Routing, not a decision. No disclosure was found for the no-show strike

`economy.ts:105-106` records a strike on no-show. A strike blocks the top tier and is shown to
restaurants. **Re-searched with `git grep` on `origin/main`**, because the Grep tool in this repo
has returned false nulls on Persian patterns. Every «تخلف» (strike) in `apps/customer/js` is about
cancelling:

```text
apps/customer/js/data/booking.js:113     before booking: a late cancel records a strike
apps/customer/js/features/trips.js:182   at cancel time: "if you cancel now, a strike is recorded"
apps/customer/js/reservation.js:96       after the free window: "cancelling now records a strike"
apps/customer/js/reservation.js:29       no_show:'cancelled'  (a no-show is displayed as "cancelled")
```

None mentions not showing up, and the app displays a no-show as a cancellation. That search covers
the customer app only, not SMS templates or other surfaces. **Routed:** `rezv-bc` gave the
verification to `rezv-6f` (19:24:29Z). If it is confirmed, the no-show case goes into the pre-booking
copy.

*(`POSITIONING.md` §1 cited `booking.js:84`, which has drifted to `:113`. Corrected in the same commit.)*

### Not asked, on purpose: `E-001`

`audit/ESCALATIONS.md:134` records the founder's ruling: the name is not settled, and **it is no
longer asked every round.** `BRIEF-2026-09-11.md` §4(a) asked it again anyway. That was the previous
Marketer's error, and I withdraw the question. Work that needs the name stays parked.

## 4. One boundary I am keeping

`marketer.md` reserves four approvals for the founder: anything that goes public, spends money,
promises anything to anyone, or touches investors. I route those through `rezv-bc`, but I treat them
as approved **only when the founder's own words are relayed**, not on a session's judgement, unless
the founder says `rezv-bc` may approve them. Tonight's two answers were the founder's own selections
in AskUserQuestion, so they meet that bar.

## 5. Next, without waiting

`restaurants/PITCH.md`: the pitch to a restaurant owner, built from `docs/audit/research/MATRIX.md`,
REAL claims only, name-free (⟨NAME⟩). Then `restaurants/FIRST-TEN.md` (categories, not businesses).
Then `fundraising/THE-ASK.md` for after launch. None of it needs `E-001`.

**Line for the founder session:** «Marketer `rezv-f7` در `docs/marketing/BRIEF-2026-09-11-2.md`
(نسخه‌ی ۲): هر دو جوابِ مالک ثبت شد — D1 «ترجیح، بعد از لانچ»، کش‌بک «هنگامِ حضور». D2ِ نسخه‌ی ۱
پرسشِ کهنه‌ی من بود. R1 نزدِ rezv-6f، R2 اجرا مانده. بعدی: restaurants/PITCH.md، بعد THE-ASK.md.»
