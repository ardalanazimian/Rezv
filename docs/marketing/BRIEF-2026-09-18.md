# BRIEF — 2026-09-18

**Session:** `rezv-aa [52177b]` (Marketer, successor in the seat — **not** `rezv-c6`) ·
**Target:** the CEO · **Status:** submitted, not closed ·
**Base:** `origin/main = ba87829` (2026-09-17 16:07), branch merged up to it at `3206321`.

**⚠️ The CEO ref this folder is addressed to no longer resolves.** Every artifact here targets
`rezv-87 [09dbab]`. Today `rezv-87` is in neither `ListAgents` (5 interactive peers: `rezv-46`,
`rezv-45`, `rezv-8c`, `rezv-1b`, `rezv-18`) nor `~/.claude/sessions/*.json`. Per `ROUTING.md` I do
not map a peer to the CEO by position or by elimination. **So this is a file, not a message, and the
owner routes it.**

**What it needs:** **Owner** — the nine decisions in §4, and the push in §3. **CEO** (once
identified) — §5.

---

## 0. The recommendation first

**The marketing corpus is finished and correct; what is missing is that none of it has reached the
product.** Every business decision the owner made on 09-17 — prices, VAT, no trial, no founding
offer — is recorded in `docs/marketing/` and **applied nowhere in the code**. The gap is not analysis.
It is one push and one implementation pass.

Three things, in order:

1. **Land the corpus on `main`.** It merges with **zero conflicts** (§3). Until it does, the
   Implementation Team is reading 18/34/65 while the owner decided 18/33/60.
2. **Answer the two decisions that block everything else** (§4.1, §4.2): the runway question, open
   since 09-11, and *who makes the sales calls*. Without the second, Phase 1 of the business plan
   has nobody to execute it, and no amount of further writing changes that.
3. **Do not write more marketing documents until 1 and 2 are done.** The corpus is ahead of the
   company; adding to it now is motion, not progress. The next real input is not a document — it is
   the first restaurant-owner interviews (`BUSINESS-MODEL.md` §7).

---

## 1. The seat, and why this row changed

The owner seated this session today, in his own words: *"you are marketing, business plan and
business model"*. That matches the scope `rezv-c6` held (`ROUTING.md`, Marketer row 2: growth ·
positioning · fundraising · branding without a name · business model · business plan), so **the
charter is unchanged** — `docs/audit/prompts/marketer.md`, read in full today, with the scope
extension the CEO granted `rezv-c6` under D-10.

Identity measured two ways, not inferred: `ListAgents` ("This session is `rezv-aa [52177b]`") and
`~/.claude/sessions/3160.json`, which puts `name = rezv-aa` next to
`sessionId = f9f256d4-e850-4a0f-b2cd-de942bdd1c2a` — the same sessionId this session's scratchpad
path carries. `rezv-c6` is not in `ListAgents` and not in the registry; I record that its ref **does
not resolve**, not that the session is dead.

**One standing instruction I could not follow.** The owner's original order for this seat was
«... قبل تغییر یا انجام کاری با ceo rezv-87 باید هماهنگ شی» — coordinate with the CEO before doing
anything. That ref is stale (see the header). I therefore limited myself to what the charter makes
unambiguously mine: reading, measuring, and writing **only** inside `docs/marketing/` plus my own
`ROUTING.md` row. I edited no code, no seed, no landing copy, and no other role's files.

## 2. What I measured today — the owner's 09-17 decisions have not reached the product

All on `origin/main = ba87829`. Every negative grep carries a positive control, per the repo's rule.

| # | Owner's decision (09-17) | State on `main` today | Measurement |
|---|---|---|---|
| 1 | Prices **18 / 33 / 60M** | **m3 correct, m6 and m12 are not.** Seed still has `18,000,000` / **`34,000,000`** / **`65,000,000`** | `api/prisma/seed/site-content.json:10,29,49` |
| 2 | `compareAt` values | **Correct, no change needed.** `36,000,000` / `72,000,000` still equal 2× and 4× the m3 price of 18M | same file `:30,50` |
| 3 | Every price surface shows «+ ۱۰٪ مالیات بر ارزش افزوده» | **Not applied.** «ارزش افزوده» → 0 in both the seed and the pricing page. The page carries a vaguer line instead: «مالیات طبقِ قوانینِ جاری محاسبه می‌شود» — **under-specified, not false** | `apps/landing/app/pricing/page.tsx:150`; control «تومان» → 1 |
| 4 | Remove the free 30-day demo (decision 9) | **Not applied.** `apps/landing/app/demo/page.tsx` and `components/forms/TrialForm.tsx` are both still on `main`; the OG image still reads «دموی ۳۰ روزه‌ی رایگان» | `apps/landing/app/opengraph-image.tsx:84` |
| 5 | M-12 honesty fixes | **None applied.** In the seed on `main`: «کارتِ هدیه» ×8 (flag off), «هوشمند» ×6 (rule engine, not AI), «محبوب‌ترین» ×1 (no sales exist), «بازبینیِ فصلی» ×1 (nobody staffed), «رزرونو» ×38 (`E-001` open) | `api/prisma/seed/site-content.json`; **positive control** `priceToman` → 3 |

**The honest severity, stated down not up.** No buyer is seeing these today: no production
environment has been shown to exist, and as of 09-17 the owner reported the domain bought with **the
name and DNS unverified and hosting unpaid** (S-06). So this is a **launch blocker, not a live
leak** — and it is the single largest one in this folder, because it is the set of things the owner
has already decided and paid attention to.

## 3. The corpus is ready to land — measured, not assumed

Eleven of the seventeen files in `docs/marketing/` exist **only** on `session/rezv-c6-marketing`:
`BRAND.md`, `BUSINESS-MODEL.md`, `BUSINESS-PLAN.md`, `PRICING.md`, both playbooks, and all four
research files. `main` carries only `POSITIONING.md`, `BRIEF-2026-09-11.md`, `fundraising/` and the
`.gitkeep`s.

**This is by design, not drift** — D-14 says these reports live on the session branch until the
owner pushes. The point of §3 is only that the gate is now a day old and the chain behind it
(CEO verify → Implementation edits the seed → owner pushes) has not started.

```text
git merge-tree --write-tree --name-only origin/main session/rezv-c6-marketing
  EXIT=0 · conflicted paths: 0
```

I then merged `origin/main` into the branch (`3206321`, exit 0) so it is current. **The diff against
`main` is documentation only.** Nothing in it touches code, and pushing it changes no behaviour.

## 4. The nine decisions that are the owner's alone

Consolidated from four documents into one list so they can be answered in one sitting. Each carries
my recommendation, because a decision with no recommendation attached is just a question.

| # | Decision | Blocks | My recommendation |
|---|---|---|---|
| **1** | **Runway: is raising money a constraint or a preference?** Open since `BRIEF-2026-09-11.md` | `fundraising/THE-ASK.md` entirely, and Phase 3 of the plan | Answer it even if the answer is "preference" — that alone unblocks the file. **The reasoning and every option belong in the private repo** (`Rezv-security`), per the owner's 09-17 ruling, so this row is a pointer and nothing more. See §7 |
| **2** | **Who makes the sales calls and venue visits in Phase 1?** | `BUSINESS-PLAN.md` Phase 1, `restaurants/PLAYBOOK.md` §4–5 | **The highest-value answer in this table.** Ten restaurants sold by hand needs a named person. If it is the owner himself, say so — that is a valid answer and it sizes the plan honestly |
| **3** | **Which Tehran district, and which venue categories** | The first-ten plan | My proposed categories stand: sit-down restaurants with evening peaks that already take DM bookings, plus cafés with weekend queues (the waitlist case). The district is local knowledge I do not have |
| **4** | **A restaurant→restaurant referral benefit: real and paid, or none** | Referral channel in both playbooks | **Say "none" for launch.** We removed a referral promise once already because the code never paid it (`POSITIONING.md` §3). A second unpaid promise on the same mechanic would be the one thing the brand cannot afford |
| **5** | **Visual identity: A (indigo everywhere), B (warm everywhere), or C (split, one mark)** | `BRAND.md` §3, and the Designer's queue | **C now, A after launch.** C costs one file (`apps/landing/public/icon.svg`) and removes the three-mark problem; B touches every app in launch week |
| **6** | **Confirm or replace the brand idea «روراست»** | All copy | Confirm it. It is the only brand idea that is *cheaper* to keep than to break, and §2 of `BRAND.md` derives the whole voice from it |
| **7** | **No-exclusivity clause in the restaurant terms** | `restaurants/PLAYBOOK.md` §1 | Confirm there is none. It lets the pitch answer the POS question honestly — we work alongside, not instead |
| **8** | **`E-001`, the name.** Domain bought 09-17; **name and DNS still unverified** | Every printed / at-venue item (QR, table tent) | Verify the domain resolves before anything is printed. Digital channels are unblocked either way |
| **9** | **The Foodro check** — optional, two minutes, his own phone | Sharpens `BUSINESS-MODEL.md` §8 | Worth doing. It converts our single largest competitive `UNKNOWN` into a fact. Not urgent |

**Already decided on 09-17 and recorded — no action needed:** prices, VAT +10%, deposits off, no
self-serve trial, team-created accounts, 15-day renewal notice, price lock on sold subscriptions, no
founding offer, discretionary free accounts, the free listing moved to after launch.

## 5. What the CEO must do, once a CEO ref resolves

1. **Route the pricing chain.** Verify `PRICING.md` v2, then Implementation applies §7: prices
   `18/33/60` at `api/prisma/seed/site-content.json:10,29,49`, the VAT line, the M-12 copy rows 1–6,
   and the demo/trial removal (decision 9) — in one commit the CEO verifies before `main`.
2. **Certify the three Phase 0 gates it owns** (`BUSINESS-PLAN.md` §5): M-12 shipped, real SMS
   delivery proven, CI green on `main`.
3. **Re-verify the REAL classes** in `BUSINESS-MODEL.md` §2 when `CHAIN-MAP.md` lands.

## 6. Limits of this brief — what I did not do

- **I did not identify the CEO.** Five interactive peers exist; none announced itself, and I did not
  map one by position or elimination.
- **I re-measured the code, not the market.** Every §2 row is a fresh measurement on `ba87829`. The
  market research is `rezv-c6`'s of 09-16 and I did not re-fetch it; it was not a day old.
- **I wrote no new positioning, pitch or campaign copy.** None is blocked on me — it is blocked on
  §4 and on the CEO's Phase 0 gates.
- **I edited no code and no other role's files.** Two files changed: this brief and my own
  `ROUTING.md` row.

## 7. Unasked-for: the 09-17 move to the private repo is incomplete at the tip

`INVESTORS.md` was correctly gutted to a pointer (`190c4fd`). **Two files kept the strategy it was
moved for**, and one of them is already public on `main`:

| Where | What it still says | Repo state |
|---|---|---|
| `docs/marketing/BUSINESS-PLAN.md:180-181` | The capital ladder in order: «accelerator or صندوق نوآوری و شکوفایی first, then licensed crowdfunding, then VC, and project participation before equity unless a fund asks otherwise» — this is the substance of `INVESTORS.md` §6, restated | On the branch, **would land on `main` with the push in §3** |
| `docs/marketing/fundraising/PAYMENT-PATHS.md:67` | «**Domestic capital**: VC, accelerators, licensed crowdfunding … ✅ **Recommended**, after launch» | **Already on `main` today.** Public now |

**Why this is worth a ruling rather than a quiet edit.** The known residual everyone has been
tracking is *git history*. This is different and worse: it is the **current tip**, in two files
nobody re-read after the move, and one of them is public this minute. It also may be deliberate —
a capital *posture* is much milder than an investor map with names and check sizes, and the owner
may be content to have it public.

**My recommendation:** treat the PAYMENT-PATHS row as acceptable (it names no counterparty and no
number) and **move the BUSINESS-PLAN ladder to the private repo before the §3 push**, leaving a
pointer. It is two lines, and doing it before the push is free; doing it after adds another history
residual. **This is the CEO's call to route and the owner's to rule** — I have changed neither file.

---

**Line for the CEO:** «Marketer `rezv-aa` → `docs/marketing/BRIEF-2026-09-18.md` (submitted):
هیچ‌کدام از تصمیم‌های ۰۹-۱۷ِ مالک به کد نرسیده — روی `main` قیمت‌ها هنوز ۱۸/۳۴/۶۵ است نه ۱۸/۳۳/۶۰،
مالیات و حذفِ دمو اعمال نشده، و هر پنج موردِ M-12 زنده است (با کنترلِ مثبت). شاخه‌ی بازاریابی با
**صفر تداخل** به main می‌رود و حالا با main به‌روز است. نُه تصمیمِ مالک در §۴ یک‌جا جمع شده، هرکدام با
پیشنهاد. ⚠️ `rezv-87` دیگر resolve نمی‌شود — این پرونده است نه پیام، و مسیریابی با مالک است.»
