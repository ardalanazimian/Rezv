# BRIEF — 2026-09-18

**Session:** `rezv-aa [52177b]` (Marketer, successor in the seat — **not** `rezv-c6`) ·
**Target:** the CEO · **Status:** submitted, not closed ·
**Base:** `origin/main = ba87829` (2026-09-17 16:07), branch merged up to it at `3206321`.

**⚠️ The CEO ref this folder is addressed to no longer resolves.** Every artifact here targets
`rezv-87 [09dbab]`. Today `rezv-87` is in neither `ListAgents` (5 interactive peers: `rezv-46`,
`rezv-45`, `rezv-8c`, `rezv-1b`, `rezv-18`) nor `~/.claude/sessions/*.json`. Per `ROUTING.md` I do
not map a peer to the CEO by position or by elimination. **So this is a file, not a message, and the
owner routes it.**

**What it needs:** **Owner** — the nine decisions in §4, and the push in §3. **CEO** (once
identified) — §5.

---

## 0. The recommendation first

**The marketing corpus is finished and correct; what is missing is that none of it has reached the
product.** Every business decision the owner made on 09-17 — prices, VAT, no trial, no founding
offer — is recorded in `docs/marketing/` and **applied nowhere in the code**. The gap is not analysis.
It is one push and one implementation pass.

Three things, in order:

1. **Land the corpus on `main`.** It merges with **zero conflicts** (§3). Until it does, the
   Implementation Team is reading 18/34/65 while the owner decided 18/33/60.
2. ~~Answer the two blocking decisions.~~ ✅ **Both answered by the owner on 2026-09-18** — raising is
   a preference, and **he sells personally**. Phase 1 now has an executor, and `THE-ASK.md` is
   unblocked. §4 is updated; §9 of the plan works through what founder-led selling actually changes.
3. **Do not write more marketing documents.** The corpus is ahead of the company; adding to it now is
   motion, not progress. **With the owner selling, the next real input is his first ten
   conversations** — they answer the question desk research could not (which price band the buyer
   compares us with, `BUSINESS-MODEL.md` §7). That is worth more than any document I could write.
4. **Two competitor corrections are pending and are mine, not lost** (§8): MeNEW's published prices
   and the Vendo reclassification, both measured by `rezv-8c` and neither yet applied.

---

## 1. The seat, and why this row changed

The owner seated this session today, in his own words: *"you are marketing, business plan and
business model"*. That matches the scope `rezv-c6` held (`ROUTING.md`, Marketer row 2: growth ·
positioning · fundraising · branding without a name · business model · business plan), so **the
charter is unchanged** — `docs/audit/prompts/marketer.md`, read in full today, with the scope
extension the CEO granted `rezv-c6` under D-10.

Identity measured two ways, not inferred: `ListAgents` ("This session is `rezv-aa [52177b]`") and
`~/.claude/sessions/3160.json`, which puts `name = rezv-aa` next to
`sessionId = f9f256d4-e850-4a0f-b2cd-de942bdd1c2a` — the same sessionId this session's scratchpad
path carries. `rezv-c6` is not in `ListAgents` and not in the registry; I record that its ref **does
not resolve**, not that the session is dead.

**One standing instruction I could not follow.** The owner's original order for this seat was
«... قبل تغییر یا انجام کاری با ceo rezv-87 باید هماهنگ شی» — coordinate with the CEO before doing
anything. That ref is stale (see the header). I therefore limited myself to what the charter makes
unambiguously mine: reading, measuring, and writing **only** inside `docs/marketing/` plus my own
`ROUTING.md` row. I edited no code, no seed, no landing copy, and no other role's files.

## 2. What I measured today — the owner's 09-17 decisions have not reached the product

All on `origin/main = ba87829`. Every negative grep carries a positive control, per the repo's rule.

| # | Owner's decision (09-17) | State on `main` today | Measurement |
|---|---|---|---|
| 1 | Prices **18 / 33 / 60M** | **m3 correct, m6 and m12 are not.** Seed still has `18,000,000` / **`34,000,000`** / **`65,000,000`** | `api/prisma/seed/site-content.json:10,29,49` |
| 2 | `compareAt` values | **Correct, no change needed.** `36,000,000` / `72,000,000` still equal 2× and 4× the m3 price of 18M | same file `:30,50` |
| 3 | Every price surface shows «+ ۱۰٪ مالیات بر ارزش افزوده» | **Not applied.** «ارزش افزوده» → 0 in both the seed and the pricing page. The page carries a vaguer line instead: «مالیات طبقِ قوانینِ جاری محاسبه می‌شود» — **under-specified, not false** | `apps/landing/app/pricing/page.tsx:150`; control «تومان» → 1 |
| 4 | Remove the free 30-day demo (decision 9) | **Not applied.** `apps/landing/app/demo/page.tsx` and `components/forms/TrialForm.tsx` are both still on `main`; the OG image still reads «دموی ۳۰ روزه‌ی رایگان» | `apps/landing/app/opengraph-image.tsx:84` |
| 5 | M-12 honesty fixes | **None applied.** In the seed on `main`: «کارتِ هدیه» ×8 (flag off), «هوشمند» ×6 (rule engine, not AI), «محبوب‌ترین» ×1 (no sales exist), «بازبینیِ فصلی» ×1 (nobody staffed), «رزرونو» ×38 (`E-001` open) | `api/prisma/seed/site-content.json`; **positive control** `priceToman` → 3 |

**The honest severity, stated down not up.** No buyer is seeing these today: no production
environment has been shown to exist, and as of 09-17 the owner reported the domain bought with **the
name and DNS unverified and hosting unpaid** (S-06). So this is a **launch blocker, not a live
leak** — and it is the single largest one in this folder, because it is the set of things the owner
has already decided and paid attention to.

## 3. The corpus is ready to land — measured, not assumed

Eleven of the seventeen files in `docs/marketing/` exist **only** on `session/rezv-c6-marketing`:
`BRAND.md`, `BUSINESS-MODEL.md`, `BUSINESS-PLAN.md`, `PRICING.md`, both playbooks, and all four
research files. `main` carries only `POSITIONING.md`, `BRIEF-2026-09-11.md`, `fundraising/` and the
`.gitkeep`s.

**This is by design, not drift** — D-14 says these reports live on the session branch until the
owner pushes. The point of §3 is only that the gate is now a day old and the chain behind it
(CEO verify → Implementation edits the seed → owner pushes) has not started.

```text
git merge-tree --write-tree --name-only origin/main session/rezv-c6-marketing
  EXIT=0 · conflicted paths: 0
```

I then merged `origin/main` into the branch (`3206321`, exit 0) so it is current. **The diff against
`main` is documentation only.** Nothing in it touches code, and pushing it changes no behaviour.

## 4. The nine decisions that are the owner's alone

Consolidated from four documents into one list so they can be answered in one sitting. Each carries
my recommendation, because a decision with no recommendation attached is just a question.

| # | Decision | Blocks | My recommendation |
|---|---|---|---|
| ~~**1**~~ | ~~Runway: constraint or preference?~~ | — | ✅ **ANSWERED 2026-09-18: a preference** («جذب سرمایه ترجیح هست»). Consequences recorded in the private repo, not here. `THE-ASK.md` is unblocked |
| ~~**2**~~ | ~~Who makes the sales calls in Phase 1?~~ | — | ✅ **ANSWERED 2026-09-18: the owner himself** («تیم فروش با خودم»). See `BUSINESS-PLAN.md` §9 — it changes Phase 1's shape, it does not just fill a blank |
| **3** | **Which Tehran district, and which venue categories** | The first-ten plan | My proposed categories stand: sit-down restaurants with evening peaks that already take DM bookings, plus cafés with weekend queues (the waitlist case). The district is local knowledge I do not have |
| **4** | **A restaurant→restaurant referral benefit: real and paid, or none** | Referral channel in both playbooks | **Say "none" for launch.** We removed a referral promise once already because the code never paid it (`POSITIONING.md` §3). A second unpaid promise on the same mechanic would be the one thing the brand cannot afford |
| **5** | **Visual identity: A (indigo everywhere), B (warm everywhere), or C (split, one mark)** | `BRAND.md` §3, and the Designer's queue | **C now, A after launch.** C costs one file (`apps/landing/public/icon.svg`) and removes the three-mark problem; B touches every app in launch week |
| **6** | **Confirm or replace the brand idea «روراست»** | All copy | Confirm it. It is the only brand idea that is *cheaper* to keep than to break, and §2 of `BRAND.md` derives the whole voice from it |
| **7** | **No-exclusivity clause in the restaurant terms** | `restaurants/PLAYBOOK.md` §1 | Confirm there is none. It lets the pitch answer the POS question honestly — we work alongside, not instead |
| **8** | **`E-001`, the name.** Domain bought 09-17; **name and DNS still unverified** | Every printed / at-venue item (QR, table tent) | Verify the domain resolves before anything is printed. Digital channels are unblocked either way |
| **9** | **The Foodro check** — optional, two minutes, his own phone | Sharpens `BUSINESS-MODEL.md` §8 | Worth doing. It converts our single largest competitive `UNKNOWN` into a fact. Not urgent |

**Already decided on 09-17 and recorded — no action needed:** prices, VAT +10%, deposits off, no
self-serve trial, team-created accounts, 15-day renewal notice, price lock on sold subscriptions, no
founding offer, discretionary free accounts, the free listing moved to after launch.

## 5. What the CEO must do, once a CEO ref resolves

1. **Route the pricing chain.** Verify `PRICING.md` v2, then Implementation applies §7: prices
   `18/33/60` at `api/prisma/seed/site-content.json:10,29,49`, the VAT line, the M-12 copy rows 1–6,
   and the demo/trial removal (decision 9) — in one commit the CEO verifies before `main`.
2. **Certify the three Phase 0 gates it owns** (`BUSINESS-PLAN.md` §5): M-12 shipped, real SMS
   delivery proven, CI green on `main`.
3. **Re-verify the REAL classes** in `BUSINESS-MODEL.md` §2 when `CHAIN-MAP.md` lands.

## 6. Limits of this brief — what I did not do

- **I did not identify the CEO.** Five interactive peers exist; none announced itself, and I did not
  map one by position or elimination.
- **I re-measured the code, not the market.** Every §2 row is a fresh measurement on `ba87829`. The
  market research is `rezv-c6`'s of 09-16 and I did not re-fetch it; it was not a day old.
- **I wrote no new positioning, pitch or campaign copy.** None is blocked on me — it is blocked on
  §4 and on the CEO's Phase 0 gates.
- **I edited no code and no other role's files.** Two files changed: this brief and my own
  `ROUTING.md` row.

## 7. Unasked-for: the 09-17 move to the private repo is incomplete at the tip

`INVESTORS.md` was correctly gutted to a pointer (`190c4fd`). **Two files kept the strategy it was
moved for**, and one of them is already public on `main`:

> **✅ RULED AND CARRIED OUT, 2026-09-18.** The owner ruled this class sensitive, in his own words:
> «توصیه سرمایه داخلی و ... رو به رسپو private اتقال بده». Both passages — and two more that the same
> sweep turned up — are now in `Rezv-security` (`business/fundraising/PAYMENT-PATHS.md` and
> `CAPITAL-STRATEGY-EXCERPTS.md`), and the public copies are pointers. **The text of each passage is
> therefore no longer quoted here**, since quoting it in this file would reproduce exactly what was
> moved. What follows is the record of where each one was, not what it said.

| Where | What it was | Repo state when found |
|---|---|---|
| `BUSINESS-PLAN.md` §5 Phase 3.2 | The capital ladder — the substance of `INVESTORS.md` §6, restated | **Public**, on `origin/session/rezv-c6-marketing` |
| `fundraising/PAYMENT-PATHS.md` §2 row B | The domestic-capital verdict | **Public**, on `main` |
| `BUSINESS-PLAN.md` §0.1 | "Launch first, then raise", citing `INVESTORS.md` §6 | **Public**, on the branch |
| `BRIEF-2026-09-11.md` §3–§4 | Two recommendation rows naming the posture outright | **Public**, on `main` |

> ### ✅ Correction to the first version of this section, made the same day
>
> The first version of this table said `BUSINESS-PLAN.md` was "on the branch, **would** land on
> `main` with the push" — i.e. not yet exposed. **That was wrong, and wrong in the direction that
> makes the problem look smaller.** `ardalanazimian/Rezv` is a **public** repository, and GitHub
> serves *every* ref in a public repo, not just `main`. The file has been publicly readable since
> `rezv-c6` pushed it, and my own push did not cause it.
>
> Measured, not reasoned: an unauthenticated `GET api.github.com/repos/ardalanazimian/Rezv` returns
> HTTP 200 with `"private": false`, and an unauthenticated fetch of
> `raw.githubusercontent.com/ardalanazimian/Rezv/session/rezv-c6-marketing/docs/marketing/BUSINESS-PLAN.md`
> returns **HTTP 200, 18,784 bytes**, containing the capital-ladder line at line 180. Negative
> control (a string that should not appear) → 0.
>
> **This changes the remediation, which is why it is worth the paragraph.** "Move it before the push"
> — my earlier recommendation — is no longer available for this file: the exposure already happened.
> Deleting it from the tip now produces the *same* history residual that `INVESTORS.md` already has.
>
> A peer (`rezv-76`) independently re-measured this section, confirmed the `PAYMENT-PATHS` row, could
> not find `BUSINESS-PLAN.md` on `main`, and concluded the exposure was **one** file rather than two.
> That conclusion is the optimistic error, and only re-measuring caught it: the file is not on `main`
> **and** is public anyway.

**Why this is worth a ruling rather than a quiet edit.** The known residual everyone has been
tracking is *git history*. This is different and worse: it is the **current tip**, in two files
nobody re-read after the move, and one of them is public this minute. It also may be deliberate —
a capital *posture* is much milder than an investor map with names and check sizes, and the owner
may be content to have it public.

**My recommendation, rewritten after the correction above.** Both files are already public, so there
is no longer a free fix, and the decision is now about what is worth paying:

1. **Rule first on whether this class is sensitive at all.** Both rows state a capital *posture*
   («domestic capital, after launch») with **no counterparty, no valuation, no check size and no
   number**. That is a long way from the investor map that `INVESTORS.md` was moved for. The owner
   may reasonably decide this is fine to be public, and if so the cheapest correct action is
   **nothing**, plus a line in the private repo recording that it was considered and allowed.
2. **If he rules it sensitive**, the honest options are gutting both tips to pointers — which
   produces the *same* history residual `INVESTORS.md` already has and does not un-publish anything —
   or a history rewrite, which is expensive and coordinated across every worktree and every peer's
   refs. Neither restores privacy for content already served; treat them as stopping further spread,
   not as undoing it.
3. **The reusable lesson, which is worth more than either file.** The 09-17 move treated "remove it
   from the public repo" as "remove it from `main`". In a public repo those are different
   statements, and a session branch is not a private space. Whatever the owner rules here, that
   sentence belongs in the convention so the next move does not repeat it.

### 7.1 That lesson, applied — and it is much larger than my two lines

`rezv-76` widened the question to all refs, and I swept them. **The 09-17 move (D-30, `592bfca`) was
applied to `main` only. The full pre-move content is still served from public branches.** The commit
gutted eleven files; those files survive at full size elsewhere:

> ### The one number to give the owner: **the ruling reached 5 refs out of 92**
>
> Counting the branches that carry the *gutted* file is sharper than counting the ones that carry the
> full one, and neither `rezv-76` nor I said it first — it fell out of comparing our two sweeps.
> **`stub = 5`.** The owner ruled once on 2026-09-17; the ruling took effect on five refs and 50+
> still serve the original. He is not being asked to re-decide sensitivity — he is being told his
> decision was executed on 5% of the repository.

| File gutted on `main` | Stub | Full (public) | Absent | Largest |
|---|---|---|---|---|
| `docs/marketing/fundraising/INVESTORS.md` | 5 | **50** | 37 | 12,923 B |
| `docs/audit/redteam/ATTACKS.md` | 5 | **51** | 36 | 14,266 B |
| `docs/audit/redteam/LEDGER-ADDITIONS.md` | 5 | **51** | 36 | 18,702 B |
| `docs/audit/redteam/RETEST-2026-09-13.md` | 5 | **26** | 61 | 18,827 B |

**The mechanism, measured — and it makes the number predictable instead of mysterious.** `rezv-76`
observed that `stub` is *identical* (5) for all nine files it swept while `full` ranges 26–51, and
inferred one merge landing on 5 refs rather than eleven separate outcomes. That inference makes a
checkable prediction, so I checked it rather than accepting it:

```text
the 5 stub refs, per file           → identical for every file tested:
  main · session/rezv-46-ceo · session/rezv-87-ceo · session/rezv-8c-launch · session/rezv-c6-marketing
  (a 6th line, bare "origin", is origin/HEAD — a symbolic pointer to main, not a branch)

refs whose history contains 592bfcad → 5 / 92   ← exactly the same five
```

**So `stub = contains(592bfcad)`, with no exceptions.** The remediation is not partial in any
interesting sense: it is **one commit that only ever reaches a ref when that ref merges it.** The five
are `main` plus the branches that have merged `main` since 09-17 — and `session/rezv-c6-marketing` is
on that list only because I merged `main` into it today, which is worth saying plainly because it
means **I moved this number by one while measuring it.**

**The consequence the owner needs:** the other **87 refs will never receive the fix on their own.**
Active branches will pick it up as they merge `main`; the stale ones — most of the 92 — serve the
original content indefinitely. This is why the fix-shaped question is about **refs, not files**, and
why "stop adding refs" is the only free mitigation: the existing 87 are not going to drain.

**Corroborated by two independent methods, and the counts move — so re-measure at send time.**
I walked refs with `cat-file -s`; `rezv-76` pinned `refs/remotes/origin` with `for-each-ref` and
resolved each with `rev-parse <sha>:<path>`. **All four largest-byte figures are identical.** The
full-counts differed by exactly +1 at first (I measured 90 heads, it measured 92) — that is the
branch count moving between measurements, not a disagreement; after re-fetching I reproduce its
numbers exactly. Figures above are at **92 heads**.

**Proved by unauthenticated fetch, not inferred from the object store** — anyone can do this, with no
account:

```text
GET raw.githubusercontent.com/…/session/rezv-f7/docs/marketing/fundraising/INVESTORS.md
  → HTTP 200 · 12,923 B · "# INVESTORS — the map, and the honest read of where we sit on it"
GET raw.githubusercontent.com/…/audit/features-2026-09-16/docs/audit/redteam/ATTACKS.md
  → HTTP 200 · 11,085 B · "# ATTACKS — the catalogue, extended"
negative control on both fetches → 0
```

`git ls-remote --heads origin` → **90 branches**, every one public.

**Two reasons this outranks the capital-posture rows that started this section.** First, the
`INVESTORS.md` still being served is the *actual* investor map, and it contains the candid internal
read — «no production is reachable», users «Unknown», the name unsettled, revenue none. Second,
`ATTACKS.md` and the RETEST files are **attack material against our own product**, which is a
security question, not a competitive one, and belongs to Red Team rather than to me.

**Scope limit, and who is closing it.** I measured four of the eleven files `592bfca` touched, so
this table is **≥**, not **=**. `rezv-76` is sweeping the remaining seven (`BRIEF-2026-09-09`,
`HANDOFF-2026-09-09`, `READY-TO-MERGE-2026-09-11`, the 09-09/09-10/09-11 RETESTs, and
`probe-runner-integrity.mjs`) with the pinned-ref method, which turns the `≥` into an exact figure
for all eleven. I am **not** duplicating that sweep. It also independently confirmed the order
itself rather than trusting the sha: `592bfcad`, 2026-09-17, eleven files, 44 insertions, 1,556
deletions.

**The part neither sweep covers, and it is the larger one:** *nobody has swept for sensitive content
that was never in that commit.* Both of us are measuring the blast radius of one known ruling. The
question "what else is public that should not be" is unasked, and it is **Red Team's to run**, not
marketing's.

**Recommendation.** Route this to Red Team and the owner **today**, ahead of everything else in this
brief, and treat it as one decision rather than eleven: the owner ruled once on 09-17 and the ruling
was carried out on one ref out of ninety. Deleting from branch tips now un-publishes nothing — the
honest options are to accept it with a recorded decision, or to rewrite history across 90 refs, which
is a coordinated operation nobody should start without the owner's explicit go. **The cheap, real
mitigation available immediately is to stop adding refs: no further `session/*` push of anything in
this class.**

**This is the CEO's call to route and the owner's to rule** — I have changed none of these files.

## 8. Carried forward — two competitor corrections, measured by `rezv-8c`, not yet applied

Recorded here so they do not evaporate across a pause. **Both are mine to apply and neither is done.**
I have **not** re-measured them myself, so they carry `rezv-8c`'s name, not my verification — and
that is exactly the second-hand-row problem this brief opened with, so **re-measure before applying**.

| # | Correction | Where it lands | State |
|---|---|---|---|
| C-1 | **MeNEW publishes prices**: ≈1M / 2M / 2.85M toman **per month**, billed in 3-month cycles, 30% off annual | `PRICING.md` §3 competitor table, and `research/COMPETITORS-IRAN-2026-09-16.md` | Reported by `rezv-8c`. **Unverified by me** |
| C-2 | **Vendo is misclassified** in our research as a POS reseller. It also has **its own commission-free direct-ordering product** | `research/COMPETITORS-IRAN-2026-09-16.md`, and the competitive-risk section `BUSINESS-MODEL.md` §8 | Reported by `rezv-8c`. **Unverified by me** |

**Why C-1 matters more than a table row.** At ~1–2.85M toman/month, MeNEW sits **below** our 5.0–6.0M
monthly equivalent, in a band `PRICING.md` currently has nobody in. Our published-price advantage
("the only reservation-first product with a public price") may be **wrong as written**. That claim is
in `BUSINESS-MODEL.md` §3 and it is exactly the kind of line that reaches a pitch deck. **Do not
repeat it until C-1 is verified.**

**Why C-2 matters.** A commission-free direct-ordering product is closer to our positioning than a
POS reseller is, so it belongs in the §8 risk table next to Foodro, not in the POS paragraph.

---

**Line for the CEO:** «Marketer `rezv-aa` → `docs/marketing/BRIEF-2026-09-18.md` (submitted):
هیچ‌کدام از تصمیم‌های ۰۹-۱۷ِ مالک به کد نرسیده — روی `main` قیمت‌ها هنوز ۱۸/۳۴/۶۵ است نه ۱۸/۳۳/۶۰،
مالیات و حذفِ دمو اعمال نشده، و هر پنج موردِ M-12 زنده است (با کنترلِ مثبت). شاخه‌ی بازاریابی با
**صفر تداخل** به main می‌رود و حالا با main به‌روز است. نُه تصمیمِ مالک در §۴ یک‌جا جمع شده، هرکدام با
پیشنهاد. §۷ ناخواسته: انتقالِ ۰۹-۱۷ به ریپوی خصوصی در **نوکِ درخت** ناقص ماند و **هر دو فایل همین
الان عمومی‌اند** — یکی روی `main`، یکی روی شاخه‌ی pushشده؛ مخزن عمومی است (اندازه‌گیری‌شده:
`"private": false`، و fetchِ بی‌احراز از raw.githubusercontent → HTTP ۲۰۰، ۱۸٬۷۸۴ بایت). تصحیحِ
خودم را همان‌جا ثبت کردم: نسخه‌ی اولِ §۷ گفته بود فایلِ دوم هنوز عمومی نشده — غلط بود، و در جهتی که
مسئله را کوچک‌تر نشان می‌داد. ⚠️ `rezv-87` دیگر resolve نمی‌شود — این پرونده است نه پیام، و
مسیریابی با مالک است.»
