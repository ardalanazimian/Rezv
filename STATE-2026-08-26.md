# رزرونو — وضعیتِ راستی‌آزمایی‌شده‌ی مخزن · ۲۰۲۶-۰۸-۲۶

> **این سند «منبعِ وضعیت» است، نه ممیزیِ تازه.** هر ادعایش با اجرایِ واقعیِ
> دستور یا خواندنِ کدِ همان لحظه تأیید شده، و روشِ تأیید کنارش نوشته است.
> آنچه تأیید نشده، صریح «تأییدنشده» علامت خورده.
>
> قاعده‌ی حاکم (از `CLAUDE.md`): **یک ادعای «چک‌شده» فقط تا تاریخِ همان چک
> معتبر است.** پیش از تکیه به هر خطِ این فایل، خودت با `ls`/`grep` تأییدش کن.
>
> پایه: کامیتِ `dc6dedb` · برنچِ `reconcile/audit-plus-features`

---

## ۰. مهم‌ترین یافته‌ی این پاس — یک الگو، نه یک باگ

در سه اپِ مستقل، با کدهای کاملاً جدا، **یک اشتباهِ واحد** پیدا شد:

> ### «نتوانستیم بارگیری کنیم» به «وجود ندارد» ترجمه می‌شود.

| اپ | جا | کاربر چه می‌بیند | واقعیت |
|---|---|---|---|
| `apps/seo` | `r/[slug]/page.tsx:41` | صفحه‌ی رستوران **۴۰۴** | رستوران هست؛ API یک لحظه قطع بود |
| `apps/landing` | `order/[code]/page.tsx:88` | «درخواستی با این کد پیدا نشد — کد را دوباره بررسی کنید» | کاربر واقعاً خرید کرده؛ API در دسترس نبود |
| `apps/business` | `crm.js:50` | «هنوز نظری ثبت نشده · ۰.۰ · ۰ نظر» | رستوران واقعاً ۴۰ نظر دارد |
| `apps/business` | `crm.js:1068` | ✅ سبزِ «فعلاً مشتریِ نیازمندِ پیگیریِ فوری نیست» | لیست اصلاً بارگیری نشده |
| `apps/business` | `crm.js:600` | «۰ مشتری VIP» کنارِ اعدادِ واقعی | فراخوانی شکست خورده |

**چرا این یک الگوست و نه تصادف:** در `crm.js` خطوطِ ۵۸۶–۵۹۷ یک رفعِ *قبلی*
وجود دارد که برایِ `rfmRes` حالتِ خطای درست می‌سازد، با کامنتی که صریح
می‌گوید **«خطای واقعی باید دیده شود، نه پنهان»**. دو خط پایین‌تر، خطوطِ
۶۰۰ و ۶۰۱ دقیقاً همان اشتباه را برایِ دو فراخوانیِ دیگرِ همان `Promise.all`
تکرار می‌کنند. یعنی تیم یک نمونه را دید و رفع کرد و دو نمونه‌ی کنارش را ندید.

**اصلِ درست (پروتکل §۹/§۱۰، در جهتِ معکوس):** سه حالت هرگز نباید یکی شوند —
«هست»، «نیست» (بالادست صریح گفت ۴۰۴/خالی)، «نمی‌دانیم» (شکستِ زیرساخت).
صفر فقط وقتی مجاز است که **اندازه گرفته باشیم**؛ وگرنه باید «—» یا حالتِ خطا
با «تلاش دوباره» باشد. این همان قاعده‌ای است که `docs/ML_CONTRACT.md` برایِ
مدل‌ها گذاشته (`insufficient_data` به‌جای صفر) — ولی در UI رعایت نشده بود.

---

## ۱. وضعیتِ گیت‌ها — همه اجرا شد، همه سبز

| گیت | دستور | نتیجه |
|---|---|---|
| دیزاین‌سیستم | `sh tools/sync-design-system.sh --check` | ✅ صفر مغایرت |
| بسته‌ی آفلاین | `python tools/build-standalone.py --check` | ✅ هم‌خوان (+ هشدارِ تازه، §۴.۳) |
| تایپ‌چکِ بک‌اند | `cd api && npx tsc --noEmit` | ✅ صفر خطا |
| لینتِ بک‌اند | `cd api && npm run lint` | ✅ صفر خطا |
| تستِ بک‌اند | `cd api && npm test` | ✅ **۹۴۶ / ۹۴۶** (۹۳۶ پایه + ۱۰ تستِ تازه‌ی پیامک) |
| landing | tsc · lint · test · build | ✅ **۷۸ / ۷۸** (۷۱ + ۷ تستِ تازه) |
| seo | tsc · lint · test · build | ✅ **۳۴ / ۳۴** (۲۲ + ۱۲ تستِ تازه) |
| E2E | `cd e2e && npx playwright test` | ✅ **۴۰۰ پاس · ۲ skip · ۰ شکست** — هر سه پروفایل (بعد از تغییرِ پنل هم دوباره اجرا شد) |
| وابستگی‌ها | `npm audit` در هر ۴ پکیج | ✅ **صفر آسیب‌پذیری** |

**جمع: ۱۰۵۸ تستِ واحد/یکپارچگیِ سبز + ۴۰۰ تستِ E2E.**

> ۲۹ تستِ تازه در این پاس: ۱۲ (seo) + ۷ (landing) + ۱۰ (پیامک). هیچ تستی حذف
> یا ضعیف نشد.

> ⚠️ `docs/recovery/BASELINE-TEST-STATUS.md` هنوز می‌گوید «۲ high severity
> vulnerabilities» و «۵۱۰/۵۱۰». هر دو **کهنه‌اند**. نمونه‌ی همان قاعده‌ی بالا.

### محیطِ لازم برای تست‌های بک‌اند
```bash
docker start rezv-test-pg rezv-test-redis
export DATABASE_URL="postgresql://test:test@localhost:55432/rezervno_test"
export REDIS_URL="redis://localhost:56379"
export JWT_ACCESS_SECRET="testsecret_access_0123456789abcdef"
export JWT_REFRESH_SECRET="testsecret_refresh_0123456789abcdef"
```
🚨 پیش از هر `npm install` حتماً `unset NODE_ENV`. در این ماشین
`NODE_ENV=production` ست است و طبقِ `CLAUDE.md` کلِ زنجیره‌ی ابزار را پاک می‌کند.

---

## ۲. معماری — اعدادِ شمرده‌شده‌ی امروز

| چیز | عدد | روشِ شمارش |
|---|---|---|
| routeهای API | **۱۴۶** | `find api/src/app/api -name route.ts` |
| ماژول‌های `api/src/lib` | **۸۴** | `ls` |
| مدل‌های Prisma | **۶۶** | `grep '^model '` |
| migrationهای SQL | **۶۴** | `ls api/prisma/sql/*.sql` |
| جاب‌های CI | **۹** (بود ۸) | `.github/workflows/ci.yml` |

⚠️ **تفاوتی که هر تحلیلی باید بداند:** `apps/customer` با
`<script type="module">` لود می‌شود — تابع باید صریح رویِ `window` بنشیند.
ولی `apps/business` و `apps/company` اسکریپتِ **classic** دارند — تابعِ
سطحِ‌بالا خودش global است. بدونِ این تفکیک، هر اسکنِ «دکمه‌ی مرده»
**۱۸۷ false-positive** می‌دهد (خودم اول در همین دام افتادم).

---

## ۳. آنچه راستی‌آزمایی شد و **سالم بود**

نتایجِ منفیِ ارزشمند — بررسی شدند، مشکلی نبود:

| دامنه | روش | نتیجه |
|---|---|---|
| کدِ مرده در `api/src/lib` | استخراجِ **همه‌ی** صادراتِ ۸۴ ماژول + شمارشِ ارجاع در `api/src`، `api/tests`، `cron`، `observability`، `tools`، `.github` | **صفر** صادراتِ بدونِ ارجاع |
| routeهای یتیم | اسکنِ هر ۱۴۶ route مقابلِ همه‌ی مصرف‌کننده‌ها | دقیقاً همان **۴** موردِ از‌قبل‌مستند |
| جداسازیِ تنانت | هر routeِ `/v1/restaurant/**` | **۱۰۰٪** از `with-restaurant-auth` استفاده می‌کند |
| گاردِ ادمین / کاربر / نگهداری | هر routeِ `/v1/admin/**`، `/v1/me/**`، `/v1/maintenance/**` | **۱۰۰٪** گارد دارند |
| چرخه‌ی عمرِ رزرو | همه‌ی نقاطِ تغییرِ وضعیت | تک‌مسیره — `transitionReservation` تنها نویسنده؛ بقیه فقط `depositStatus`/`cancelReason` |
| double-booking | `prisma/sql/011,016,026` | `EXCLUDE USING gist (table_id, tsrange(slot_start, block_end))` — در سطحِ DB |
| idempotencyِ migrationها | هر ۶۴ فایل | همه idempotent |
| ارجاعِ فایل در HTML | هر `src`/`href` سه پنل | صفر مسیرِ شکسته |
| fake-success در پنل‌ها | هر `toast()`ِ ادعای-موفقیت + بررسیِ `await`ِ شبکه | صفر یافته‌ی واقعی (۴۵ بررسی، همه false-positive) |
| دکمه‌ی مرده | هر شناسه‌ی `on*=` مقابلِ تعریفش | صفر |
| cronها | `cron/crontab` مقابلِ routeها | هر ۸ جابِ نگهداری زنده |
| تکرار در `lib` | خوشه‌های reservation / loyalty / notify | عاملِ بررسی‌کننده **صفر تکرار** گزارش کرد ⚠️ *(این سه لنز نیمه‌تمام ماندند — §۸)* |

---

## ۴. آنچه در این پاس **رفع شد**

### ۴.۱ 🔴 قطعیِ API → ۴۰۴ِ کش‌شده در `apps/seo` و `apps/landing`
`fetchRestaurant` / `fetchPublicMenu` / `fetchRestaurantList` / `getOrderStatus`
در **هر** شکستی `null`/`[]` می‌دادند و صفحه‌ها رویش `notFound()` می‌زدند.
چون ISR است (`revalidate = 300`)، آن ۴۰۴ **کش می‌شد** و به Googlebot می‌رسید ⇒
حذف از ایندکس — یعنی نابودیِ دلیلِ وجودیِ `apps/seo` (ADR 0001).

**راستی‌آزمایی با مستندِ خودِ Next.js v16.2.9 (نه استنتاج):**
- مسیرِ not-found **کش می‌شود** (ورودیِ کش با `value: null` + همان `revalidate`) ⇒ ۴۰۴ِ اشتباه می‌ماند و سرو می‌شود.
- در مقابل **throw** هنگامِ revalidate ⇒ «آخرین صفحه/دادهٔ موفق همچنان از کش سرو می‌شود». یعنی رفع نه‌تنها ۴۰۴ را می‌بندد، **صفحه‌ی سالم را هم سرِ جایش نگه می‌دارد** (۵۰۰ فقط وقتی هیچ نسخه‌ی کش‌شده‌ای نباشد).
- همان مستند صریحاً همین را توصیه می‌کند: «به‌جای return یک خطا throw کن تا کش تا درخواستِ موفقِ بعدی به‌روز نشود».

**رفع:** `UpstreamUnavailableError` (۴۰۴ → `null` → `notFound()`؛ هر شکستِ
زیرساختی → throw) + پارامترِ `strict` برایِ فهرست‌هایی که تصمیمِ ۴۰۴ می‌سازند
+ `OrderLookup` سه‌حالته در landing. **۱۹ تستِ رگرسیون.**

### ۴.۲ 🟠 گیتِ اجباریِ `build-standalone` در CI وجود نداشت
`CLAUDE.md` §۴b آن را «اجباری» اعلام کرده و یک حادثه‌ی واقعی را مستند می‌کند،
ولی **هیچ جابی اجرایش نمی‌کرد**. جابِ `standalone` اضافه شد (CI: ۸ → ۹).
**گیتی که فقط در markdown زندگی می‌کند، گیت نیست.**

### ۴.۳ 🟠 `standalone/website.html` آرتیفکتِ بی‌صاحب بود
`README-website.md` آن را به کاربر می‌فروشد، ولی `build-standalone.py` فقط
`(customer, business, company)` را می‌سازد ⇒ `--check` هرگز نگاهش نمی‌کرد و با
وجودِ **۴۰ کامیت کهنگی** «✓ هم‌خوان است» چاپ می‌کرد (اسنپ‌شاتِ ۲۰۲۶-۰۸-۲۰، از
قبلِ حذفِ وابستگیِ Google Fonts). این همان تله‌ی §۴b است یک لایه بالاتر:
**گیتی که کهنگی را نمی‌بیند، از نبودِ گیت بدتر است — اطمینانِ کاذب می‌دهد.**
**رفع:** `--check` حالا آرتیفکتِ بی‌صاحب را صریح اعلام می‌کند؛ README صریح
می‌گوید اسنپ‌شاتِ تاریخ‌دار است. حذف/بازتولیدش **تصمیمِ محصولی** است (§۸).

### ۴.۴ سرتیترِ گمراه‌کننده‌ی migration
`059-telemetry-trust-boundary.sql` سرتیترش «046» بود. اصلاح شد. تصادمِ شماره‌ی
۰۵۹ عمداً **تغییرِ نام نداد** (اثرِ اجرایی ندارد؛ تغییرِ نام دو سندِ ارجاع‌دهنده
را می‌شکند).

---

## ۵. ✅ P0ِ رفع‌شده — متنِ کمپین حالا واقعاً ارسال می‌شود

| لایه | واقعیت |
|---|---|
| UI | `apps/business/js/marketing.js:156` — «**دقیقاً همینطوری برای مشتری ارسال می‌شه**»، و `confirmCamp()`: «این کار برگشت‌پذیر نیست» |
| کلاینت | خطِ ۱۸۱ — `API.sendSms({kind:'campaign', segment, message: window._campMessage})` |
| اسکیمای سرور | `sms/route.ts:15` — `message` را می‌پذیرد ✅ پس **۲۰۰ برمی‌گردد** |
| مسیرِ ارسال | `sms/route.ts:~83` — `tokens = [t.name یا 'مهمان', restaurant.name]` |
| **تنها جایی که `b.message` استفاده می‌شود** | **فقط خطِ ۱۰۷** → `campaignLog.message` (لاگِ DB). **هرگز به `enqueueSms` نمی‌رسد.** |

⇒ رستوران‌دار متن می‌نویسد، تأیید می‌کند، **پولِ واقعیِ پیامک خرج می‌شود**، و
مشتری متنِ کاملاً دیگری می‌گیرد. تأییدشده با خواندنِ هر سه لایه.

**دو یافته‌ی همراهِ تأییدشده:**
- **نگاشتِ سگمنت شکسته** — `marketing.js:153` مقادیرِ `at_risk`/`all` می‌فرستد، ولی `sms/route.ts:13` فقط `z.enum(['gold','silver','bronze'])` می‌پذیرد ⇒ **۳ از ۴ کارتِ سگمنت همیشه ۴۰۰** می‌گیرند؛ فقط VIP کار می‌کند. ضمناً نگاشتِ «تولد این ماه» → `all` اگر پذیرفته می‌شد به **همه** پیامک می‌داد، نه متولدین.
- **کدِ تخفیفِ جعلی + نامِ هاردکد** — `marketing.js:144` کدی از `Math.random()` می‌سازد که در جدولِ کوپن‌ها **وجود ندارد**، و `fa()` ارقامش را فارسی می‌کند («SPECIAL۷۳۴» — غیرقابلِ تایپ). نامِ فرستنده «کافه‌رستوران ویستا» در خطِ ۱۵۸ هاردکد است و به هر تنانتی همان را نشان می‌دهد.

### رفع (۲۰۲۶-۰۸-۲۶) — راهِ «ب»، که با مهاجرتِ ارائه‌دهنده ممکن شد

علتِ ریشه‌ای این بود که **کاوه‌نگار اصلاً نمی‌توانست متنِ آزاد بفرستد** — فقط
`verify/lookup`ِ الگومحور داشت. پس متنِ رستوران‌دار جایی برای رفتن نداشت و
بی‌صدا دور ریخته می‌شد.

با مهاجرت به **ملی‌پیامک** (§۵.۱) مسیرِ `SendSMS`ِ متن‌آزاد باز شد و
`b.message` حالا واقعاً به `enqueueSms` می‌رسد
(`sms/route.ts` — `customText`)، با جای‌گذاریِ `{نام}` به‌ازای هر گیرنده.
یعنی ادعای «دقیقاً همینطوری برای مشتری ارسال می‌شه» **حالا درست است**.

⚠️ متنِ آزاد خطِ اختصاصی می‌خواهد (`MELIPAYAMAK_FROM`). اگر تنظیم نباشد،
لایه‌ی sms **صریح شکست می‌خورد و لاگ می‌کند** — عمداً به قالب برنمی‌گردد،
چون آن دقیقاً همان باگِ اصلی را برمی‌گرداند.

**کدِ تخفیفِ جعلی هم رفع شد:** `pickSeg` دیگر کدی از `Math.random()` نمی‌سازد
(کدی که در جدولِ کوپن‌ها وجود نداشت و `fa()` ارقامش را هم غیرقابلِ‌تایپ
می‌کرد)، و نامِ رستوران از `RESTAURANT.name`ِ واقعی می‌آید نه رشته‌ی هاردکدِ
«کافه ویستا».

### ❗ هنوز باز: نگاشتِ سگمنت

`marketing.js` مقادیرِ `at_risk`/`all` می‌فرستد ولی بک‌اند فقط
`z.enum(['gold','silver','bronze'])` می‌پذیرد ⇒ **۳ از ۴ کارتِ سگمنت همیشه
۴۰۰** می‌گیرند؛ فقط VIP کار می‌کند. عمداً رفع **نشد**: رفعِ درست یعنی تصمیم
درباره‌ی اینکه «مشتری جدید» و «در خطر ریزش» اصلاً چه مخاطبی هستند — یک
تصمیمِ محصولی، نه یک نگاشتِ فنی. (مسیرِ `phones:[…]` از قبل هست و
`sendBirthdayGreetings` با موفقیت از آن استفاده می‌کند، پس پیاده‌سازی‌اش
آسان است — فقط تعریفِ مخاطب لازم است.)

---

## ۵.۱ مهاجرتِ ارائه‌دهنده‌ی پیامک: کاوه‌نگار → ملی‌پیامک (۲۰۲۶-۰۸-۲۶)

درخواستِ اولیه «این SDKِ پایتون را نصب کن» بود. **نصب نشد و لازم هم نبود:**
`api/Dockerfile` رویِ `node:20-alpine` است و هیچ رانتایمِ پایتونی ندارد. در
عوض همان REST API مستقیم در TypeScript پیاده شد — **صفر وابستگیِ جدید**.

| | کاوه‌نگار (قبل) | ملی‌پیامک (حالا) |
|---|---|---|
| الگو | با **نام** (`rezervno-otp`) | با **شناسه‌ی عددی** (`bodyId`) |
| endpoint | `verify/lookup` | `BaseServiceNumber` |
| متنِ آزاد | ❌ ندارد | ✅ `SendSMS` + خطِ اختصاصی |

- قراردادِ `SmsJob` **دست‌نخورده** ماند ⇒ هر ۹ فراخوان‌کننده بدونِ تغییر کار می‌کنند.
- `bodyId` نداری؟ آن نوع پیامک **ارسال نمی‌شود و صریح لاگ می‌شود** — هیچ الگویِ حدسی‌ای جایگزین نمی‌شود.
- تفکیکِ موفق/ناموفق قفل شد: ارائه‌دهنده خطا را **داخلِ بدنه‌ی ۲۰۰** برمی‌گرداند، پس فقط نگاه‌کردن به `res.ok` هر ردی را «ارسال شد» گزارش می‌کرد. **۱۰ تستِ رگرسیون** اضافه شد.
- ⚠️ **راستی‌آزمایی‌نشده:** جداکننده‌ی توکن‌های الگو (`;`) را ملی‌پیامک عمومی مستند نکرده و هیچ‌کدام از SDKهای رسمی‌اش نمونه‌ی چندتوکنی ندارند. قابلِ‌تنظیم گذاشته شد (`MELIPAYAMAK_TOKEN_SEPARATOR`) ولی **باید از پنلِ خودت تأیید شود**.

**یک باگِ واقعی که همین مهاجرت لو داد:** تستِ تازه در اجرایِ تک‌فایل سبز بود
ولی در سوئیتِ کامل **دو تستِ کوپن را هم می‌انداخت**. علت در خودِ کد بود، نه
تست: `bodyId`ها در زمانِ **لودِ ماژول** خوانده می‌شدند در حالی که اعتبارنامه‌ها
در زمانِ **فراخوانی** — یک ناسازگاری که هم تغییرِ env را نیازمندِ ری‌استارت
می‌کرد و هم تست را مجبور به `await import()` (که ترتیبِ رانر را به‌هم می‌ریخت).
هر دو lazy شدند. **سوئیتِ کامل: ۹۴۶/۹۴۶.**

---

## ۶. یافته‌های بازِ دیگر (تأییدشده، رفع‌نشده)

| # | یافته | چرا رفع نشد |
|---|---|---|
| ۱ | `crm.js:50` و `~36` — `loadReviews`/`loadGallery` شاخه‌ی `else` ندارند ⇒ شکست = «۰ نظر / ۰ عکس» | رفعش ساده است ولی به تصمیمِ الگویِ §۰ گره می‌خورد؛ بهتر است یک‌جا و یکنواخت انجام شود |
| ۲ | `crm.js:1068` — شکستِ `crmRecommendations` = ✅ سبزِ «کسی نیاز به پیگیری ندارد» | همان |
| ۳ | `crm.js:600/601` — `vipRes`/`aiRes` شکست = «۰ مشتری VIP» و حذفِ بی‌صدای باکس | همان — **دو خط زیرِ کامنتی که همین کار را ممنوع کرده** |
| ۴ | `crm.js:837` — «تعطیلاتِ خاص» فقط در حافظه می‌ماند؛ `_hoursDirty` نوشته می‌شود ولی **هیچ‌جا خوانده نمی‌شود** ⇒ کاربر تعطیلی را می‌بیند، تب عوض می‌کند، بی‌صدا از بین می‌رود و رستوران آن روز رزرو می‌گیرد | نیازِ تصمیم: ذخیره‌ی مستقیم یا برچسبِ «ذخیره‌نشده» + گاردِ خروج |
| ۵ | `crm.js:1001` — شمارِ «تولد این ماه» از `CLUB`ِ محلی که در این مسیر هرگز پر نمی‌شود؛ ضمناً در `selectBranch` ریست نمی‌شود ⇒ عددِ شعبه‌ی قبلی نشان داده می‌شود | همان خانواده‌ی §۰ |
| ۶ | `crm.js:474` — ورود به تبِ «مشتریان» هر چهار زیرتب را eager رندر می‌کند: **۹ درخواست که ۲ تایش تکراری است** | بهینه‌سازی، نه باگ؛ نیازِ اندازه‌گیری قبل از تغییر (§۲۶) |
| ۷ | `POST /waitlist/:id/accept` هدرِ `Idempotency-Key`ی را که کلاینت **می‌فرستد** نادیده می‌گیرد | دابل‌تپ خطرناک نیست (`acceptOffer` ادعای اتمیک دارد)، ولی رویِ retryِ پس از گم‌شدنِ پاسخ، کاربری که رزروش ساخته شده «مهلت آفر تمام شد» می‌گیرد. رفع = مسیرِ رزرو ⇒ PR + تستِ همزمانی |
| ۸ | `standalone/website.html` بازتولید نمی‌شود | ساختِ builder برای اپِ Next.js با مسیرِ پویا کارِ جدید است، نه پاک‌سازی |
| ۹ | ۴ backendِ بدونِ UI: `/admin/settings` · `/me/incentives` · `/restaurant/fraud-signals` · `/restaurant/reports` | قابلیتِ واقعی‌اند، نه کدِ مرده. حذف ممنوع (§۸) |
| ۱۰ | دو سندِ رقیبِ «دانشِ پروژه»: `PROJECT-KNOWLEDGE.md` (ریشه، ۲۰۲۶-۰۷-۳۰) و `docs/PROJECT_KNOWLEDGE.md` (مدعیِ canonical) | هر دو ادعای مرجع‌بودن دارند، یکی کهنه است. ادغام تصمیمِ مالکِ سند است |
| ۱۱ | ۱۰۷+ فایلِ markdown (۲۲ ریشه، ۸۵ در `docs/`) | بارِ ناوبریِ واقعی، ولی حذفِ سندی که تصمیمِ واقعی ثبت کرده ممنوع است |
| ۱۲ | ۱۴ یافته‌ی lowِ `apps/customer` + ۸ یافته‌ی a11yِ راستی‌آزمایی‌نشده | از پاسِ قبلی — `docs/recovery/OPEN-FINDINGS.md` |

> یافته‌های ۵ لنزِ دیگر (`co-sales`، `ml-honesty`، `dead-exports` و…) در
> خروجیِ خامِ workflow هست و **هنوز تک‌تک راستی‌آزمایی نشده‌اند** — طبقِ §۲
> پروتکل تا راستی‌آزمایی، اقدام نمی‌شود.

---

## ۷. ابزارِ بیرونی (MCP) — تأییدشده با فراخوانیِ واقعی

| سرویس | وضعیت |
|---|---|
| **graphify** | ✅ کار می‌کند — ولی ایندکسش رویِ `8a6aeec`، **۶۶ کامیت عقب**. هر پاسخش وضعیتِ قدیمی می‌دهد (هنوز امضایِ قدیمیِ `getOrderStatus` را نشان می‌داد). فقط سرنخ، همه‌چیز محلی تأیید شد |
| **Context7** | ✅ — برای تأییدِ سمنتیکِ ISRِ Next.js استفاده شد (§۴.۱) |
| **Supabase** | ⚠️ پروژه‌ها را لیست می‌کند (`rezervno` ACTIVE_HEALTHY · PG17) ولی **کوئری نمی‌زند**: `28P01 password authentication failed` برایِ هر دو نقشِ `postgres` و `supabase_read_only_user` ⇒ **راستی‌آزماییِ اسکیمای تولید ممکن نشد** |
| **Vercel** | ✅ متصل — ولی **صفر پروژه**؛ چیزی برای بررسی نیست |
| **Prisma-Local** | ❌ ناسازگار: MCP با Prisma CLI **7.9.1** اجرا می‌شود، پروژه رویِ **5.20** ⇒ `P1012` |
| **Figma · NotebookLM** | ✅ احراز شده (به این کار ربطی نداشتند) |
| **Sentry · LlamaParse · Postman** | ❌ خطای اتصال |
| Cloudflare · Google Calendar · Google Drive · HubSpot · Linear · Mobbin · Semrush · Unblocked | 🔒 **احراز هویت نشده** — نشستِ غیرتعاملی؛ از تنظیماتِ connectorهای claude.ai یا `/mcp` در نشستِ تعاملی مجاز کن |

---

## ۸. صداقتِ روش — چه چیزی انجام **نشد**

- **fan-outِ چندعاملی دو بار شکست خورد و بارِ سوم نیمه‌کاره ماند.**
  دورِ اول: **۰ از ۱۲** عامل (۳٫۹M توکن، ۷۱۵ فراخوانِ ابزار، صفر نتیجه).
  دورِ دوم/سوم با دامنه‌ی باریک: **۵ از ۱۴** (۲٫۰M توکن) — همان‌هایی که
  یافته‌های §۵ و §۶ را دادند. علت در هر سه بار یکی بود:
  `Connection lost mid-response`. این **دقیقاً** همان ناپایداری‌ای است که
  `docs/recovery/OPEN-FINDINGS.md` §۱ و §۳ از قبل ثبت کرده بود.
  ⇒ بیشترِ §۱–§۴ **مستقیم و مکانیکی** انجام شد، نه با عامل.
- **لنزهایی که هرگز کامل نشدند و هیچ ادعایی درباره‌شان نمی‌شود:**
  `biz-data` · `biz-resv` · `biz-overview` · `co-intel` · `co-rest` ·
  `lib-resv-dup` · `lib-loyalty-dup` · `lib-notify-dup` · `docs-claude-md`.
  (پس «صفر تکرار در `lib`» در §۳ **ضعیف** است — لنزش نیمه‌تمام ماند.)
- **هزینه‌ی زمانِ اجرا در `apps/business`/`apps/company`** — همچنان اندازه‌گیری
  نشده، مثلِ پاسِ قبلی.
- **اسکیمای تولید راستی‌آزمایی نشد** (Supabase قابلِ کوئری نبود) — کورِ کامل.
- **دو اشتباهِ خودم که پیش از گزارش گرفته شد** (ثبت می‌شود چون روش را نشان می‌دهد):
  ۱. اول فکر کردم `sms.ts:67` فقط `tokens[0]` را می‌فرستد؛ خطوطِ ۶۸–۶۹
  `token2`/`token3` را می‌فرستند — **باگ نبود**.
  ۲. اسکنِ اولِ «دکمه‌ی مرده» **۱۸۷ false-positive** داشت تا وقتی تفاوتِ
  module/classic لحاظ شد.

---

## ۹. گامِ بعدی، به ترتیبِ ارزش

1. **تصمیم درباره‌ی P0ِ کمپین** (§۵) — تنها موردی که پولِ واقعی و اعتمادِ
   رستوران‌دار را هم‌زمان درگیر می‌کند.
2. **رفعِ یکنواختِ الگویِ §۰** در `crm.js` (۵ نقطه) — یک هِلپرِ واحد
   («نمی‌دانیم» ≠ «نیست») به‌جای رفعِ تک‌به‌تک، وگرنه نمونه‌ی ششم هم جا می‌ماند.
3. **نگاشتِ سگمنت + کدِ تخفیفِ جعلی** — مستقل از تصمیمِ ۱، همین حالا قابلِ رفع.
4. **`_hoursDirty` که خوانده نمی‌شود** (§۶.۴) — از دست رفتنِ بی‌صدای تعطیلی
   یعنی رزروگرفتن در روزِ تعطیل.
5. **رمزِ Supabase را برای connector درست کن** تا انحرافِ اسکیمای تولید قابلِ
   بررسی شود.
6. **ایندکسِ graphify را بازبساز** (۶۶ کامیت عقب، وگرنه گمراه‌کننده است).
7. **لنزهای نیمه‌تمامِ §۸** — ترجیحاً مکانیکی/مستقیم، نه با fan-outِ عاملی، تا
   وقتی ناپایداریِ اتصال برطرف نشده.
