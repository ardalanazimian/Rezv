User-agent: *
Allow: /
Disallow: /api/

# ⚠️ ۲۰۲۶-۰۹-۰۸ — خطِ `Sitemap: https://rezervno.ir/sitemap.xml` از اینجا حذف شد.
#
# این فایل از `app.rezervno.ir` سرو می‌شود، ولی sitemap ی را برای میزبانِ
# **دیگری** اعلام می‌کرد. یک دستورِ Sitemap که به میزبانِ دیگری اشاره کند
# نادیده گرفته می‌شود مگر همان دامنه در Search Console تأییدشده باشد — یعنی
# در بهترین حالت بی‌اثر بود و در بدترین حالت گمراه‌کننده.
#
# پس از تصمیمِ D-004 (apex به `apps/landing` رسید)، sitemapِ apex را همان
# اپ منتشر می‌کند و اعلامش هم کارِ اوست، نه این اپ. اینجا هیچ sitemapی
# اعلام نمی‌شود چون این اپ هیچ نقشه‌ی سایتی ندارد.
#
# ⚠️ تصمیمِ باز که عمداً اینجا گرفته نشد: این اپ یک پوسته‌ی جاوااسکریپتی است
# (۹ تگِ script، بدونِ رندرِ سمتِ سرور) و چیزی برای ایندکس ندارد. `noindex`
# صادقانه‌تر است، ولی اگر روزی SSR اضافه شود آن را هم می‌بندد. تا وقتی مالک
# تصمیم نگرفته، خزیدن مجاز می‌ماند و صفحه صرفاً چیزی برای ایندکس ندارد —
# که ضررِ خنثی است، نه ادعای غلط.
