# RezervnoOSv2 - قوانین و راهنمای پروژه برای Claude

> این فایل قبلاً به‌خاطر یک خرابیِ کدگذاری (CRLF + از دست رفتنِ نشانه‌های Markdown) به‌سختی خوانا شده بود. در ۲۰۲۶-۰۸-۰۷ بازنویسی و با ساختارِ واقعیِ پروژه هماهنگ شد — محتوا همان قوانینِ قبلی است، فقط درست‌خوان و به‌روز شده.

## معماری کلی (برای درک ساختار)
- **پنج اپ مجزا** (نه سه‌تا — این بخش قبلاً قدیمی/غلط بود):
  - `apps/customer/` — اپ مشتری (کشف رستوران، رزرو، باشگاه مشتریان)
  - `apps/business/` — پنل رستوران (مدیریت رزرو، سالن، لیست انتظار، کارکنان)
  - `apps/company/` — پنل شرکت/پلتفرم (مدیریتِ تنانت‌ها، بازبینیِ عکس، صف فعال‌سازی)
  - `apps/landing/` و `apps/seo/` — وب‌سایتِ عمومی/مارکتینگ (Next.js + React جدا از بقیه)
  - `apps/customer`, `apps/business`, `apps/company` **Vanilla JS هستند، بدون build و بدون فریم‌ورک** (نه React) — همه‌ی اسکریپت‌های هر اپ یک scope مشترک دارند (بدون import/export ماژول، به‌جز `shared/js/icons.js` که هم‌زمان به‌صورتِ ES module هم classic script لود می‌شود).
- بک‌اند: `api/` با **Next.js 16** (App Router، Turbopack) — همه‌ی routeهای API اینجا هستند؛ خودِ اپ‌های پنل چیزی سرور-ساید ندارند.
- `apps/landing/` و `apps/seo/` هرکدام Next.js+React مستقلِ خودشان را دارند (package.json/typecheck/lint/test جدا).
- اشتراک‌گذاریِ کد بینِ سه پنل: پوشه‌ی `shared/` — شاملِ `shared/css/` (توکن‌های دیزاین)، `shared/js/` (چند اسکریپتِ کمکیِ مشترک: api-core، format، icons، analytics)، `shared/content/` (JSONِ محتوای مشترک). **shared/ کامپوننت/هوکِ React ندارد** — این هم قبلاً در این فایل اشتباه نوشته شده بود.
- دیتابیس: PostgreSQL با Prisma ORM. مسیرِ اسکیما **دومرحله‌ای** است و هر دو مرحله لازم‌اند:
  ۱) `prisma migrate deploy` → مهاجرتِ پایه‌ی `0_init` (ساختِ جداولِ اصلی)
  ۲) `api/prisma/apply-sql.sh` → SQLِ خامِ افزایشی در `api/prisma/sql/NNN-*.sql`
  ⚠️ **اصلاح‌شده (۲۰۲۶-۰۸-۲۰):** این خط قبلاً می‌گفت «اعمال‌شده با apply-sql.sh — نه
  `prisma migrate deploy`». غلط بود: `api/docker-entrypoint.sh` هر دو را اجرا می‌کند
  (خطِ ۳۹ `migrate deploy`، خطِ ۵۴ `apply-sql.sh`). این اشتباه بی‌اهمیت نبود —
  دروازه‌ی `tools/check-schema-drift.sh` دقیقاً روی همین مسیرِ دومرحله‌ای بنا شده،
  و `apply-sql.sh` به‌تنهایی نمی‌تواند از یک DBِ خالی اسکیما بسازد (مهاجرتِ ۰۰۱
  فرض می‌کند جداولِ پایه از قبل هستند).
- کش + rate-limit + pub/sub: Redis.
- احراز هویت: JWT (بدون NextAuth) — `AccessPayload` یا `{sub, kind:'customer'}` یا `{sub, kind:'staff', tenantId, role}`.
- حالت دمو: OTP ثابت `1234` — **دو مسیرِ متفاوتِ کاملاً قانونی، هیچ‌کدام را عوض نکن**:
  1. بک‌اند با `OTP_DEV_MODE=true` کدِ واقعیِ تصادفی را در پاسخِ API برمی‌گرداند (برای تستِ محلی/CI، نه ثابت روی 1234).
  2. کلاینت (مثلاً `apps/customer/js/auth.js`, `apps/business/js/staff-system.js`) وقتی بک‌اند اصلاً در دسترس نیست (`location.protocol==='file:'` یا پاسخِ آفلاین) به‌صورتِ محلی کدِ ثابتِ `1234` را می‌پذیرد — این fallbackِ کاملاً آفلاین است، نه رفتارِ عادیِ سرور.
- زبان UI: فارسی، راست‌چین (RTL) با فونتِ Vazirmatn — **self-hosted** از
  `shared/fonts/vazirmatn-var.woff2` (یک فایلِ variable، وزنِ ۱۰۰..۹۰۰)، نه از
  Google Fonts. عمدی است: `fonts.googleapis.com` در ایران در دسترس نیست و تا
  ۲۰۲۶-۰۸-۱۹ فونت بی‌صدا روی sans-serifِ سیستم می‌افتاد. `@font-face` در
  `shared/css/tokens.css` است و فایل با `sync-design-system.sh` به هر سه اپ
  کپی می‌شود. **هرگز `<link>`ِ Google Fonts را برنگردان.**

---

## 🚨 چک‌های اجباری قبل از هر پوش (Commit & Push)

**قبل از اینکه حتی به فکر `git push` بیفتی، یکی‌یکی این موارد رو چک کن. اگر هرکدوم خطا داد، پوش ممنوع:**

1. **همگام‌سازی طراحی (تفاوت صفر)**: از ریشه‌ی ریپو اجرا کن `sh tools/sync-design-system.sh --check` — باید خروجی «صفر مغایرت» بین `shared/` و `apps/*` نشون بده.
2. **تایپ‌چک، لینت و تست بک‌اند**: داخل پوشه‌ی `api/` (بعد از اجرای `npx prisma generate`) به ترتیب این سه دستور رو اجرا کن تا همه‌شون پاک (بدون خطا) باشن:
   - `npx tsc --noEmit`
   - `npm run lint`
   - `npm test`
   (همین سه دستور را برای `apps/landing/` و `apps/seo/` هم اگر آن‌ها را تغییر دادی جداگانه اجرا کن — هرکدام package.json و تست‌های خودشان را دارند.)
3. **تست E2E Playwright**: حتماً برای بخش‌هایی که تغییر دادی، تست‌های E2E باید موبایل + دسکتاپ سبز بشن. (CI دقیقاً روی iPhone 13 / Pixel 5 / Desktop Chrome اجرا می‌شه. تستی که فقط روی دسکتاپ پاس بشه قبول نیست.)
4. **بررسی فایل‌های مرجع**: تمامِ ارجاع‌های script و css در فایل‌های HTML و همچنین تمامِ importهای ماژول‌های ES باید به یک فایلِ واقعی و موجود اشاره کنن (هیچ مسیر شکسته‌ای نباشه).
4b. **تازگیِ بسته‌ی آفلاین**: اگر چیزی در `apps/customer`, `apps/business` یا
   `apps/company` عوض کردی، از ریشه اجرا کن `python tools/build-standalone.py --check`.
   ⚠️ **چرا این یک گیتِ اجباری است** (یافته‌ی واقعیِ ۲۰۲۶-۰۸-۲۳): `standalone/*.html`
   خروجیِ **تولیدشده‌ی commit‌شده** است، نه منبع. از ۲۰۲۶-۰۸-۱۸ بازتولید نشده بود
   و در نتیجه بسته‌ی آفلاین — همان چیزی که `standalone/README-website.md` به
   کاربر می‌گوید بازش کند — هنوز **همه‌ی** باگ‌هایی را تحویل می‌داد که در منبع رفع
   شده بودند، از جمله یک P0 که کلِ مسیرِ رزرو را با بک‌اندِ واقعی می‌شکست.
   آرتیفکتی که بی‌صدا کهنه شود از نبودش بدتر است: ظاهرِ به‌روز دارد ولی کدِ
   قدیمی می‌دهد. اگر check قرمز شد، `python tools/build-standalone.py` را اجرا کن.
5. **حریمِ امنیتی**: هرگز secret، key یا فایلِ `.env` واقعی رو کامیت نکن.
6. **دیتای دمو**: اگر داده‌ی آزمایشی اضافه می‌کنی، حتماً برچسبِ `[DEMO]` روش بذار. هرگز اسمِ رستوران‌های واقعی رو جعل نکن.
7. **دروازه‌ی انحرافِ اسکیما** (اگر `schema.prisma` یا `prisma/sql/` را دست زدی): از ریشه اجرا کن
   `ADMIN_URL=postgresql://…/postgres sh tools/check-schema-drift.sh` — باید «بدونِ انحراف» بدهد.
   ⚠️ چرا این جدا از تایپ‌چک و تست است: `db push` (مسیرِ CI) و `migrate deploy + apply-sql.sh`
   (مسیرِ تولید) دو اسکیمای متفاوت می‌سازند. اگر فیلدی به `schema.prisma` اضافه کنی و مهاجرتِ
   SQL ننویسی، **همه‌ی تست‌ها سبز می‌شوند و تولید در زمانِ اجرا می‌شکند**. این دقیقاً همان
   «CI سبز، تولید خراب» است که هیچ تستی نمی‌گیردش. در CI جابِ `schema-drift` همین را اجرا می‌کند.
8. **مهاجرتِ جدید**: همیشه فایلِ `api/prisma/sql/NNN-*.sql` با شماره‌ی *بعدی* بساز و idempotent
   بنویسش (`IF NOT EXISTS`). **هرگز فایلِ مهاجرتِ قبلی را ویرایش نکن** — روی DBهایی که آن را
   از قبل اجرا کرده‌اند دوباره اجرا نمی‌شود.

> **وضعیتِ فعلیِ CI (۲۰۲۶-۰۸-۲۰):** ۸ جاب — `test`, `build`, `lint`/`security`,
> `design-system`, `schema-drift`, `e2e`, `landing`, `seo`. همه باید سبز باشند (merge-on-green).

---

## 📝 قوانین کامیت و گزارش‌دهی (صداقت در کار)
- **پیامِ کامیت**: حتماً فارسی بنویس و دقیقاً مشخص کن:
  - چه کاری انجام شده؟
  - چرا این کار انجام شده؟
  - «تست شده» یا «فقط تایپ‌چک»؟ (هرگز بیش از حد ادعا نکن و نگو «تست شده» مگر اینکه واقعاً اجرا/تست کرده باشی — تایپ‌چکِ پاک به‌تنهایی دلیل نمی‌شود چیزی واقعاً کار می‌کند.)
- **تغییراتِ پرریسک** (مثلِ تغییر در اسکیماهایِ دیتابیس، سیستمِ احرازِ هویت، منطقِ رزرو یا قفل‌هایِ همزمانی/double-booking): حتماً به‌جایِ پوشِ مستقیم به `main`، یک Pull Request باز کن و منتظر بمان تا تأیید و CI سبز بشه.
- اندازه‌یِ PRها رو کوچک و تک‌منظوره نگه دار و فقط زمانی مرج کن که CI کاملاً سبز باشه (merge-on-green).
- اگر باگیِ واقعی با تستِ end-to-end (نه فرض) پیدا کردی، در پیامِ کامیت/توضیحِ PR صریح بگو با چه روشی پیدا شد (curl مستقیم، مرورگرِ واقعی، …) — این خودش شواهدِ کیفیتِ کار است.

---

## دستورات پرکاربرد
⚠️ **ریشه‌یِ ریپو `package.json` ندارد** — بجز دو شل‌اسکریپتِ سراسری (`tools/sync-design-system.sh` و `tools/check-schema-drift.sh`) و دستوراتِ `docker compose`، همه‌یِ دستورهایِ npm باید **داخلِ `api/`** (یا `apps/landing/`, `apps/seo/` برایِ همان اپ‌ها) اجرا شوند، نه از ریشه.

🚨 **`npm install` را با `NODE_ENV=production` اجرا نکن** (یافته‌ی واقعیِ
۲۰۲۶-۰۸-۲۳، تأییدشده با `--dry-run`): وقتی `NODE_ENV=production` در محیط ست
باشد، npm خودکار `omit=dev` می‌گیرد و یک `npm install`ِ ساده **کلِ زنجیره‌ی
ابزار را پاک می‌کند** — `typescript`، `tsx`، `eslint`، `@typescript-eslint/*`،
`esbuild` و در `e2e/` خودِ Playwright. اندازه‌ی خسارت در همین مخزن:
`api` −۱۲۳ بسته · `apps/landing` −۳۲۷ · `apps/seo` −۳۲۷ · `e2e` −۸۹.
نتیجه: هر چهار گیتِ اجباریِ بالا (typecheck/lint/test/E2E) می‌شکنند و پیامِ
خطا هیچ ربطی به علت ندارد.

- برایِ توسعه همیشه: `NODE_ENV=development npm install --include=dev`
  (یا اول `unset NODE_ENV`). چک: `npm config get omit` باید خالی باشد، نه `dev`.
- برایِ ایمیجِ تولید **چیزی عوض نکن**: `api/Dockerfile` خودش در مرحله‌ی runtime
  صریحاً `npm ci --omit=dev` می‌زند و مرحله‌ی build جدا و کامل نصب می‌کند.
- ⚠️ به همین دلیل **`.npmrc`ِ پروژه با `include=dev` نساز**: در npm اگر یک نوع
  هم در `include` و هم در `omit` بیاید، `include` برنده می‌شود — یعنی همان فایل
  `--omit=dev`ِ Dockerfile را هم بی‌اثر می‌کند و ایمیجِ تولید را باد می‌کند.

- **همگام‌سازیِ طراحی** (از ریشه): `sh tools/sync-design-system.sh` (بعد از هر تغییر در `shared/`)
- **بازتولیدِ بسته‌ی آفلاین** (از ریشه): `python tools/build-standalone.py` — بعد از
  هر تغییر در `apps/{customer,business,company}`. برایِ فقط بررسی: `--check`.
  (خروجی: `standalone/{customer,business,company}.html` — تولیدشده، نه منبع.)
- **اجرایِ migration دیتابیس** (توی `api/`): `npm run db:migrate`
- **seed دیتابیس** (توی `api/`): `npm run db:seed`
- **اجرایِ SQLِ دستی/همه‌یِ migrationها** (توی `api/`): `prisma/apply-sql.sh`
  ⚠️ **اصلاح‌شده (۲۰۲۶-۰۸-۲۳، تأییدشده با اجرایِ واقعی):** این خط قبلاً می‌گفت
  «روی یک DBِ خالی» — که **غلط است**. `prisma/sql/001-*` یک migrationِ *افزایشی*
  (ایندکس رویِ جدولِ موجود) است، نه سازنده‌ی اسکیما؛ رویِ DBِ کاملاً خالی با
  `P1014 The underlying table for model 'reservations' does not exist` شکست می‌خورد.
  ترتیبِ درست برایِ یک DBِ خالی: اول `npx prisma db push` (ساختِ اسکیمایِ پایه از
  `schema.prisma`)، **بعد** `prisma/apply-sql.sh` (ایندکس‌ها/کانسترینت‌ها/پارتیشن‌ها).

  🚫 **`prisma db push` فقط برایِ همان بوت‌استرَپِ اولیه است — هرگز رویِ یک DBِ
  migrate‌شده اجرایش نکن** (تأییدشده با اجرایِ واقعی، ۲۰۲۶-۰۸-۲۳): `block_end`
  یک ستونِ `GENERATED ALWAYS … STORED` است که Prisma نمی‌تواند بیانش کند، پس
  `db push` همیشه قصدِ DROPش را دارد و Postgres ردش می‌کند
  («constraint no_table_overlap depends on it»). یعنی دستور شکست می‌خورد —
  ولی *بعد از* اینکه هر ایندکسی را که در `schema.prisma` اعلام نشده DROP کرده.
  هر تغییرِ اسکیما باید یک migrationِ شماره‌دار در `prisma/sql/` باشد.

  ⚠️ **هر ایندکسِ جدید باید در هر دو جا باشد** — `@@index` در `schema.prisma`
  (با `map:` اگر نامِ SQL دلخواه است) **و** migrationِ SQL. فقط یکی از دو تا
  یعنی یا `db push` حذفش می‌کند یا یک ایندکسِ تکراری با نامِ دیگر ساخته می‌شود.
  گاردِ خودکار: `api/tests/schema-drift.integration.test.mts`.
- **اجرایِ محلی با داکر**: `docker compose --profile http up -d --build`
- **اجرایِ تولید با HTTPS**: `docker compose -f docker-compose.prod.yml up -d --build` (قبلش `DOMAIN=...` رو در `.env` ست کن)
- **تستِ واحدِ بک‌اند**: `cd api && npm test`
- **اجرایِ یک فایلِ تست به‌تنهایی**: `cd api && npm run test:one -- tests/<file>.test.mts`
  ⚠️ `npx tsx --test tests/<file>` **خام** را اجرا نکن: به‌خاطرِ هندلِ بازِ Redis
  پروسه هرگز exit نمی‌کند و پروسه‌یِ یتیم جا می‌گذارد — که بعداً باعثِ کندیِ E2E و
  قفلِ `EPERM` رویِ DLLِ Prisma می‌شود. `test:one` همان دستور با
  `--test-force-exit` است.
- **تایپ‌چکِ بک‌اند**: `cd api && npm run typecheck`
- **لینتِ بک‌اند**: `cd api && npm run lint`
- **تستِ E2E با Playwright** (توی `e2e/`): `npm test`
  ⚠️ **اصلاح‌شده (۲۰۲۶-۰۸-۲۰):** قبلاً `npm run test:e2e` نوشته بود که در پوشه‌ی
  `e2e/` اصلاً وجود ندارد و خطا می‌دهد؛ آن اسکریپت مالِ `api/` است. در `e2e/`
  اسکریپت‌ها این‌ها هستند: `test` (هر سه پروژه)، `test:mobile`، `test:desktop`،
  `test:ui`، `report`.
  `serviceWorkers: 'block'` از قبل در `e2e/playwright.config.ts` (خطِ ۴۳) تنظیم
  شده — **حذفش نکن**، لازم نیست دوباره تنظیمش کنی.
- **دسترسی به شلِ PostgreSQL** (در محیطِ داکر): `docker exec -it rezervno-postgres psql -U postgres -d rezervnodb`

---

## ساختار پوشه‌های مهم
- `api/src/app/api/v1/` → مسیرهایِ App Router برایِ APIِ نسخه‌ی ۱ (هر `route.ts` یک endpoint)
- `api/src/lib/` → توابعِ کمکی، کلاینت‌هایِ دیتابیس/Redis، منطقِ کسب‌وکار (auth، RBAC، rate-limit، ML، …)
- `api/prisma/sql/NNN-*.sql` → migrationهایِ افزایشی (idempotent: `ADD COLUMN IF NOT EXISTS`, …) — همیشه با پیشوندِ عددیِ بعدی اضافه کن، فایلِ قبلی رو ادیت نکن.
- `shared/css/` → توکن‌ها/فاندیشنِ دیزاین‌سیستمِ **پنل‌ها** (بدون build)
- `shared/js/` → اسکریپت‌هایِ کمکیِ مشترکِ سه پنل
- `apps/landing/app/globals.css` → دیزاین‌سیستمِ **وب‌سایتِ مارکتینگ** (ADR 0002) — موازی و جدا از `shared/css/`.
- `apps/seo/app/globals.css` → استایلِ **صفحاتِ SEOِ رستوران‌ها** (ADR 0001) — سومین سطحِ CSS، جدا از دو تای بالا.
  ⚠️ **اصلاح‌شده (۲۰۲۶-۰۸-۲۰):** اینجا قبلاً — خودش به‌عنوانِ یک «اصلاحِ چک‌شده»یِ
  ۲۰۲۶-۰۸-۱۲ — با تأکید می‌گفت «`apps/seo` اصلاً هیچ فایلِ CSS یا Icon.tsx‌ای نداره
  (نه tailwind، نه import استایل در layout.tsx)». آن ادعا **دیگر درست نیست**:
  `apps/seo/app/globals.css` در PR #32 (کامیتِ `99655c1`) اضافه شد و در
  `apps/seo/app/layout.tsx` خطِ ۵ ایمپورت می‌شود.
  درسِ این مورد: یک ادعای «چک‌شده» فقط تا تاریخِ همان چک معتبر است — قبل از تکیه
  به هر ادعایِ این فایل، خودت با `ls`/`grep` تأییدش کن.
- `deploy/` → فایل‌هایِ داکر و Caddy برایِ استقرارِ تولید
- `docs/ML_CONTRACT.md` → **قراردادِ الزام‌آورِ هر کارِ مدل/هوش/سنجش.** قبل از دست‌زدن به
  `no-show-model.ts`, `demand-forecast.ts`, `prediction-ledger.ts`, `model-drift.ts`,
  `no-show-features.ts` یا `outreach-ledger.ts` حتماً بخوانش. قاعده‌یِ حاکم بر همه‌شان:
  **هرگز عملکردی را که اندازه نگرفته‌ای گزارش نکن** — اگر شواهد کافی نیست
  `insufficient_data`/`null` برگردان، نه صفر. (صفر یعنی «اندازه گرفتیم و هیچ بود»؛
  ادعایی که اغلب نداریم.)

---

## نکاتِ امنیتی و عملکرد
- همه‌یِ APIها باید ورودی‌ها رو با اعتبارسنجی چک کنن — کتابخانه‌یِ Zodِ واقعی نصب نیست؛ به‌جایش یک شِیمِ سبکِ داخلی با APIِ شبیه‌به-Zod در `api/src/lib/validate.ts` وجود دارد (`z.object/string/number/…`، بازصادرشده از `api/src/lib/schemas.ts`) — همان انضباط را رعایت کن، ولی امضایِ متدها را با Zodِ واقعی اشتباه نگیر (مثلاً `.min(n)` اینجا پیامِ سفارشی نمی‌گیرد).
- برایِ نگاشتِ آمارِ خام از Postgres، هوشیارِ `bigint`→`BigInt` باش: `SUM(...)`/`COUNT(*)` در `$queryRaw` مقدارِ `BigInt`ِ جاوااسکریپت برمی‌گردانند، نه `number`، حتی اگر جنریکِ TypeScript بگوید `number` (فقط assertion است، نه تضمینِ runtime). همیشه `::int`/`::bigint` در SQL **و** `Number(x)` در جاوااسکریپت بزن (هردو لایه، نه فقط یکی).
- از React Query برای کشِ داده‌هایِ سرور در بخش‌هایِ Reactی (apps/landing، apps/seo) استفاده کن؛ پنل‌هایِ vanilla-JS کشِ خودشان را با متغیرهایِ global + fetch دستی دارند.
- برایِ تصاویر در بخش‌هایِ Reactی از `next/image` و برایِ لودِ تنبل از `next/dynamic` استفاده کن.
- هدرهایِ امنیتی (CSP، HSTS، …) در `api/src/middleware.ts` تنظیم شده‌اند — قبل از تغییرشان مطمئن شو یک درخواستِ SSE/streaming را خراب نمی‌کنی (اگر چنین چیزی اضافه شد).

---

## کار با Git و برنچ‌ها
⚠️ این بخش قبلاً یک گردش‌کارِ git-flow (برنچِ `develop` + `feature/...`) توصیف می‌کرد که **هیچ‌وقت واقعاً استفاده نشده** — این ریپو چنین برنچی ندارد. رویه‌یِ واقعی:
- برنچِ اصلی: `main` (همیشه پایدار و قابلِ‌استقرار؛ merge-on-green).
- هر دسته‌کارِ جدید مستقیماً از `main` برنچ می‌گیرد (اسمِ رایج: `claude/توضیح-کوتاه` یا `feature/توضیح-کوتاه`) و با PR به `main` برمی‌گردد.
- وقتی PRِ یک برنچ مرج شد، آن برنچ تمام‌شده حساب می‌شود؛ کارِ بعدی روی همان اسمِ برنچ از رویِ `main`ِ تازه دوباره ساخته می‌شود (`git checkout -B <branch> origin/main`)، نه اینکه رویِ تاریخچه‌یِ مرج‌شده کامیتِ جدید اضافه شود.
- قبل از کامیت، حتماً چک‌لیستِ بالا (تایپ‌چک/لینت/تست/دیزاین‌سیستم) رو کامل اجرا کن.

---
## یادآوریِ نهایی (همیشه به خاطر بسپار)
- تمامِ ارتباطات با کاربر به زبانِ فارسی انجام می‌شه.
- UI پروژه فارسی و راست‌چین (RTL) با فونتِ Vazirmatn هست.
- هیچ‌وقت مستقیم توی `node_modules` یا `.next` تغییر نده.
- **اولویتِ اول: کارکردِ درست و بدونِ باگ، نه ویژگیِ جدید.** اگر بینِ «ساختنِ چیزِ تازه» و «تست/رفعِ چیزی که از قبل هست»‌ باید انتخاب کنی، دومی رو انتخاب کن — لانچ‌کردن با باگ بدترین حالت است؛ رفعِ باگ همیشه مجاز و مطلوب است.
- هر جا دیتایِ فیک/هاردکد پیدا کردی که باید واقعی باشد (مثلاً از سرور نمی‌آید ولی باید بیاید)، آن را در گزارش/کامیت به‌عنوانِ یافته ثبت کن، حتی اگر همان لحظه رفعش نکنی.
- اگر مطمئن نیستی، قبل از کامیت از خودت بپرس: «آیا این تغییر با قوانینِ این فایل سازگاره؟»

---

## 🎨 تبدیلِ طرحِ Figma به کد

قبل از تولیدِ کد از رویِ طرحِ Figma، حتماً `docs/figma-mcp-rules.md` را بخوان.
نکته‌یِ حیاتی: پروژه **سه** سطحِ CSSِ موازی دارد که هیچ‌کدام به هم sync نمی‌شوند:
| سطح | فایل | برایِ |
|---|---|---|
| ۱ | `shared/css/` (بدونِ build، با `sync-design-system.sh` به سه پنل کپی می‌شود) | پنل‌هایِ customer/business/company |
| ۲ | `apps/landing/app/globals.css` | وب‌سایتِ مارکتینگ (ADR 0002) |
| ۳ | `apps/seo/app/globals.css` | صفحاتِ SEOِ رستوران‌ها (ADR 0001) |

⚠️ **اصلاح‌شده (۲۰۲۶-۰۸-۲۰):** این بخش قبلاً می‌گفت «**دو** دیزاین‌سیستم» و صریحاً
اضافه می‌کرد «نه `apps/seo` — آن اپ اصلاً فایلِ CSS ندارد». از PR #32 به بعد غلط است.

توکن‌هایِ هم‌نام بینِ این سه وجود دارند ولی فایل‌ها کاملاً جدا هستند — تغییر در یکی
هیچ اثری روی بقیه ندارد. قبل از ویرایش، مطمئن شو کدام سطح را دست می‌زنی.
