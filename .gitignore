.env
.env.local
*.env
node_modules/
api/node_modules/
.next/
api/.next/
*.log
.DS_Store
api/prisma/*.db

# بک‌آپ‌های دیتابیس (نباید در git بروند)
backup/data/*.sql.gz
backup/data/*.sql

# ── تکمیل‌شده در آماده‌سازیِ گیت (۲۰۲۶-۰۷-۲۳) ──
# ابزار Vercel (شاملِ لینکِ پروژه — نباید در مخزن باشد)
.vercel

# خروجیِ build و کش
out/
dist/
build/
*.tsbuildinfo
.turbo
.eslintcache
.cache/
coverage/

# گزارش‌های تست
e2e/playwright-report/
e2e/test-results/
playwright/.cache/

# متغیرهای محیطیِ محیط‌های خاص (‎.env.example استثناست چون به *.env ختم نمی‌شود)
.env.production
.env.development
.env.*.local

# اسنپ‌شاتِ Redis هنگامِ اجرای محلی (در api/ ساخته می‌شود؛ نباید کامیت شود)
dump.rdb

# کشِ پایتونِ ابزارها
tools/__pycache__/

# بسته‌های تولیدشده برای تحویل (از standalone/ و git خودش دوباره ساخته می‌شوند)
/rezervno-website.zip
/rezervno-site.bundle
